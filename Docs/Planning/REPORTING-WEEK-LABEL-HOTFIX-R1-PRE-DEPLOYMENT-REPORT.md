# REPORTING-WEEK-LABEL-HOTFIX-R1 — PRE-DEPLOYMENT REPORT

**التاريخ:** 2026-07-19 — **النطاق:** Frontend فقط (لا Backend / DB / Migration / Seeder) — **الحالة:** بلا نشر (تقرير ما قبل النشر فقط).

---

## 1) المشكلة والهدف

عند نشر `mod-insights-r1` بُنِيت الحزمة من git HEAD الذي **يفتقر** إلى واجهة نظام
`ROLE-AWARE-REPORTING-CALENDAR` (لم تُلتزَم في git قطّ) ⇒ فُقِدت منتقيات الدورة الأسبوعية/اليومية
واستهلاك `my-cycles`/`my-days` ونافذة السبت→الجمعة، وعادت الصفحات لإدخال أسبوع يدويّ (`2026-W25`).

**الهدف:** حزمة Frontend موحّدة = **اتحاد** كل ميزات الإنتاج الحالية **+** إعادة دمج نظام التقويم المفقود.

---

## 2) المعمارية المعتمَدة (شفافية)

- **الأساس (BASE):** مصدر الإنتاج الحالي المكتمل الميزات `/tmp/release-mod-insights-r1-20260718`
  (يحوي Client360 / Execution / Governance / mod-insights).
- **يُعاد دمجه فوقه:** نظام التقويم من الأساس الصحيح `/tmp/release-mb-v6-c1-prod-baseline-20260718-100827`.

هذا يَعكِس الترتيب الحرفيّ للمهمّة («ابدأ من التقويم وطبّق دلتا mod-insights») **عمدًا وبشفافية**، لأن أساس
التقويم يسبق ميزات Client360/Execution/Governance التي يجب الحفاظ عليها. النتيجة النهائية = نفس الاتحاد المطلوب.

**worktree الدمج المعزول:** `/tmp/hotfix-week-label-r1-20260719-125808/reporting-frontend`.

---

## 3) الحالة النهائية للصفحات (Phase 2)

| الصفحة/الملف | الأساس الصحيح للتقويم | الإنتاج الحالي (قبل) | الحالة النهائية |
|---|---|---|---|
| `SubmissionsPage.tsx` | WeeklyCycleCalendarPicker + DailyCalendarPicker | إدخال `2026-W25` يدويّ | منتقي أسبوعي (Report) + منتقي يومي، بلا إدخال يدويّ، عنوان الدورة الكامل |
| `ReportCalendarPage.tsx` | استهلاك my-cycles + السبت→الجمعة | حساب مفاتيح بالواجهة | نصّ «دورة التقارير من السبت إلى الجمعة» + منتقي الدورة الخادميّ |
| `KpiPage.tsx` | منتقي الدورة (Kpi) | إدخال أسبوع يدويّ | منتقي الدورة الأسبوعية (استخراج جراحيّ فقط، بلا حوكمة/Toast) |
| `dashboardPeriod.ts` | مرساة السبت (`-6`) | مرساة الخميس (`-4`) | مرساة السبت — API عام مطابق (drop-in، صفر كسر للمستهلكين) |

**مضاف حرفيًّا:** `useReportingCalendar.ts`, `WeeklyCycleCalendarPicker.tsx`(+test), `DailyCalendarPicker.tsx`(+test),
`dashboardPeriod.test.ts`, وكتلة أنواع التقويم في `types/api.ts` (استُبعِدت أنواع الأرشفة — 0 تطابق).

---

## 4) الاختبارات (Phase 3) — Gates A–F

| Gate | المحتوى | النتيجة |
|---|---|---|
| A | SubmissionsPage: منتقي ظاهر + عنوان الدورة الكامل + لا إدخال `2026-W25` | ✅ |
| B | ReportCalendarPage: نصّ السبت→الجمعة + طلب `my-cycles` من الخادم | ✅ |
| C | KpiPage: منتقي الدورة بلا إدخال يدويّ (نموذج الإدارة) | ✅ |
| D | dashboardPeriod: مرساة السبت W27→W33 + ثبات وسط الدورة + حدّ السنة | ✅ (ضمن `dashboardPeriod.test.ts`) |
| E | بوّابات الحزمة (§5) | ✅ |
| F | انحدار كامل: كل اختبارات الإنتاج + tsc + build | ✅ |

- **الحزمة الكاملة:** `32 ملف / 275 اختبار — نجاح 100%` (270 سابقة + 5 جديدة في `WeekLabelHotfix.test.tsx`).
- **tsc -b:** exit 0 (نظيف).
- **npm run build:** ناجح (تحذير `/*#__PURE__*/` من `@microsoft/signalr` فقط — حميد ومعروف).

---

## 5) مقارنة الحِزَم — إثبات الاتحاد (Phase 4)

- **الحزمة الجديدة:** `index-Bpd--Clz.js` (يشير إليها `index.html` صحيحًا).
- **الأساس الصحيح للتقويم** (rLMxWp0z-equivalent) = `/tmp/prod-prev-bundle.js`.
- **الإنتاج الحيّ الحالي** (C0M_G0Dp-equivalent) = `/tmp/prod-current-bundle.js`.

### 5.1 علامات التقويم (يجب أن تطابق الأساس الصحيح، غائبة عن الحيّ)
| علامة | prev (صحيح) | current (حيّ) | **الجديدة** |
|---|---|---|---|
| my-cycles | 2 | 0 | **2** |
| my-days | 2 | 0 | **2** |
| reporting-calendar | 4 | 0 | **4** |
| cycleLabel | 1 | 0 | **1** |
| «تقويم دورات التقارير الأسبوعية» | 1 | 0 | **1** |
| «دورة التقارير من السبت إلى الجمعة» | 1 | 0 | **1** |

### 5.2 علامات الإنتاج الحالي (يجب أن تطابق الحيّ، غائبة عن أساس التقويم)
| علامة | prev (صحيح) | current (حيّ) | **الجديدة** |
|---|---|---|---|
| incoming_messages | 0 | 3 | **3** |
| avg_response_minutes | 0 | 4 | **4** |
| problematic_comments | 0 | 3 | **3** |
| converted_opportunities | 0 | 3 | **3** |
| cases_grid | 0 | 2 | **2** |
| «الرسائل الواردة» | 0 | 1 | **1** |
| «جهات الاتصال» | 0 | 3 | **3** |
| «القنوات» | 0 | 3 | **3** |
| clients/ | 18 | 29 | **29** |
| projects/ | 16 | 22 | **22** |
| escalations | 46 | 60 | **60** |
| الحوكمة | 26 | 31 | **31** |

⟹ **الجديدة = التقويم (من prev) ∪ ميزات الإنتاج (من current).** كل عمود «الجديدة» يطابق المصدر المتوقَّع تمامًا.

### 5.3 بوّابات التسرّب/الأساس (Gate E)
| فحص | prev | current | **الجديدة** | الحكم |
|---|---|---|---|---|
| `http://localhost` | 2 | 2 | **2** | ثابت مكتبة axios/signalr (موجود في الحزمة المقبولة إنتاجيًّا) — حميد |
| 5090 / 5272 / 127.0.0.1 / reporting_test | 0 | 0 | **0** | لا تسرّب |
| قاعدة الـAPI | — | — | **`Us=`/api``** | صحيحة |

---

## 6) القيود المُلتزَمة

- Frontend فقط: **لا** تغيير Backend / Schema / DB / Migration / Seeder.
- **لا** نسخ عشوائيّ من worktrees قديمة (استخراج جراحيّ محدَّد للتقويم فقط).
- **لا** إدخال ميزات WIP غير منشورة (استُبعِدت أنواع/شاشات الأرشفة؛ KpiPage استُخرج منه سطور المنتقي فقط دون حوكمة/Toast).
- **لا** نشر RC/Production قبل بوّابة موافقة صريحة.

---

## 7) الحكم

**HOTFIX BUILD PASSED — READY FOR RC DEPLOYMENT**

كل البوّابات A–F خضراء، والاتحاد مُثبَت بالعلامات (التقويم مُستعاد + كل ميزات الإنتاج محفوظة)، بلا تسرّبات،
وقاعدة API = `/api`. **لا يُنشَر الآن** — بانتظار موافقة النشر الصريحة (RC ثم Production).
