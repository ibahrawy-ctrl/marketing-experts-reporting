# LEAVE-APPROVAL-WORKFLOW-STATE-TRANSITION-R1 — تقرير تشخيص إنتاجي قراءة-فقط

- **الخطورة:** HIGH
- **النطاق:** تشخيص إنتاجي **قراءة-فقط** + جذر السبب + تصميم الإصلاح + خطة مرشّح RC — **بلا أيّ كتابة على الإنتاج**.
- **القرار النهائي:** `ROOT CAUSE CONFIRMED — RC HOTFIX READY FOR IMPLEMENTATION`
- **التاريخ:** 2026-08-03
- **درجة الثقة:** **عالية (High)** — مسنودة بأدلّة من الكود + بيانات الإنتاج + مسح سكّاني.

---

## 1) Preflight الإنتاجي (قراءة-فقط)
| البند | القيمة |
|---|---|
| زمن التنفيذ | 2026-08-03 15:56:42 UTC (≈ 18:56 الرياض) |
| حالة الخدمة | `active` |
| MainPID / NRestarts | 353548 / **0** (لا إعادة تشغيل أثناء التشخيص) |
| Health الداخلي | `127.0.0.1:5090/health` = **200** |
| عدد الهجرات / الرأس | **30** / `20260724224053_AddReportApproverAndKpiReviewerOverrides` |
| نشر/هجرة/تشغيل خلفي أثناء التشخيص | **لا شيء** |

**تعهّد السلامة:** لم تُنفَّذ أيّ عبارة `UPDATE/INSERT/DELETE`؛ كل الاستعلامات `SELECT` فقط؛ لم تُطبع أسرار.

---

## 2) تحديد الحالتين (بالمعرّفات لا الأسماء)

### الحالة 1 — نور الدين رجب (SEO)
| الحقل | القيمة |
|---|---|
| LeaveRequestId | `5ed1efa3-b0da-4063-aca1-d27a2d13a8cf` |
| RequesterUserId | `3f2e7a2b-4e8a-4d0d-ab3a-69e0db63aba3` (نور الدين رجب) |
| TeamId / الفريق | `a7ef8832-ba4f-4a7b-838f-be6f2525ff42` / تحسين محركات البحث SEO |
| Team.TeamLeaderId | `0e30b66a-0301-4116-ac96-a034fafa424a` (**شيماء عيد**) |
| ManagerId | `0e30b66a-0301-4116-ac96-a034fafa424a` (**شيماء عيد — نفس قائد الفريق**) |
| النوع / التواريخ | Leave / 2026-07-19 → 2026-07-19 |
| Status / CurrentStep | `ManagerApproved` / `Hr` |
| CreatedAtUtc | 2026-07-19 07:36:53 UTC |

### الحالة 2 — محمد إبراهيم (سوشيال البود الأول 777)
| الحقل | القيمة |
|---|---|
| LeaveRequestId | `f40ee93c-c050-4137-b8e7-0d6c66bf7439` |
| RequesterUserId | `50a2fa2c-2abc-4b84-a328-7735470f56d7` (محمد إبراهيم) |
| TeamId / الفريق | `698c5e0e-9014-480c-8bc7-ee73d6b2d5cd` / سوشيال — البود الأول - 777 |
| Team.TeamLeaderId | `8be4ba0c-1af5-46ae-b1b2-296866d2655d` (**بسنت محمد**) |
| ManagerId | `8be4ba0c-1af5-46ae-b1b2-296866d2655d` (**بسنت محمد — نفس قائد الفريق**) |
| النوع / التواريخ | Leave / 2026-07-28 → 2026-07-28 |
| Status / CurrentStep | `ManagerApproved` / `Hr` |
| CreatedAtUtc | 2026-07-29 10:17:04 UTC |

**القاسم المشترك الحاسم:** في الحالتين `Team.TeamLeaderId == RequesterUser.ManagerId` — أي أنّ **المعتمِد المعيّن كقائد فريق هو نفسه المدير المباشر** للموظّف.

**أدوار المعتمِدين (Identity):** بسنت = `TeamLeader` فقط؛ شيماء عيد = `TeamLeader` فقط؛ محسن مجدي = `HR` + `Admin`.

---

## 3) سجلّ الاعتماد الزمنيّ (`leave_request_events`)

### نور الدين (`5ed1efa3`)
| التوقيت (UTC) | الإجراء | الخطوة | من → إلى | الفاعل |
|---|---|---|---|---|
| 2026-07-19 07:36:53 | submitted | Employee | Draft → Submitted | نور الدين رجب |
| 2026-07-19 08:06:09 | team_leader_approved | TeamLeader | Submitted → TeamLeaderApproved | **شيماء عيد** |
| **2026-08-03 12:13:46** | manager_approved | Manager | TeamLeaderApproved → ManagerApproved | **محسن مجدي (Admin)** |

⟵ **عَلِق عند `TeamLeaderApproved / Manager` لمدة 15 يومًا** (07-19 → 08-03) حتى تدخّل Admin.

### محمد إبراهيم (`f40ee93c`)
| التوقيت (UTC) | الإجراء | الخطوة | من → إلى | الفاعل |
|---|---|---|---|---|
| 2026-07-29 10:17:04 | submitted | Employee | Draft → Submitted | محمد إبراهيم |
| **2026-08-03 12:10:28** | team_leader_approved | TeamLeader | Submitted → TeamLeaderApproved | **بسنت محمد** |
| **2026-08-03 12:15:39** | manager_approved | Manager | TeamLeaderApproved → ManagerApproved | **محسن مجدي (Admin)** |

⟵ عَلِق عند `Submitted / TeamLeader` من 07-29، ثم اعتمدت بسنت خطوة قائد الفريق 08-03، وفورًا احتاج تدخّل Admin لخطوة المدير (خلال 5 دقائق) لأنّ بسنت — المدير المعيّن — لا تستطيع اعتماد خطوة ثانية.

**إثبات صحّة الاعتمادات:** كل خطوة اعتماد مسجّلة مرّة واحدة فقط، بالخطوة والحالة الصحيحتين، بلا تكرار. اعتماد شيماء/بسنت لخطوة قائد الفريق **موجود وصحيح**.

---

## 4) الانتقال المتوقَّع مقابل الفعليّ (Workflow State Transition)

**السلسلة المتوقَّعة:** Employee → TeamLeader → Manager → Hr (نهائي) → خصم الرصيد.

- الخصم يقع **حصرًا** عند بلوغ `HrApproved` داخل `DecideAsync` عبر `ApplyApprovalDeductionAsync` (نفس المعاملة، idempotent عبر `(RelatedRequestId, Source)`) — `LeaveRequestService.cs:593`.
- الانتقال الفعليّ للحالتين توقّف عند خطوة **Manager** لأنّ خطوة المدير لم تكن قابلة للاعتماد من المدير المعيّن (انظر §11).
- «عدم خصم الرصيد» = **نتيجة صحيحة** لعدم بلوغ `HrApproved`، **وليس عيبًا مستقلًّا**.

---

## 5) منطق قائمة الموارد البشرية (`GetPendingAsync`)
قائمة HR مبنيّة على **الحالة (Status)** حصرًا (لا يوجد مفهوم `CurrentApproverId` على هذا الكيان). الطلب لا يظهر لدى HR إلا بعد بلوغ `ManagerApproved`. لذا الطلبان العالقان عند `TeamLeaderApproved / Manager` **لم يظهرا في قائمة HR إطلاقًا** قبل تدخّل Admin — سلوك متّسق مع حالة الـWorkflow، **لا انحراف** بين القائمة والحقيقة.

---

## 6) اشتقاق الواجهة (Frontend Derivation) — نفي فرضية أوّليّة
`LeaveRequestsPage.tsx` (بطاقة «المعتمِدون») تشتقّ اكتمال كل خطوة عبر `approverLine()` (سطر 737-740): تعرض «بانتظار» إن كان اسم مُراجِع الخطوة `null`.

- في الحالتين قبل تدخّل Admin: `ManagerReviewerName = null` ⇒ عرضت الواجهة «المدير: بانتظار» — وهو **عرض صحيح** لأنّ خطوة المدير لم تُعتمَد فعليًّا.
- **الخلاصة:** فرضية `FRONTEND_WRONG_DERIVATION` **مرفوضة** لهذه الحالات. الواجهة صادقة؛ العلّة في **الـBackend workflow** لا في الاشتقاق. (يبقى ملاحظة ثانويّة: خطوات مُتخطّاة شرعيًّا بـ`ReviewerId=null` تُعرَض «بانتظار» — تجميليّ فقط، خارج نطاق هذا الإصلاح.)

---

## 7) الرصيد والـLedger (Phase 6)
- عدد حركات `employee_balance_ledger` للحالتين (`5ed1efa3`, `f40ee93c`) = **0**.
- عدد الحركات للثلاث المجمّدة الأخرى (عائشة/ريم/أحمد نصار) = **0**.
- **لا معاملة جزئية، لا حركة يتيمة، لا تعارض idempotency.** الغياب متّسق تمامًا مع عدم بلوغ `HrApproved`.

---

## 8) المسح السكّاني (قراءة-فقط)
| المقياس | العدد |
|---|---|
| موظّفون نشطون حيث `Team.TeamLeaderId == ManagerId` (تعرّض بنيويّ) | **19** |
| طلبات عادية عالقة حاليًّا (`Submitted`/`TeamLeaderApproved`) | 6 |
| منها بنمط `TL == Manager` | **5** |
| طلبات اعتُمدت خطوة قائد فريقها من نفس شخص المدير | **9** |
| طلبات فكّها محسن (Admin) عبر خطوة المدير | **3** (الحالتان + طلب ثالث) |

**تفصيل الطلبات الستة العالقة:**
| الموظّف | الحالة/الخطوة | أُنشئ | TL==Mgr | تشخيص |
|---|---|---|---|---|
| عائشة كمال | TeamLeaderApproved/Manager | 07-16 | ✔ | **مجمّد الآن (~18 يومًا)** |
| ريم جاب الله | TeamLeaderApproved/Manager | 07-20 | ✔ | **مجمّد الآن** |
| أحمد نصار | TeamLeaderApproved/Manager | 07-21 | ✔ | **مجمّد الآن** |
| حبيبة | Submitted/TeamLeader | 07-15 | ✔ | سيُجمَّد فور اعتماد قائد الفريق |
| سمر مجدي | Submitted/TeamLeader | 08-03 | ✔ | سيُجمَّد فور اعتماد قائد الفريق |
| بسنت محمد | TeamLeaderApproved/Manager | 07-28 | ✖ | معلّق شرعيًّا (T-WF1 self-skip، ينتظر مديرها الأعلى) |

**الاستنتاج:** المشكلة **بنيويّة منهجيّة** لا شذوذ بيانات فرديّ: 19 موظفًا معرّضون، و**3 طلبات مجمّدة فعليًّا** الآن بنفس النمط بالضبط.

---

## 9) تصنيف جذر السبب (Root Cause) — **درجة الثقة: عالية**

**التصنيف:** `STAGE_NOT_ADVANCED` (جمود بنيويّ في الـWorkflow) — **عيب طبقة واحدة (Backend workflow)**.

**الآليّة الدقيقة (مسنودة بالكود):**
1. خطوة المدير محكومة بالنطاق الشخصيّ فقط (`AuthorizeDecisionAsync`, `LeaveRequestService.cs:691-695`) — بلا قيد دور.
2. لكن الحارس في `LeaveRequestService.cs:543` يمنع **الشخص نفسه** من اتخاذ قرار على خطوتين لنفس الطلب:
   ```csharp
   if (entity.TeamLeaderReviewerId == uid || entity.ManagerReviewerId == uid || entity.HrReviewerId == uid)
       return Failure("لا يمكنك اتخاذ قرار على خطوة أخرى لنفس الطلب.", "auth.forbidden");
   ```
3. حين يكون `Team.TeamLeaderId == RequesterUser.ManagerId` (19 موظفًا)، يعتمد هذا الشخص خطوة **قائد الفريق** ⇒ `TeamLeaderReviewerId = هو`. ثم ينتقل الطلب لخطوة **المدير**، والمدير المعيّن هو **نفسه** — لكنّه ممنوع بالسطر 543. ولا أحد آخر في السلسلة العادية يملك نطاقًا شخصيًّا على الموظّف.
4. النتيجة: **جمود** عند `CurrentStep=Manager` (أو `TeamLeader` قبل اعتماده) إلى أن يتدخّل Admin/HR بنطاق شركة كامل (محسن) — وهو ما حدث فعلًا للحالتين اليوم.

**الأدلّة الداعمة:** سجلّ الأحداث (§3) + الأدوار (§2) + المسح (§8: 3 مجمّدة + 9 معتمَدة من شخص المدير) + غياب الخصم (§7).

**«عدم الخصم»** ليس جذرًا مستقلًّا بل عرَض تابع لعدم بلوغ الاعتماد النهائيّ.

---

## 10) تصميم الإصلاح (أضيق نطاق)

**المبدأ:** حين يكون قائد فريق الموظّف هو نفسه مديره المباشر، تكون خطوة المدير **زائدة بنيويًّا** ويجب طيّها — بشكل مطابق منطقيًّا لآليّة تخطّي خطوة قائد الفريق القائمة (T-WF1) عند غياب قائد فريق فعليّ.

**الإصلاح المُوصى به (Option A — طيّ خطوة المدير عند اعتماد قائد الفريق):**
- داخل مسار اعتماد خطوة **TeamLeader**، إن كان `RequesterUser.ManagerId == المُعتمِد الحاليّ (قائد الفريق)`، تُطوى خطوة المدير في **نفس المعاملة**: ضبط `ManagerReviewerId = المُعتمِد`، `ManagerDecisionAtUtc = now`، `Status = ManagerApproved`, `CurrentStep = Hr`، مع حدث `manager_step_collapsed` (أو `manager_approved` بتعليق توضيحيّ). فيصل الطلب إلى HR طبيعيًّا باعتماد واحد من الشخص الذي يجمع الدورين.
- **يعمّم على الحالتين** ويغطّي الـ19 موظفًا المعرّضين دون تمييز بالاسم.

**بدائل مرفوضة:**
- تعديل السطر 543 للسماح للشخص نفسه بخطوتين ⇒ **مرفوض** (يُضعف فصل الواجبات لكل الطلبات، واسع وخطر).
- تخطّي خطوة المدير عند الإنشاء مع ترك `ManagerReviewerId=null` ⇒ يعيد إنتاج عرض «المدير: بانتظار» في الواجهة (فجوة تجميليّة جديدة). Option A أنظف لأنه يسجّل المُعتمِد.

**قيود ملتزَمة:** بلا إعادة تصميم للـWorkflow، بلا توسّع نطاق، بلا تغيير سياسة إجازات، بلا تعديل رصيد تاريخيّ، بلا إصلاح مربوط باسم، **بلا Migration** (كله code-only على `LeaveRequestService.cs`).

---

## 11) قرار الـMigration
**لا حاجة لأيّ Migration.** الإصلاح منطق داخل `LeaveRequestService.cs` فقط. لا تغيير سكيمة، لا حقل جديد (كل حقول اللقطة والمُراجِعين موجودة). آخر هجرة تبقى `20260724224053`.

---

## 12) خطة مرشّح RC (Phase 11)
يُبنى المرشّح من أساس الإنتاج الحاليّ (رأس `20260724224053`)، Backend فقط. **20 سيناريو RC:**
1. موظّف قائد فريقه ≠ مديره ⇒ TL يعتمد ⇒ يتوقّف عند Manager (سلوك قائم دون تغيير).
2. نفس الأعلاه ⇒ المدير المختلف يعتمد ⇒ Hr.
3. **موظّف TL==Manager ⇒ TL/المدير يعتمد خطوة قائد الفريق ⇒ يُطوى تلقائيًّا إلى `ManagerApproved/Hr`** (الإصلاح الجوهريّ).
4. السيناريو 3: `ManagerReviewerId` = نفس المُعتمِد، `ManagerDecisionAtUtc` مضبوط.
5. السيناريو 3: حدث `manager_step_collapsed`/`manager_approved` مسجّل مرّة واحدة.
6. السيناريو 3: بلوغ HR ثمّ اعتماد HR نهائيّ ⇒ خصم رصيد واحد صحيح (idempotent).
7. لا خصم مزدوج عند إعادة محاولة اعتماد HR.
8. موظّف بلا قائد فريق (TL-skip قائم) ⇒ يبدأ عند Manager ⇒ سلوك دون تغيير.
9. الطالب نفسه قائد فريق (T-WF1) ⇒ سلوك دون تغيير.
10. طلب HR (`IsHrRequest`) ⇒ مساره الخاص دون تغيير (لا طيّ خطأ).
11. TL==Manager لكن HR ترفض لاحقًا ⇒ لا خصم، حالة `HrRejected` سليمة.
12. TL==Manager: منع اعتماد الطالب طلبه (auth.forbidden) يبقى.
13. TL==Manager: شخص ثالث خارج النطاق ⇒ 403 عند خطوة قائد الفريق (T-WF2 سليم).
14. TL==Manager مع نوع Permission ⇒ حارس الحد الشهريّ يبقى فعّالًا عند HR.
15. طيّ خطوة المدير لا يتخطّى حارس الرصيد/الإقرار عند الإنشاء.
16. Idempotency: إعادة اعتماد قائد الفريق بعد الطيّ ⇒ `invalid_state` (لا طيّ مكرّر).
17. الإشعارات: طيّ الخطوة يُطلق إشعار «وصول لـHR» مرّة واحدة.
18. لا تغيير على مسارات الرفض في أيّ خطوة.
19. سيناريوهات عدم-انحدار للإجازة والاستئذان القائمة (T-WF1/T-WF2/Leave Balance Guard/ESS-L2) خضراء.
20. مسح ثبات: لا تغيير سلوك لموظّف قائد فريقه ≠ مديره (السواد الأعظم).

**التحقّق:** مجموعة اختبارات التكامل الكاملة خضراء + بناء أخضر + لا تغيير سكيمة (`has-pending-model-changes = No changes`).

---

## 13) خطة معالجة الحالات القائمة (منفصلة — تتطلّب تصريحًا مستقلًّا)
**لا تُنفَّذ ضمن هذا التشخيص.** بعد نشر الإصلاح:
- **الحالتان المرجعيّتان** (`5ed1efa3`, `f40ee93c`): فُكّتا يدويًّا اليوم من محسن (Admin)، وهما الآن `ManagerApproved/Hr` تنتظران **الاعتماد النهائيّ من HR** فقط — لا تحتاجان أكثر من إتمام خطوة HR الطبيعيّة.
- **3 طلبات مجمّدة الآن** (عائشة كمال، ريم جاب الله، أحمد نصار — `TeamLeaderApproved/Manager`): بعد نشر الإصلاح، لن يُعالَجها تلقائيًّا (الإصلاح يعمل عند خطوة قائد الفريق المستقبليّة). خياران بتصريح مستقلّ: (أ) اعتماد Admin/HR ليدويّ لخطوة المدير (كما فعل محسن) ⇒ تصل HR؛ (ب) أداة معالجة idempotent محدودة تطوي خطوة المدير للطلبات المطابقة للنمط. **لا حذف/تعديل رصيد.**
- **2 طلبات ستُجمَّد** (حبيبة، سمر): سيغطّيها الإصلاح تلقائيًّا فور اعتماد قائد الفريق بعد النشر.

---

## 14) سلامة الإنتاج
- **صفر كتابة** على الإنتاج خلال التشخيص (كل الاستعلامات SELECT).
- **لم تُعدَّل** أيّ طلبات، لم يُغيَّر `Status/CurrentStep/ReviewerId`، لم يُنشأ Ledger، لم يُمَسّ رصيد.
- **لم يُنشر** كود، لم تُطبَّق هجرة، لم تُعَد تشغيل الخدمة (NRestarts=0).
- الأفعال المؤرَّخة 2026-08-03 (اعتمادات محسن/بسنت) **سابقة لهذا التشخيص** ومن مستخدمين حقيقيّين عبر التطبيق، لا من هذا العمل.

---

## 15) القرار النهائيّ
**`ROOT CAUSE CONFIRMED — RC HOTFIX READY FOR IMPLEMENTATION`.**

جذر السبب: **جمود بنيويّ في خطوة المدير حين يكون قائد فريق الموظّف هو نفسه مديره المباشر** — الحارس «لا خطوتين لنفس الشخص» (`LeaveRequestService.cs:543`) يمنع طيّ الخطوة المزدوجة، فيعلق الطلب حتى تدخّل Admin/HR. الإصلاح code-only بلا Migration (طيّ خطوة المدير عند اعتماد قائد الفريق حين `ManagerId == TeamLeaderId`). في انتظار تصريح صريح لبناء مرشّح RC + المعالجة اللاحقة للطلبات القائمة.
