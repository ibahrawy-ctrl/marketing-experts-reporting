# CPW-UNIFIED-UAT-R1 — تقرير الانحدار النهائيّ (المرحلة I)

- **التذكرة:** CPW-R2 + CPW-R3 — UNIFIED UAT, SECURITY CLOSURE & RC READINESS
- **المرحلة:** I — بوّابة إغلاق UAT + الانحدار الموحّد النهائيّ
- **التاريخ:** 16 أغسطس 2026
- **المرشَّح:** `81ee1455eb995b4cd823d0308e83186ee1e7ef9a`
- **الأساس المقارَن:** `f355164` (أب المرشَّح — النَسَب الموحّد قبل إصلاحات المرحلة H)
- **النتيجة:** **I_GATE = PASS · Candidate Regression = 0**

---

## 1) الانحدار الكامل على قاعدتَين نظيفتَين متكافئتَين

التزامًا بالقاعدة الحاكمة «لا يُصنَّف فشلٌ من جولة واحدة»، نُفِّذت **دورتان كاملتان** على قاعدتَين أُنشئتا `createdb` من الصفر، مع تطبيق عبارة `Classification='Supplementary'` على كلتيهما بعد البذر وقبل الانحدار.

| | الأساس `f355164` | المرشَّح `81ee145` |
|---|---|---|
| القاعدة | `reporting_uni_base2` (نظيفة) | `reporting_uni_final` (نظيفة) |
| الهجرات المطبَّقة | 35 · الرأس `20260811142239_AddProject360Foundation` | 35 · نفس الرأس |
| اختبارات الوحدة | **115 / 115 PASS** | **115 / 115 PASS** |
| اختبارات التكامل | 1510 نجاح · **1 فشل** من 1511 | 1515 نجاح · **1 فشل** من 1516 |
| زمن التكامل | 4 د 30 ث | 4 د 31 ث |
| الاختبار الفاشل | `EmployeeProfileScopeTests.Profile_Summary_Reflects_Submitted_Kpi` | **الاختبار نفسه حرفيًّا** |

### الاستنتاج الحاسم
- **الفارق في العدد الإجماليّ = +5 اختبارات بالضبط** — وهي اختبارات المرحلة H الخمسة المضافة في `Project360ApiSurfaceTests`، وكلّها ناجحة.
- **الفشل الوحيد متطابق على الجانبَين** ⇒ هو `BASELINE-DEFECT-02` المسجَّل مسبقًا كعيب أساس مفتوح (order-dependent)، **ولم يُحدثه هذا المرشَّح**.
- **Candidate Regression = 0** · **Candidate Fix Delta = +5**.

> `BASELINE-DEFECT-01` (`AdminGovernanceTests.Hr_CanFlagCommentRequestReopen_…`) نجح في هاتين الدورتين — وهذا متّسق مع توصيفه المسجَّل بأنّه order-dependent. العيبان يبقيان مفتوحَين ولا يُعالَجان ضمن هذه التذكرة.

---

## 2) بوّابة إغلاق UAT على TEST (إعادة تشغيل كاملة على البناء المنشور)

أُعيد تشغيل **كلّ** بوّابات UAT على `81ee145` المنشور فعلًا على TEST، لا على نتائج سابقة:

| البوّابة | النتيجة | الحكم |
|---|---|---|
| CPW-R2 — مستندات وأصول العميل | **137 / 137** | `R2_GATE=PASS` |
| CPW-R3 — Project 360 | **136 / 136** | `R3_GATE=PASS` |
| الصلاحيّات ومكافحة التعداد | **45 / 45** | `G_GATE=PASS` |
| إغلاق UAT-DEF-01/02 | **11 / 11** | `H_GATE=PASS` |
| مصفوفة الأدوار والنطاق | **143 / 143** | `ROLE_GATE=PASS` |
| Smoke الموحّد على TEST | **34 / 34** | `SMOKE_GATE=PASS` |
| UAT بصريّة/UX/RTL (المرحلة F) | 18 دليلًا · 0 عيب حاجب | `F_GATE=PASS` |

**مجموع الفحوص الخضراء على TEST = 506** (137+136+45+11+143+34).

---

## 3) تصحيح أداة الـSmoke (لا تغيير منتج)

أخفقت `uni-smoke.sh` بـ7 فحوص عند إعادة التشغيل. **جميعها أخطاء أداة لا عيوب منتج**، وأُثبِت السبب لكلٍّ منها:

| الفحص المخفق | السبب الجذريّ |
|---|---|
| `documents/{docId}` → 404 · `download` → 404 | السكربت يقرأ `CLIENT_ID` و`DOC_ID` من **استعلامَين مستقلَّين** بـ`limit 1` بلا `ORDER BY`، فقد يعودان من صفَّين مختلفَين ⇒ المستند لا يخصّ العميل ⇒ 404 صحيح |
| `document visible in list` 11 ≠ 1 | توقُّع مطلق قديم؛ بذر UAT المُصرَّح به أضاف مستندات موسومة |
| `client_documents` 16 ≠ 1 · `client_document_versions` 21 ≠ 1 · `clients` 6 ≠ 4 · `projects` 8 ≠ 5 | نفس السبب: توقُّعات مطلقة سابقة لبذر UAT |

**التصحيح** في `/root/uni-smoke2.sh` (نفس المجسّات حرفيًّا):
1. قراءة `CLIENT_ID` و`DOC_ID` من **الصفّ نفسه** (`select "ClientId" || '|' || "Id" … order by "CreatedAtUtc" limit 1`).
2. تحويل فحوص أعداد الصفوف إلى **«≥ خطّ الأساس قبل UAT»** بدل `==`، مع إبقاء **عدد الهجرات `== 35` بالمساواة الصارمة**.

النتيجة بعد التصحيح: **PASS=34 FAIL=0 · SMOKE_GATE=PASS**.

---

## 4) صمت قناة البريد (مؤكَّد بعد كلّ نشاط UAT)

| المؤشّر | القيمة |
|---|---|
| `EmailNotifications__Mode` | `DryRun` |
| `Email__Enabled` | `false` |
| `Reminders__Enabled` | `false` |
| `email_outbox` قبل/بعد | `0` / `0` — بلا تغيّر |
| رسائل مُرسَلة في آخر ساعة | `0` |

---

## 5) الحكم

| البند | النتيجة |
|---|---|
| الانحدار على قاعدتَين نظيفتَين متكافئتَين | PASS |
| Candidate Regression | **0** |
| اختبارات جديدة مضافة وناجحة | **+5** |
| عيوب الأساس المفتوحة | 2 (بلا تغيّر — خارج نطاق التذكرة) |
| بوّابات UAT على TEST | 506 / 506 |
| هجرة جديدة (#36) | **لا توجد** — الرأس ثابت |
| صمت البريد والتذكيرات | PASS |

**I_GATE = PASS**
