# PROJECT360-WORKFLOW-R1 — 06 · خطّة التنفيذ الذرّيّة (المرحلة H)

**التذكرة الأمّ:** `PROJECT360-OPERATIONAL-WORKFLOW-CLOSURE-R1` · **17 أغسطس 2026**
**حالة الخطّة:** **مقترحة — لم يُنفَّذ منها شيء.** لا كود · لا هجرة · لا التزام · لا نشر. كلّ تذكرة أدناه تحتاج **تصريحًا صريحًا مستقلًّا** لبدئها.
**شرط سابق مُلزِم:** لا تُبدأ التذاكر 4 و5 قبل حسم قرارات مالك المنتج في `-05 §القرارات المطلوبة`.

## ترتيب التبعيّة

```
1 · P360-PROJECT-ROLE-ASSIGNMENT-R1
        │
        ├──► 2 · P360-TEAM-LEADER-OPERATIONAL-WRITES-R1
        │            │
        │            └──► 3 · P360-PERMISSION-AWARE-UI-R1
        │
        └──► (مستقلّة) 4 · P360-PROGRESS-ROLLUP-R1 ──► 5 · P360-HEALTH-PROPAGATION-R1
                       6 · P360-PROJECT-GOVERNANCE-WIRING-R1   (مستقلّة تمامًا)
                       7 · P360-TEAM-REPORT-EXECUTION-BRIDGE-R1 (الخيار B — بعد النشر)
                                            │
        كلّ ما سبق ──────────────────────────┴──► 8 · P360-WORKFLOW-UAT-R1  (بوّابة الإنتاج)
```

**الخيار A لإغلاق الإنتاج = التذاكر 1 · 2 · 3 · 4 · 5 · 8.** التذكرة 6 مستحسنة معها. التذكرة 7 مؤجَّلة للخيار B بعد النشر.

---

## 1 · `P360-PROJECT-ROLE-ASSIGNMENT-R1`

**الهدف:** إحياء `Project.TeamLeaderId` و`ProjectOwnerId` بمسار إسناد حقيقيّ. يغلق `GAP-01`.

**قواعد العمل**
- الإسناد من **الإدارة فقط** (`Roles.ProjectPlanManagers`) — القرار #2 لمالك المنتج قد يوسّعها لمدير الحساب.
- المُسنَد قائدَ فريق يجب أن يحمل دور `TeamLeader`؛ والمُسنَد مالكًا يجب أن يكون من الإدارة. **يُتحقَّق في الخادم.**
- الحقلان اختياريّان (`NULL` مسموح) — لا كسر للبيانات القائمة.
- إسناد قائد فريق **يمنحه رؤية المشروع** ⟹ يُضاف `p.TeamLeaderId ∈ uids` إلى `ClientProjectAccess`، وإلّا أُسند لمشروع لا يراه.

**الخادم:** `ProjectModels.cs` (حقلان في طلبَي الإنشاء والتعديل) · `ProjectService.CreateAsync/UpdateAsync` (كتابة + تحقّق الدور) · `ClientProjectAccess.ResolveAsync` (مصدر رؤية سادس) · تدقيق `project.roles_assigned`.
**الواجهة:** حقلا اختيار في نموذج المشروع · عرضهما في `ProjectBriefTab` بأسماء لا معرّفات · `types/project360.ts`.
**البيانات:** **صفر هجرة** — العمودان والفهرسان موجودان منذ `20260811142239_AddProject360Foundation`.
**الصلاحيّات:** توسيع رؤية مقصود ومحدود (المُسنَد يرى مشروعه). لا توسيع كتابة.
**الاختبارات:** وحدويّة (تحقّق الدور · رفض معرّف غير موجود) · تكامليّة (الإسناد ثمّ رؤية المشروع بحساب قائد الفريق · عدم رؤية مشروع آخر) · واجهة (ظهور الحقلين للإدارة فقط).
**UAT:** إدارة تُسند · قائد الفريق يفتح المشروع · قائد فريق آخر يحصل على 404.
**معايير القبول:** `with_team_leader > 0` على قاعدة الاختبار · قائد الفريق المسنَد يمرّ R4 · غير المسنَد لا يراه.
**التراجع:** التغيير إضافيّ؛ التراجع بإرجاع الحزمة وترك الحقلين `NULL` — بلا فقدان بيانات.

---

## 2 · `P360-TEAM-LEADER-OPERATIONAL-WRITES-R1`

**الهدف:** إصلاح **انقلاب الصلاحيّات**: قائد الفريق يملك اليوم تعديل/أرشفة/**حذف** أيّ مشروع يراه ولا يملك تحديث مخرَج فيه. يغلق `GAP-06` و`GAP-08` و`GAP-16` (خادميًّا).

**قواعد العمل**
- التعديل/الأرشفة/إعادة التفعيل/الحذف: **الإدارة (`ProjectPlanManagers`)** أو **مالك المشروع/قائد فريقه المسنَد** — لا مجرّد الرؤية.
- الكتابة التشغيليّة تبقى على R4 كما هي (سليمة تصميمًا).
- **توحيد القواعد الخمس في ثلاث**: بنيويّة · تشغيليّة · رؤية. تُلغى قاعدة `CanManagePlanAsync` الخامسة وتُستبدل بالبنيويّة أو التشغيليّة بقرار صريح.
- **توحيد الرفض على 404** في `ProjectService` مطابقةً لعقد `Project360Authorization` (مكافحة التعداد · FINDING-W6-04).

**الخادم:** `ProjectsController.cs` (سياسة أدقّ من `ManagementOnly`) · `ProjectService.cs:106,130,147,172` · `ProjectWorkstreamService.CanManagePlanAsync` · `Roles.cs`/`Policies` عند الحاجة لسياسة جديدة.
**الواجهة:** لا شيء إلزاميّ (التذكرة 3 تتكفّل بالعرض).
**البيانات/الهجرة:** **لا شيء.**
**الصلاحيّات:** **تقليص** — يجب إعلانه في ملاحظات الإصدار ومقارنته بمصفوفة أدوار مجمَّدة خليّة بخليّة قبل الدمج.
**الاختبارات:** مصفوفة 8 أدوار × 6 عمليّات مشروع × (مسؤول/غير مسؤول) · تأكيد 404 لا 403 لكلّ رفض نطاق.
**UAT:** قائد فريق غير مسؤول يُمنع من الحذف · المسؤول ينجح في التحديث التشغيليّ.
**القبول:** صفر مسار يسمح بحذف مشروع لغير الإدارة/المسؤول · صفر رفض نطاق يرجع 403.
**التراجع:** إرجاع الحزمة؛ لا أثر بيانات.

---

## 3 · `P360-PERMISSION-AWARE-UI-R1`

**الهدف:** إنهاء فجوة العرض/الفرض: لا زرّ يظهر ويُرفَض، ولا صلاحيّة تُمنَح بلا زرّ. يغلق `GAP-16` و`GAP-11` و`GAP-13`.

**قواعد العمل**
- `access` في الواجهة يُشتقّ من **نفس القواعد الثلاث بعد التوحيد**، ويُفضَّل أن يعيدها الخادم في `overview` كحقول `canManage/canOperate/canAdminister` بدل إعادة استنتاجها في العميل.
- كلّ رفض من الخادم يُعرَض برسالة الخادم لا برسالة عامّة.
- المطّلع خارج النطاق لا يبقى في حالة تحميل أبديّة (`GAP-13`).

**الخادم:** إضافة كتلة `access` إلى `ProjectOverviewDto`.
**الواجهة:** `Project360Page.tsx:48-55` (استهلاك بدل استنتاج) · `ProjectStrategyTab:99` · `ProjectObjectivesTab:132` · `ProjectKpisTab:91` · `ProjectContractDeliverablesTab:69` · `ProjectGovernanceTab` · `ProjectHealthPanel`.
**الهجرة:** لا شيء.
**الاختبارات:** واجهة لكلّ دور على كلّ تبويب (إظهار/إخفاء) · حالة 404/403 تعرض شاشة خطأ لا تحميلًا.
**القبول:** صفر زرّ ظاهر يُرفَض · صفر إجراء مسموح بلا زرّ.
**التراجع:** إرجاع حزمة الواجهة (`dist`) ذرّيًّا كما في سابقة `test-frontend-api-base-hotfix`.

---

## 4 · `P360-PROGRESS-ROLLUP-R1`  ⚠ محجوبة بقرار مالك المنتج

**الهدف:** منح `Project.ProgressPercent` مصدرًا حقيقيًّا. يغلق `GAP-02` و`GAP-03`.

**البوّابة:** **لا يُكتب سطر واحد قبل اختيار البديل من `-04 §6`** (الترشيح الفنّيّ: A1 متوسّط المخرَجات).

**قواعد العمل (على فرض A1)**
- `ProgressPercent = Round(Average(نسب المخرَجات التعاقديّة النشطة))`.
- **مشروع بلا مخرَجات نشطة ⟹ `NULL`** وتُسقَط مكوّنته من معادلة الصحّة (كما تُسقَط مكوّنة المؤشّرات اليوم) — قرار #3 لمالك المنتج.
- يُعاد الاحتساب عند: تحديث تقدّم مخرَج · إنشاء/تعديل/تفعيل/تعطيل/حذف مخرَج.
- **يزول الرقم المزدوج**: `overallProgressPercent` العرضيّ يُستبدل بالقيمة المخزَّنة نفسها.

**الخادم:** `ProjectContractDeliverableService.cs` (كلّ مسارات الكتابة) · دالّة احتساب مركزيّة في `ProjectHealthPolicy` أو خدمة مخصّصة · `ProjectOverviewService.cs:152-154`.
**الهجرة:** لا شيء لـA1. (A2/A3/A5 تستلزم أعمدة جديدة ⟹ خارج الخيار A.)
**البيانات:** **تتغيّر أرقام تاريخيّة معروضة** (0.00 ← محسوب). إفصاح إلزاميّ. لا حذف ولا إعادة كتابة سجلّات.
**الاختبارات:** وحدويّة للمعادلة (صفر مخرَجات · كلّها 0 · كلّها 100 · مختلطة · معطَّلة) · تكامليّة: `PATCH` واحد يغيّر تقدّم المشروع في نفس النداء.
**القبول:** بعد `PATCH` مخرَج، تعكس `Project.ProgressPercent` القيمة المتوقّعة بلا إجراء ثانٍ · صفر تعارض بين رقمَي الشاشة.
**التراجع:** إرجاع الحزمة يعيد السلوك القديم؛ العمود يحتفظ بآخر قيمة محسوبة (غير ضارّ).

---

## 5 · `P360-HEALTH-PROPAGATION-R1`

**الهدف:** جعل الصحّة تعكس **كلّ** ما يغيّر مدخلاتها، وإنهاء الأخضر الكاذب. يغلق `GAP-04` و`GAP-05` و`GAP-12`.

**قواعد العمل**
- كلّ مسار يغيّر مدخلًا من مدخلات الصحّة يستدعي `SaveWithHealthAsync` — بما فيها **تحديث تقدّم المخرَج** و**تحديث حالة الهدف** (الشاذّان اليوم).
- **`ProjectHealthStatus.NotEvaluated`**: `HealthComputedAtUtc = NULL` ⟹ حالة رابعة معروضة صراحةً، لا أخضر.
- مراجعة عتبة الأخضر بعد إصلاح التقدّم (كانت غير قابلة للبلوغ رياضيًّا: `Health ≤ 0.5×kpiScore + 15`).

**الخادم:** `Enums.cs:770-775` · `ProjectHealthPolicy.cs` · `ProjectHealthService.SaveWithHealthAsync` · `ProjectContractDeliverableService` · `ProjectObjectiveService.UpdateStatusAsync`.
**الواجهة:** `ProjectHealthPanel` + كلّ شاشة تعرض لون الصحّة (قائمة المشاريع · اللوحة التنفيذيّة).
**الهجرة:** قيمة تعداد جديدة فقط (بلا DDL إن كان التخزين نصّيًّا) — **إضافيّة بحتة**.
**البيانات:** 5 من 9 مشاريع على TEST تنتقل من «أخضر» إلى «لم تُقيَّم». **تصحيح صدق لا فقدان.**
**الاختبارات:** وحدويّة لكلّ فرع من المعادلة والحالة الرابعة · تكامليّة لتغيّر الصحّة بعد `PATCH` مخرَج · واجهة للحالة الرابعة على كلّ شاشة.
**القبول:** صفر مشروع يعرض أخضر بـ`HealthComputedAtUtc = NULL` · الأخضر قابل للبلوغ بمشروع منضبط.
**التراجع:** إرجاع الحزمة؛ القيم المخزَّنة تبقى صالحة للقراءة القديمة (يجب التحقّق من تحمّل الحزمة القديمة للقيمة الرابعة قبل النشر).

---

## 6 · `P360-PROJECT-GOVERNANCE-WIRING-R1`

**الهدف:** توصيل علاقة قائمة أصلًا في الدومين. يغلق `GAP-19` و`GAP-17`.
**تصنيف حاسم:** **توصيل واجهة (Wiring) لا تغيير دومين.** `Risk.ProjectId` و`Decision.ProjectId` و`ManagementNote(EntityType=Project, EntityId)` موجودة وتعمل والقراءة ترشّح بها بدقّة. **لا هجرة ولا كيان جديد.**

**قواعد العمل**
- شاشة الحوكمة العامّة تُرسل `projectId` عند اختيار مشروع.
- زرّ إنشاء مخاطرة/قرار **من داخل** Project 360 يمرّر `projectId` تلقائيًّا.
- `ManagementNotesPanel` يُركَّب على صفحة المشروع بـ`entityType="Project"`.
- **إضافة `ProjectId` إلى `UpdateRiskRequest`/`UpdateDecisionRequest`** كي يُصحَّح ربط خاطئ لاحقًا.
- **مرفوض صراحةً:** كتابة اسم المشروع داخل عنوان البند بديلًا عن العلاقة. حلّ مؤقّت غير مقبول كعلاقة بيانات دائمة.

**الخادم:** `GovernanceModels.cs` (حقلان في طلبَي التعديل) · `GovernanceService` (حفظهما).
**الواجهة:** `GovernancePage.tsx` (صفر ذكر لـ`projectId` اليوم) · `ProjectGovernanceTab.tsx` (أزرار إنشاء) · صفحة المشروع (لوحة الملاحظات).
**الاختبارات:** تكامليّة: إنشاء من الشاشة العامّة مع مشروع ⟹ ظهوره في تبويب المشروع · عدم تسريب بنود مشروع آخر · ترشيح الملاحظة بالنوع **و**المعرّف معًا.
**القبول:** صفر بند حوكمة يحمل اسم مشروع في عنوانه بديلًا عن `ProjectId` · كلّ بند مُنشأ من الواجهة قابل للربط.
**التراجع:** إرجاع الحزمة؛ البنود المرتبطة تبقى مرتبطة (غير ضارّ).

---

## 7 · `P360-TEAM-REPORT-EXECUTION-BRIDGE-R1`  ⏸ الخيار B — بعد النشر

**الهدف:** إيصال تنفيذ عضو الفريق إلى المشروع **بالمعرّف لا بالاسم**. يغلق `GAP-09` و`GAP-21`.
**بوّابة مسبقة:** قرار مالك المنتج #6 — **أيّ رقم هو تقدّم المشروع رسميًّا؟** بلا حسمه يُنتِج هذا الجسر مصدرًا ثالثًا.

**قواعد العمل**
- حقل اختيار مشروع (`ProjectId`) في نموذج التسليم، **اختياريّ** في الإصدار الأوّل.
- بطاقة «آخر تنفيذ على هذا المشروع» داخل Project 360 (قراءة فقط).
- **لا يغذّي `ProgressPercent`** في هذه التذكرة — عرض فقط، حتّى يُحسَم مصدر التقدّم الرسميّ.
- التجميع بالنصّ (`PodExecutionAggregationService`) يبقى كما هو ويُوسَم في الواجهة بأنّه مشتقّ من التقارير لا من Project 360.

**الخادم:** خدمة قراءة جديدة في Project 360 تقرأ `ReportSubmissions` بـ`ProjectId`.
**الواجهة:** `SubmissionsPage.tsx` (حقل المشروع) · تبويب/بطاقة جديدة في Project 360.
**الهجرة:** `ReportSubmission.ProjectId` موجود بالفعل ⟹ **صفر هجرة**. (TEST: 0/13 تسليم يحمله.)
**المخاطر:** كسر دورة اعتماد قائمة ⟹ الحقل اختياريّ · ضجيج بيانات تاريخيّة بلا مشروع ⟹ عرض «غير مرتبط».
**القبول:** تسليم مرتبط يظهر في مشروعه · غير المرتبط لا يظهر في أيّ مشروع.

---

## 8 · `P360-WORKFLOW-UAT-R1`  🚦 بوّابة الإنتاج

**الهدف:** تنفيذ دورة UAT وظيفيّة متعدّدة الأدوار على البناء المكتمل واعتماد مالك المنتج. يغلق `GAP-26`.
**الوضع اليوم:** **0 من 90 حالة منفَّذة** — الكرّاسة نموذج فارغ، وحقول «النتيجة الفعليّة/الحالة/الدليل» فارغة في 90/90. تنفيذ إعداد الدليل كان **«تنفيذًا مرجعيًّا / دليلًا توثيقيًّا»** لا اعتمادًا.

**النطاق**
- الحزم `UAT-A` … `UAT-K` (90 حالة) على البناء الجديد، **زائد** حزمة جديدة `UAT-L` لسير العمل التشغيليّ الكامل بعد التذاكر 1–6.
- تنفيذ متعدّد الأدوار فعليّ: `Admin · CEO · GeneralManager · Manager · TeamLeader (مسؤول) · TeamLeader (غير مسؤول) · مدير حساب · Employee · Viewer`.
- كلّ حالة تُملأ بنتيجة فعليّة ولقطة دليل.
- توقيع صفحة الاعتماد من مالك المنتج.

**القبول:** 0 حالة فاشلة حاجبة · كلّ سطور كتلة التأكيد في `-07` = `YES` بدليل حيّ · توقيع مالك المنتج.
**التراجع:** لا ينطبق (لا تغيير كود).

---

## بوّابة ما قبل النشر (بعد اكتمال 1–6 و8)

| الفحص | المصدر |
|---|---|
| `dotnet test` وحدويّ + تكامليّ على **قاعدة نظيفة معزولة** (`createdb` ثمّ `dropdb`) | قاعدة حاكمة: لا قياس على `reporting_test` الملوَّثة |
| **الأساس والمرشَّح على قاعدتين نظيفتين متكافئتين** | لا يُصنَّف فشل من جولة واحدة |
| `tsc -b && vite build` | البوّابة الصحيحة للواجهة — لا `vite build` وحده |
| بناء الواجهة بـ`VITE_API_BASE_URL` صريحًا + **دخان متصفّح حقيقيّ بعد النشر** | درس `test-frontend-api-base-hotfix` |
| مصفوفة أدوار مطابقة خليّة بخليّة للمجمَّدة | كشف أيّ توسّع/تقليص غير مقصود |
| `PROD-READINESS-01` (`FileStorage__DocumentsRootPath`) **مغلق** | حاجب إنتاج مستقلّ قائم |
| Model Sync CLEAN · صفر تصادم هجرات | بوّابة النَسَب |

**تصريحات مطلوبة منفصلة:** بدء كلّ تذكرة · إنشاء الهجرة · الالتزام والدفع · النشر على TEST · النشر على RC · النشر على الإنتاج. **التصريح السابق لا يمتدّ.**
