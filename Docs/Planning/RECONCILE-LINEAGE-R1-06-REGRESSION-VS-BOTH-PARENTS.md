# RECONCILE-PROD-DEVELOP-LINEAGE — التقرير 06: الانحدار الكامل مقابل الأبوَين

> ## SUPERSEDED / INVALID — قياس باطل إجرائيًّا · لا يُستشهد به كحكم
> **الرقم 118 (ومنه 130) باطل** ولا يُعاد إلى أيّ حكم. السبب الجذريّ: قاعدة قياس
> ملوَّثة/مُعاد استعمالها + تمهيد ناقص + ترقيع اختبارات ضائع — التفصيل في التقرير 15 §3.2.
> **الحكم النافذ** في التقريرين 13 و15: **True Unified Candidate Regression = 0**
> بجولة `cand10` (`Failed 1 / Passed 1992 / Total 1993`)، والفشل الوحيد
> `BASELINE-DEFECT-01` مشترك مع الأبوين ⟹ ليس انحدار مرشَّح.
> يُحفَظ هذا التقرير لقيمته المنهجيّة: كيف يُنتج إجراء قياس معيب رقمًا مقنِعًا وخاطئًا.

**التاريخ:** 16 أغسطس 2026 · **المرحلة:** H · ~~**النتيجة: حاجب — الانحدار الموحّد = 118 ≠ 0**~~ (مُبطَل)

## 0. لماذا أُعيد القياس من الصفر

الجولة الأولى كانت **باطلة منهجيًّا** لثلاثة أسباب مُثبَتة:

| السبب | الإثبات |
|---|---|
| شجرة أب develop لا تدعم `TEST_DB_CONNECTION` (`CustomWebApplicationFactory.cs:16` بلا `??`) ⟹ شغّلت على `reporting_test` الملوَّثة | `recon_reg_devparent` بقيت **0 مستخدم** بعد الجولة · المدّة 1 س 43 د |
| `Project360ApiSurfaceTests.cs:30` يُصلِّب `reporting_test` في **الأشجار الثلاث** | قراءة مباشرة للملفّ |
| المصانع المعزولة تتقاسم `reporting_calendar_iso`/`reporting_pfe_iso` بين الجولات المتوازية | `42P07` متبقّية في السجلّ |

أُعيد القياس بالبوّابة غير المتلِفة الموثَّقة في `testing-environment-rules.md` §16: توجيه **كلّ** سلسلة اتّصال مُصلَّبة إلى قواعد خاصّة بالشجرة (`rr_<tag>_main|cal|pfe`)، `createdb` + `dotnet ef database update` بتجميعة الشجرة نفسها، تهيئة البذر بتشغيل مصغَّر، ثمّ `UPDATE report_templates SET "Classification"='Supplementary'` للعناوين الخمسة، ثمّ **تشغيل تتابعيّ بلا تزاحم**.

## 1. النتائج على قواعد نظيفة متكافئة

| الشجرة | الالتزام | اختبارات الوحدة | اختبارات التكامل | الفاشل |
|---|---|---|---|---|
| قاعدة التفرّع | `6fd2253` | — | 1292 | **29** |
| أب develop | `10c26f7` | 115/115 | 1516 | **2** |
| أب الإنتاج | `ce166662` | 313/313 | 1778 | **159** |
| المرشّح المدموج | `ac0d86c` (+ ترقيعان) | 359/359 | 1982 | **130** |

فشلا develop هما `BASELINE-DEFECT-01/02` المعروفان مسبقًا.

## 2. إسناد الفشل — من كسر ماذا

| الحساب | العدد |
|---|---|
| أحمر عند قاعدة التفرّع | 29 |
| منها **أصلحه develop** | **29 (كلّها)** |
| منها **لا يزال أحمر عند الإنتاج** | **29 (كلّها)** |
| أحمر عند الإنتاج و**أخضر عند قاعدة التفرّع** ⟹ انحدار أحدثه نَسَب الإنتاج | **130** |
| فشل المرشّح المشترك مع أب الإنتاج | 117 |
| فشل المرشّح غير الموجود عند أب الإنتاج | 13 |
| **الانحدار الموحّد** = فاشل عند المرشّح وناجح عند أحد الأبوَين على الأقلّ | **118** |
| منها كان أحمر أصلًا عند قاعدة التفرّع | 9 |

**الخلاصة القاطعة:** نَسَب `develop` **شفى** كلّ الاحمرار الموروث (29 ⟶ 0)، بينما نَسَب الإنتاج **أبقاه وأضاف 130 احمرارًا جديدًا** (29 ⟶ 159). الشجرة المدموجة ترث 117 منها لأنّ حلّ التعارض رجّح كود الإنتاج في الأنظمة المشتركة.

## 3. توزيع الانحدار الموحّد الـ118 بحسب الصنف

`ReportsTests` 15 · `ProjectRepeatableGridTests` 15 · `B2bByServiceTemplateTests` 11 ·
`PositionsTests` 9 · `B2cByCourseValidationTests` 9 · `SubmissionTests` 6 · `ReportViewGrantTests` 6 ·
`MultiProjectSectionTests` 5 · `ScopeEnforcementTests` 4 · `SalesAggregationTests` 4 ·
`ComplianceDueLateTests` 4 · `B2cNewOldTests` 4 · `SalesAggregationCourseGroupedTests` 3 ·
`NotificationLinkTests` 3 · وبقيّتها 1–2 لكلّ صنف.

## 4. سببان جذريّان مُثبَتان

### 4.1 حذفٌ صامت أحدثه الدمج (أُصلِح)

ملفّان عدّلهما نَسَب الإنتاج **بالحذف المحض** ولم تلمسهما develop ⟹ أخذ الدمج نسخة الإنتاج بلا علامة تعارض:

| الملفّ | المحذوف | المضاف | الأثر |
|---|---|---|---|
| `Services/ScopeResolver.cs` | 60 | 0 | ضياع `ResolvePositionScopeAsync` كاملةً ⟹ سقوط اتّحاد نطاقات المناصب |
| `Common/RoleCapabilities.cs` | 1 | 0 | ضياع قدرة `positions.manage` |

أُعيدا حرفيًّا من `10c26f7`. هذا صنف من الضياع **لم يرصده التقرير 03** لأنّه قاس الملفّات المحذوفة والملفّات المتعارضة فقط، لا التعديلات أحاديّة الجانب. المسح الشامل أثبت أنّ هذين الملفّين هما **الوحيدان** المتبقّيان من هذا الصنف (28 ملفًّا آخر كانت قد استُعيدت في المرحلة E).

### 4.2 تشدّد تقويميّ في نَسَب الإنتاج (لم يُصلَح — قرار مطلوب)

`ReportingAggregationService` استبدل حصر النطاق المعجميّ بـ`ReportingCalendarPolicy.TryCanonicalDay`،
فصار **يُسقِط أيّ `PeriodKey` لا يقبل التقويم** بدل تمريره. اختبارات `ReportsTests` تستعمل مفاتيح
أسبوعيّة تركيبيّة خارج المدى (`2026-W60`, `W71–W74`, `W81–W86`, `W91–W96`)، فتُسقَط كلّها ⟹
`Reporters = 0`. مُثبَت أنّ `ReportsTests.MediaBuyerRollup_Manager_AggregatesReportsBestWorst`
يفشل على أب الإنتاج **على قاعدة نظيفة وعلى `reporting_test` معًا** ⟹ ليس أثر بيئة.

هذا يفسّر **15** من الـ118. الـ103 الباقية تحتاج تشخيصًا فرديًّا لم يُنجَز بعد.

## 5. الحكم

بوّابة القبول §14 من التذكرة تشترط `Unified Candidate Regression = 0`. القيمة المقيسة **118**.

| البوّابة | المطلوب | المقيس | الحالة |
|---|---|---|---|
| Unified Candidate Regression | 0 | **118** | ✘ |
| Production Live Feature Regression | 0 | غير قابل للقياس قبل خفض الرقم أعلاه | ✘ |
| بناء الخلفيّة/الواجهة | أخضر | أخضر (0 خطأ · 548/548 واجهة) | ✔ |
| اختبارات الوحدة | لا انحدار | 359/359 (مجموعة فوقيّة على 313 و115) | ✔ |

⟹ **المرحلة I (الدفع إلى `develop`) والمرحلة J (إعادة نشر TEST) موقوفتان.** الدفع بحالته الراهنة
يستورد 118 اختبارًا أحمر إلى فرع `develop` **الأخضر حاليًّا** (فشلان معروفان فقط) — وهو ضرر على
حالة مشتركة يصعب التراجع عنه، ويخالف بوّابة القبول التي أقرّتها التذكرة نفسها.

## 6. المصنوعات

`/tmp/lineage/reg2/` — `base.log` `dev.log` `prod.log` `cand.log` (وسجلّ ما قبل الترقيع
`cand-before-fix.log`) · `F_base.txt` `F_dev.txt` `F_prod.txt` `F_cand.txt` · `A_*.txt` (الجرد الكامل
عبر `--list-tests`) · `P_dev.txt` `P_prod.txt` `P_any.txt` · **`R_unified.txt` (الانحدار الـ118)** ·
`prep.sh` `run.sh` (قابلان لإعادة الإنتاج حرفيًّا).
