# NAVIGATION-REGRESSION-HOTFIX-R1 — تقرير قبول RC UAT

- التاريخ: 2026-07-15 (دورة W29: السبت 07-11 → الجمعة 07-17)
- النطاق: إصلاح انحدار التنقّل — **واجهة فقط** (لا Backend، لا Migration، لا تغيير بيانات).
- البيئة: RC — `khubara-reporting-rc.service`:5092، `rc-report.emarketingacademy.net` (BasicAuth: rcpreview)، DB `reporting_rc`.
- الهدف: استعادة التنقّل المدمج المعتمد (شريط جانبي بوحدات رئيسية محدودة + تبويبات أفقية للصفحات الفرعية) بدل قائمة الأكورديون الطويلة التي عادت بعد نشر ADMIN-GOVERNANCE-R1.

## Phase 9 — نشر RC (واجهة فقط) — مكتمل

| البند | قبل | بعد | الحالة |
|------|-----|-----|--------|
| حزمة الواجهة | `index-B_lNG4Zb.js` (1317012 بايت) | `index-B-y7LHB8.js` (1281278 بايت) | مُبدَّلة |
| CSS | `index-eBeHKKuv.css` | `index-C8CkvKW-.css` | مُبدَّلة |
| Reporting.Api.dll | `a6e8ea98…2596e7` | `a6e8ea98…2596e7` | **لم تتغيّر** |
| Reporting.Application.dll | `7b0a6900…2861da` | `7b0a6900…2861da` | **لم تتغيّر** |
| Reporting.Infrastructure.dll | `aa460ece…88596d` | `aa460ece…88596d` | **لم تتغيّر** |
| الخدمة | active | active (بلا إعادة تشغيل) | سليمة |
| الصحة الداخلية `/health` | 200 | 200 | سليمة |

- النسخة الاحتياطية: `/opt/reporting-rc/frontend/dist-backup-navhotfix-20260715-161825`.
- الحزمة الجديدة تُقدَّم علنًا عبر HTTPS (BasicAuth): `index.html`→200 يشير للحزمة الجديدة، `index-B-y7LHB8.js`→200، `index-C8CkvKW-.css`→200، الحزمة القديمة `index-B_lNG4Zb.js`→**404**.
- فحص تسريب المضيف في الحزمة المُقدَّمة: `localhost:509x`=0، مضيفات RC/TEST=0. قاعدة الـAPI نسبية `/api` (محايدة للمضيف).
- **إثبات frontend-only**: بصمات DLL الثلاث مطابقة بايتًا لخط الأساس؛ لم تُطبَّق أي هجرة؛ الخدمة لم تُعَد تشغيلها.

## Phase 5 (مُعاد التحقق على القطعة المنشورة) — بوابة تسريب التنقّل الاستراتيجي = 0

فحص الحزمة المنشورة فعليًّا على RC لرموز المسارات/الوحدات الاستراتيجية المحظورة:

| الرمز | العدّ في الحزمة الجديدة |
|------|----|
| execution-taxonomy | 0 |
| Workstream / workstreams | 0 |
| Deliverable | 0 |
| Client 360 / client-360 | 0 |
| governance-workspace / GovernanceWorkspace | 0 |
| execution/team-dashboard | 0 |
| execution-reports | 0 |
| FlexiblePositions | 0 |
| **/app/positions (مسار تنقّل)** | **0** (كان 2 في حزمة الانحدار) |
| PositionsPage | 0 |

- **النتيجة: STRATEGIC NAVIGATION LEAKS = 0** على مستوى التنقّل (مسارات/وحدات).
- ملاحظة مقصودة وخارج النطاق: النصّ `المناصب المرنة` يظهر مرة واحدة كنصّ داخل صفحة المستخدمين (`UsersPage.tsx` — محرّر UserPositionsEditor للأدمن)، **ليس وحدة تنقّل ولا مسارًا**، وموجود مسبقًا في حزمة الإنتاج/RC السابقة (كان 3). تعديل `UsersPage/usePositions` خارج نطاق تحرير إصلاح التنقّل المسموح. صافي الأثر: الإصلاح **أزال** مسار التنقّل الاستراتيجي `/app/positions` (2→0) وقلّص أثر المناصب (3→1).

## Phase 10 — UAT بالأدوار: مصفوفة التنقّل القاطعة

الرؤية دالّة خالصة من `(roles, isSalesRep)` في `navConfig.ts` (المصدر داخل الحزمة المنشورة). كل دور يرى شريطًا جانبيًّا قصيرًا (≤8 وحدات)، بلا أي وحدة استراتيجية:

| الدور | الوحدات الظاهرة | العدد |
|------|------|:----:|
| Admin | الرئيسية · التقارير · الأداء وKPI · المشاريع والعملاء · الفرق والموظفون · الحوكمة والمتابعة · الإجازات والطلبات · الإعدادات | 8 |
| CEO | الكل عدا «المشاريع والعملاء» | 7 |
| GeneralManager | الكل عدا «المشاريع والعملاء» | 7 |
| HR | الرئيسية · التقارير · الأداء · الفرق والموظفون · الحوكمة · الإجازات · الإعدادات | 7 |
| Manager | الرئيسية · التقارير · الأداء · الفرق والموظفون · الحوكمة · الإجازات | 6 |
| TeamLeader | الرئيسية · التقارير · الأداء · الفرق والموظفون · الحوكمة · الإجازات | 6 |
| Employee | الرئيسية · التقارير · الأداء · الحوكمة · الإجازات | 5 |
| AccountManager (AccountPortfolioReader) | الرئيسية · التقارير · الأداء · المشاريع والعملاء · الإجازات | 5 |

روابط المبيعات الثلاثة = تبويبات ضمن وحدة «التقارير»:
- «تجميع المبيعات» ⇐ Admin/CEO/GeneralManager/Manager.
- «لوحة مبيعات الفريق» ⇐ TeamLeader فقط.
- «لوحة مبيعاتي» ⇐ مندوب المبيعات (isSalesRep) فقط — لا الأدمن.

### أدلة UAT الحيّة (مستوى القطعة + السلوك)
1. الحزمة تُقلِع وتُقدَّم على RC عبر nginx/HTTPS/BasicAuth الحقيقي (index+assets 200، القديمة 404).
2. علامات التنقّل المدمج حاضرة في الحزمة المنشورة: «الرئيسية»، «التقارير المقدَّمة»، «تقويم التقارير»، «تجميع المبيعات»، «لوحة مبيعات الفريق»، «لوحة مبيعاتي»، مفتاح الطيّ `me_nav_collapsed_v2`.
3. الأدوار الحاضرة على RC تغطّي كل أدوار UAT: Admin(3)، CEO(1)، GeneralManager(1)، Manager(4)، TeamLeader(5)، HR(1)، Employee(20)، AccountPortfolioReader(2)، إضافة CeoSupport/Accountant/FinanceManager.
4. مدخلات التنقّل (roles + isSalesRep) تتدفّق من خادم RC غير المُعدَّل (DLL مطابقة بايتًا للإنتاج) ⇒ سلوك المصادقة مطابق للإنتاج.
5. مواصفة تنفيذية: 150 اختبار Vitest أخضر (7 سيناريوهات أدوار في `DashboardShell.nav.test.tsx`) تُثبت الرؤية أعلاه.

### قيد موثّق (شفافية)
الالتقاط البصري المُصادَق لكل دور مباشرةً على RC لم يُنفَّذ لأن: (أ) حسابات أدوار RC هي مستخدمو UAT حقيقيون ولن أعيد تعيين كلمات مرورهم (تفاديًا لتعطيل حسابات حقيقية)؛ (ب) لا تتوفّر أتمتة متصفّح مُصادَق ضد RC البعيد (BasicAuth+HTTPS) بالأدوات الحالية. التغطية البديلة = أدلة القطعة الحيّة (1-4) + المواصفة التنفيذية (5). هذا متّسق مع نمط النشر المعتمد في المشروع (إثبات RBAC عبر السياسة-في-DLL + الاختبارات عند غياب حسابات حيّة).

## Phase 6 (حماية الانحدار) — محفوظ
diff الواجهة مقابل الأساس `4a3ff8c` = 6 ملفات تنقّل فقط (App.tsx، DashboardShell.tsx، navConfig.ts الجديد، حذف GovernanceWorkspacePage/PositionsPage/useGovernanceWorkspace). صفر تغيير Backend/Migration/تقويم/KPI/تسليمات. بصمات DLL الثلاث لم تتغيّر ⇒ كل تغييرات ADMIN-GOVERNANCE-R1 وROLE-AWARE-CALENDAR-R1 المنشورة محفوظة.

## الحُكم

**NAVIGATION REGRESSION HOTFIX — RC ACCEPTED**

- التنقّل المدمج مُستعاد (شريط وحدات قصير + تبويبات أفقية)، بلا قائمة أكورديون طويلة، بلا «فتح/طي الكل».
- تسريب استراتيجي = 0 (مسارات/وحدات تنقّل)؛ مسار `/app/positions` أُزيل.
- frontend-only مُثبَت (DLL مطابقة، لا هجرة، لا إعادة تشغيل، الصحة 200).
- **الوقوف هنا**: لا نشر إنتاج (Phase 12) إلا بموافقة صريحة جديدة بعد قبول RC.
