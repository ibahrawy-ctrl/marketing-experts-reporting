# CPW-UNIFIED-UAT-R1 — تقرير تنفيذ الـUAT الموحّدة

- **التذكرة:** CPW-R2 + CPW-R3 — UNIFIED UAT, SECURITY CLOSURE & RC READINESS
- **المراحل المغطّاة:** C (تحضير البيانات) · D (UAT وظيفيّة CPW-R2) · E (UAT وظيفيّة CPW-R3) · G (الصلاحيّات ومكافحة التعداد) · H (إغلاق العيوب وإعادة النشر)
- **التاريخ:** 15–16 أغسطس 2026
- **البيئة:** TEST/UAT — `khubara-reporting-test` · `reporting_test_uat` · المنفذ 5091
- **البناء المفحوص:** `81ee1455eb995b4cd823d0308e83186ee1e7ef9a` (مثبَت من SourceLink داخل `Reporting.Api.dll` المنشور)
- **النتيجة:** **UAT_GATE = PASS · 0 عيب حاجب مفتوح**

---

## 1) المرحلة C — تحضير بيانات UAT الموحّدة

بيانات وُسِمت كلّها بـ`CPW-UNIFIED-UAT-R1` لتكون قابلة للتمييز والتنظيف، ولم يُحذف أو يُعدَّل أيّ سجلّ سابق.

| المكوَّن | العدد | ملاحظة |
|---|---|---|
| عملاء | 2 | `clientMain` داخل النطاق · `clientOutOfScope` خارجه (لاختبار العزل) |
| مشاريع | 3 | `projectGreen` (صحّة خضراء) · `projectAtRisk` (معرَّض للخطر) · `projectOutOfScope` |
| أهداف | 3 | هدفان على الأخضر وواحد على المعرَّض للخطر |
| مؤشّرات KPI | 3 | مقابلة للأهداف |
| مخرجات تعاقديّة | 3 | من كتالوج تصنيف تنفيذيّ فيه **18** نوعًا |
| مستندات عميل | 10 | مصنَّفة بنطاقات رؤية مختلفة (`MGMTONLY` · `FINONLY` · `HRMGMT` · `MGMTFIN` · `PROJTEAM` · `CLIENTSCOPED` · `CUSTOMROLES` · `CUSTOMUSERS-AM` · `VERSIONED` · `ARCHIVE-TARGET`) |
| روابط خارجيّة | 2 | `drive` · `site` |
| مخاطرة | 1 | مربوطة بالمشروع المعرَّض للخطر |
| مستخدمون في الدليل | 17 | قُرئوا ولم يُنشأ أيّ حساب جديد خارج تدوير المرحلة A |

**مدى رؤية المستندات المبذورة (تحقّق مباشر بعد البذر):** CEO = 7 · GM = 7 · FIN_MGR = 3 · HR = 2 — وهي بالضبط النتيجة المتوقَّعة من مصفوفة التصنيف، أي أنّ البذر نفسه صار أوّل دليل على صحّة نموذج الرؤية.

بيان التنظيف مسجَّل في `/root/uat-cleanup-manifest.json` على الخادم، ومنه تُحذف بيانات UAT عند الطلب دون المساس بغيرها.

---

## 2) المرحلة D — UAT الوظيفيّة لـCPW-R2 (مستندات العميل وأصوله)

**النتيجة: 137 / 137 PASS · 0 FAIL** (`/root/uat-r2-results.json`)

| المحور | عدد الفحوص | ما أُثبِت |
|---|---|---|
| مصفوفة الرؤية حسب التصنيف | **40** | كلّ مستند يظهر لمن يملك حقّ رؤيته فقط، عبر التصنيفات العشرة المبذورة |
| ظهور/اختفاء عبر الأدوار | **40** | نفس المصفوفة مطبَّقة من جهة القوائم لا التفاصيل فقط |
| نطاق مدير العمليّات (`OPS_MGR`) | 9 | وصول كامل للمستندات والروابط داخل نطاقه |
| العزل خارج النطاق | 5 | العميل خارج النطاق يعود **404** لا 403 ولا تسريب وجود |
| المعرّفات الوهميّة | 3 | نفس رمز الخطأ ونفس الصياغة تمامًا كحالة «خارج النطاق» |
| مسارات القراءة الأساسيّة | 4 | `documents` · `links` · `document detail` · `storage-usage` |
| التنزيل | 3 | تنزيل النسخة الحاليّة ونسخة تاريخيّة ورفض التنزيل خارج النطاق |
| الإصدارات | 1 + 1 | مستند متعدّد النسخ يُرجع النسخة الحاليّة ويحتفظ بتاريخه |
| دورة الحياة | 2 + 2 + 2 | إنشاء · تعديل · حذف/أرشفة — كلّها Idempotent وبلا فقد بيانات |
| الحالة الفارغة | 2 | عميل بلا مستندات يُرجع قائمة فارغة لا خطأ |

**لا فقد بيانات:** لم يُحذف أيّ مستند قائم؛ عمليّات الحذف اقتصرت على مستندات موسومة أُنشئت داخل UAT نفسها.

---

## 3) المرحلة E — UAT الوظيفيّة لـCPW-R3 (Project 360)

**النتيجة: 136 / 136 PASS · 0 FAIL · `findings = []`** (`/root/uat-r3-results.json`)

| المحور | عدد الفحوص | ما أُثبِت |
|---|---|---|
| مصفوفة الأدوار على مساحة المشروع | **70** | 11 دورًا × سطوح القراءة — لا توسيع صلاحيّة ولا تضييق غير مقصود |
| مسارات القراءة | 13 | `overview` · `strategy` · `strategy/schema` · `objectives` · `kpis` · `contract-deliverables` · `risks` · `decisions` · `notes` … |
| المعرّفات الوهميّة | **11** | كلّها **404** بنفس الرمز — لا فرق يميّز «غير موجود» عن «خارج نطاقك» |
| التعديل | 4 | أهداف/مؤشّرات/مخرجات/مخاطر |
| إعادة احتساب الصحّة | 3 | `recompute` يعطي نتيجة مستقرّة عند التكرار (Idempotent) |
| التفعيل/التعطيل | 3 + 3 | مع الأرشفة والاستعادة |
| الإنشاء والحذف | 2 + 2 | على العناصر الموسومة فقط |
| ملاحظات إداريّة | 2 | على مشروع وعلى عميل |
| العزل بين المشاريع | 2 | عنصر يُنشأ على مشروع **لا يظهر** على مشروع آخر |

---

## 4) المرحلة G — الصلاحيّات ومكافحة التعداد

**النتيجة: 45 / 45 PASS · 0 FAIL** (`/root/uat-g-results.json`)

| المحور | عدد | الخلاصة |
|---|---|---|
| الوصول المجهول (`anonymous`) | 8 | **401** على كلّ سطوح CPW-R2 و CPW-R3 بلا استثناء |
| رموز فاسدة/عشوائيّة (`garbage`) | 4 | **401** — لا 500 ولا تسريب استثناء |
| رفض حسب الدور (EMP · VIEWER · SALES · TL · AM) | 11 | **404 موحّد** في كلّ الحالات |
| سطوح المشروع | 3 | لا يُفرَّق بين مشروع غير موجود ومشروع خارج النطاق |
| رموز صالحة داخل النطاق | 2 | **200** — إثبات أنّ الاختبار يميّز فعلًا ولا يعيد 404 دائمًا |
| تجميع المصفوفة | 1 | تطابق كامل مع العقد المعلن |

**عقد مكافحة التعداد مُثبَت:** «غير موجود» و«خارج النطاق» و«ممنوع بالسياسة» تُنتج جميعها الاستجابة نفسها حرفيًّا — الرمز والصياغة والحالة — فلا يمكن استنتاج وجود مورد من فرق في الردّ.

---

## 5) المرحلة H — إغلاق العيبَين وإعادة نشر TEST

عيبان اكتُشفا في المرحلتَين D/E وأُصلحا داخل النطاق المصرَّح به، ثمّ أُعيد بناء TEST ونُشر عليها البناء الجديد وأُعيد التحقّق:

**النتيجة بعد إعادة النشر: 11 / 11 PASS · `H_GATE = PASS`** (`/root/uat-h-results.json`)

| # | الفحص | النتيجة |
|---|---|---|
| 1 | إنشاء قرار مع `projectId` | PASS |
| 2 | القرار يظهر على مشروعه | PASS |
| 3 | القرار **غائب** عن مشروع آخر | PASS |
| 4 | قرار بمشروع مجهول → 404 `decision.project.not_found` | PASS |
| 5 | قرار بلا `projectId` لا يزال مقبولًا (لا انحدار) | PASS |
| 6 | إنشاء ملاحظة إداريّة على مشروع | PASS |
| 7 | الملاحظة تظهر على مشروعها | PASS |
| 8 | الملاحظة **غائبة** عن مشروع آخر | PASS |
| 9 | إنشاء ملاحظة إداريّة على عميل | PASS |
| 10 | ملاحظة على مشروع مجهول → 404 `note.entity.not_found` | PASS |
| 11 | **الموظّف لا يستطيع كتابة ملاحظة مشروع** (لا توسيع صلاحيّة) | PASS |

الفحص رقم 11 مقصود ومحوريّ: الإصلاح وسّع **التحقّق من وجود الكيان** فقط، ولم يوسّع **من يملك حقّ الكتابة** — وهو ما نصّ عليه التعليق التوثيقيّ داخل `ManagementNoteService.CanAccessEntityAsync`.

تفاصيل العيبَين وسببهما الجذريّ في سجلّ العيوب — التقرير 3.

---

## 6) صمت قناة البريد طوال UAT

| المؤشّر | القيمة |
|---|---|
| `EmailNotifications__Mode` | `DryRun` |
| `Email__Enabled` | `false` |
| `Reminders__Enabled` | `false` |
| `email_outbox` قبل/بعد كامل نشاط UAT | `0` / `0` |
| رسائل مُرسَلة خلال آخر ساعة | `0` |

لم تُفعَّل قناة بريد ولا مجدول تذكيرات في أيّ لحظة، امتثالًا للحظر الصريح.

---

## 7) الحكم

| البوّابة | النتيجة | الحكم |
|---|---|---|
| C — تحضير البيانات الموسومة | مكتمل بلا فقد بيانات | PASS |
| D — CPW-R2 وظيفيّ | 137 / 137 | `R2_GATE=PASS` |
| E — CPW-R3 وظيفيّ | 136 / 136 | `R3_GATE=PASS` |
| G — صلاحيّات ومكافحة تعداد | 45 / 45 | `G_GATE=PASS` |
| H — إغلاق UAT-DEF-01/02 | 11 / 11 | `H_GATE=PASS` |
| مصفوفة الأدوار والنطاق | 143 / 143 | `ROLE_GATE=PASS` |
| Smoke موحّد على TEST | 34 / 34 | `SMOKE_GATE=PASS` |
| صمت البريد والتذكيرات | مؤكَّد | PASS |

**مجموع الفحوص الخضراء على TEST = 506** · **UAT_GATE = PASS**

> هذه UAT **تقنيّة**، ولا تُعدّ بديلًا عن اعتماد مالك المنتج (Product Owner Sign-off).
