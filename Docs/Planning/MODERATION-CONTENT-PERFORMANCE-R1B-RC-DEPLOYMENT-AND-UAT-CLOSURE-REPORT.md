# MODERATION-CONTENT-PERFORMANCE-R1B — تقرير نشر RC واختبار القبول (UAT) والإغلاق

- **التاريخ:** 2026-07-20
- **البيئة:** Release Candidate — `rc-report.emarketingacademy.net`، خدمة `khubara-reporting-rc.service`، ربط داخلي `127.0.0.1:5092`، قاعدة `reporting_rc`، `ASPNETCORE_ENVIRONMENT=ReleaseCandidate`، Basic Auth + SSL + noindex.
- **الالتزام (Commit):** `1b8dc7f` (الأب `265fa7a`).
- **المسار المعتمَد مسبقًا:** نشر V6 على RC **دون إعادة توجيه أي تسليم** (No-Repoint / Path #1). لا إنشاء مسودّة V5 اصطناعية، ولا تعديل W28، ولا Migration، ولا نشر/إعادة تشغيل خدمة الـ API، ولا Seeder عام، ولا SQL INSERT يدويّ.

---

## 1) نطاق النشر على RC
- نُشر **إصدار القالب V6** لقالب «تقرير المديرشن الأسبوعي» (`b51a6176-2367-48ad-9bf3-e1ba7a159891`) عبر أداة النشر المعزولة (Publisher، خارج الحل)، **بلا إعادة توجيه أي مسودّة** (المجموعة المؤهَّلة فارغة).
- نُشرت حزمة الواجهة الجديدة على `/opt/reporting-rc/frontend/dist/`.
- **لم تُطبَّق أي Migration**؛ عدّاد الهجرات ثابت **29** (بلا تغيير).

## 2) نسخ احتياطية قبل النشر (RC)
- قاعدة RC: `/root/db-backups/reporting_rc-premodr1b-20260720-130401.dump`.
- واجهة RC: `/root/db-backups/rc-frontend-dist-premodr1b-20260720-130401.tar.gz`.
- طابع النشر: `/root/modr1b-rc-ts.txt`.

## 3) مسودّة UAT الطبيعية (RC-6)
- أُنشئت مسودّة UAT عبر **المسار الطبيعي للنظام** (`POST /api/submissions` — نفس المسار العام الذي تستخدمه الواجهة)، لا عبر SQL/Publisher/Seeder.
- استخدم مستخدم RC اختباري مؤقّت `mod-rc-uat@test.local` يحمل المسمّى الوظيفي `SOCIAL_MOD` (لعبور حارس إسناد القالب TEMPLATE-ROLE-GUARD — لا إعفاء للأدمن)، أُنشئ عبر واجهة الأدمن الطبيعية.
- **PeriodKey = `2026-W29`** (أسبوع التشغيل الحالي، غير W28، بلا تصادم مع أي تسليم سابق لنفس القالب/المُرسِل/الفترة).
- إثبات: التسليم الجديد `1d6a8f3c-052f-4e85-844c-f9266fe43d1b` — الحالة **Draft**، مرتبط بـ **V6** (`b51a6176`) لا V5، بلا تكرار، بلا أي أثر على W28.

## 4) اختبار القبول الوظيفي (RC-7)
نُفِّذ على مستوى الـ API (تعذّر وصول أدوات المعاينة إلى RC خلف Basic Auth)، وغطّى:
- تصيير الأقسام الستة و**21 حقلًا فرعيًّا** (القديمة + الجديدة) على V6.
- حفظ/إعادة تحميل كل الحقول القديمة والجديدة.
- بطاقات «تحليل المحتوى» (`content_highlights`): إضافة/حذف/**إعادة ترتيب**/حفظ/إعادة تحميل — ثبات الترتيب مؤكَّد.
- رؤى الجمهور، الدروس المستفادة، القرارات المطلوبة.
- مفتاح المخاطر (نعم/لا) **دون فقدان صامت** لـ `risk_note` عند التبديل.
- **كل التحقّقات خضراء** (SAVE v1/RELOAD1/SAVE v2/RELOAD2). لا 500 ولا أخطاء.

اختبار الحالات Submitted/Closed على بيانات أعمال حقيقية **غير مطلوب** وفق النطاق المعتمَد.

## 5) التحقّق من البيانات (RC-8) — للقراءة فقط
| البند | النتيجة |
|---|---|
| عدّاد الهجرات | **29 (ثابت)** |
| V5 (`1638f686`) عدد الحقول الفرعية | **15** — العقد بلا تغيير (md5 `99e4dfe9…`) |
| V6 (`b51a6176`) عدد الحقول الفرعية | **21** — منشور |
| V6 = V5 + الجديد | الحقول الـ15 القديمة **محفوظة بنفس الترتيب** + 6 حقول جديدة: `content_highlights, audience_insight, lessons_learned, decisions_required, risk_exists, risk_note` |
| حقول محذوفة/مُعاد تسميتها (V5−V6) | **لا شيء (فارغ)** |
| W28 (`2a7a831c`) | **Submitted، على V5 (`1638f686`)، IsDeleted=false — بلا تغيير** |
| مسودّة UAT | مرتبطة بـ **V6** |
| ترتيب صفوف `content_highlights` في المسودّة | `[أضعف محتوى, أفضل محتوى, محتوى رابع]` — مطابق لنتيجة إعادة الترتيب في RC-7 |
| حفظ `risk_note` مع `risk_exists=لا` | **محفوظ** («خطر تراجع الوصول العضوي — ملاحظة مهمة يجب عدم فقدانها») |
| تسليمات مكرّرة (submitter, periodKey) | **لا شيء** |
| قيم يتيمة (submission_field_values بلا تسليم أب) | **0** |
| قوالب تغيّرت خارج المديرشن | **لا شيء** — القالب الوحيد الذي حصل على إصدار عند/بعد توقيت إنشاء V6 = «تقرير المديرشن الأسبوعي» فقط |
| Migration / إعادة تشغيل خدمة / تغيير env | **لا شيء** |

## 6) تصريح No-Repoint (إلزاميّ)
> بيئة RC **لم تحتوِ على مسودّة W30 مؤهَّلة** لإعادة التوجيه؛ ومن ثمّ **لم تُنفَّذ أي عملية إعادة توجيه شبيهة بالإنتاج على RC**. يبقى سلوك إعادة التوجيه (V5→V6) مغطّى بالكامل عبر **مصفوفة الاختبارات الآليّة المعزولة** لأداة النشر. استُخدم في اختبار قبول RC **مسودّة V6 أُنشئت طبيعيًّا** عبر مسار النظام العام.

## 7) تنظيف بيانات الاختبار (RC)
نُفِّذ داخل معاملة واحدة على `reporting_rc`، ثم تُحقِّق:
- حُذفت: `submission_field_values` (1) → `report_submissions` (1) → `refresh_tokens` (3) → `AspNetUserRoles` (1) → `audit_logs` (المرتبطة بالمستخدم المؤقّت) → `AspNetUsers` (1).
- بعد التنظيف: المسودّة=0، القيم=0، المستخدم المؤقّت=0، مستخدمو `@test.local`=0، تسليمات على V6=0.
- **W28 سليم (1)، V6 منشور (1)، V5 منشور (1)** — بلا أي مساس.
- أُزيلت سكربتات RC من الخادم (`/root/rc7-uat.mjs`, `/root/rc8-*.sql`).

## 8) الحدود التي لم تُمَسّ
لا نشر/دفع للإنتاج، لا Migration، لا نشر/إعادة تشغيل خدمة الـ API، لا Account Manager، لا Email/Scheduler/KPI/Client360/Project-First، لم يُعدَّل V5، لم تُحذَف W30، لا Seeder عام، ولم تُمَسّ قاعدة `reporting_test_uat`. لم تُطبَع أي أسرار.

---

## الحكم النهائي

**MODERATION R1B RC DEPLOYED AND UAT PASSED — READY FOR PRODUCTION APPROVAL**
