# ROLE-AWARE CALENDAR R1 — RC UAT ACCEPTANCE REPORT

> **الحزمة:** التقويم المدرك للدور (Role-Aware Reporting Calendar R1) — أسبوعي + يومي + KPI.
> **النطاق:** نشر Release Candidate (RC) فقط + UAT وظيفي كامل خادميّ.
> **البيئة:** `khubara-reporting-rc.service` @ `http://127.0.0.1:5092` (ASPNETCORE_ENVIRONMENT=ReleaseCandidate)، قاعدة `reporting_rc`، VPS `srv1747233`.
> **التاريخ المرجعي للاختبار:** 2026-07-15 (الأربعاء). الدورة الحالية 2026-W29 (السبت 11 يوليو → الجمعة 17 يوليو).
> **قرار الإنتاج:** لم يُنشَر على الإنتاج ولا على TEST — بانتظار موافقة صريحة جديدة.

---

## 1. تصحيح السياسة الإلزامي (منفَّذ ومُثبَت)

- **أيام العمل اليومية:** السبت، الأحد، الاثنين، الثلاثاء، الأربعاء، الخميس.
- **العطلة الأسبوعية الوحيدة = الجمعة فقط.**
- **السبت يوم عمل كامل** (قابل للاختيار/الإنشاء/الإرسال) — لا يُخلَط بسياسة الأسبوع (التي تبدأ دورتها السبت).
- الكود الحاسم: `ReportingCalendarPolicy.IsDailyHoliday(DateOnly date) => date.DayOfWeek is DayOfWeek.Friday;`

**إثبات UAT مباشر على RC:**
- A1: الجمعة 07-10 = `isHoliday=true`, `isSelectable=false`, `status=Holiday`. ✅
- A2: السبت 07-11 = `isHoliday=false`, `isSelectable=true`, `isOpenForDraft=true`, `status≠Holiday`. ✅
- A5: الجمعة التالية 07-17 = `Holiday` (قاعدة الجمعة-فقط، العطلة تسبق المستقبل). ✅
- C1: مفتاح الجمعة مرفوض خادميًّا `calendar.day_is_holiday` (400). ✅
- C5: مفتاح السبت **مقبول** خادميًّا، أُنشئت مسودّة `periodKey=2026-07-11`. ✅

---

## 2. البصمات (Hashes) — قبل/بعد

النشر لم يغيّر أي DLL أو bundle بعد Phase 3؛ البصمات المؤكَّدة والمنشورة على RC:

| المكوّن | البصمة (SHA-256، مختصرة) |
|---|---|
| `Reporting.Api.dll` | `a6e8ea98…2596e7` |
| `Reporting.Infrastructure.dll` | `aa460ece…88596d` |
| `Reporting.Application.dll` | `7b0a6900…2861da` |
| Frontend bundle `index-B_lNG4Zb.js` | `2b5bfb00…dff85f` |

بعد اكتمال UAT والتنظيف: **لا إعادة نشر، لا تغيير بصمات** (فقط استعادة env + إعادة تشغيل الخدمة).

---

## 3. النسخ الاحتياطية (Backup Paths)

- **env قبل UAT (مفاتيح Seed الأصلية):** `/root/khubara-reporting-rc.env.bak-caluat-20260714-220848`
- **env قبل الاستعادة (لقطة أمان لمفاتيح CAL-UAT):** `/root/khubara-reporting-rc.env.pre-restore-<ts>`
- نُسخ backend/frontend وقواعد RC أُخذت في Phase 2 (موثّقة سابقًا، غير مُتغيّرة).

---

## 4. نتائج البناء والاختبارات (Phase 1 — Final Candidate)

- Backend build: **0 errors**.
- Unit tests: **133/133** أخضر.
- DailyCalendarTests (معزول): **10/10**.
- Frontend: `tsc` **0**، `vite build` ناجح، `vitest` **150/150** (يشمل DailyCalendarPicker.test.tsx = 7 اختبارات: لا input[type=date]، اختيار اليوم تلقائيًّا، أزرار السابق/التالي، «العودة إلى اليوم»، شارات الحالات، تصحيح السبت، سلوك فشل الواجهة).
- `dotnet ef migrations has-pending-model-changes` = **No changes**، **0 ملف هجرة جديد** (الالتزام بمنع أي Migration).

---

## 5. نتائج UAT الوظيفي على RC الحيّ

### Phase 4 — Weekly UAT: **19/19 PASS**
- W1-W4: اختيار الدورة الحالية تلقائيًّا، `CurrentCycleKey=2026-W29`، أسبوع السبت→الجمعة، دورات سابقة/حالية/تالية.
- W5.*: مواعيد الاستحقاق حسب الدور (موظف=أربعاء off4، قائد=خميس off5، مدير=أحد off8، GM/CEO=اثنين off9).
- W6-W7: كل الأدوار تشترك في نفس CurrentCycleKey ونفس نافذة السبت→الجمعة.
- W8-W11: المسودّة المبكّرة ليست متأخّرة، الدورة الماضية متأخّرة بعد الاستحقاق، الدورة المستقبلية مقفلة.
- W12-W15: تسميات عربية، نوافذ مؤرّخة صحيحة (W28 السبت 4→الجمعة 10)، الدورة الحالية السبت 11→الجمعة 17، السياق=Report.

### Phase 5 — KPI Weekly UAT: **11/12 آليًّا + aggregate 200 (كل الفحوص الجوهرية الـ8 خضراء)**
- K1/K2: سياق KPI يُرجِع نفس بنية/مفاتيح الدورة كسياق التقرير (السبت→الجمعة، نفس CurrentCycleKey).
- K3a: مفتاح غير مطابق للصيغة مرفوض `period_format_invalid` (400).
- K3b: مفتاح دورة غير صالح (W99) مرفوض `kpi.cycle_key_invalid` (400).
- K4: دورة مستقبلية مرفوضة `calendar.cycle_not_open` (400).
- K4b: نوع فترة غير أسبوعي مرفوض `period_type_not_supported`.
- K5: الإنشاء بالمفتاح الحاليّ الصحيح ينجح، `PeriodKey==CurrentCycleKey`.
- K6: حفظ النتائج يحافظ على PeriodKey (لا انزياح).
- K7a/K7c: خط الأحداث الزمني + قائمة الحوكمة يستجيبان 200.
- K8: admin-delete (حذف ناعم بسبب إلزامي) ينجح.
- **K7b (aggregate)**: كان خطأ في مواصفة الاختبار (استخدمت `granularity=weekly` غير المدعومة — المدعوم Monthly/Quarterly/Yearly/Custom). أُعيد الاختبار بـ Monthly=200 (avg=98.25) وQuarterly=200 → التجميع سليم.

### Phase 6 — Daily Sales UAT: **21/21 PASS**
- A1-A5: قواعد العمل (الجمعة العطلة الوحيدة، السبت عمل كامل، الأحد/الاثنين/الثلاثاء عمل، اليوم مستحقّ).
- B1-B8: السلوك (اختيار اليوم تلقائيًّا، اسم عربي «الأربعاء» + تسمية كاملة، مفاتيح السابق/التالي، السبت قابل للإنشاء، شارات الحالات، الجمعة عطلة بسبب قفل، الخميس مستقبل مقفل، تنقّل المرساة).
- C0-C6: التحقّق الخادميّ (الجمعة `day_is_holiday`، المستقبل `future_day_locked`، غير صالح `daily_key_invalid`، cadence خاطئ `cadence_invalid`، السبت واليوم مقبولان).
- D1: الحفظ يحافظ على مفتاح اليوم.

### Phase 7 — Regression UAT: **7/7 PASS**
- R1: إرسال تقرير يوميّ فارغ يُمنَع بتحقّق الحقول/سير العمل `submission.required_fields_missing` (400) — **ليس** خطأ تقويم (Save-then-Submit ومنع الفراغ سليمان).
- R2: قائمة التسليمات 200 (العودة للقائمة سليمة).
- R3: قائمة حوكمة KPI 200.
- R4: `dashboard/me` 200.
- R5: `notifications` 200 (الشريط الجانبي/الفوري سليم).
- R6: `report-templates` 200.
- R7: تقويم الأسبوعي `my-cycles?context=report` 200 مع `currentCycleKey=2026-W29`.

**لم يُرسَل أي بريد حقيقي** (Email معطّل على RC، والاختبارات كلها قراءة/كتابة داخلية بلا مسار إرسال).

---

## 6. أخطاء الـ Console / الشبكة

- كل استدعاءات API الوظيفية أعادت أكواد الحالة المتوقّعة (200/400/401 حسب السيناريو).
- الخطأ الوحيد المعروف في الواجهة = فشل SignalR negotiation (سابق، غير متعلّق بحزمة التقويم).
- لا أخطاء 500 ولا استثناءات خادميّة أثناء أي مرحلة.

---

## 7. تنظيف بيانات UAT — قبل/بعد (Baselines)

| العدّاد | الأساس (قبل UAT) | أثناء UAT | بعد التنظيف | الحالة |
|---|---|---|---|---|
| `AspNetUsers` | 35 | 42 | **35** | ✅ مُستعاد |
| مستخدمو CAL-UAT | 0 | 7 | **0** | ✅ |
| `report_submissions` | 35 | 37 | **35** | ✅ مُستعاد |
| `kpi_evaluations` | 1 | 2 | **1** | ✅ مُستعاد |
| `kpi_evaluation_review_events` | 0 | 1 | **0** | ✅ مُستعاد |
| عدد الهجرات `__EFMigrationsHistory` | 27 | 27 | **27** | ✅ ثابت |

**ما حُذف (معاملة واحدة، ترتيب آمن للـ FK):** 4 kpi_results + 1 review_event + 1 KPI eval (5c7437be) + 2 مسودّات يومية (07-11 السبت، 07-15 اليوم) + قيمها + 2 notifications + 2 audit_logs + 15 refresh_tokens + 7 AspNetUserRoles + 7 CAL-UAT users (الأسبوعيون الخمسة + مبيعات + الأدمن المؤقت).

**استعادة مفاتيح Seed:** استُعيد `/etc/khubara-reporting-rc.env` من النسخة الاحتياطية (Seed__AdminEmail/Password فارغان كالأصل) ثم أُعيد تشغيل الخدمة → **الأدمن المؤقت لم يُعَد بذره** (caluat=0 بعد إعادة التشغيل)، health=200.

**نظافة الخادم:** حُذفت كل سكربتات UAT (`/root/cal-*.mjs`) وملفات SQL المؤقتة.

---

## 8. الحكم (Go / No-Go للإنتاج)

**GO مشروط** — الحزمة اجتازت كل معايير القبول على RC:
- تصحيح السياسة اليومية (الجمعة عطلة وحيدة، السبت عمل) مُثبَت خادميًّا وواجهيًّا.
- الأسبوعي + KPI + اليومي: كل الفحوص الجوهرية خضراء (19 + 8 + 21 + 7).
- لا تسريب استراتيجيّ، لا Migration جديدة، لا مساس بالإنتاج/TEST.
- البيئة نظيفة والأساس مُستعاد بالكامل.

**⛔ لا نشر على الإنتاج قبل موافقة صريحة جديدة.**

---

## 9. خطة الرجوع (Rollback) لو نُشر على الإنتاج مستقبلًا

1. **Backend/Frontend:** استعادة نُسخ `publish-backup`/`dist-backup` الخاصة بنافذة النشر + `chown www-data` + `systemctl restart`.
2. **قاعدة البيانات:** لا هجرة جديدة في هذه الحزمة ⇒ **لا عكس مخطط**. الحزمة code-only (منطق تقويم في Application/Infrastructure + مكوّنات واجهة)، فالرجوع = إرجاع الـ DLLs/bundle فقط.
3. **التحقّق بعد الرجوع:** health=200، عدد الهجرات ثابت، إنشاء تقرير/تقييم بالسلوك القديم.

---

**النهاية — بانتظار موافقة الإنتاج الصريحة قبل أي خطوة تالية.**
