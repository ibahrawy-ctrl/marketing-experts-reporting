# RC-SCHEMA-MIGRATION-AND-DATA-PRESERVATION-REPORT
## RECONCILE-PROD-DEVELOP-LINEAGE — تطبيق الهجرات وحفظ البيانات على RC (Phase S)

**التاريخ:** 16 أغسطس 2026 · **البيئة:** RC (`reporting_rc`) · **الإنتاج: لم يُمسّ.**

---

## 1) تطبيق الهجرات

الهجرات تُطبَّق تلقائيًّا عند الإقلاع (`db.Database.MigrateAsync()`)، وهي **إضافيّة بحتة**.
بعد الجسر أقلع RC بلا تصادم ولا استثناء.

| القياس | القيمة |
|---|---|
| هجرات قبل النشر | 30 |
| صفّا الجسر | 2 |
| هجرات مطبَّقة فعليًّا | 8 |
| **الإجمالي بعد النشر** | **40** ✅ |
| رأس السجلّ | `20260811142239_AddProject360Foundation` |
| تصادم هجرات (`42P07`) | **0** |

## 2) بصمة المخطَّط — RC مقابل TEST بنفس الطريقة تمامًا

قِيست البصمة بنفس السكربت (`/root/mhbridge/fingerprint.sql` → `md5`) على القاعدتَين في
نفس اللحظة، لا بثابت محفوظ من تشغيل سابق:

| القاعدة | البصمة | الجداول | الأعمدة | الهجرات |
|---|---|---|---|---|
| `reporting_rc` | `3b3eb6b04fc0e6b1898468bd2cfed546` | 78 | 928 | 40 |
| `reporting_test_uat` | `3b3eb6b04fc0e6b1898468bd2cfed546` | 78 | 928 | 38 |
| **الفرق البنيويّ (`diff` سطريّ)** | **0 سطر** | 0 | 0 | 2 (صفّا الجسر) |

**Schema Fingerprint = MATCH.** الفارق الوحيد بين البيئتَين صفّان في *سجلّ* الهجرات لا في
*المخطَّط*؛ ولذلك خرجت البصمة متطابقة حرفيًّا رغم اختلاف عدّ السجلّ.

الدليل: `/root/backups/20260816-rc-deploy/rc-vs-test-schema-fingerprint.txt`.

## 3) حفظ البيانات — بالمقارنة بخطّ أساس ما قبل النشر لا بثوابت

خطّ الأساس مستخرَج من قياس ما قبل النشر المحفوظ في
`/root/backups/20260816-rc-deploy/baseline-counts.env` (ومعه نسخة `pg_dump` كاملة).

| الجدول | قبل النشر | بعد النشر والتنظيف | الحكم |
|---|---|---|---|
| `AspNetUsers` | 36 | 36 | ✅ محفوظ |
| `clients` | 8 | 8 | ✅ محفوظ |
| `projects` | 32 | 32 | ✅ محفوظ |
| `report_templates` | 41 | 41 | ✅ محفوظ |
| `report_submissions` | 39 | 39 | ✅ محفوظ |
| `email_outbox` | 0 | 0 | ✅ محفوظ |
| `client_documents` | 0 | 0 | ➖ جدول أنشأه هذا النشر |
| `client_document_versions` | 0 | 0 | ➖ جدول أنشأه هذا النشر |
| `execution_taxonomy_values` | 0 | 208 | ➖ جدول أنشأه هذا النشر (تمهيد الكتالوج) |
| ملفّات التخزين (`md5` تجميعيّ) | `d41d8cd9…8427e` | `d41d8cd9…8427e` | ✅ مطابق |

**Unexpected Data Loss = 0.** الجداول المعلَّمة ➖ لا خطّ أساس لها لأنّها لم تكن موجودة قبل
النشر، فقياس «الحفظ» عليها **غير منطبق** لا «ناجح» — وهذا ما تُخرِجه البوّابة صراحةً كـ`N/A`.

## 4) تمهيد الكتالوج (Execution Taxonomy)

| القياس | القيمة |
|---|---|
| مجموع النطاقات المعنيّة (`strategy_section`/`strategy_field`/`contract_deliverable`) | **38** |
| تكرارات (`Domain`,`Code`) | **0** |
| إجمالي صفوف الكتالوج | 208 |

## 5) الحكم

**RC Migrations = PASS · Schema Fingerprint = MATCH · Unexpected Data Loss = 0 · Migration Collision = 0.**
