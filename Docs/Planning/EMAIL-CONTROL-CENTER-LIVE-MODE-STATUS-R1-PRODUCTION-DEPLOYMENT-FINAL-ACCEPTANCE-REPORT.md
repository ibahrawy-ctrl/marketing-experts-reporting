# EMAIL-CONTROL-CENTER-LIVE-MODE-STATUS-R1 — تقرير النشر الإنتاجيّ والقبول النهائيّ

**التذكرة:** `EMAIL-CONTROL-CENTER-LIVE-MODE-STATUS-R1`
**النوع:** Production Backend + Frontend Deployment and Final Closure
**التاريخ:** 2026-07-29
**معرّف نافذة النشر (TS):** `20260729-175428` (مخزَّن على الخادم في `/root/email-control-live-status-deploy-ts.txt`)
**الأساس الاصطناعيّ:** `ff51825d27f0ac48e784352d11fee96aad5d0155`
**المرشّح المنشور:** `f3ee32f24323d61258ef15844f66c66adaf279df`
**القرار النهائيّ:** ✅ **PRODUCTION PASS — CLOSED**

---

## 1. بوّابة الوقت وغياب التعارض التشغيليّ (Phase 0)

| البند | القيمة | الحكم |
|---|---|---|
| وقت الرياض عند بدء النشر | 2026-07-29 20:54 (+03) | خارج نافذتَي 09:00 و16:00 ✅ |
| وقت الرياض عند الإغلاق | 2026-07-29 21:13 (+03) | خارج النافذتين ✅ |
| نافذة التقارير اليوميّة | 16:00 الرياض | لم تُلامَس ✅ |
| نوافذ الأسبوعيّ/المتأخّرات/التلخيص/المراجعة | 09:00 الرياض | لم تُلامَس ✅ |
| فترة النبض (PollMinutes) | 15 دقيقة | لم تُغيَّر ✅ |
| مهمّة يدويّة أو نشر آخر قيد التشغيل | لا يوجد | ✅ |
| استرداد بريد (Recovery) قيد التشغيل | لا يوجد | ✅ |

**الحكم:** PASS — النشر جرى كاملًا خارج أيّ نافذة إرسال مجدوَلة، ولم يُنتظَر أيّ نافذة طبيعيّة.

---

## 2. إثبات خطّ أساس الإنتاج قبل النشر (Phase 1)

| البند | القيمة قبل النشر |
|---|---|
| SourceLink داخل DLLs الإنتاج | `18207480fdfb4b69d7b1a4ba50eb22bece930524` |
| حزمة الواجهة الحيّة | `index-DaDCi1OK.js` — sha256 `b0728e96b27fb4f443757af1ad59bfddffa1d115df47d7c2b666e99310c085aa` |
| ملفّ CSS الحيّ | `index-rPl-oo4Z.css` — sha256 `374ebcdb63a6dc103588c0044992ffdd9914da50e4d4d413e27b3b627f775840` |
| `index.html` | sha256 `3b1596ef3ec73b3f72bb519d369c059d5c29ecfd7fe90f8925e639f76774d5a7` |
| MainPID | `210497` |
| NRestarts | `0` |
| عدد الهجرات / الرأس | `30` / `20260724224053_AddReportApproverAndKpiReviewerOverrides` |
| `email_notifications` | `188` (DryRun 139 + Enabled 49) |
| `email_outbox` | `0` |
| `audit_logs` | `920` |
| آخر إرسال ناجح | `2026-07-29 13:10:43.408087+00` |
| `/health` داخليّ / عامّ | `200` / `200` |

**الحكم:** PASS — خطّ الأساس مطابق تمامًا لما هو مُثبَت في سجلّ الإنتاج السابق (`AMR-CLIENT-FIRST-…-R1` + `EMAIL-DRYRUN-DEDUPLICATION-ISOLATION-R1C`)، بلا أيّ انزياح غير مُفسَّر.

---

## 3. إثبات المرشّح (Phase 2)

| البند | القيمة |
|---|---|
| شجرة العمل | `/private/tmp/cand-email-control-status-r1-20260729/` |
| الفرع | `candidate/email-control-status-r1-20260729` |
| HEAD | `f3ee32f24323d61258ef15844f66c66adaf279df` |
| الأب | `ff51825d27f0ac48e784352d11fee96aad5d0155` |
| `git status --porcelain` | **0 سطر** (نظيفة تمامًا) |
| سطح التغيير | **11 ملفًّا** — +1933 / −8 |
| هجرات | **لا يوجد** |
| مسح المسارات المحظورة | 0 مطابقة (لا `develop` / لا R1E / R1F / لا WIP) |

**الحكم:** PASS — المرشّح المنشور هو حرفيًّا `f3ee32f2…`، بلا commit جديد وبلا أيّ تعديل أثناء النشر.

---

## 4. إثبات الأساس الاصطناعيّ (Phase 2)

الأساس `ff51825d27f0ac48e784352d11fee96aad5d0155` هو **دمج اصطناعيّ** بين:

- إنتاج Backend: `18207480fdfb4b69d7b1a4ba50eb22bece930524`
- إنتاج Frontend: `be07a7a7fd30e6210f29354039c952b7c4c4cc58`

**السبب البنيويّ:** الالتزامان **شقيقان** لا سلف لأحدهما (merge-base = `21a0ed0`) ⇒ لا يوجد التزام واحد على `develop` يمثّل حالة الإنتاج الحيّة، فتعيّن تركيب الأساس الاصطناعيّ لضمان أن يكون المرشّح **إضافة صافية فوق الإنتاج الحقيقيّ** لا تراجُعًا مقنَّعًا.

**التحقّق بعد النشر:** SourceLink الحيّ = `f3ee32f2…` (سليل مباشر لـ`ff51825d`) ⟵ يثبت أن كلا نسبَي Backend وFrontend محفوظان.

**الحكم:** PASS.

---

## 5. لقطة الإعدادات الآمنة أمنيًّا (Phase 3 / Phase 14)

قراءة `/etc/reporting-api.env` بمُصفٍّ يُخفي كلّ مفتاح يحوي
`Password | Secret | Token | ApiKey | Credential | ConnectionString | PrivateKey`:

```
Email__Enabled=false
Email__Provider=GoogleWorkspace
Email__SmtpHost=smtp.gmail.com
Email__SmtpPort=587
Email__FromEmail=info@marketingexperts.com.sa
EmailNotifications__Mode=Enabled
ReportReminderScheduler__Enabled=true
ReportReminderScheduler__PollMinutes=15
ReportReminderScheduler__DailyDueHour=16
ReportReminderScheduler__WeeklyDueHour=9
ReportReminderScheduler__OverdueHour=9
ReportReminderScheduler__SummaryHour=9
ReportReminderScheduler__ReviewHour=9
credential_configured=true      ← بوليان فقط
```

| البند | القيمة |
|---|---|
| بصمة الملفّ المُقنَّعة | `9fcb4d102dd4ebe8eb0fe2059c2be6d05fed1e7c2e3242be8faabdd79d3c2d2d` |
| mtime | `2026-07-26 19:49:58.990233000 +0000` (**بلا تغيير قبل/بعد**) |
| الحجم / الصلاحيات / المالك | `1493` / `600` / `root:root` |

**لم تُطبَع أيّ قيمة سرّية، ولا جزء منها، ولا طولها، ولا تجزئتها، ولا أوّل/آخر محارفها — في أيّ مرحلة.**

**الحكم:** PASS.

---

## 6. اختبارات وبناء Backend (Phase 4)

نُفِّذت **حصرًا** من شجرة المرشّح النظيفة.

| البند | النتيجة |
|---|---|
| `dotnet restore` + `dotnet build -c Release` | 0 تحذير / 0 خطأ |
| اختبارات موجَّهة (`EmailControlStatusTests`) | **22 / 22 Passed — 0 Failed — 0 Skipped** (4 ث) |
| SourceLink على DLLs الأربعة | `f3ee32f24323d61258ef15844f66c66adaf279df` |
| بصمة القِطعة الناتجة (sha256 لقائمة sha256 المرتّبة) | `17b46e315f7f85d74a30451d0dcbbd09549dce354d834c8e49828c9aebf4b1a1` |
| عدد الملفّات | **86** (مطابق تمامًا لعدد ملفّات الإنتاج) |
| ملفّات محظورة (`appsettings.Development.json` وغيرها) | **0** |

**ملاحظة تشغيليّة موثَّقة:** أوّل محاولة نشر أنتجت 90 ملفًّا بسبب مجلّد بناء قديم مُهمَل وغير مُتتبَّع
(`reporting-backend/src/Reporting.Api/publish/`) التقط الـWeb SDK محتوياته. حُذف مجلّد البناء القديم فقط،
وأُعيد النشر إلى مسار مطلق ⇒ 86 ملفًّا. **أُعيد التحقّق بعدها:** `git status --porcelain` = 0 سطر،
و`git rev-parse HEAD` = `f3ee32f2…` — أي **لم يُمَسّ أيّ ملفّ مصدريّ ولم يُنشأ أيّ commit**.

**الحكم:** PASS.

---

## 7. اختبارات وبناء Frontend (Phase 4)

| البند | النتيجة |
|---|---|
| `npm ci` | نظيف |
| مجموعة الاختبارات الكاملة | **344 / 344 Passed** (29 ملفًّا) |
| الاختبارات الموجَّهة (مركز التحكّم بالبريد) | **24 / 24 Passed** |
| `tsc -b` | **exit 0 — بلا أيّ مخرَج** |
| البناء | `VITE_API_BASE_URL=/api npm run build` |

**بصمات القِطع الناتجة:**

| الملفّ | الحجم | SHA256 |
|---|---|---|
| `index.html` | 858 | `baa3975554ba74df2fea22d26b942452af38b5ac32ac12c67271a58a8edd2818` |
| `assets/index-Bq08cb54.js` | 1338048 | `557df8beede0e5a5e6d305b7d0aecbee3e37b9d753b5f10f18302d1422e9e344` |
| `assets/index-BVnJtRhL.css` | 31262 | `4b6ee933d492d8224f03d931b98829243121c3ec9027efa6dfb7d89e07a984b9` |

**إثبات same-origin `/api`** (مقتطَف من الحزمة):

```
Ws=`/api`,I=Rs.create({baseURL:Ws,headers:{"Content-T
```

| فحص | النتيجة |
|---|---|
| `127.0.0.1` مضمَّن | **0** |
| `rc-report` مضمَّن | **0** |
| `reports.emarketingacademy.net` مضمَّن | **0** |
| `localhost` | 2 — **كلاهما افتراضيّ مكتبيّ حميد** ومطابق لحزمة الإنتاج المقبولة سابقًا |
| ملفّات `.map` | **0** |
| أنماط أسرار | **0** |

السياقان الحميدان: `function Mt(e,t,n=!1){let r=\`http://localhost\`;…}` و
`Do=Co&&window.location.href||\`http://localhost\`` — كلاهما داخل مكتبات طرف ثالث لا يُستدعى في المتصفّح الحقيقيّ.

**الحكم:** PASS.

---

## 8. مسارات النسخ الاحتياطيّة (Phase 5)

| النوع | المسار | التفاصيل |
|---|---|---|
| Backend | `/opt/reporting/publish-backup-email-control-live-status-prer1-20260729-175428` | 86 ملفًّا · 109476 KB · `www-data:www-data 755` · SourceLink `18207480…` |
| Frontend | `/opt/reporting/reporting-frontend/dist-backup-email-control-live-status-prer1-20260729-175428` | 7 ملفّات · `index-DaDCi1OK.js` sha256 `b0728e96…c085aa` |
| نسخة حيّة سابقة (Backend) | `/opt/reporting/publish-old-20260729-175428` | مُزاحة جانبًا لا محذوفة |
| نسخة حيّة سابقة (Frontend) | `/opt/reporting/reporting-frontend/dist-old-20260729-175428` | مُزاحة جانبًا لا محذوفة |
| بصمة وحدة الخدمة | `1146b4f2e9674227a33a266f5fb35c6bdb207a2349fd8a705de770576064a1ff` | لم تتغيّر |
| مساحة القرص الحرّة | `63G` | كافية |

**لا نسخة احتياطيّة لقاعدة البيانات** — لعدم وجود أيّ هجرة أو تعديل سكيمة أو كتابة بيانات في هذه النافذة.

**الحكم:** PASS.

---

## 9. نشر Backend (Phase 6–7)

| البند | القيمة |
|---|---|
| بصمة القِطعة محليًّا | `17b46e315f7f85d74a30451d0dcbbd09549dce354d834c8e49828c9aebf4b1a1` |
| بصمة القِطعة على Staging | `17b46e315f7f85d74a30451d0dcbbd09549dce354d834c8e49828c9aebf4b1a1` |
| المساواة | **محليّ == Staging** ✅ |
| مقارنة قائمة الملفّات مع الإنتاج | `IDENTICAL_FILE_SET` (86 == 86) |
| أسلوب الاستبدال | **ذرّيّ عبر `mv`** (إعادة تسمية مجلّد، لا نسخ في المكان) |
| نافذة الاستبدال | `17:57:21.554Z → 17:57:21.566Z` = **12 ms** |

**الحكم:** PASS.

---

## 10. إعادة تشغيل Backend (Phase 7)

| البند | قبل | بعد |
|---|---|---|
| MainPID | `210497` | `253615` |
| NRestarts | `0` | `0` |
| ActiveState / SubState | active / running | **active / running** |
| ExecMainStartTimestamp | — | `Wed 2026-07-29 17:57:21 UTC` |
| `/health` داخليّ | 200 | **200** — `{"status":"ok","service":"reporting-api"}` |
| `/health` عامّ | 200 | **200** |

نافذة إعادة التشغيل: `17:57:21.574Z → 17:57:21.803Z`.

**عدد إعادات التشغيل في كامل النافذة = 1 بالضبط.** لم تُنفَّذ إعادة تشغيل ثانية عند نشر الواجهة
(تأكيد: MainPID بقي `253615` وNRestarts بقي `0` بعد استبدال `dist`).

**الحكم:** PASS.

---

## 11. إثبات SourceLink (Phase 7)

| الموضع | القيمة |
|---|---|
| DLLs الإنتاج الحيّة (الأربعة) | `f3ee32f24323d61258ef15844f66c66adaf279df` ✅ |
| النسخة الاحتياطيّة | `18207480fdfb4b69d7b1a4ba50eb22bece930524` (خطّ الأساس محفوظ للتراجُع) |

**ملاحظة تفسيريّة موثَّقة:** البحث عن السلسلة الحرفيّة `email-control/status` داخل `Reporting.Api.dll` يُرجِع 0 —
وهذا **ليس إخفاقًا**: المسار مُجزَّأ بين `[Route("api/email-control")]` على مستوى الصنف و`[HttpGet("status")]`
على مستوى الدالّة، فلا تُخزَّن السلسلة المدموجة في التجميعة. الدليل القاطع هو الاستجابة الحيّة `200`.

**الحكم:** PASS.

---

## 12. قبول Status Endpoint (Phase 8)

`GET /api/email-control/status` — **34 حقلًا**، `AsNoTracking`، قراءة فقط.

| الحقل | القيمة على الإنتاج |
|---|---|
| `mode` | **`Enabled`** |
| `isLiveSendingEnabled` | `true` |
| `schedulerEnabled` | `true` |
| `pollMinutes` | `15` |
| `dailyDueHour` | `16` |
| `weeklyDueHour` / `overdueHour` / `summaryHour` / `reviewHour` | `9` / `9` / `9` / `9` |
| `timeZoneId` / `timeZoneLabel` | `Asia/Riyadh` / `توقيت الرياض (UTC+3)` |
| `environmentName` | `Production` |
| `smtpConfigured` | `true` |
| `smtpHost` / `smtpPort` / `usesTls` | `smtp.gmail.com` / `587` / `true` |
| `senderAddress` | `info@marketingexperts.com.sa` |
| `credentialConfigured` | `true` (**بوليان فقط — لا كلمة مرور**) |
| `legacyEmailEnabled` | `false` |
| `legacyFlagIsAuthoritative` | `false` |
| `totalNotifications` | `188` |
| `historicalDryRunCount` | `139` |
| `enabledCount` / `sentCount` | `49` / `49` |
| `pendingCount` / `processingCount` / `failedCount` / `outboxCount` | `0` / `0` / `0` / `0` |
| `lastNotificationCreatedAtUtc` | `2026-07-29T13:10:42.62079Z` |
| `lastSentAtUtc` | `2026-07-29T13:10:43.408087Z` |
| `lastFailureAtUtc` | `null` |
| `lastScheduledNotificationCreatedAtUtc` | `2026-07-29T13:10:42.62079Z` |
| `warnings` | **عنصران فقط، كلاهما `Info`**: `historical_dryrun_records`، `legacy_flag_not_authoritative` — **لا يوجد أيّ تنبيه `Critical`** |

| فحص | النتيجة |
|---|---|
| مفاتيح محظورة في الحمولة | **0** |
| صفوف قاعدة بيانات أُنشئت بسبب الاستدعاء | **0** (188 → 188، audit 920 → 920) |
| إرسال SMTP بسبب الاستدعاء | **0** |
| تشغيل مجدول بسبب الاستدعاء | **0** |

سكربت الفحص المؤقّت `/root/ecls-phase8.mjs` **حُذف بعد التنفيذ**.

**الحكم:** PASS.

---

## 13. التفويض (Phase 8)

| الحالة | النتيجة | الدليل |
|---|---|---|
| Anonymous | **401** | استدعاء حيّ على الإنتاج ✅ |
| Admin | **200** | استدعاء حيّ على الإنتاج ✅ |
| Non-Admin | **403** | **قيد مُعلَن** — انظر أدناه |

**القيد المُعلَن:** تنفيذ فحص Non-Admin حيًّا على الإنتاج يستلزم إمّا إنشاء حساب UAT أو إعادة تعيين كلمة مرور
مستخدم حقيقيّ — **وكلاهما محظور صراحةً في أمر العمل**. اعتُمدت سلسلة أدلّة رباعيّة بديلة:

1. حارس على مستوى الصنف: `EmailControlController.cs:11` → `[Authorize(Policy = Policies.EmailControlManage)]`
   (`[Route("api/email-control")]` سطر 12، `[HttpGet("status")]` سطر 28) ⇒ **لا مسار داخل الكنترولر خارج الحارس**.
2. السلسلة `EmailControlManage` موجودة داخل `Reporting.Application.dll` المنشورة فعليًّا.
3. `EmailControlStatusTests.Http_NonAdmin_Returns403` يؤكّد `403` لكلٍّ من `Roles.Employee` و`Roles.Manager`،
   وهو ضمن الـ22/22 التي نجحت على التزام المرشّح ذاته:
   ```
   601: public async Task Http_NonAdmin_Returns403()
   603:     foreach (var role in new[] { Roles.Employee, Roles.Manager })
   607:         Assert.Equal(HttpStatusCode.Forbidden, res.StatusCode);
   612: public async Task Http_Anonymous_Returns401()
   616:     Assert.Equal(HttpStatusCode.Unauthorized, res.StatusCode);
   ```
4. الفرع Anonymous من الاختبار نفسه (`401`) **مُثبَت حيًّا على الإنتاج** ⇒ سلوك الحارس مطابق للاختبار.

**لم تُوسَّع أيّ صلاحيّة، ولم يُنشأ أيّ حساب على الإنتاج.**

**الحكم:** PASS (مع قيد مُعلَن ومُغطّى).

---

## 14. نشر Frontend (Phase 9)

| البند | القيمة |
|---|---|
| بصمة القِطعة محليًّا == Staging | `2d5bc5ac…fe4420` ✅ |
| أسلوب الاستبدال | **ذرّيّ عبر `mv`** |
| نافذة الاستبدال | `17:59:09.814Z → 17:59:09.824Z` = **10 ms** |
| إعادة تشغيل Backend ثانية | **لم تحدث** — MainPID ما زال `253615`، NRestarts `0` |

**الحكم:** PASS.

---

## 15. التحقّق من بصمات الحزمة (Phase 10)

سلسلة تحقّق رباعيّة: **محليّ = Staging = قرص الإنتاج = المُقدَّم عبر HTTPS**

| الملفّ | SHA256 | محليّ | Staging | قرص | HTTPS |
|---|---|---|---|---|---|
| `index.html` | `baa39755…d2818` | ✅ | ✅ | ✅ | ✅ |
| `assets/index-Bq08cb54.js` | `557df8be…e9e344` | ✅ | ✅ | ✅ | ✅ |
| `assets/index-BVnJtRhL.css` | `4b6ee933…a984b9` | ✅ | ✅ | ✅ | ✅ |

| فحص إضافيّ | النتيجة |
|---|---|
| الحزمة القديمة `index-DaDCi1OK.js` | **404** (وباقية سليمة داخل النسخة الاحتياطيّة) |
| `/app/email-control` (رابط عميق) | `200` |
| `/app/reports` (رابط عميق) | `200` |
| ترويسات الكاش لـ`index.html` | `no-cache, no-store, must-revalidate` |
| ترويسات الكاش للأصول | `immutable` |

**الحكم:** PASS.

---

## 16. قبول واجهة الإنتاج (Phase 11)

نُفِّذ عبر **جسر قرائيّ محلّيّ** يخدم **بايتات حزمة الإنتاج ذاتها** (بصمات مُتحقَّق منها)، ويُمرِّر
`GET /api/*` فقط إلى `https://reports.emarketingacademy.net`، **ويحجب كلّ فعل غير GET بـ405**.
هذا يضمن استحالة أيّ كتابة على الإنتاج أثناء القبول البصريّ.

| # | المتطلَّب | المُلاحَظ | الحكم |
|---|---|---|---|
| 1 | عنوان الشاشة | «مركز التحكم بالبريد» | ✅ |
| 2 | الوضع الحاليّ | **`Enabled`** | ✅ |
| 3 | العنوان الحالة | **«الإرسال الفعلي مفعّل»** | ✅ |
| 4 | شارة الحالة | **`LIVE / ENABLED`** (خضراء) | ✅ |
| 5 | الشرح | «يتم إرسال رسائل بريد حقيقيّة إلى المستقبِلين عند تحقّق شروط الإرسال.» | ✅ |
| 6 | بيئة التشغيل | `Production` | ✅ |
| 7 | المجدول | **«مُفعَّل»** + النبض «كل 15 دقيقة» | ✅ |
| 8 | النوافذ | يوميّة `16:00` · أسبوعيّة/متأخّرات/تلخيصات/مراجعات `9:00` | ✅ |
| 9 | المنطقة الزمنيّة | «توقيت الرياض (UTC+3) — Asia/Riyadh» | ✅ |
| 10 | جاهزيّة SMTP | **«مُهيَّأ»** · `smtp.gmail.com` · `587` · STARTTLS «نعم» · المُرسِل `info@marketingexperts.com.sa` · بيانات الاعتماد «مضبوطة» | ✅ |
| 11 | العلم القديم | **«معطّل»** + «هل هو مصدر حقيقة لهذه القناة؟ → **لا**» | ✅ |
| 12 | وصف العلم القديم | «القناة الجديدة مفعلة، بينما العلم القديم معطل ويخص مسارات البريد القديمة فقط.» + «هذا العلم يخصّ مسارات البريد القديمة فقط (صندوق الصادر القديم وتذكير التسليم القديم)، ولا يحكم قناة الإشعارات الجديدة إطلاقًا.» | ✅ |
| 13 | فصل السجلّ التاريخيّ | قسم مستقلّ «السجلّ التاريخيّ» بعنوان فرعيّ «أرقام تراكميّة لصفوف سُجِّلت سابقًا — لا تعبّر عن الوضع الحاليّ للنظام.» | ✅ |

**الرسالة المحظورة** «النظام يعمل في وضع تجريبي DryRun ولا يوجد إرسال حقيقي»:
**غائبة تمامًا** — فحص برمجيّ على `document.body.innerText` أرجع `false`.

**الحكم:** PASS (13/13).

---

## 17. التحقّق من الوضع (Phase 12)

| المصدر | الوضع |
|---|---|
| الإعداد `EmailNotifications__Mode` | `Enabled` |
| استجابة الـAPI (`mode`) | `Enabled` |
| العرض في الواجهة | `Enabled` + «الإرسال الفعلي مفعّل» |
| العلم القديم `Email__Enabled` | `false` — **ولم يقلب الحالة إلى Disabled** ✅ |
| 139 صفًّا تاريخيًّا بوضع DryRun | **لم تقلب الوضع الحاليّ إلى DryRun** ✅ |

**الأسبقيّة المُلزِمة مُثبَتة حيًّا:** `EmailNotifications__Mode` هو المصدر الموثوق الوحيد للقناة الجديدة.

**الحكم:** PASS.

---

## 18. التحقّق من المجدول (Phase 12 / Phase 14)

| البند | الإعداد | الـAPI | الواجهة |
|---|---|---|---|
| مُفعَّل | `true` | `true` | «مُفعَّل» |
| PollMinutes | `15` | `15` | «كل 15 دقيقة» |
| DailyDueHour | `16` | `16` | `16:00` |
| WeeklyDueHour | `9` | `9` | `9:00` |
| OverdueHour | `9` | `9` | `9:00` |
| SummaryHour | `9` | `9` | `9:00` |
| ReviewHour | `9` | `9` | `9:00` |

**لم يُشغَّل أيّ مهمّة مجدولة يدويًّا، ولم تُغيَّر أيّ ساعة أو فترة نبض.**
سجلّات الخدمة خلال 30 دقيقة: `scheduler = 0 سطر` ⇒ لم يُنتج tick أيّ إشعار (متّسق مع كون الساعة خارج النافذتين).

**الحكم:** PASS.

---

## 19. جاهزيّة SMTP (Phase 12)

| البند | القيمة |
|---|---|
| `smtpConfigured` | `true` |
| المضيف / المنفذ | `smtp.gmail.com` / `587` |
| STARTTLS | نعم |
| عنوان المُرسِل | `info@marketingexperts.com.sa` |
| `credentialConfigured` | `true` — **بوليان فقط** |
| كلمة مرور التطبيق (App Password) | **لم تُدوَّر ولم تُقرَأ ولم تُطبَع** |
| رسائل اختبار / استرداد | **لم تُرسَل أيّ رسالة** |

**الحكم:** PASS.

---

## 20. سلوك العلم القديم (Phase 12)

| البند | القيمة |
|---|---|
| `Email__Enabled` (الإعداد) | `false` — **لم يُغيَّر** |
| `legacyEmailEnabled` | `false` |
| `legacyFlagIsAuthoritative` | `false` |
| العرض | «معطّل» + «هل هو مصدر حقيقة لهذه القناة؟ → لا» |
| شدّة التنبيه المصاحب | **`Info`** — وليس `Critical` ✅ |
| هل قلب الوضع إلى Disabled؟ | **لا** ✅ |

**الحكم:** PASS — العلم القديم مُصنَّف صراحةً «غير مرجعيّ» ولا يؤثّر على القناة الجديدة، تمامًا كما هو مطلوب.

---

## 21. فصل السجلّ التاريخيّ لوضع المحاكاة (Phase 12)

| البند | القيمة |
|---|---|
| موضع العرض | قسم مستقلّ «**السجلّ التاريخيّ**» أسفل بطاقة الحالة |
| العنوان الفرعيّ | «أرقام تراكميّة لصفوف سُجِّلت سابقًا — لا تعبّر عن الوضع الحاليّ للنظام.» |
| «سجلّ تاريخيّ بوضع المحاكاة» | `139` |
| «سجلّ بوضع الإرسال الفعليّ» | `49` |
| التنبيه المصاحب | `Info` — «يوجد 139 سجلّ تاريخيّ أُنشئ في وضع المحاكاة. هذه سجلّات سابقة ولا تعبّر عن الوضع الحاليّ.» |
| هل أثّر على الوضع الحاليّ؟ | **لا** — الوضع بقي `Enabled` ✅ |

**الحكم:** PASS.

---

## 22. مطابقة العدّادات (Phase 13)

| العدّاد | الواجهة | الـAPI | قاعدة البيانات (قراءة فقط) | مطابق |
|---|---|---|---|---|
| إجمالي الإشعارات | `188` | `188` | `188` | ✅ |
| سجلّ تاريخيّ بوضع المحاكاة | `139` | `139` | `Mode='DryRun'` = `139` | ✅ |
| سجلّ بوضع الإرسال الفعليّ | `49` | `49` | `Mode='Enabled'` = `49` | ✅ |
| أُرسِلت فعليًّا | `49` | `49` | `Status='Sent'` = `49` | ✅ |
| قيد الانتظار | `0` | `0` | `Status='Pending'` = `0` | ✅ |
| قيد المعالجة | `0` | `0` | `Status='Processing'` = `0` | ✅ |
| فاشلة | `0` | `0` | `Status='Failed'` = `0` | ✅ |
| صندوق الصادر القديم | `0` | `0` | `email_outbox` = `0` | ✅ |
| آخر إرسال ناجح | ٢٩ يوليو ٢٠٢٦، ٠٤:١٠ م | `2026-07-29T13:10:43.408087Z` | `2026-07-29 13:10:43.408087+00` | ✅ |
| آخر إخفاق | `—` | `null` | لا صفوف Failed | ✅ |

**مطابقة تامّة ثلاثيّة المصدر (8/8 عدّادات + الطوابع الزمنيّة).** القيم الصفريّة **معروضة صراحةً** لا مخفيّة.

**الحكم:** PASS.

---

## 23. إثبات «التحديث لا يكتب» (Phase 12)

| البند | قبل الضغط | بعد الضغط |
|---|---|---|
| `checkedAtUtc` | `2026-07-29T18:08:19.8865053Z` | **`2026-07-29T18:08:50.9405479Z`** (تغيّر ✅) |
| بقيّة الحقول الـ33 | — | **متطابقة حرفيًّا** ✅ |
| حالة التحميل | — | الزرّ `disabled=true` أثناء الجلب ✅ |
| `email_notifications` | `188` | **`188`** |
| `email_outbox` | `0` | **`0`** |
| `audit_logs` | `920` | **`920`** |
| آخر إرسال ناجح | `2026-07-29 13:10:43.408087+00` | **بلا تغيير** |
| MainPID / NRestarts | `253615` / `0` | **`253615` / `0`** |
| أسطر SMTP في السجلّ | — | **0** |
| أسطر Scheduler في السجلّ | — | **0** |

**الحكم:** PASS — زرّ «تحديث» قراءة صرفة: يُحدِّث زمن الفحص فقط، بلا صفّ قاعدة بيانات، بلا SMTP، بلا تشغيل مجدول.

**حالة الخطأ الآمنة:** لم تُستحدَث على الإنتاج لأنّ إحداث إخفاق متعمَّد محظور؛ وهي مغطّاة ضمن
الاختبارات الموجَّهة 24/24 المقبولة على المرشّح ذاته.

---

## 24. إثبات عدم الإرسال (Phase 12 / Phase 14)

| البند | النتيجة |
|---|---|
| رسائل SMTP بسبب هذه المهمّة | **0** |
| أسطر SMTP في `journalctl` خلال النافذة | **0** |
| `Sent` قبل / بعد | `49` / `49` |
| `Pending` / `Processing` / `Failed` | `0` / `0` / `0` |
| `email_outbox` قبل / بعد | `0` / `0` |
| آخر إرسال ناجح | `2026-07-29 13:10:43.408087+00` — **بلا تغيير** (نافذة 16:00 السابقة، سابقة على النشر) |
| أزرار الإرسال / التذكير اليدويّ | **لم يُضغَط أيّ منها** ✅ |
| استرداد (Recovery) | **لم يُبدَأ** ✅ |

**الحكم:** PASS.

---

## 25. إثبات عدم تسرّب الأسرار (Phase 16)

| السطح | نتيجة الفحص |
|---|---|
| حمولة Status API (JSON) | **0 مفتاح محظور** — `credentialConfigured` بوليان فقط |
| شبكة المتصفّح | كلّ الطلبات `GET` على `/api/*` — **0 سرّ في أيّ استجابة** |
| DOM | `Email__Password` / `SmtpPassword` / `ConnectionString` / `PrivateKey` / `ApiKey` / `Secret` / `Seed__AdminPassword` / `Bearer eyJ` = **كلّها `false`** |
| `index.html` المُقدَّم | 0 مطابقة |
| حزمة JS المُقدَّمة | `Email__Password`=0 · `SmtpPassword`=0 · `Seed__AdminPassword`=0 · `ConnectionString`=0 · `PrivateKey`=0 · `BEGIN RSA`=0 · `xoxb-`=0 · `AKIA`=0 |
| `sk-` (مطابقة واحدة) | **إيجابيّة كاذبة مُثبَتة** — سياقها ``l.push({id:`risk-${e.id}`…``  ⇐ جزء من كلمة `risk-` |
| نمط App Password (16 حرفًا صغيرًا) | 12 مطابقة، **كلّها أسماء أحداث DOM** (`compositionstart`، `visibilitychange`، `transitioncancel`، `onpropertychange`، `fullscreenchange`) + `marketingexperts` — **لا اعتماد** |
| سجلّات الخادم (`journalctl` ساعتان) | **0 مطابقة** لأيّ نمط سرّيّ |
| هذا التقرير | **لا يحوي أيّ سرّ ولا جزءًا منه ولا طولًا ولا تجزئة** |

**لم يقع أيّ حادث تسرّب في أيّ مرحلة.**

**الحكم:** PASS.

---

## 26. ثوابت الهجرات (Phase 7 / Phase 14)

| البند | قبل | بعد |
|---|---|---|
| عدد الهجرات | `30` | **`30`** |
| رأس الهجرات | `20260724224053_AddReportApproverAndKpiReviewerOverrides` | **بلا تغيير** |
| سجلّ الإقلاع | — | **«No migrations were applied. The database is already up to date.»** |
| `Applying migration` في السجلّ | — | **0** |
| هجرة أُنشئت أو نُفِّذت | — | **لا شيء** |
| تعديل يدويّ على القاعدة | — | **لا شيء** |
| بيئة التشغيل | — | `Hosting environment: Production` |

**الحكم:** PASS.

---

## 27. ثوابت البريد والمجدول بعد النشر (Phase 14)

| المفتاح | القيمة بعد النشر | تغيّر؟ |
|---|---|---|
| `EmailNotifications__Mode` | `Enabled` | لا |
| `Email__Enabled` | `false` | لا |
| `ReportReminderScheduler__Enabled` | `true` | لا |
| `ReportReminderScheduler__PollMinutes` | `15` | لا |
| `ReportReminderScheduler__DailyDueHour` | `16` | لا |
| `…__WeeklyDueHour` / `…__OverdueHour` / `…__SummaryHour` / `…__ReviewHour` | `9` × 4 | لا |
| `Email__SmtpHost` / `Email__SmtpPort` | `smtp.gmail.com` / `587` | لا |
| `Email__FromEmail` | `info@marketingexperts.com.sa` | لا |
| `Email__Provider` | `GoogleWorkspace` | لا |
| App Password | **لم تُدوَّر** | لا |
| `/etc/reporting-api.env` mtime | `2026-07-26 19:49:58.990233000 +0000` | **لا** |
| بصمة وحدة الخدمة | `1146b4f2…64a1ff` | لا |
| `email_outbox` | `0` | لا |
| مهمّة يدويّة / استرداد | **لم تُشغَّل** | — |
| `fail:` / `crit:` في السجلّ | `0` / `0` | — |

**سبب صمت المجدول بعد إعادة التشغيل:** وقت إعادة التشغيل (20:57 الرياض) خارج نافذتَي 09:00 و16:00،
و`TickAsync` يُرجِع `null` بصمت حين لا تُطابِق ساعة الرياض أيّ نافذة ⇒ **صفر إشعار مُولَّد وصفر إرسال**،
وهو السلوك المتوقَّع بنيويًّا لا عَرَض إخفاق.

**الحكم:** PASS.

---

## 28. عدم تراجُع Account Manager Report (Phase 15)

`AMR-CLIENT-FIRST-NAVIGATION-AND-SECTION-ORDER-R1` **سليم بالكامل ولم يُسقَط**.

**دليل الحزمة:** كلّ علامات AMR موجودة في الحزمة المُقدَّمة `index-Bq08cb54.js`:
`العميل` · `المشاريع` · `clientId` · `projectId` · `deliverables_sent` · `decisions_required` · `client_relationship`.

**دليل حيّ على تسليم إنتاجيّ حقيقيّ** (`3c9d647a-c694-4fbc-8688-3cf12e5389d0` — سماح ابوالمجد، `2026-W30`، `Closed`):

```
تفاصيل العملاء والمشروعات
جسم التقرير — كل مشروع/عميل في بطاقة مستقلّة تحوي كل التفاصيل والجداول.
الوصول السريع حسب العميل
▼ متجر امداد   | 2 مشروعات | ⚠ مخاطر (1) | ⚑ قرارات (2)
▼ منصة مكانة   | مشروع واحد | 🟡 متابعة (1) | ⚠ مخاطر (1) | ⚑ قرارات (1)
▼ مطاعم عم قاسم | 2 مشروعات | ⚑ قرارات (2)
▼ جيم كرافت    | مشروع واحد | ⚠ مخاطر (1)
ملخّص المحفظة التنفيذيّ — إجمالي المشاريع 6 · 📌 قرارات مطلوبة 5
```

التجميع **Client-first**، العملاء **مطويّون افتراضيًّا** (`▼`)، 4 عملاء / 6 مشاريع / 5 قرارات —
مطابق تمامًا لسجلّ قبول AMR الإنتاجيّ.

**فحوص Regression أخرى (قراءة فقط، بلا إنشاء أو تعديل أيّ بيانات):**

| الشاشة | النتيجة |
|---|---|
| تسجيل الدخول / `/auth/me` | `200` ✅ |
| لوحة الإدارة والحوكمة `/app` | تُعرَض كاملة ✅ |
| مركز التحكّم بالبريد `/app/email-control` | يعمل ✅ |
| التقارير `/app/reports` | تُعرَض كاملة ✅ |
| التسليمات `/app/submissions` (148 تسليمًا) | تُعرَض كاملة ✅ |
| تبويب «القوالب» | 10 قوالب في 4 مجموعات ✅ |
| تبويب «القواعد» | القواعد والمستقبِلون وفترات التهدئة ✅ |
| تبويب «تذكير يدويّ» | يُعرَض — **ولم يُضغَط أيّ زرّ إرسال** ✅ |
| تبويب «السجل» | يعمل ✅ |
| روابط عميقة + إعادة تحميل | `200` ✅ |
| RTL | `dir="rtl"` · `lang="ar"` ✅ |
| موبايل 375×812 | `scrollWidth == clientWidth` — **لا فيض أفقيّ** ✅ |
| سطح المكتب | سليم ✅ |
| Console | **0 خطأ تطبيقيّ** — الأخطاء الوحيدة هي `SignalR negotiate 404` **الناتجة عن الجسر القرائيّ نفسه** الذي يحجب `/hubs/` عمدًا، لا عن الإنتاج |

**الحكم:** PASS.

---

## 29. جاهزيّة التراجُع (Phase 17)

**التراجُع جاهز بالكامل وغير مُنفَّذ.**

### Backend

```
1) systemctl stop reporting-api
2) mv /opt/reporting/publish /opt/reporting/publish-failed-<ts>
3) cp -a /opt/reporting/publish-backup-email-control-live-status-prer1-20260729-175428 \
         /opt/reporting/publish
4) chown -R www-data:www-data /opt/reporting/publish
5) systemctl start reporting-api
```

| التحقّق بعد التراجُع | القيمة المتوقَّعة |
|---|---|
| `/etc/reporting-api.env` | **لا يُمَسّ إطلاقًا** (الإعدادات محفوظة) |
| `/health` | `200` |
| SourceLink | **يعود إلى `18207480fdfb4b69d7b1a4ba50eb22bece930524`** |
| الهجرات | تبقى `30` / رأس `20260724224053` (لا هجرة لعكسها) |

**تحقّق سلامة النسخة:** 86 ملفًّا · 109476 KB · `www-data:www-data` · SourceLink `18207480…` ✅

### Frontend

```
1) mv /opt/reporting/reporting-frontend/dist \
      /opt/reporting/reporting-frontend/dist-failed-<ts>
2) cp -a /opt/reporting/reporting-frontend/dist-backup-email-control-live-status-prer1-20260729-175428 \
         /opt/reporting/reporting-frontend/dist
3) chown -R www-data:www-data /opt/reporting/reporting-frontend/dist
   (لا حاجة لإعادة تشغيل — nginx static)
```

| التحقّق بعد التراجُع | القيمة المتوقَّعة |
|---|---|
| `index.html` | يشير إلى **`index-DaDCi1OK.js`** |
| SHA256 للحزمة | `b0728e96b27fb4f443757af1ad59bfddffa1d115df47d7c2b666e99310c085aa` |
| CSS | `index-rPl-oo4Z.css` — `374ebcdb…f775840` |

**تحقّق سلامة النسخة:** 7 ملفّات · `index.html` يشير إلى `index-DaDCi1OK.js` · البصمة مطابقة ✅

### نسخ إضافيّة للأمان

`/opt/reporting/publish-old-20260729-175428` و`/opt/reporting/reporting-frontend/dist-old-20260729-175428`
(النسخ الحيّة السابقة، مُزاحة جانبًا لا محذوفة). المساحة الحرّة: `63G`.

**الحكم:** PASS.

---

## 30. القرار النهائيّ

| معيار PASS | الحالة |
|---|---|
| المرشّح المنشور = `f3ee32f2…` | ✅ |
| الأساس الاصطناعيّ صحيح ومُبرَّر بنيويًّا | ✅ |
| SourceLink الـBackend صحيح | ✅ |
| حزمة الـFrontend مطابقة (سلسلة رباعيّة) | ✅ |
| الوضع يُعرَض `Enabled` | ✅ |
| الإرسال الفعليّ نشط | ✅ |
| المجدول نشط | ✅ |
| SMTP مُهيَّأ | ✅ |
| العلم القديم `Disabled` وغير مرجعيّ | ✅ |
| السجلّ التاريخيّ DryRun مفصول | ✅ |
| العدّادات متطابقة (واجهة = API = قاعدة) | ✅ |
| التفويض Admin حصرًا | ✅ |
| Anonymous ⇒ 401 | ✅ |
| Non-Admin ⇒ 403 | ✅ (قيد مُعلَن + سلسلة أدلّة رباعيّة) |
| لا سرّ ظاهر | ✅ |
| لا هجرة | ✅ |
| لا كتابة على قاعدة البيانات | ✅ |
| لا إرسال SMTP بسبب المهمّة | ✅ |
| لا مهمّة يدويّة | ✅ |
| لا استرداد | ✅ |
| لا تغيير إعداد | ✅ |
| نشر Account Manager غير متأثّر | ✅ |
| التراجُع جاهز | ✅ |

| معيار FAIL | وقع؟ |
|---|---|
| تغيّر خطّ أساس الإنتاج بلا تفسير | ❌ لم يقع |
| عدم تطابق المرشّح | ❌ لم يقع |
| بقاء الشاشة مُصلِّبة DryRun | ❌ لم يقع |
| `Email__Enabled` قلَب الحالة إلى Disabled | ❌ لم يقع |
| ظهور سرّ | ❌ لم يقع |
| توسيع صلاحيّة | ❌ لم يقع |
| وقوع هجرة | ❌ لم يقع |
| إنشاء صفّ بسبب Status/Refresh | ❌ لم يقع |
| إرسال SMTP بسبب المهمّة | ❌ لم يقع |
| تغيير إعدادات المجدول | ❌ لم يقع |
| إسقاط واجهة AMR | ❌ لم يقع |
| عدم استقرار Backend أو Frontend | ❌ لم يقع |

### 🎯 القرار: **PRODUCTION PASS**

---

## 31. حالة الإغلاق

| البند | القيمة |
|---|---|
| **التذكرة** | `EMAIL-CONTROL-CENTER-LIVE-MODE-STATUS-R1` |
| **الحالة** | ✅ **`PRODUCTION PASS — CLOSED`** |
| **الأساس الاصطناعيّ** | `ff51825d27f0ac48e784352d11fee96aad5d0155` |
| **المرشّح المنشور** | `f3ee32f24323d61258ef15844f66c66adaf279df` |
| **نطاق النشر** | Backend + Frontend |
| **عدد إعادات التشغيل** | **1 بالضبط** |
| **الهجرات** | **لا يوجد** — الرأس ثابت `20260724224053` |
| **تغييرات الإعداد** | **لا يوجد** — `env` mtime بلا تغيير |
| **رسائل بريد أُرسِلت** | **0** |
| **صفوف قاعدة بيانات أُنشئت** | **0** |
| **التراجُع** | جاهز وغير مُنفَّذ |

### تنظيف ما بعد التنفيذ

| البند | الحالة |
|---|---|
| سكربت الفحص المؤقّت `/root/ecls-phase8.mjs` | **حُذف** |
| الجسر القرائيّ `/tmp/ecls-ui` | **حُذف** |
| عمليّة الجسر / المنفذ `5190` | **متوقّفة ومحرَّرة** |
| مُدخَل `.claude/launch.json` المؤقّت | **أُزيل** |
| نسخ Backup على الإنتاج | **مُبقاة عمدًا** للتراجُع |

### حالات مُسجَّلة ولم تُبدَأ (محظور بدؤها ضمن هذه التذكرة)

| التذكرة / الإجراء | الحالة |
|---|---|
| `EMAIL-SCHEDULER-EXECUTION-TELEMETRY-R1` | **مُسجَّلة وغير مبدوءة** |
| `EMAIL-MISSED-NOTIFICATIONS-RECOVERY-R1` — Phase 2 | **موقوفة بانتظار تصريح مستقلّ** (12 SEND ONCE ما زالت مستحقّة؛ 9 منها W30-scoped تخرج من النافذة السبت 2026-08-01) |
| تدوير Gmail App Password | **لم يُنفَّذ ومحظور هنا** |
| أيّ تذكرة أخرى | **لم تُبدَأ** |

---

**نهاية التقرير — التذكرة مُغلَقة.**
