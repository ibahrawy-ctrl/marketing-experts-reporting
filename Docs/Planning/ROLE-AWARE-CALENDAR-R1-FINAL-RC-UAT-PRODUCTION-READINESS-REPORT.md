# ROLE-AWARE CALENDAR R1 — FINAL RC UAT & PRODUCTION READINESS REPORT

> **الحزمة:** التقويم المُدرِك للدور (Role-Aware Reporting Calendar R1) — أسبوعي + KPI أسبوعي + يوميّ مبيعات.
> **النطاق:** UAT نهائيّ موثَّق على **Release Candidate (RC) فقط** + حكم جاهزية الإنتاج. **بلا نشر/دمج/دفع للإنتاج.**
> **البيئة:** `khubara-reporting-rc.service` @ `http://127.0.0.1:5092` (ASPNETCORE_ENVIRONMENT=ReleaseCandidate)، قاعدة `reporting_rc`، VPS `srv1747233` (`187.127.72.232`).
> **التاريخ المرجعيّ للاختبار:** 2026-07-15 (الأربعاء). الدورة الحالية **2026-W29** (السبت 11 يوليو → الجمعة 17 يوليو).
> **قرار الإنتاج:** لم يُنشَر على الإنتاج ولا على TEST — بانتظار موافقة صريحة جديدة.

---

## 0. الالتزام بالقواعد العشر الإلزامية

| # | القاعدة | الحالة |
|---|---|---|
| 1 | العمل على `reporting_rc` + `khubara-reporting-rc.service` فقط | ✅ |
| 2 | عدم مسّ `reporting_prod` / `reporting-api.service` إطلاقًا | ✅ (لم يُلمَس) |
| 3 | لا نشر جديد (البصمات مطابقة للحزمة المعتمَدة) | ✅ بلا drift |
| 4 | لا Migration ولا تغيير كود | ✅ (migrations=27 ثابتة) |
| 5 | كل مستخدمي/بيانات الاختبار ببادئة `CAL-R1-FINAL-UAT` | ✅ |
| 6 | تسجيل معرّفات كل مستخدم/تقرير/KPI أُنشئ | ✅ (`ids.json`) |
| 7 | Email/Reminders/Scheduler/BackgroundJobs = false | ✅ (email_outbox=0) |
| 8 | عدم طباعة توكنات/كلمات مرور/سلاسل اتصال | ✅ |
| 9 | عدم تعديل/حذف أيّ بيانات حقيقية | ✅ |
| 10 | عند drift الأساس/البصمات ⇒ `STOP — RC STATE DRIFT DETECTED` | ✅ لم يقع drift |

---

## 1. تأكيد حالة RC (Phase 1 — قراءة فقط، GO)

- **البصمات (SHA-256، مختصرة) — مطابِقة للحزمة المعتمَدة (بلا drift قبل/بعد):**

| المكوّن | البصمة |
|---|---|
| `Reporting.Api.dll` | `a6e8ea98…` |
| `Reporting.Application.dll` | `7b0a6900…` |
| `Reporting.Infrastructure.dll` | `aa460ece…` |
| Frontend bundle | `index-B_lNG4Zb.js` |

- **الهجرات:** 27، الرأس `20260713171040_AdminGovernanceReportKpiCorrection` — ثابتة قبل/بعد.
- **الأساس قبل UAT:** 35 users / 35 submissions / 1 kpi_eval / 0 review_events / 0 outbox.
- **الصحّة:** `/health` = 200.

**تصحيح السياسة اليومية المؤكَّد:** أيام العمل = السبت→الخميس؛ **العطلة الأسبوعية الوحيدة = الجمعة فقط**؛ **السبت يوم عمل كامل**.
الكود الحاسم: `ReportingCalendarPolicy.IsDailyHoliday(DateOnly date) => date.DayOfWeek is DayOfWeek.Friday;`

---

## 2. آلية الوصول (Break-Glass Admin) — شرعية وبلا نشر

- سُجِّل أدمن مؤقّت `cal-final-gate-admin@test.local` عبر مفاتيح `Seed__AdminEmail/Password` في env ثم **إعادة تشغيل الخدمة** (IdentitySeeder يبذر عند الغياب). **إعادة التشغيل ليست نشرًا** (لا تغيير بصمات/هجرات).
- استُخدم للقراءة/التشغيل الوظيفيّ فقط. **حُذف بالكامل في التنظيف** وأُفرِغت مفاتيح Seed لمنع إعادة البذر — أُثبِت بعد إعادة التشغيل النهائية (cal-users=0).

---

## 3. نتائج UAT الوظيفيّ على RC الحيّ — 44/44 PASS

### Phase 2 — التقويم الأسبوعيّ (W1–W16) — PASS
- **W1–W5:** `my-cycles` لكل الأدوار (CEO/GM/Manager/TeamLeader/Employee) يُرجِع **نفس مفتاح الدورة الموحّد** `CurrentCycleKey=2026-W29`، بداية **السبت** (getUTCDay=6)، نهاية **الجمعة** (getUTCDay=5).
- **W6–W10 (مواعيد الاستحقاق حسب الدور):** موظف=off4 (الأربعاء 07-15)، قائد=off5 (الخميس 07-16)، مدير=off8 (الأحد 07-19)، GM=off9 (الاثنين 07-20)، CEO=off9 (الاثنين 07-20).
- **W11–W13:** الدورة الماضية W28 = السبت 07-04 → الجمعة 07-10؛ السابقة/التالية محسوبة صحيحًا؛ الدورة التالية W30 **مقفلة** (مستقبل).
- **W14–W16 (تدفّق التقرير الأسبوعيّ — ثبات المفتاح):**
  - مفتاح غير صالح ⇒ `report.cycle_key_invalid` (400).
  - دورة مستقبلية ⇒ `calendar.cycle_not_open` (400).
  - إنشاء مسودّة `periodKey=2026-W29` → حفظ قيمة (42) → **إرسال: المفتاح ثابت 2026-W29**، المعتمِد الأول = **Manager**.
  - اعتماد Manager: **المفتاح ثابت 2026-W29**، الحالة `ApprovedByDirectManager`. ✅ لا انزياح في المفتاح عبر مسودّة→إرسال→اعتماد.

### Phase 3 — KPI الأسبوعيّ (K1–K13) — PASS
- **K1–K2:** سياق KPI يُرجِع نفس بنية/مفاتيح الدورة كسياق التقرير (السبت→الجمعة، نفس CurrentCycleKey).
- **K3–K6 (سلبيّات التحقّق):** صيغة خاطئة ⇒ `kpi_eval.period_format_invalid`؛ دورة غير صالحة W99 ⇒ `kpi.cycle_key_invalid`؛ دورة مستقبلية ⇒ `calendar.cycle_not_open`؛ نوع غير أسبوعيّ (Monthly) ⇒ `kpi_eval.period_type_not_supported` (كلها 400).
- **K7–K8:** إنشاء بالمفتاح الحاليّ → حفظ نتائج (درجات 90/80/100) → **إرسال: UnderReview، المراجِع = GM**، المفتاح ثابت 2026-W29.
- **K9 (فصل الواجبات):** محاولة Manager اعتماد تقييم ليس مراجِعه ⇒ **403** `auth.forbidden`.
- **K10:** اعتماد GM ⇒ **Approved، الدرجة النهائية = 90**، المفتاح ثابت.
- **K11:** التجميع Monthly ⇒ 200 (المتوسّط 90) — التجميع سليم (weekly غير مدعوم للتجميع؛ Monthly/Quarterly/Yearly/Custom).
- **K12 (RBAC حذف إداريّ):** Manager admin-delete ⇒ **403**.
- **K13:** GM admin-delete (حذف ناعم بسبب إلزاميّ) ⇒ **200**.

### Phase 4 — اليوميّ (مبيعات) (D1–D7) — PASS
- **D1:** `my-days` يُرجِع `currentDayKey=2026-07-15`.
- **D2:** الجمعة 07-10 = `isHoliday=true`, `isSelectable=false`.
- **D3:** **السبت 07-11 = `isHoliday=false`, `isSelectable=true`** (يوم عمل كامل)، الحالة Overdue.
- **D4:** إنشاء بمفتاح الجمعة ⇒ `calendar.day_is_holiday` (400).
- **D5:** مفتاح غير صالح `2026-13-40` ⇒ `report.daily_key_invalid` (400).
- **D6:** يوم مستقبليّ 07-16 ⇒ `calendar.future_day_locked` (400).
- **D7:** إنشاء/حفظ/إرسال ليوم **السبت** ⇒ نجاح، **المفتاح ثابت 2026-07-11**.

### Phase 5 — عدم التراجع (R1–R7) — PASS
- **R1:** إرسال تقرير مطلوب فارغ (قالب تكميليّ) ⇒ `submission.required_fields_missing` (400) — منع الفراغ + Save-then-Submit سليمان (ليس خطأ تقويم).
- **R2–R7:** `GET /submissions`، `/kpi-evaluations`، `/dashboard/me`، `/notifications`، `/report-templates`، `my-cycles` ⇒ كلها **200**. لا أخطاء 500، لا استثناءات خادميّة. **لم يُرسَل أيّ بريد** (email_outbox=0).

**نيّات Request B الثمانية كلها مُغطّاة:** (1) الأسبوعي ✅ (2) KPI الأسبوعي ✅ (3) اليومي المبيعات ✅ (4) الجمعة عطلة وحيدة ✅ (5) السبت عمل ✅ (6) لا إدخال فترة/تاريخ يدويّ ✅ (7) ثبات مفتاح الدورة عبر حفظ/إرسال/اعتماد ✅ (8) لا تراجع في الحوكمة/الحفظ/الإرسال ✅.

---

## 4. المعرّفات المُسجَّلة (بادئة CAL-R1-FINAL-UAT)

- **6 مستخدمين:** CEO `4080c74d`، GM `9bd6caf1`، Manager `b3ffbcfb`، TeamLeader `01b5003f`، Employee `20845647`، SalesB2C `5f89f453`.
- **3 قوالب:** أسبوعيّ `56313eab`، يوميّ `be525bee`، تكميليّ `817afa9a`.
- **3 تسليمات:** أسبوعيّ `b9119f2c`، يوميّ-سبت `d5ff02c4`، تكميليّ-فارغ `8cd5d8a9`.
- **1 تقييم KPI:** `bd125ffa` (حُذف ناعمًا في K13، ثم حُذف نهائيًّا في التنظيف).
- **أدمن مؤقّت:** `cal-final-gate-admin@test.local`.

---

## 5. تنظيف البيانات — العودة إلى الأساس (Phase 7)

- **نسخة احتياطية قبل التنظيف:** `/root/db-backups/reporting_rc-precaluatcleanup-20260715-093253.dump`.
- **معاملة ذرّية واحدة** (`BEGIN … COMMIT`، `ON_ERROR_STOP=1`، ترتيب آمن للـ FK) مع **حارس تأكيد داخليّ** (يُلغي المعاملة إن لم يعُد الأساس 35/35/1/0).

| العدّاد | الأساس | أثناء UAT | بعد التنظيف | الحالة |
|---|---|---|---|---|
| `AspNetUsers` | 35 | 42 | **35** | ✅ |
| `report_submissions` | 35 | 38 | **35** | ✅ |
| `kpi_evaluations` | 1 | 2 | **1** | ✅ |
| `kpi_evaluation_review_events` | 0 | 3 | **0** | ✅ |
| `email_outbox` | 0 | 0 | **0** | ✅ |
| `__EFMigrationsHistory` | 27 | 27 | **27** | ✅ ثابت |
| مستخدمو CAL/final-gate | 0 | 7 | **0** | ✅ |

**ما حُذف (بالترتيب الآمن):** قيم حقول التسليمات + خطوات الاعتماد + 3 تسليمات؛ 3 review_events + 3 kpi_results + 1 kpi_eval؛ إسنادات/حقول/نسخ 3 قوالب + 3 قوالب؛ إشعارات + audit_logs المنسوبة + refresh_tokens + أدوار المستخدمين؛ 7 مستخدمين (6 UAT + الأدمن المؤقّت).

**منع إعادة البذر:** أُفرِغت مفاتيح `Seed__AdminEmail/Password` في env (لقطة أمان `/root/khubara-reporting-rc.env.pre-restore-20260715-093601`) ثم أُعيد تشغيل الخدمة ⇒ **الأدمن المؤقّت لم يُعَد بذره** (cal-users=0).

**تحقّق ما بعد إعادة التشغيل:** health=200؛ الأساس 35/35/1/0/0/27؛ البصمات مطابِقة (بلا drift): Api `a6e8ea98`، App `7b0a6900`، Infra `aa460ece`، bundle `index-B_lNG4Zb.js`.

**نظافة الخادم:** حُذفت سكربتات/JSON/SQL الخاصة بـ UAT (`/root/cal-final-uat/`، `cal-final-cleanup.sql`، السكربتات المؤقتة). النسخ الاحتياطية للقاعدة و env مُحتفَظ بها.

---

## 6. الحكم — GO / NO-GO للإنتاج

### ✅ GO مشروط — الحزمة جاهزة للإنتاج

- تصحيح السياسة اليومية (الجمعة عطلة وحيدة، السبت عمل كامل) مُثبَت خادميًّا.
- الأسبوعي + KPI الأسبوعي + اليوميّ + عدم التراجع: **44/44 فحصًا خضراء**.
- ثبات مفتاح الدورة/اليوم عبر مسودّة→إرسال→اعتماد (بلا انزياح).
- فصل الواجبات وRBAC للحذف الإداريّ مُثبَتان (403 حيث يجب).
- **لا Migration، لا تغيير كود، لا drift في البصمات، لا مساس بالإنتاج/TEST.**
- البيئة نظيفة والأساس مُستعاد بالكامل (35/35/1/0).

### ⛔ شرط الإطلاق

**لا نشر على الإنتاج (ولا دمج/دفع) قبل موافقة صريحة جديدة.** هذا التقرير حكمُ جاهزية فقط.

---

## 7. خطة الرجوع (لو نُشر على الإنتاج مستقبلًا)

1. **Backend/Frontend:** استعادة نُسخ `publish-backup`/`dist-backup` الخاصة بنافذة النشر + `chown www-data` + `systemctl restart`.
2. **قاعدة البيانات:** الحزمة **code-only بلا هجرة جديدة** ⇒ لا عكس مخطط؛ الرجوع = إرجاع الـ DLLs/bundle فقط.
3. **التحقّق بعد الرجوع:** health=200، عدد الهجرات ثابت، سلوك التقويم القديم.

---

**النهاية — RC نظيفة، الأساس مُستعاد، بلا drift. بانتظار موافقة الإنتاج الصريحة قبل أيّ خطوة تالية.**
