# RECONCILE-PROD-DEVELOP-LINEAGE — التقرير 01: تدقيق نَسَب الكود الكامل

**التاريخ:** 16 أغسطس 2026 · **المرحلة:** B (Full Git Ancestry Audit)

## 1. النقاط المرجعيّة

| المرجع | الالتزام | الوصف |
|---|---|---|
| رأس develop | `10c26f7` | نتيجة CPW-R2+R3 الموحّدة (35 هجرة)، منشور على TEST |
| رأس الإنتاج و RC | `ce166662` | `candidate/leave-deduction-tl-approval-r1-20260806` (30 هجرة) |
| نقطة التفرّع (merge-base) | `6fd2253` | آخر سلف مشترك |
| **المرشَّح الموحّد** | `ac0d86c` | فرع `integration/reconcile-prod-develop-lineage-r1` |

```
git rev-parse ac0d86c^1  = 10c26f72…   (سليل develop  = YES)
git rev-parse ac0d86c^2  = ce166662…   (سليل الإنتاج = YES)
```

## 2. حجم التفرّع

- التزامات على خطّ الإنتاج فقط: **32** (منها 31 غير-دمج).
- التزامات على خطّ develop فقط: **42**.
- إجماليّ ملفّات الشجرة: الإنتاج **693** · develop **920** · **المدموج 1025**.

## 3. السبب الجذريّ للتفرّع (اكتشاف حاسم)

خطّ الإنتاج **ليس** «develop زائد ميزات». إنّه فرع **إعادة بناء مُثبَتة (reconstruction)**
مُوثَّق في المستودع نفسه بملفَّي `PRODUCTION-LINEAGE-MANIFEST.md` و`KNOWN-DIFFERENCES-VS-DEVELOP.md`
على `ce166662`: أُعيد بناؤه من `6fd2253` بفكّ تصريف الثنائيّات المنشورة (ilspycmd 8.2.0)، وطُبِّقت عليه
ثلاثة فروقات مُثبَتة قبل أن يتراكم فوقه 30 التزام ميزة حقيقيّ:

1. **حذف ميزة المناصب المرنة (FlexiblePositions)** — الإنتاج/RC لم يُبنيا بها قطّ (26 سِمة `[Migration]` فقط، ولا نوع `Positions`).
2. **إعادة تسمية معرّفَي هجرتَين** (انظر التقرير 02).
3. إعادة `ScopeResolver.cs` إلى صورته قبل المناصب.

## 4. الفخّ الصامت الذي أُبطِل

الدمج الساذج كان **سيحذف 20 ملفًّا من develop بلا أيّ علامة تعارض**، لأنّ develop لم يمسّها منذ
`6fd2253` بينما حذفها نَسَب الإنتاج عمدًا:

- ميزة المناصب المرنة كاملةً (كيانات، تهيئات، خدمة، وحدة تحكّم، صلاحيّات، اختبارات، صفحة الواجهة).
- صفحة ورشة الحوكمة `GovernanceWorkspacePage.tsx` و`useGovernanceWorkspace.ts`.
- هجرتا `20260622140138` و`20260626124527` المعتمدتان.

**الكشف:** `comm -23` بين `git ls-tree -r --name-only origin/develop` وقائمة ملفّات الشجرة المدموجة.
**العلاج:** استعادة الملفّات العشرين حرفيًّا من `origin/develop`، ثمّ إعادة `PositionScopeKind`،
وDbSets المناصب الأربعة، و`using Reporting.Application.Positions;`، وثابت `Policies.PositionManagement`
وتسجيل سياسته.

## 5. الإثبات النهائيّ للجرد المِلفّيّ

| الفحص | النتيجة |
|---|---|
| ملفّات develop غائبة عن الشجرة المدموجة | **0** |
| ملفّات الإنتاج غائبة عن الشجرة المدموجة | **4** — حصريًّا ملفّا الهجرتَين المكرّرتَي المعرّف (`.cs` + `.Designer.cs`)، مقصود ومغطّى بجسر السجلّ |
| ملفّات لمسها الإنتاج بعد التفرّع | 230 |
| منها ما زال مختلفًا في الشجرة المدموجة | 32 (22 منها إضافة محضة بلا أيّ سطر محذوف) |

انظر التقرير 03 لتصنيف الملفّات العشرة المتبقّية سطرًا بسطر.
