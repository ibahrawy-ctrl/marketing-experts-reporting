# RECONCILE-PROD-DEVELOP-LINEAGE — التقرير 02: تكافؤ الهجرات والمجموعة الموحّدة

**التاريخ:** 16 أغسطس 2026 · **المراحل:** D + F

## 1. المشكلة

| معرّف develop (المعتمد) | معرّف الإنتاج/RC (التاريخيّ) | الجدول المُنشأ |
|---|---|---|
| `20260622140138_KpiTemplateAssignmentsPhaseT1` | `20260622144900_KpiTemplateAssignmentsPhaseT1` | `kpi_template_assignments` |
| `20260626124527_AddReportViewGrants` | `20260626135944_AddReportViewGrants` | `report_view_grants` |

لأنّ المعرّفَين مختلفان، يرى EF Core هجرتَي develop **معلّقتَين** على قواعد الإنتاج/RC، فيحاول
`CREATE TABLE` على جدولَين موجودَين ⟹ **`42P07`** وتوقّف الخدمة عند الإقلاع.

## 2. إثبات التكافؤ (شرط التوقّف رقم 1 — مُصرَّف)

بعد تطبيع كلّ معرّف من 14 رقمًا (`sed -E 's/[0-9]{14}/<ID>/g'`) صار محتوى الملفَّين **متطابقًا بايتًا ببايت**:

```
md5(KpiTemplateAssignmentsPhaseT1 المُطبَّع)  = 13e7356322b59afff664213540bf093f   (الطرفان)
md5(AddReportViewGrants المُطبَّع)             = edbad105dfa9c270c0a36cdac13f89b9   (الطرفان)
```

وتؤيّده وثيقة `KNOWN-DIFFERENCES-VS-DEVELOP.md` على `ce166662` نفسه، التي تُعلن إعادة التسمية
كأحد الفروقات الثلاثة المقصودة في إعادة بناء نَسَب الإنتاج.

**إثبات مستقلّ ثانٍ (تجريبيّ):** بصمة مخطَّط نسخة الإنتاج بعد الجسر والترقية = بصمة قاعدة Fresh
المبنيّة من الصفر، سطرًا بسطر (`3b3eb6b04fc0e6b1898468bd2cfed546`).

## 3. المجموعة الموحّدة = 38 هجرة

= 35 هجرة develop **+** 3 هجرات فريدة للإنتاج:

| المعرّف | الميزة الحيّة |
|---|---|
| `20260715162851_AddBypassTeamLeaderApproval` | تجاوز اعتماد قائد الفريق |
| `20260716015239_KpiEvaluationPartialUniqueIndex` | فهرس فريد جزئيّ `WHERE "IsDeleted" = false` |
| `20260724224053_AddReportApproverAndKpiReviewerOverrides` | تجاوزا المعتمِد والمراجِع |

**المُستبعَد من التجميعة:** `20260622144900` و`20260626135944` (ملفّاتهما الأربعة). لم تُحذف
من **سجلّ** أيّ بيئة؛ تبقى صفوفًا تاريخيّة يتسامح معها EF Core.

## 4. اتّساق نموذج البيانات (Model Sync)

جُدِّدت `AppDbContextModelSnapshot.cs` بتقنيّة الهجرة الاستكشافيّة (Probe):

1. الاستكشاف الأوّل أصدر **بالضبط** عمليّات الهجرات الإنتاجيّة الثلاث ⟹ اللقطة كانت بائتة.
2. أُبقيت اللقطة المُجدَّدة وحُذف ملفّا الاستكشاف.
3. الاستكشاف الثاني أصدر **0 عمليّة** ⟹ Model Sync نظيف.

تحقّق محتوى اللقطة: `BypassTeamLeaderApproval` (1)، `KpiReviewerOverrideUserId` (3)،
`ReportApproverOverrideUserId` (3)، كيانات المناصب (2)، فلتر `"IsDeleted" = false` (2).
