# RECONCILE-PROD-DEVELOP-LINEAGE — التقرير 09: تشخيص احمرار نَسَب الإنتاج

**التاريخ:** 16 أغسطس 2026 · **المرحلة:** H3 · **الحكم: الاحمرار = دَين اختبارات لا عطب في الإنتاج الحيّ**

## 1. السؤال

`ce166662` المنشور على الإنتاج و RC يُظهر **159 فشل تكامل** على قاعدة نظيفة معزولة، مقابل **2** على
`develop`. هل الإنتاج الحيّ معطوب؟

## 2. الجواب المُثبَت: لا — السبب بوّابتا تقويم **حيّتان وصحيحتان** يخالفهما توقّع الاختبارات

خطّ الإنتاج أضاف تحقّقًا خادميًّا يمنع إنشاء تقرير/تقييم لفترة **لم تبدأ بعد**:

| الموضع | الشرط | رمز الرفض |
|---|---|---|
| `SubmissionService.cs:110–112` | `CycleRange(periodKey).Start > RiyadhToday()` | `calendar.cycle_not_open` |
| `SubmissionService.cs:128–130` | `day > RiyadhToday()` | `calendar.future_day_locked` |
| `SubmissionService.cs:124–126` | يوم جمعة | `calendar.day_is_holiday` |
| `KpiEvaluationService.cs:66–67` | دورة مستقبليّة | `calendar.cycle_not_open` |

مرجعها المعلن في الكود: `ROLE-AWARE-REPORTING-CALENDAR §2.4` و`DAILY-BUSINESS-DAY-COMPLIANCE-R1`.
وهي **سلوك حيّ منشور** ⟹ يُحفظ ولا يُمَسّ.

## 3. الخطأ في الاختبارات: مفاتيح فترات مستقبليّة مُصلَّبة

كانت الاختبارات تستعمل فترات بعيدة في المستقبل عمدًا لتفادي التصادم على قاعدة الاختبار المشتركة
`reporting_test`. بعد إضافة البوّابات صار كلّ مفتاح منها مرفوضًا.

الحساب المرجعيّ (اليوم `2026-08-16`، الدورة الحاليّة **`2026-W34`**):

| المفتاح | بداية الدورة | الحالة |
|---|---|---|
| `2026-W40` | 2026-09-26 | مستقبليّة |
| `2026-W41` … `2026-W49` | 2026-10-03 … 2026-11-28 | مستقبليّة |
| `2026-W60` · `W71–W74` · `W81–W86` · `W91–W96` | — | **غير موجودة** (خارج ترقيم ISO) |
| `2027-*` · `2028-*` · `2029-*` · `2080-*` · `2098-*` · `2099-*` | — | مستقبليّة |

**الجرد:** 144 مفتاحًا مستقبليًّا/غير صالح مُصلَّبًا في **25 ملفّ اختبار**.

## 4. الإثبات التجريبيّ (سبب ⟶ أثر)

`B2bByServiceTemplateTests`: **11 فشلًا** على المرشّح. غُيِّرت مفاتيحه فقط — بلا أيّ تعديل على المنتج —
فأصبح **14/14 أخضر**. الرسالة الأصليّة كانت سلسلة: فشل `POST /api/submissions` ⟹ `draft = null` ⟹
`PUT …/values` على `Guid.Empty` ⟹ `404 NotFound` بدل `400 BadRequest` المتوقَّع.

## 5. التصنيف الناتج لاحمرار الإنتاج

| الصنف | التقدير | الأساس |
|---|---|---|
| **F — Production Test Debt** (اختبار قديم يسبق البوّابة) | الغالبيّة العظمى | مُثبَت بالسبب والأثر أعلاه |
| **B — Production Baseline Defect** (يفشل على الإنتاج ولا يوجد عند develop) | 10 | `ProjectFirstExecutionAggregation` 8 · `RepeatableNumericValidation` 2 |
| **C — Shared Historical Defect** | 2 | `BASELINE-DEFECT-01/02` المعروفان |
| **J — Order-dependent/Flaky** | 5 | نجحت في إعادة التشغيل الاسميّة |

## 6. الخلاصة الحاكمة

- الإنتاج الحيّ **ليس معطوبًا**؛ بوّابات التقويم تعمل كما صُمِّمت.
- الاختبارات هي المتقادمة، والقاعدة المُلزِمة تنطبق: *«إذا كان العيب في Test/Fixture، صحّح الاختبار
  بدليل، ولا تغيّر المنتج لإرضائه»*.
- ⟹ **الإصلاح الصحيح:** اشتقاق مفاتيح الفترات من `ReportingCalendarPolicy.RiyadhToday()` — وهو العرف
  الذي اتّبعه خطّ الإنتاج نفسه حيث حُدِّثت الاختبارات (`MultiProjectSectionTests.cs:151`).
- ⟹ الرقم 118 **ليس Unified Candidate Regression**؛ إنّه في معظمه **Production Test Debt** ورثه المرشّح.
