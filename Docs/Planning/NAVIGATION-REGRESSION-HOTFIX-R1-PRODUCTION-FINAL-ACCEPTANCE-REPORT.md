# NAVIGATION-REGRESSION-HOTFIX-R1 — تقرير القبول النهائي على الإنتاج

- التاريخ: 2026-07-15 (دورة W29: السبت 07-11 → الجمعة 07-17)
- النطاق: إصلاح انحدار التنقّل — **واجهة فقط** (لا Backend، لا Migration، لا تغيير DB/Env/FeatureFlag، لا وحدات استراتيجية).
- البيئة: الإنتاج — `reporting-api.service`:5090، `reports.emarketingacademy.net`، DB `reporting_prod`.
- المصدر: **حصريًّا** القطعة المقبولة على RC `index-B-y7LHB8.js` (تحقّق SHA قبل النشر — لا إعادة بناء من develop، لا قطعة مختلفة).
- الطابع الزمني للنشر: `20260715-174430`.

## خط الأساس (Phase 0) — GO

| البند | القيمة |
|------|--------|
| Reporting.Api.dll | `a6e8ea98…2596e7` |
| Reporting.Application.dll | `7b0a6900…2861da` |
| Reporting.Infrastructure.dll | `aa460ece…88596d` |
| عدد الهجرات | 27 |
| رأس الهجرات | `20260713171040_AdminGovernanceReportKpiCorrection` |
| الحزمة القديمة (الانحدار) | `index-B_lNG4Zb.js` (1317012 بايت) SHA=`2b5bfb00…dff85f` |

## Phase 1 — بوابة هوية المرشّح — PASS

- SHA القطعة المرشّحة = `9363e56dc9f3c14f1abe5227029260e30d82d06d08e2b39cce11ab8255541841` (1281278 بايت) — **مطابق تمامًا لقطعة RC المقبولة**.
- CSS = `index-C8CkvKW-.css` SHA=`c412b1f5…0127bca`. index.html SHA=`f4035e98…5e92acbc38`. 0 source maps.

## Phase 2 — النسخة الاحتياطية — مكتملة (محفوظة، غير محذوفة)

- `/opt/reporting/reporting-frontend/dist-backup-navhotfix-20260715-174430` (www-data:www-data).
- المحتوى: الحزمة القديمة `index-B_lNG4Zb.js` (1317012) + `index-eBeHKKuv.css` (29228) — سليمة للرجوع الفوري.

## Phase 3 — النشر (واجهة فقط) — مكتمل

| البند | قبل | بعد |
|------|-----|-----|
| الحزمة | `index-B_lNG4Zb.js` (1317012) | `index-B-y7LHB8.js` (1281278) |
| CSS | `index-eBeHKKuv.css` | `index-C8CkvKW-.css` |
| قاعدة الـAPI | نسبية `/api` (محايدة للمضيف، بلا تسريب localhost) | كما هي |

- النشر: `rsync --delete` من المصدر المقبول → dist + `chown -R www-data:www-data`. لا إعادة تشغيل خلفية.

## Phase 4 — التحقّق التقني — PASS (إثبات frontend-only)

- HTTPS: index.html=200 يشير إلى `assets/index-B-y7LHB8.js`، الحزمة الجديدة=200، CSS الجديدة=200، **الحزمة القديمة=404**.
- SHA المُقدَّم علنًا = `9363e56d…541841` (مطابق للقرص و RC).
- **بصمات Backend الثلاث لم تتغيّر** (مطابقة لخط الأساس بايتًا).
- الهجرات=27، الرأس `20260713171040` (لم يتغيّر). الخدمة: NRestarts=0، active/running (بلا إعادة تشغيل). الصحة الداخلية+العامة=200.

## Phase 5 — فحص التنقّل — PASS

فحص القطعة المنشورة فعليًّا على الإنتاج:

| markers التنقّل المدمج | العدّ |
|------|:----:|
| me_nav_collapsed_v2 | 1 |
| التقارير المقدَّمة / تقويم التقارير | 1 / 3 |
| تجميع المبيعات / لوحة مبيعات الفريق / لوحة مبيعاتي | 2 / 3 / 2 |

| بوابة التسريب الاستراتيجي (يجب 0) | العدّ |
|------|:----:|
| /app/positions · governance-workspace · GovernanceWorkspace | 0 · 0 · 0 |
| execution-taxonomy · Workstream · workstreams · Deliverable | 0 · 0 · 0 · 0 |
| client-360 · Client360 · execution/team-dashboard · execution-reports | 0 · 0 · 0 · 0 |
| FlexiblePositions · PositionsPage | 0 · 0 |

- «طي الكل»=0 (دالّة الأكورديون القديمة غائبة). «فتح الكل»=1 (رابط داشبورد حميد `/app/submissions?tab=pending`، ليس دالّة تنقّل). «المناصب المرنة»=1 (نصّ داخل UsersPage — عنصر أدمن داخل الصفحة، ليس وحدة/مسار تنقّل؛ موجود مسبقًا) — **كلاهما مطابق لقطعة RC المقبولة**.
- **النتيجة: STRATEGIC NAVIGATION LEAKS = 0** على مستوى التنقّل (مسارات/وحدات).

### مصفوفة التنقّل بالأدوار (دالّة خالصة من `(roles, isSalesRep)` في navConfig.ts داخل القطعة المنشورة)

| الدور | عدد الوحدات |
|------|:----:|
| Admin | 8 |
| CEO / GeneralManager | 7 |
| HR | 7 |
| Manager / TeamLeader | 6 |
| Employee | 5 |
| AccountManager (AccountPortfolioReader) | 5 |

- روابط المبيعات الثلاثة تبويبات ضمن وحدة «التقارير»: «تجميع المبيعات» (Admin/CEO/GM/Manager)، «لوحة مبيعات الفريق» (TeamLeader)، «لوحة مبيعاتي» (isSalesRep فقط — لا الأدمن).

### قيد موثّق (شفافية)
الالتقاط البصري المُصادَق لكل دور مباشرةً على الإنتاج لم يُنفَّذ لأن حسابات الإنتاج مستخدمون حقيقيون ولن تُعاد تعيين كلماتهم (تفاديًا لتعطيل حسابات حقيقية)، ولا تتوفّر أتمتة متصفّح مُصادَق ضد الإنتاج بالأدوات الحالية. التغطية البديلة = بصمة القطعة الحيّة + المصفوفة المشتقّة من navConfig المنشور + 150 اختبار Vitest أخضر (7 سيناريوهات أدوار). متّسق مع نمط المشروع المعتمد (إثبات RBAC عبر السياسة-في-DLL + الاختبارات عند غياب حسابات حيّة).

## Phase 6 — تحقّق عدم الانحدار — PASS

- أعلام البريد: `Email__Enabled=false`، لا `Email__IncludedTypes`، لا `Reminders__Enabled` (معطّلة). `email_outbox` = 0 صف.
- الهجرات=27 والرأس ثابت ⇒ سكيمة قاعدة البيانات دون تغيير.
- بصمات Backend الثلاث لم تتغيّر (مُثبت Phase 4) ⇒ **كل منطق الحوكمة/التقويم الأسبوعي واليومي/إنشاء التقارير/KPI الأسبوعي/الإجازات والطلبات/الإشعارات مطابق بايتًا** — لا انحدار سلوكي ممكن من نشر واجهة فقط.

## Phase 7 — المراقبة — PASS

- الصحة ×5 (داخلي/عام) = 200/200 في كل المرّات.
- الخدمة: NRestarts=0، active/running.
- سجلّ الخلفية: لا 500 / 42501 / Unhandled / Exception.
- nginx: الأخطاء الوحيدة (أ) سابقة للنشر (connection refused / SignalR negotiate المعروف — الخلفية مستقرّة الآن) و(ب) **404 متوقّع للحزمة القديمة** `index-B_lNG4Zb.js` من متصفّحات مخبّأة (تأكيد إزالتها). **لا 404 للحزمة الحالية** `index-B-y7LHB8.js`.

## Phase 8 — التنظيف

| العنصر | الإجراء |
|------|--------|
| النسخة الاحتياطية للرجوع | **محفوظة** `dist-backup-navhotfix-20260715-174430` (سليمة) |
| ملفّا الطابع الزمني للتدقيق | محفوظان (`/tmp/navhotfix-{PROD,rc}-deploy-ts.txt`) |
| القطعة القابلة للنشر (مصدر رجوع) | محفوظة محليًّا `/tmp/release-nav-hotfix-20260715` |
| سكربتات/توكنات مؤقتة على الخادم | لا شيء (frontend-only، لم تُنشأ) |
| توكنات/ردود دخول محلية | لا شيء |
| حسابات اختبار | لا شيء (لم تُنشأ — لا بيانات) |
| إعدادات المعاينة المؤقتة | أُزيلت (`nav-hotfix-preview` من launch.json) |

## إجراء الرجوع (عند الحاجة)

استعادة `dist-backup-navhotfix-20260715-174430` → dist + `chown -R www-data:www-data` + التحقّق (الحزمة القديمة 200، الصحة 200، البصمات الثلاث، الهجرات 27) + إصدار تقرير رجوع. لا يلزم إعادة تشغيل خلفية (النسخة الاحتياطية واجهة فقط).

## الحُكم

**PRODUCTION NAVIGATION HOTFIX SUCCESSFUL**

- التنقّل المدمج مُستعاد (شريط وحدات قصير + تبويبات أفقية)، بلا قائمة أكورديون طويلة، بلا «فتح/طي الكل» كدالّة تنقّل.
- تسريب استراتيجي = 0 (مسارات/وحدات تنقّل)؛ مسار `/app/positions` مُزال.
- frontend-only مُثبَت (بصمات DLL الثلاث مطابقة، لا هجرة، لا إعادة تشغيل، الصحة 200، NRestarts=0).
- عدم انحدار مُثبَت هيكليًّا (سكيمة+DLL ثابتة، أعلام البريد معطّلة، صندوق الصادر 0).

### خارج النطاق (لم يُمَسّ)
مشكلة طلب استئذان فاطمة (تجاوز خطوة قائد الفريق) تبقى في طابور الـHotfix القادم — لم تُعالَج ضمن هذه الحزمة.
