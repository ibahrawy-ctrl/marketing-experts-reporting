# ACCOUNT-MANAGER-V5-CONTROLLED-RC-UAT-R1 — تقرير قبول UAT مُتحكَّم فيه على RC

**التاريخ:** 27 يوليو 2026 • **البيئة:** RC حصرًا (`rc-report.emarketingacademy.net`) • **الإنتاج (`reports.emarketingacademy.net`) لم يُمَسّ إطلاقًا.**
**الطبيعة:** اختبار قبول وظيفيّ حيّ على البناء المجمَّد (Frontend فقط) بتصريح كتابة محدود لتقرير اختبار واحد على النسخة المنشورة V5، ثم تنظيف كامل عبر المسار الطبيعيّ. **بلا Migration، بلا Backend، بلا Email، بلا Workflow، بلا تعديل قوالب.**
**القالب:** «🤝 تقرير إدارة الحسابات العملاء» — `TemplateId 25082123-f4a6-41b9-abdc-b26b2dfdea6c` • **V5** المنشورة.

---

## 1) نطاق الاختبار وقيوده — مُثبَت

- الهدف: التحقّق الحيّ من مخرج التقرير المُعاد تصميمه (Presentation Profile لمدير الحسابات) على تسليم حقيقيّ على V5 عبر المسار الطبيعيّ للتطبيق.
- البناء المُختبَر = **المجمَّد** (worktree `.claude/worktrees/amr-clean-r1`، فرع `amr-clean-lineage-r1`، Commit `3efbd0dc2584d2fa1bc23c5373d8e2ee1eb10457`) — لم يُعدَّل أثناء الاختبار.
- كل الكتابات على RC فقط، وكلّها عبر المسار الطبيعيّ (UI/API) — **لا INSERT/DELETE مباشر على القاعدة** ما دام المسار الطبيعيّ متاحًا.
- **الإنتاج: صفر مساس** (لا نشر، لا Tag، لا Migration، لا تعديل بيانات، لا بريد).

## 2) المرحلة 0 — إثبات ما قبل الكتابة (قراءة-فقط) — مُثبَت

- RC حصرًا: لا اتصال بقاعدة الإنتاج؛ سلسلة الاتصال في `/etc/khubara-reporting-rc.env` تشير إلى `reporting_rc`@127.0.0.1.
- V5 للقالب **منشورة** (`IsPublished=true`)؛ تحمل مخطط الـProfile الكامل (subFields: `deliverables_sent`, `decisions_required`, `client_relationship`, `project_status`, `risk_severity`, …).
- **تسليمات V5 قبل الاختبار = 0** (آخر تسليم حقيقيّ W28 كان على V2 القديمة).
- بريد RC لا يُرسِل: `EmailNotifications__Mode=DryRun` + `Email__Enabled=false`.

## 3) المرحلة 1 — هُويّة الاختبار — مُثبَت

- الحساب: **سماح ابوالمجد** — `Samahabouelmagd5@gmail.com`، `UserId aee6885e-b5e5-40c0-bd4f-53332a3f243f`، دور مدير الحسابات (`AccountPortfolioReader, Employee`)، حساب RC حقيقيّ نشط.
- الفترة الجارية استخدمت **V5** المنشورة تلقائيًّا (لا V2) — أُثبِت من `ReportTemplateVersionId` للتسليم.

## 4) المرحلة 2 — الإنشاء عبر المسار الطبيعيّ — مُثبَت

- أُنشئ تسليم واحد عبر مسار التطبيق (لا INSERT مباشر): **`SubmissionId 6166994e-0390-4ec8-8dfd-efc072c75dee`**.
- الإثبات (SELECT فقط): `TemplateVersionId` = V5 (VersionNumber=5)، `SubmitterId` = سماح، `PeriodType/PeriodKey` = أسبوعيّ 2026-W31، حالة أوّليّة Draft.
- حقل الـPRS: `Key='projects'`, `FieldType='ProjectRepeatableSection'`, `fieldId 4bee109b`؛ ولكلّ إدخال `projectId` موجود.

## 5) المرحلة 3 — بيانات الاختبار (خياليّة، بلا أسماء عملاء حقيقيّين) — مُثبَت

6 مشاريع خياليّة (مشروع ألف/باء/جيم/دال/هاء/واو — عميل تجريبيّ) تغطّي ستّة سيناريوهات حاسمة:

| # | السيناريو | السلوك المتوقَّع | النتيجة |
|---|---|---|---|
| 1 | مستقرّ، قرارات معلّقة=0 | يظهر بلا شارة/بطاقة قرار | ✅ |
| 2 | متأخّر/متوسّط مع مشاكل وقرار | شارات + قائمة مشاكل + بطاقة قرار | ✅ |
| 3 | قرار وهميّ `"لا يوجد "` (بمسافة لاحقة) | **مخفيّ ولا يُحتسَب** | ✅ |
| 4 | ضجيج `"—","-"`، مسافات، `"غير متوفر"`, `"N/A"` | مخفيّ بالكامل | ✅ |
| 5 | قرار حقيقيّ `"لا يمكن…"` | **يظهر ويُحتسَب** | ✅ |
| 6 | مختلط حقيقيّ/فارغ | الحقيقيّ فقط يظهر | ✅ |

## 6) المرحلة 4 — الحفظ + التسليم — مُثبَت

- حُفِظت المسودّة وأُثبِتت في القاعدة (قيم الحقول الفرعيّة مخزَّنة في `ValueJson`).
- سُلِّم التسليم عبر المسار الطبيعيّ ⇒ الحالة **Submitted** (أُثبِتت بالاستعلام).

## 7) المرحلة 5 — UAT الحيّ داخل التطبيق — مُثبَت (PASS)

- فُتِح التسليم الحقيقيّ على RC (نفس الأصل، عبر خادم معاينة من البناء المجمَّد + وكيل `/api`).
- **الـProfile مُفعَّل حيًّا** (لا مصيّر عامّ): ملخّص المحفظة + بطاقات المشاريع بالتصميم الجديد.
- المقاييس الحيّة صحيحة: 6 مشاريع، 3 تحتاج انتباهًا، 2 قرارات مطلوبة، 1 مخاطرة عالية… إجماليّ **قرارَين** فقط (السيناريو 2 + السيناريو 5)، والقرار الوهميّ «لا يوجد » **غير محتسَب**.
- Checklist A–T: العدّادات، لكلّ-مشروع، الحقول الفارغة، RTL، Desktop، Mobile (RTL سليم)، Print/PDF سليم، Console (فقط SignalR 404 حميد)، Network (كلّه `/api` نفس الأصل، 200)، deep-link/refresh (أُعيد التحميل من `?period=2026-W31&open=6166994e…` — لا شاشة بيضاء، أُعيد رسم الـProfile).

## 8) المرحلة 6 — عدم الانحدار (قراءة-فقط) — مُثبَت (PASS)

- تقرير W28 القديم (**V2**) يُعرَض بالمصيّر **العامّ** (DescriptionList dt/dd) حيًّا — لا Profile.
- المودريشن يحتفظ بتصميمه (مسار `moderationGroups` منفصل، خارج REGISTRY الذي = `[accountManagerProfile]` فقط) — بنيويًّا غير متأثّر.
- الرقم **0 لا يُخفى** (`isMeaningfulPresentationValue`: عدد ⇒ `Number.isFinite` ⇒ 0 دالّ).
- صفحة التسليمات بلا أخطاء Console جديدة.

## 9) المرحلة 7 — التنظيف عبر المسار الطبيعيّ — مُثبَت

- حُذف التسليم عبر **admin-delete** (المسؤول محسن، دور Admin): `POST /submissions/6166994e…/admin-delete` ⇒ 200، حذف ناعم (`IsDeleted=true`)، سجلّ التدقيق قائم (`submission.admin_deleted`, `submission.submitted`)، خطوات الاعتماد المعلّقة أُلغيت.
- أُرشِفت 6 مشاريع + العميل التجريبيّ (Closed) عبر مسار الأرشفة الطبيعيّ (كلّها 200).
- **لا DELETE مباشر على القاعدة، لا حذف سجلّات تدقيق، لا تلاعب بالتواريخ.**
- سماح ومحسن حسابان RC حقيقيّان (لا حسابات اختبار للتعطيل): كلمتا مرورهما **مُستعادتان ومُتحقَّق منهما** عبر `UatCredentialTool --restore/--verify` مقابل اللقطة `/root/rc-uat/amr-v5-snapshot-20260727.json` (كلاهما `مطابق=True`، `✓ الحالة الحاليّة مطابقة للقطة`).

## 10) المرحلة 8 — الفحوص التقنية الختامية (بلا أيّ تغيير كود) — مُثبَت (PASS)

| الفحص | النتيجة |
|---|---|
| health الخلفيّ (`127.0.0.1:5092/health`) | **200** |
| health العامّ عبر nginx | **401** = حارس basic-auth مقصود (realm «Not Production»)؛ الخلفيّ نفسه 200 — **ليس انحدارًا** |
| SHA256 للبندل المُخزَّن `assets/index-96kHwdBC.js` | `f979b8cb…172` **مطابق للمجمَّد** بايتًا ببايت |
| حجم البندل | 1,324,028 bytes (ثابت) |
| index.html يشير إلى | `index-96kHwdBC.js` (السليم) |
| جذر nginx | `/opt/reporting-rc/frontend/dist` (يخدم `/assets/` ستاتيكيًّا من نفس القرص المُتحقَّق) |
| عدد الهجرات / الـhead | **30** / `20260724224053_AddReportApproverAndKpiReviewerOverrides` — **ثابت** (المهمّة Frontend فقط، بلا Migration) |
| ثبات الـBackend | DLL `publish/Reporting.Api.dll` بطابع 2026-07-26 21:26، الخدمة تعمل منذ 2026-07-26 21:28 — **قبل** جلسة 27 يوليو ⇒ لم يُعَد نشره/تشغيله |
| إرسال البريد | **صفر** — `email_outbox` total=0، أُنشئ اليوم=0، `Mode=DryRun`، `Email__Enabled=false`، Scheduler=false |
| Console / Network | نظيف (فحص المرحلة 5) — فقط SignalR 404 حميد، كلّ `/api` نفس الأصل 200 |

## 11) الأدلّة والمصنوعات

- التسليم: `SubmissionId 6166994e-0390-4ec8-8dfd-efc072c75dee` (V5، سماح، 2026-W31).
- البندل المُقدَّم = المجمَّد: SHA256 `f979b8cb2692e5687da720c5f9e44ad077358d8eec62cca8d160f581af81e172`.
- استعلامات إثبات: `/tmp/amr-p4-proof.sql`, `/tmp/amr-v5-proof.sql`, `/tmp/amr-cleanup-verify.sql`.
- لقطة استعادة كلمات المرور: `/root/rc-uat/amr-v5-snapshot-20260727.json`.

## 12) عدم الانحدار البنيويّ (من المصدر المجمَّد) — مُثبَت

- `REGISTRY = [accountManagerProfile]` فقط ⇒ لا قالب آخر يُطابَق؛ المصيّر العامّ والمودريشن سليمان.
- `ProjectRepeatableDisplay` وحده يستدعي الـProfile؛ عند `null` يسقط للمصيّر العامّ dt/dd المطابق للسلوك القائم.

## 13) قواعد القرار — التقييم

| القاعدة | الحالة |
|---|---|
| الـProfile V5 عمل حيًّا | ✅ |
| العدّادات صحيحة | ✅ |
| «لا يوجد » ليس قرارًا | ✅ |
| «لا يمكن…» الحقيقيّ يظهر ويُحتسَب | ✅ |
| الرقم 0 يظهر | ✅ |
| Mobile / Print سليمان | ✅ |
| V2 / المودريشن / العامّ غير متأثّرة | ✅ |
| لا أخطاء Console/Network جديدة | ✅ |
| لا تعديل خارج RC | ✅ |
| لا حاجة لتعديل الكود المجمَّد | ✅ |

**لا يوجد أيّ مُطلِق لحالة FAIL/NO-GO** (لم يسقط للعامّ، TemplateVersionId صحيح V5، القرار الوهميّ غير محتسَب، 0 لم يختفِ، القديم/المودريشن غير متأثّرين، لا كتابة إنتاج، لا تعديل كود مجمَّد).

## 14) عدم المساس بالإنتاج — مُثبَت

- الإنتاج (`reports.emarketingacademy.net`): **لم يُمَسّ** (لا نشر، لا Tag، لا Migration، لا تعديل بيانات، لا بريد).
- كلّ الكتابات محصورة في `reporting_rc` وعبر المسار الطبيعيّ فقط.

## 15) تنظيف الـHarness — مُثبَت

- نفق SSH (`5092:127.0.0.1:5092`) **مُغلق**.
- خادم المعاينة `amr-clean-fe` (`7b39bc13-78f5-4f61-a586-863c64a06db2`، منفذ 5219) **موقوف**.

## 16) الحكم النهائي

**GO — القبول مقبول بالكامل.** الـProfile المُعاد تصميمه لمدير الحسابات عمل حيًّا على تسليم V5 حقيقيّ عبر المسار الطبيعيّ على RC، بعدّادات صحيحة ومنطق قرار سليم (القرار الوهميّ مُستبعَد، الحقيقيّ محتسَب، الرقم 0 ظاهر)، مع Mobile/Print سليمين، بلا أيّ انحدار على التقارير القديمة (V2) أو المودريشن أو المصيّر العامّ، وبلا أخطاء Console/Network جديدة، وبلا أيّ مساس بالإنتاج أو الكود المجمَّد.

## 17) التوصية والتوقّف

- عند رغبة الإدارة، يمكن ترقية هذا البناء المجمَّد إلى الإنتاج عبر مسار نشر مستقلّ معتمَد (Frontend فقط، بلا Migration) — **لا يُنفَّذ الآن**.
- **التوقّف هنا حسب التعليمات** — بلا نشر إنتاج، بلا بدء لوحة Cross-Functional، بلا توسيع لتقارير أخرى، بلا أيّ إجراء إضافيّ.
