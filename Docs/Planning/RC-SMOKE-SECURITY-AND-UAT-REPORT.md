# RC-SMOKE-SECURITY-AND-UAT-REPORT
## RECONCILE-PROD-DEVELOP-LINEAGE — الدخان والأمان والـUAT على RC (Phases U · V · W)

**التاريخ:** 16 أغسطس 2026 · **البيئة:** RC · **الإنتاج: لم يُمسّ.**

---

## 1) الحصيلة الرقميّة

| البوّابة | النتيجة |
|---|---|
| بوّابة الدخان (`uni-smoke.sh`) | **PASS=36 · FAIL=0 · NA=5 → SMOKE_GATE=PASS** |
| بوّابة الأدوار والنطاق ومكافحة التعداد (`uat-role-gate.py`) | **PASS=143 · FAIL=0 → ROLE_GATE=PASS** |
| UAT النَسَب الوظيفيّة (`uni-lineage-uat.py`) | **PASS=27 · FAIL=0 → LINEAGE_UAT=PASS** |
| **الإجمالي** | **206 فحصًا · 0 فشل** |
| UAT بصريّ (متصفّح حقيقيّ بلا رأس) | **0 خطأ طرفيّة · 0 طلب فاشل · 7 لقطات** |

الفحوص الخمسة المعلَّمة `N/A` ليست ناجحة ولا فاشلة: أربعة جداول أنشأها هذا النشر (لا خطّ
أساس للحفظ عليها)، وواحد لفحوص المستند الواحد بعد أن أُفرِغت بيانات UAT الموسومة عمدًا.

## 2) بوّابة الأدوار — 11 هويّة × 12 فحصًا

الأدوار المغطّاة: `VIEWER · EMP · SALES · TL · OPS_MGR · HR · FIN_EMP · FIN_MGR · AM · GM · CEO`.
كلّ هويّة تخضع لـ7 فحوص نطاق (CPW-R2/R3) و5 فحوص مكافحة تعداد.

- **مكافحة التعداد: 55/55 خليّة = `404`، صفر تسريب.** لا يُفرَّق بين «غير موجود» و«ممنوع».
- المصفوفة محفوظة في `/root/rc-uat-role-matrix.json`.

## 3) الحسم الحاسم — هل توسّعت الصلاحيّات؟

**السؤال:** خليّة واحدة (`FIN_MGR / R2 doc detail`) خرجت `200` على RC مقابل `404` على TEST.
ظاهرها **توسّع صلاحيّات**، وهو ما لا يجوز قبوله ولا رفضه بتخمين.

**التحقيق:** الاختلاف كان في **خصائص المستند** لا في القرار الأمنيّ — مستند بوّابة RC كان
`ManagementAndFinance` بينما مستند TEST كان `ClientScoped/Restricted`. فبُنيت على RC **مجموعة
مستندات تغطّي أنواع الرؤية الستّة كاملة** وقيست الخلايا مقابل TEST بنفس الأنواع:

| المقارنة | العدد |
|---|---|
| خلايا قابلة للمقارنة | **30** |
| متطابقة | **24** |
| **أضيق** على RC | **6** |
| **أوسع (توسّع صلاحيّات)** | **0** ✅ |

خليّة `ManagementAndFinance / FIN_MGR` تساوي `200` على **البيئتَين معًا** — فلا توسّع.

**تفسير الستّ الأضيق من العقد لا من التخمين:** `ClientProjectVisibility.SeesAll` محصورة في
`Admin/CEO/GM/CeoSupport`؛ ومَن دونهم (`Manager`/مدير العميل) يحتاج نطاقًا من الرسم التنظيميّ.
مستند TEST كان مملوكًا لحساب `AM` الاختباريّ نفسه بينما مستند RC مملوك لمستخدم حقيقيّ آخر.

**الإثبات الحيّ:** أُنشئ عميل موسوم مديره هو حساب `AM` الموسوم، فخرجت النتيجة:
`AM=200 · OPS_MGR=404 · EMP=404 · CEO=200` — أي المسار سليم ويعمل كما يوصّفه العقد.

**Privilege Expansion = 0.**

## 4) UAT النَسَب الوظيفيّة (27 فحصًا)

تثبت أنّ الميزات التي كانت **حيّة على الإنتاج وغائبة عن نموذج `develop`** تعمل فعلًا بعد
التوحيد، وأنّ ميزات `develop` المعتمَدة لم تنحدر:

- **المناصب المرنة**: `/api/positions` و`/api/positions/permission-options` للمدير `200`؛
  ممنوعة بالتصميم لغيره؛ المجهول `401`؛ المعرّف الوهميّ `404` لا `403`.
- **منح الرؤية**: `/api/report-view-grants` للمدير `200`، و`effective/me` لأيّ مستخدم `200`.
- **ورشة الحوكمة**: `risks` · `escalations` · `decisions` = `200`.
- **التذكيرات**: مسار جافّ فقط (Dry-Run)، وممنوع على الموظّف.
- **بلا انحدار على `develop`**: `execution-taxonomy` · `dashboard/me` · `pending-reports` ·
  `notifications` · `reporting-calendar/my-cycles` · `my-days` · `missing-reports` = `200`.

## 5) UAT البصريّ — متصفّح حقيقيّ لا استدلال على الحزمة

شُغِّل Chrome بلا رأس عبر بروتوكول CDP بلا أيّ اعتماديّة خارجيّة (WebSocket الأصليّ في Node)،
مع تمرير مصادقة nginx على مستوى التحدّي (`Fetch.authRequired` → `continueWithAuth`) بدلًا من
حقن ترويسة `Authorization` — لأنّ حقنها يطمس ترويسة التطبيق (وهو ما كشف `DEFECT-RC-02`).

| الصفحة | ما ظهر فعلًا | حجم النصّ |
|---|---|---|
| `/login` | نموذج تسجيل دخول RTL سليم | — |
| بعد الدخول | `after_login_path=/app` · `authenticated=true` | — |
| `/app/positions` | **المناصب المرنة** | 824 حرفًا |
| `/app/governance-workspace` | **ورشة الحوكمة** | 1716 حرفًا |
| `/app/clients` | **العملاء** | 1331 حرفًا |
| `/app/projects` | **المشاريع** | 3345 حرفًا |

**0 خطأ طرفيّة · 0 طلب فاشل.** الأدلّة: 7 لقطات PNG + `visual-report.json` في
`/root/backups/20260816-rc-deploy/visual-uat/` مع بصمات `sha256`.

هذا يغلق حارس `routeRegistry.test.ts`: المساران المستعادان بعد توحيد النَسَب
(`/app/positions` و`/app/governance-workspace`) مسجَّلان **ويعرضان صفحتَيهما فعلًا**.

## 6) التنظيف والعودة إلى خطّ الأساس

حُذفت كلّ البيانات الموسومة في معاملة واحدة (11 هويّة + عميل + 6 مستندات + 9 ملفّات تخزين)،
ثمّ أُزيلت هويّة المدير المؤقّتة وأُعيد ملفّ بيئة RC إلى حالته قبل البذر.

| القياس | خطّ الأساس | بعد التنظيف | الحكم |
|---|---|---|---|
| `AspNetUsers` | 36 | **36** | ✅ |
| `clients` | 8 | **8** | ✅ |
| `client_documents` | 0 | **0** | ✅ |
| `projects` | 32 | **32** | ✅ |
| `report_submissions` | 39 | **39** | ✅ |
| ملفّات التخزين | 0 (`d41d8cd9…`) | 0 (`d41d8cd9…`) | ✅ |
| `.htpasswd` لـRC | `166b9711…` | `166b9711…` | ✅ مطابق بايتًا |
| ملفّ بيئة RC | قبل البذر | **مطابق بايتًا** (`cmp`) | ✅ |

بقيت هويّة واحدة يشبه بريدها نمط الوسم (`amrcf-uat@rc.local`) — وقد **ثبت** أنّها من خطّ
الأساس لا من هذه التذكرة: تاريخ إنشائها 18 يونيو 2026 وهي موجودة داخل نسخة `pg_dump`
المأخوذة قبل النشر. **صفر بقايا من هذه التذكرة.**

## 7) عيبا الأساس المفتوحان

`BASELINE-DEFECT-01` و`BASELINE-DEFECT-02` يبقيان خارج نطاق هذه التذكرة (order-dependent،
ينجحان منفردَين ويفشلان ضمن المجموعة الكاملة). لم يُلمَسا هنا ولم يتغيّر وضعهما.

## 8) الحكم

**RC Smoke = PASS · RC Security Gate = PASS · RC Functional UAT = PASS · RC Visual UAT = PASS ·
Production Live Features = PASS · CPW-R2/R3 Regression = 0 · Privilege Expansion = 0 ·
Email/Scheduler Leakage = 0 · Unresolved = 0.**
