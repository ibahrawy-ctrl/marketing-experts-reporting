# CPW-R3 · Project 360 — تقرير بوّابة الاختبار والانحدار الكامل (R1)

**التذكرة:** CPW-R3 — Project 360 Completion Candidate
**المرحلة:** G — انحدار كامل على قاعدة بيانات معزولة نظيفة
**التاريخ:** 15 أغسطس 2026
**النتيجة:** **Candidate Regression = 0** · **Unresolved = 0**

---

## 1) لماذا قاعدة معزولة — لا `reporting_test`

التعليمات تمنع صراحةً استعمال `reporting_test` لإثبات سلامة المرشَّح، وتمنع تعديلها أو تنظيفها. والسبب ظهر بالقياس:

| الجدول في `reporting_test` | عدد الصفوف المقيس (15 أغسطس 2026) |
| --- | --- |
| `email_notifications` | **10,290,000+** |
| `notifications` | ~847,000 |
| `refresh_tokens` | ~172,000 |
| `AspNetUsers` | ~136,000 |

القاعدة قاعدة **دائمة مشتركة** تتراكم فيها حسابات `TestAuth` عبر كلّ التذاكر. أثر التراكم على الانحدار: زمن التشغيل ~**1 ساعة و48 دقيقة**، و**18 فشلًا** كلّها بسبب مهلات زمنيّة لا بسبب عيب في الكود.

⟹ إثبات سلامة المرشَّح عليها كان سيخلط عيوب التذكرة بعيوب البيئة، وهو بالضبط ما حدث في W5.

---

## 2) الإعداد المعزول — كيف نُفِّذ

| العنصر | القيمة |
| --- | --- |
| شجرة المرشَّح | `/tmp/cpw-r3-candidate-20260815` (git worktree على `c157829`) |
| شجرة الانحدار | `/tmp/cpw-r3-regression-20260815` (rsync، باستثناء `bin/ obj/ .git node_modules/ dist/`) |
| قاعدة البيانات | **`reporting_cpwr3_iso`** — أُنشئت بـ`createdb`، اسم صريح آمن لا يشبه أيّ بيئة حيّة |
| إعادة توجيه الاتّصال | `sed` على `CustomWebApplicationFactory.cs` و`Project360ApiSurfaceTests.cs` |
| التحقّق من العزل | إشارات متبقّية إلى `Database=reporting_test` = **0** |

**لماذا شجرة انحدار منفصلة عن شجرة المرشَّح؟** لأنّ إعادة توجيه سلسلة الاتّصال تعديل **اختباريّ مؤقّت** لا يجوز أن يدخل الالتزامات. فصله في نسخة تُحذَف بعد الانتهاء يضمن أنّ المرشَّح الملتزَم يشير إلى القاعدة الأصليّة كما كان.

---

## 3) نتائج البوّابات

### 3.1 المخطَّط والهجرات

| البند | النتيجة |
| --- | --- |
| عدد الهجرات المطبَّقة | **33** |
| رأس الهجرات | `20260811142239_AddProject360Foundation` |
| هجرة #34 | **غير موجودة** ✅ |
| تعديل الهجرة #33 | **صفر** ✅ |
| تعديل Model Snapshot | **صفر** ✅ |
| `dotnet ef migrations has-pending-model-changes` | `No changes have been made to the model since the last migration.` ✅ |

### 3.2 الكتالوج — 38 قيمة وعدم أثر التكرار

| المجال | المبذور | الخام بعد الانحدار |
| --- | --- | --- |
| `strategy_section` | 6 | 7 |
| `strategy_field` | 14 | 16 |
| `contract_deliverable` | 18 | 19 |
| **المجموع** | **38** ✅ | 42 |

الفارق **4 قيم أنشأتها اختبارات** لا البذر: `w4_test_section_a` · `w4_test_field_a` · `w4_test_field_b` · `w4_test_monthly_report`.

**نقطة الإثبات الأقوى:** المبذور بقي **38 بالضبط** بعد **مئات** عمليّات إقلاع التطبيق خلال الانحدار الكامل (كلّ اختبار تكامل يُقلع المضيف ويشغّل `ExecutionTaxonomySeeder.SeedAsync`). أي أنّ عدم أثر التكرار مُثبَت على **مستوى القاعدة عبر مئات التشغيلات**، لا داخل معاملة واحدة تُرجَع.

### 3.3 اختبارات Project 360 المستهدَفة

| المجموعة | العدد | النتيجة |
| --- | --- | --- |
| وحدة (`ProjectHealthPolicyTests` + `ReportWorkingDaysPolicyTests`) | 46 | ✅ **الكلّ ناجح** |
| تكامل (`Project360FoundationTests` + `Project360ApiSurfaceTests` + `Project360HealthAndAuthorizationTests`) | 52 | ✅ **الكلّ ناجح** |

### 3.4 الانحدار الخلفيّ الكامل

| المجموعة | الإجمالي | ناجح | فاشل |
| --- | --- | --- | --- |
| `Reporting.UnitTests` | 115 | **115** | **0** |
| `Reporting.IntegrationTests` | 1,462 | **1,461** | **1** |

### 3.5 الانحدار الأماميّ

| البوّابة | النتيجة |
| --- | --- |
| `tsc -b --force` | نظيف — 0 أخطاء ✅ |
| `vitest run` | 30 ملفًّا · **271 اختبارًا · 0 فاشل** ✅ |
| `vite build` | ✓ built in 965ms ✅ |
| `eslint` | 31 ملاحظة (18 خطأ · 13 تحذير) — **كلّها في ملفّات خطّ الأساس، 0 في أيّ ملفّ Project 360** |
| Playwright E2E | **1 passed** ✅ |

### 3.6 جرد المسارات

**34 مسارًا** — 33 قائمة + 1 جديد (`POST /health/recompute`):

| المتحكّم | المسارات |
| --- | --- |
| `ProjectObjectivesController` | 8 |
| `ProjectKpisController` | 10 |
| `ProjectStrategyController` | 3 |
| `ProjectContractDeliverablesController` | 8 |
| `ProjectOverviewController` | 1 |
| `ProjectGovernanceReadController` | 3 |
| `ProjectHealthController` | 1 |

**تحقّقان حرجان:**
- كلّ مسارات كتابة المؤشّرات تحت `objectives/{objectiveId:guid}`؛ المسار الوحيد على مستوى المشروع هو **قراءة** `[HttpGet("kpis")]` ⟹ **لا مسار إنشاء مؤشّر على مستوى المشروع**.
- مسارات المشاريع القائمة قبل CPW-R3: **صفر محذوف · صفر مغيَّر**.

---

## 4) تصنيف الإخفاقات

الفشل الوحيد في الانحدار الكامل:

```
Reporting.IntegrationTests.EmployeeProfileScopeTests.Profile_Summary_Reflects_Submitted_Kpi
Assert.NotNull() Failure: Value of type 'Nullable<decimal>' does not have a value
   at EmployeeProfileScopeTests.cs:line 152
```

| البند | القيمة |
| --- | --- |
| **التصنيف** | **Baseline Defect** (`BASELINE-DEFECT-02`) |
| الدليل | يُعاد إنتاجه على قاعدة **نظيفة** ومن نسخة `HEAD` من ملفّ الاختبار — أي بلا أيّ مساس من CPW-R3 |
| الإجراء | **لا يُصلَح** — التعليمات تمنع إصلاح عيوب خطّ الأساس ضمن هذه التذكرة |

### 4.1 جدول التصنيف الكامل

| التصنيف | العدد | البيان |
| --- | --- | --- |
| **Candidate Regression** | **0** | — |
| **Baseline Defect** | **1** | `EmployeeProfileScopeTests.Profile_Summary_Reflects_Submitted_Kpi` |
| **Pre-existing Dirty Worktree** | **1** (أماميّ) | `LeaveRequestsPage/useToast` — يختفي في المرشَّح النظيف |
| **Shared-State-Order-Dependent** | 0 | — |
| **Environment / Infrastructure** | 0 (في القاعدة المعزولة) | كان **22** على `reporting_test` |
| **Flaky** | 0 | — |
| **Unresolved** | **0** | — |

### 4.2 اكتشاف مهمّ: `BASELINE-DEFECT-01` لم يكن عيبًا

`AdminGovernanceTests.Hr_CanFlagCommentRequestReopen_ButNot_ApproveRejectReopenDelete` — المسجَّل في الذاكرة كعيب أساس — **ينجح على القاعدة النظيفة**.

⟹ كان **تلوّث بيئة** لا عيبًا في الكود. مقارنة الأرقام:

| البيئة | الإجمالي | ناجح | فاشل |
| --- | --- | --- | --- |
| `reporting_test` (W5) | 1,461 | 1,438 | **23** |
| `reporting_cpwr3_iso` (نظيفة) | 1,462 | **1,461** | **1** |

**22 من 23 فشلًا كانت تلوّث بيئة.** هذه نتيجة تتجاوز نطاق CPW-R3 وتستحقّ تسجيلًا مستقلًّا: قياس صحّة أيّ تذكرة على `reporting_test` يُنتج ضجيجًا يفوق الإشارة بـ22 ضعفًا.

---

## 5) معايير GO للمرشَّح (§14)

| المعيار | المطلوب | الفعليّ | ✅ |
| --- | --- | --- | --- |
| Candidate Regression | 0 | **0** | ✅ |
| Unresolved | 0 | **0** | ✅ |
| بناء الخلفيّة | 0 أخطاء | **0** | ✅ |
| بناء الواجهة | 0 أخطاء | **0** | ✅ |
| وحدة/تكامل Project 360 | PASS | **98/98** | ✅ |
| اختبارات واجهة Project 360 | PASS | **44/44** | ✅ |
| الانحدار الكامل مُصنَّف | نعم | **نعم** | ✅ |
| مزامنة النموذج | نظيفة | **نظيفة** | ✅ |
| عدد الهجرات | 33 | **33** | ✅ |
| هجرة #34 | غير موجودة | **غير موجودة** | ✅ |
| الكتالوج | 38 + عديم أثر التكرار | **38 ✓ + مُثبَت** | ✅ |
| مسارات المشاريع القائمة محذوفة/مغيَّرة | 0 | **0** | ✅ |
| مسار إنشاء مؤشّر على مستوى المشروع | غير موجود | **غير موجود** | ✅ |
| تخزين الصحّة | مُتحقَّق | **مُتحقَّق** | ✅ |
| إعادة احتساب الصحّة | مُتحقَّقة | **مُتحقَّقة** | ✅ |
| أسباب الصحّة غير مخزَّنة | مُتحقَّق | **مُتحقَّق** | ✅ |
| مكافحة التعداد | مُتحقَّقة | **مُتحقَّقة** | ✅ |
| عدد استعلامات النظرة العامّة | بلا انحدار | **بلا انحدار** | ✅ |
| المستندات/المهامّ/CRM/الماليّة | بلا مساس | **بلا مساس** | ✅ |

**النتيجة: 19/19 ⟹ GO للمرشَّح.**

---

## 6) التنظيف

بعد الدفع تُحذف الموارد المؤقّتة مع إثبات الحذف: `dropdb reporting_cpwr3_iso` · حذف `/tmp/cpw-r3-regression-20260815` · إزالة شجرة عمل المرشَّح.

**القواعد الحيّة لم تُمَسّ إطلاقًا:** `reporting_prod` · `reporting_rc` · `reporting_test_uat` · `reporting_test` — **صفر اتّصال، صفر كتابة**.
