# LEAVE-HR-QUEUE-VISIBILITY-AND-FINAL-APPROVAL-R1 — تقرير تشخيص إنتاجيّ قراءة‑فقط

> **الطبيعة:** تشخيص **قراءة‑فقط بالكامل** — صفر كتابة، صفر اعتماد، صفر Ledger، صفر خصم رصيد، صفر إعادة تشغيل، صفر نشر، صفر تعديل دور/سياسة، صفر طباعة أسرار. لم يُنفَّذ أيّ إصلاح — التصميم فقط.
> **التاريخ:** 6 أغسطس 2026. **البيئة:** Production (`reporting_prod`). **اللغة:** عربية كاملة.

---

## 1) فحص ما قبل الإنتاج (Production Preflight)
- الوقت: UTC والرياض (UTC+3) متزامنان؛ التشخيص جرى صباح 2026‑08‑06.
- `ASPNETCORE_ENVIRONMENT=Production`، خدمة `reporting-api` **نشطة**، `MainPID 505567`، `NRestarts 0`.
- Health داخليّ `http://127.0.0.1:5090/health`=200 وعامّ `https://reports.emarketingacademy.net/health`=200.
- Backend SourceLink الحيّ = `2d282cebf0a22f65b78cd751de17d6c927128d0d`.
- Migrations = **30**، الرأس `20260724224053` (بلا تغيير).
- Frontend bundle الحيّ = `index-Bq08cb54.js`.
- لا نشر/هجرة/أداة تسوية جارية؛ `email_outbox`=0؛ Email Pending/Processing/Failed=0/0/0.

## 2) حالة الطلبات الأربعة المرجعيّة (بالمعرّفات الكاملة، الحالة الفعليّة لا المتوقَّعة)
| الطلب | الموظف (Requester) | الحالة | الخطوة | ManagerReviewer | HrReviewer | Ledger | خصم رصيد |
|---|---|---|---|---|---|---|---|
| `9d445a3e-3470-46c4-b884-7fa356eb05ce` | أحمد نصار (`1c7f0896`) | **HrApproved** | **Completed** | `f6380cb1` (TL، طيّ P2) | **`249be4c0`** | مطبَّق | نعم |
| `2407739b-0c53-4abd-ad88-2a0ee0bbbbe2` | سمر مجدي (`d352528f`) | **HrApproved** | **Completed** | `8be4ba0c` (TL، طيّ P2) | **`249be4c0`** | مطبَّق | نعم |
| `f40ee93c-c050-4137-b8e7-0d6c66bf7439` | محمد إبراهيم (`50a2fa2c`) | **ManagerApproved** | **Hr** | **`249be4c0`** | **NULL** | 0 | لا |
| `5ed1efa3-b0da-4063-aca1-d27a2d13a8cf` | نور الدين رجب (`3f2e7a2b`) | **ManagerApproved** | **Hr** | **`249be4c0`** | **NULL** | 0 | لا |

**تغيّر مثبَت منذ الجلسة السابقة:** أحمد نصار وسمر مجدي **اعتُمِدا نهائيًّا من HR** صباح 2026‑08‑06 (≈07:03 UTC) وأصبحا `HrApproved/Completed` بخصم رصيد. الفارق الحاسم: `ManagerReviewerId` فيهما = **قائد الفريق** (نتيجة طيّ P2) وليس `249be4c0` ⇒ سمح للـHR باعتمادهما. أمّا محمد إبراهيم ونور الدين فبقيا عالقَين لأنّ `ManagerReviewerId` فيهما = **`249be4c0`** = مستخدم HR نفسه.

## 3) هوية مستخدم HR الفعليّ
| الحقل | القيمة |
|---|---|
| UserId | `249be4c0-729a-4e83-8791-01e8c19d6f9f` |
| Email (مقنّع) | `***2828@gmail.com` |
| FullName | محسن مجدي |
| IsActive | نعم |
| ManagerId | `f4e25122` |
| TeamId | NULL |
| JobRoleId | `0e77a46f` |
| **Roles** | **Admin + HR** |

## 4) أدوار/سياسات HR — والتعداد الكامل للمعتمِدين
- مستخدم HR **الوحيد** في النظام كلّه = محسن مجدي (صفّ واحد بدور HR).
- محسن يجتاز `Policies.LeaveReview` (يرى تبويب «بانتظار قراري») ويجتاز `Policies.LeaveFinalApproval` (`LeaveFinalApprovers = {Hr, Admin, Ceo, GeneralManager}`) — **لا نقص دور ولا سياسة**.
- تعداد المعتمِدين النهائيين النشطين (5): أحمد عبدالرؤوف (GeneralManager)، إبراهيم البحراوي (CEO)، **محسن مجدي (Admin+HR — المحجوب)**، مدير النظام `***wy@gmail.com` (Admin)، مدير النظام `***xperts.local` (Admin).
- **نتيجة مفصليّة:** يوجد **4 معتمِدين نهائيين آخرين** ليسوا مراجعين على الطلبات العالقة ⇒ يرونها في طابورهم ويستطيعون اعتمادها.

## 5) نقطة نهاية الطابور (Backend)
- `GET /api/leave-requests/pending` → `[Authorize(Policy = Policies.LeaveReview)]` → `LeaveRequestService.GetPendingAsync`.
- الاعتماد النهائي: `POST /api/leave-requests/{id}/hr/approve` → `[Authorize(Policy = Policies.LeaveFinalApproval)]` → `DecideAsync(step=Hr)`.

## 6) مُرشِّحات الطابور — والجواب الحاسم
`GetPendingAsync` يبني **baseQuery** موحّدًا لكلّ الطوابير (`LeaveRequestService.cs:86-88`):
```csharp
var baseQuery = _db.LeaveRequests.AsNoTracking()
    .Where(r => r.RequesterUserId != uid
        && r.TeamLeaderReviewerId != uid && r.ManagerReviewerId != uid && r.HrReviewerId != uid);
```
ثمّ فرع الاعتماد النهائي (`:110-111`): `baseQuery.Where(r => !r.IsHrRequest && r.Status == ManagerApproved)`.

**الجواب الدقيق على سؤال المهمّة:** لا يوجد عمود `CurrentApproverId` في السكيمة أصلًا؛ المعتمِد يُشتقّ من `CurrentStep` + حقول المراجِعين. الطلب يظهر في الطابور إذا كان `Status=ManagerApproved` و`CurrentStep=Hr` **بشرط ألّا يكون المستخدم الحاليّ أحد مراجعيه**. الطلبات العالقة `ManagerReviewerId = 249be4c0 = uid` ⇒ الشرط `r.ManagerReviewerId != uid` يصبح **false** ⇒ **تُستبعَد من الطابور كليًّا**. لا علاقة للأمر بـ`HrReviewerId=NULL` (وهو NULL في الطلب الظاهر أيضًا).

## 7) نتائج GET بهوية HR (محاكاة الاستعلام قراءة‑فقط)
> لا أملك كلمة مرور الحساب الحقيقيّ (محسن مجدي)، وممنوع طباعة توكن أو إعادة تعيين كلمة حساب حقيقيّ. لذا نُفِّذت **محاكاة أمينة للاستعلام الخلفيّ ذاته** على القاعدة (READ‑ONLY)، وهي دليل حتميّ لأنّ الاستعلام تحكّميّ. أربعة طلبات عند `ManagerApproved/Hr`:

| الطلب | الموظف | ManagerReviewer | ظهور لدى محسن (baseQuery) |
|---|---|---|---|
| `ca6831b9` | بسنت محمد | `f4e25122` | **VISIBLE_IN_QUEUE** |
| `5e0a681d` | شيريهان القاضي | `249be4c0` | **EXCLUDED** |
| `5ed1efa3` | نور الدين رجب | `249be4c0` | **EXCLUDED** |
| `f40ee93c` | محمد إبراهيم | `249be4c0` | **EXCLUDED** |

## 8) نتائج GET بهوية Admin (للمقارنة القرائيّة فقط)
- استُخدم Admin **للمقارنة القرائيّة حصرًا** لا للحكم على صلاحية HR. baseQuery نفسه مبنيّ على `uid` بصرف النظر عن الدور: أيّ معتمِد نهائيّ **ليس مراجعًا** على هذه الطلبات (مثل CEO إبراهيم البحراوي أو GM أحمد عبدالرؤوف أو Admin آخر) ⇒ الطلبات الثلاثة **تظهر** في طابوره **ويستطيع اعتمادها** (حارس السطر 543 لا ينطبق عليه). هذا يؤكّد أنّ العيب مرتبط بهوية المراجِع لا بالبيانات ولا بالواجهة.

## 9) الواجهة الأماميّة (Route/Component/Hook)
- المسار `/app/leave-requests`، المكوّن `LeaveRequestsPage.tsx`، تبويب `PendingTab` (`:381-384`) يستدعي `GET /leave-requests/pending` بمفتاح `['leave-requests-pending']`.
- **لا فلترة من جانب العميل ولا إخفاء لحالة `ManagerApproved/Hr`.** حالة الفراغ «لا توجد طلبات بانتظار قرارك» (`:393`) هي **عَرَض** لمخرَج خلفيّ فارغ لا سبب. التبويب مُتاح لمحسن (دور HR ضمن `LeaveReviewers`).

## 10) مُرشِّحات الواجهة
لا Tabs/Status‑mapping/CurrentStep‑mapping تُسقط الطلب؛ الواجهة تعرض بأمانة ما يرجعه الخادم. **الواجهة ليست العيب** (لم يُفترَض ذلك — قورِنت استجابة API الفعليّة بما يُعرَض).

## 11) تفويض الاعتماد النهائي (سبب تعذّر الاعتماد عند فتح الملف)
`CanViewAsync` (`:842-849`) يمنح `LeaveFinalApprovers` **حقّ العرض** لأيّ طلب ⇒ محسن يقدر يفتح تفاصيل الطلبات الثلاثة عبر ملف الموظف. لكن عند الاعتماد، `DecideAsync` يفرض حارسًا قبل أيّ تفويض بالخطوة (`:543-544`):
```csharp
if (entity.TeamLeaderReviewerId == uid || entity.ManagerReviewerId == uid || entity.HrReviewerId == uid)
    return Result<...>.Failure("لا يمكنك اتخاذ قرار على خطوة أخرى لنفس الطلب.", "auth.forbidden");
```
`ManagerReviewerId == uid` ⇒ **403 `auth.forbidden`** حتميّ. `CurrentApproverId` ليس شرطًا (لا وجود له)، و`HrReviewerId=NULL` ليس هو المانع؛ المانع هو **كون محسن مُعتمِد خطوة المدير**. السياسة `LeaveFinalApproval` تُجتاز على مستوى نقطة النهاية، ثمّ يسقط الطلب داخليًّا على حارس السطر 543.

## 12) المسح الشامل للطلبات (Population‑Wide Scan)
كلّ الطلبات عند `Status=ManagerApproved AND CurrentStep=Hr`: **الإجمالي = 4**، **Ledger=0 للأربعة**، **HrReviewerId=NULL للأربعة**.
- **A (ظاهر للطابور):** 1 — بسنت محمد (`ca6831b9`).
- **B (مخفيّ لأنّ HR هو مُعتمِد خطوة المدير):** **3** — شيريهان القاضي (`5e0a681d`)، نور الدين رجب (`5ed1efa3`)، محمد إبراهيم (`f40ee93c`).
- C/D/E/F = 0.

**اكتشاف إضافي:** الطلبات المتأثّرة **ثلاثة** لا اثنان — شيريهان القاضي طلب ثالث لم يكن ضمن المرجعيّة الأصليّة. العيب **محصور** في هذه الثلاثة (كلّها حيث محسن هو مُعتمِد خطوة المدير)، وليس انحرافًا سلوكيًّا عامًّا في الكود.

## 13) السبب الجذريّ (Root Cause)
**التصنيف: `MULTI_LAYER_DEFECT` — Confidence: HIGH** (مُثبَت بالكود + البيانات معًا). طبقتان لحارس فصل الواجبات نفسه (لا يبتّ الشخص خطوتين على الطلب ذاته):
1. **طبقة الطابور** — `GetPendingAsync` baseQuery (`:86-88`) تُخفي الطلب ⇒ سبب «لم يجدها في شاشة المراجعة».
2. **طبقة القرار** — `DecideAsync` (`:543-544`) تُرجِع 403 ⇒ سبب «لا توجد طريقة للاعتماد عند فتح الملف».

**الجذر البنيويّ:** محسن مجدي **مستخدم HR الوحيد** وهو **في الوقت نفسه مُعتمِد خطوة المدير** لهؤلاء الموظفين ⇒ اصطدام هيكليّ من صنف جمود P2 الأصليّ، لكن عند خطوة HR. **استبعادات مؤكَّدة:** ليس `HR_ROLE_MISSING` (لديه HR+Admin)، ولا `HR_ACCOUNT_MISMATCH` (الحساب الصحيح والوحيد)، ولا `LEAVE_FINAL_APPROVER_POLICY_MISMATCH` (السياسة تُجيز له بالدور)، ولا `FRONTEND_*` (الواجهة أمينة)، ولا `HR_REVIEWER_NOT_ASSIGNED` كسبب (NULL نتيجة لا سبب — الطلب الظاهر أيضًا NULL).

## 14) الأثر (Impact)
- **3 طلبات** عالقة عند `ManagerApproved/Hr` بلا اعتماد نهائيّ ولا خصم رصيد؛ أصحابها لا يحصلون على القرار.
- محصور تمامًا بالطلبات التي محسن مُعتمِدها الإداريّ؛ بسنت محمد غير متأثّرة. لا أثر على أيّ طلب آخر أو بيانات أخرى.

## 15) نطاق الإصلاح البرمجيّ (Hotfix Scope) — تصميم فقط
**العيب حقيقيّ وقابل للإصلاح برمجيًّا، لكن يوجد مسار تشغيليّ أضيق لا يمسّ الكود.**

- **الخيار A (تشغيليّ، صفر كود، مُوصى به — يحافظ على فصل الواجبات):** يعتمد **معتمِد نهائيّ آخر** (CEO إبراهيم البحراوي، أو GM أحمد عبدالرؤوف، أو Admin آخر) الطلبات الثلاثة — فهي تظهر في طابوره ولا يحجبه الحارس. يبقى بذلك **شخص مختلف** يبتّ خطوة HR عمّن بتّ خطوة المدير ⇒ فصل الواجبات محفوظ. **صفر كود/هجرة/إعادة تشغيل.**
- **الخيار B (Hotfix برمجيّ، أضيق نطاق):** استثناء «HR الوحيد» — السماح لمن كان مُعتمِد خطوة المدير بأداء اعتماد HR النهائيّ فقط حين يحمل دور HR **ولا يوجد مستخدم HR آخر** (نظير منطق «لا بديل تشغيليّ» في طيّ P2)، بتعديل موضعين متطابقين مفهومًا: baseQuery (`:86-88`) وحارس `DecideAsync` (`:543-544`). **تحذير:** يُضعِف فصل الواجبات (نفس الشخص يبتّ خطوتين) ⇒ يتطلّب قرار أعمال صريحًا، ويُفضَّل الخيار A عليه.
- **الخيار C (هيكليّ/تنظيميّ):** تعيين مستخدم HR ثانٍ، أو إخراج محسن من كونه مُعتمِد خطوة المدير في هذه السلاسل. تنظيميّ، صفر كود.

## 16) الحاجة لتصحيح بيانات (Data Repair)
**لا حاجة لتصحيح بيانات.** البيانات متّسقة وصحيحة (Status/CurrentStep/المراجِعون سليمة، Ledger=0 صحيح لعدم الاعتماد). الطلبات الثلاثة في حالة شرعيّة تنتظر اعتماد HR؛ لا تلف ولا انزياح.

## 17) الحاجة لتصحيح دور/سياسة (Role/Policy)
**لا خلل في الدور/السياسة.** محسن يحمل HR+Admin ويجتاز `LeaveReview` و`LeaveFinalApproval`. المانع حارس فصل واجبات لا نقص صلاحية. (الخيار C التنظيميّ — تعيين HR ثانٍ — يعالج الجذر البنيويّ لكنّه قرار إداريّ لا تصحيح تهيئة.)

## 18) سلامة الإنتاج
تشخيص قراءة‑فقط بالكامل: صفر كتابة، صفر UPDATE/INSERT/DELETE، صفر اعتماد، صفر Ledger، صفر خصم، صفر إعادة تشغيل، صفر نشر، صفر تعديل دور/سياسة/مراجِع، صفر طباعة أسرار. الاستعلامات كلّها `SELECT`. Manifest المهمّة السابقة `/root/secure/…` لم يُمَسّ.

## 19) الخطوة التالية
عرض الخيارات (A تشغيليّ مُوصى به / B هوتفكس أضيق / C تنظيميّ) على صاحب القرار، واختيار المسار. **لم يُنفَّذ أيّ إصلاح؛ لا اعتماد؛ لا كود؛ لا نشر** — بانتظار موافقة صريحة مستقلّة على المسار.

## 20) القرار النهائي
السبب الجذريّ مُثبَت بثقة عالية: **حارس فصل الواجبات ثنائيّ الطبقة (طابور + قرار) يصطدم بكون محسن مجدي مستخدم HR الوحيد وهو نفسه مُعتمِد خطوة المدير** ⇒ 3 طلبات مخفيّة وغير قابلة للاعتماد منه. يوجد **مسار تشغيليّ آمن (معتمِد نهائيّ آخر)** يحلّها بلا كود ويحفظ فصل الواجبات، وبديلٌ برمجيّ أضيق مصمَّم عند الطلب.

---

**`LEAVE-HR-QUEUE-VISIBILITY-AND-FINAL-APPROVAL-R1 / ROOT CAUSE CONFIRMED — READY FOR HOTFIX DESIGN APPROVAL`**
(المسار المُوصى به: تصحيح تشغيليّ عبر معتمِد نهائيّ بديل — قد يُغني عن الهوتفكس كليًّا. لا تنفيذ بلا تصريح جديد.)
