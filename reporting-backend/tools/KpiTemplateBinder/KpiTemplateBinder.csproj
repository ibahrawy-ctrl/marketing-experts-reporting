<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>KpiTemplateBinder</RootNamespace>
    <AssemblyName>KpiTemplateBinder</AssemblyName>
    <!-- أداة آمنة لمرة واحدة خارج الحل (Reporting.sln) — لا تُنشر مع الـ API ولا تدخل الاختبارات. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Reporting.Infrastructure\Reporting.Infrastructure.csproj" />
  </ItemGroup>

</Project>
