# تعليمات المستودع — كفاءة السياق (Context Efficiency)

هذه القواعد **افتراضات تشغيليّة مرنة** لا موانع مطلقة: خالِفها عند وجود ضرورة مُبرَّرة، وصرّح بالسبب في سطر واحد عند المخالفة.
يُستثنى ما عُلِّم بـ**[إلزاميّ]** — فهو غير قابل للمخالفة، وبعضه مفروض آليًّا عبر `permissions.deny` في `.claude/settings.local.json` (يُرفَض النداء من الأداة نفسها).
السياق التشغيليّ للمشروع (البيئات، الحوكمة، لغة التواصل، تاريخ التذاكر) موجود في ذاكرة الوكيل التلقائيّة ولا يُكرَّر هنا.

## 1) القراءة
- قبل `Read` لملفّ كبير: ابدأ بـ`Grep` لتحديد الموضع، ثمّ `Read` بـ`offset`/`limit`.
- افتراضيًّا اقرأ ≤400 سطر في النداء الواحد، والملفّات >40KB تُقرأ مقطعيًّا لا كاملة.
- **[إلزاميّ]** `Docs/**/*.md` **لا يُقرأ ملفٌّ منها كاملًا أبدًا** (110 ملفّات، متوسّط 22KB، أقصى 115KB ≈ 40k توكن = 20% من النافذة في نداء واحد؛ العربيّة ≈2.9 بايت/توكن أي أغلى من الكود بـ35%): `Grep` لتحديد الموضع ثمّ `Read` بـ`offset`/`limit` **≤200 سطر**. الاستثناء الوحيد: طلب صريح من المستخدم باستيعاب ملفّ بعينه كاملًا.
- **محجوبة آليًّا** (لا تحاول `Read` ولا `Grep` عليها): `*.Designer.cs` · `*ModelSnapshot.cs` (34 ملفًّا/3.29MB مولَّدة) · `node_modules/**` · `bin/**` · `obj/**` · `dist/**` · `package-lock.json`. عند الحاجة الحقيقيّة لمحتواها استعمل `find … -exec grep -c` (مسار غير مباشر مُتحقَّق منه، والبناء والهجرات لا تتأثّر بالحجب إطلاقًا).
- لا تُعِد قراءة ملفّ قرأته في نفس الجلسة إلّا إذا تغيّر فعلًا (بعد تعديل أو خطأ «modified since read»).

## 2) الأوامر والمخرجات
- أيّ أمر `Bash` متوقَّع الإطالة يُقيَّد بـ`| tail -N` أو `| head -N` أو `| wc -l`.
- للفروقات: `git diff --stat` أو `git diff -- <path>` أوّلًا؛ الـdiff الكامل عند الحاجة فقط.
- مخرجات `ssh`/السجلّات/الاختبارات: صفِّها بالمصدر (`grep`, `tail`) بدل جلبها كاملة ثمّ تصفيتها ذهنيًّا.
- `Grep`: ابدأ بـ`output_mode: files_with_matches`، ثمّ `content` مع `-C` صغير على الملفّات المرشَّحة.

## 3) أدوات المعاينة (`preview_*`)
- `preview_snapshot` / `preview_console_logs` للاستكشاف والتشخيص.
- `preview_screenshot` و`preview_network` مكلفتان جدًّا (حتّى 55KB للنداء الواحد) ⇒ استعملهما للإثبات النهائيّ للمستخدم لا للاستكشاف المتكرّر.

## 4) الوكلاء الفرعيّون (`Agent`)
- استعمل `Agent/Explore` للاستكشاف الواسع (>3 استعلامات متوقَّعة) — وهو الخيار المفضَّل لحماية السياق الرئيسيّ.
- ضمِّن في نصّ المهمّة إلزامًا صريحًا: **«أعِد خلاصة منظَّمة ≤2000 حرف: المسارات وأرقام الأسطر والاستنتاج فقط، بلا اقتباس كود»**.
- إن كانت التفاصيل ضروريّة، اطلب من الوكيل الفرعيّ كتابتها في ملفّ (`Docs/` أو `/tmp/`) وإعادة المسار فقط.
- لا تُكرّر في السياق الرئيسيّ بحثًا أنجزه وكيل فرعيّ.

## 5) الذاكرة التلقائيّة
- `MEMORY.md` يُحقن كاملًا في كلّ جلسة ⇒ أبقِه ≤90 سطرًا/≤15KB. التفاصيل تذهب إلى ملفّات المواضيع والأرشيف.
- أيّ محتوى يخرج من `MEMORY.md` يُنقَل حرفيًّا إلى ملفّ أرشيف مع إثبات تطابق (`cmp`/`md5`) قبل حذفه من المصدر.

## 6) التفاصيل الكبيرة وحدود السياق
- التحاليل والتقارير الطويلة يكتبها **الوكيل الرئيسيّ** أيضًا في ملفّ (`Docs/Planning/…` أو `Ops/…` بعد إقرار الموضع)، ويُعاد إلى السياق ملخّص منظّم فقط: النتيجة، الأدلّة الرقميّة، المسارات، الخطوة التالية.
- عند اقتراب السياق من الامتلاء: أوقف التوسّع فورًا، اكتب الحالة والنتائج الجزئيّة إلى ملفّ، وأبلِغ المستخدم بنقطة التوقّف بدل الاستمرار حتّى الانقطاع.
