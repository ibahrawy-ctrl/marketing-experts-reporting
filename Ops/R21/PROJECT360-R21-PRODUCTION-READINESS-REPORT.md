# تقرير جاهزيّة الإنتاج — Project 360 R2.1

**التذكرة:** PROJECT360-R21-PRODUCTION-READINESS-AND-ISOLATED-REHEARSAL
**التاريخ:** 23 أغسطس 2026
**الطبيعة:** هذا التقرير قرار جاهزيّة (Readiness) لا **تصريح نشر**. أيّ نشر فعليّ على الإنتاج يتطلّب رسالة تصريح صريحة جديدة ومنفصلة بعد مراجعة هذا التقرير.

---

## 0) خطّ الأساس المرجعيّ (يُفصَل بوضوح)

| المقدار | القيمة |
|---|---|
| `PRODUCT_SOURCE_SHA` (مصدر المرشّح) | `7e063b493b50ad90ba6131e47042c7cd035fb65b` |
| `DOCUMENTATION_HEAD` (رأس التوثيق على develop) | `d3d2e62` |
| `PRODUCTION_CURRENT_SHA` (المنشور حاليًّا على الإنتاج) | `ce166662` |

الثلاثة كيانات مختلفة عمدًا: المرشّح المبنيّ (`7e063b4`) غير المنشور، ورأس التوثيق الأحدث (`d3d2e62`)، والإنتاج الحيّ الثابت (`ce166662`).

---

## 1) نطاق العمل والحوكمة

نُفِّذت التذكرة كاملةً على **نُسخ معزولة فقط**: نسخة احتياطيّة بصلاحيّات مقيّدة، ودليل Staging غير مُشار إليه من الخدمة/nginx، وقاعدة مستنسخة مؤقّتة من نسخة الإنتاج الاحتياطيّة، وتطبيق معزول على منفذ محلّيّ غير مستعمَل (5099). **لم يُمَسّ الإنتاج الحيّ إطلاقًا** (لا حزمة، لا قاعدة، لا هجرة، لا إعداد، لا nginx، لا إعادة تشغيل، لا أدوار، لا صلاحيّات)، و`origin/main` لم يتحرّك.

---

## 2) بناء مرشّح الإنتاج — `PRODUCTION_CANDIDATE_BUILT = YES`

- **الخلفيّة:** أُعيد استعمال قطعة RC المختبَرة **مطابقة بايتًا** (أصل الـAPI إعداد تشغيليّ في `/etc/reporting-api.env` لا مخبوز في الـDLL). بصمة الشجرة `36d7f525…` (86 ملفًّا).
- **الواجهة:** أُعيد بناؤها من `7e063b4` بعنوان الإنتاج `https://reports.emarketingacademy.net/api`. بصمة `165bdd1a…` (7 ملفّات، 0 خرائط مصدر). دليل الحتميّة: الـCSS مطابق بايتًا لبناء RC، والفارق الوحيد في الـJS هو سلسلة أصل الـAPI.
- `CANDIDATE_BACKEND_SHA256 = 36d7f525dc0ee132a4490c08994479b0776e3e89430a7d125dcc0c56f7fc21b4`
- `CANDIDATE_FRONTEND_SHA256 = 165bdd1a05bc42105315d1c20ff0c73bc3b5e6ace6cd4775ef5288685e591a97`
- أصول مخبوزة: إنتاج ×1، localhost/127/test/rc ×0. **مسح أسرار الحزمة:** لا أسرار في Staging.

التفصيل: `Ops/R21/prod-readiness/candidate-build.json`.

---

## 3) النسخ الاحتياطيّ والاستعادة — `PROD_BACKUP = PASS` · `PROD_RESTORE_VERIFIED = YES`

نسخة كاملة في `/opt/backups/prod-readiness-20260823T113453Z-r21` (0700): dump مزدوج (Fc+Fp)، والحِزم (publish/dist/env/service/nginx/DataProtection)، وSHA256SUMS. تحقّق الاستعادة على **نسخة مستنسخة معزولة** (لا فوق أيّ قاعدة حيّة): 30=30 هجرة، 57=57 جدولًا، 637=637 عمودًا، `audit_logs` 1401=1401، بصمة البنية مطابقة. لا نسخة سابقة حُذفت، ولا سرّ طُبع.

التفصيل: `Ops/R21/prod-readiness/backup-and-restore.json`.

---

## 4) نَسَب الهجرات — `MIGRATION_LINEAGE = NOT_CLEAN` (جسر مطلوب، ومُثبَت)

نَسَب متشعّب: هجرتان منطقيّتان بمعرّفَين مختلفَين على نَسَب الإنتاج مقابل نَسَب المرشّح ⟹ نشر ساذج يُعيد تشغيل `Up()` على جدولَين موجودَين ⟹ `42P07`. العلاج جسر يعلّم صفّي المرشّح «مطبَّقَين» بلا `Up()`. الحالة: `PROD=30` · `RC=42` · ملفّات المرشّح=40.

التفصيل: `Ops/R21/prod-readiness/migration-lineage-audit.json`.

---

## 5) التجربة المعزولة — `ISOLATED_MIGRATION_REHEARSAL = PASS` · `ISOLATED_HEALTH = 200`

على قاعدة مستنسخة طازجة وتطبيق على 5099 (loopback فقط، تكاملات معطّلة، اتّصال خارجيّ واحد فقط 127.0.0.1:5432):
- مسار الهجرات: 30 ⟵ (+2 جسر) ⟵ 32 ⟵ (+10 حقيقيّة) ⟵ **42**، الرأس `AddProjectExecutionUpdateProposals`، **صفر 42P07**.
- **صفر فقدان بيانات:** audit_logs 1401 · AspNetUsers 34 · projects 34 · clients 11 — مطابقة لأساس الإنتاج.
- إعادة تشغيل: 0 «Applying migration»، صحّة 200، التاريخ يبقى 42.
- 79 جدولًا · 947 عمودًا (يطابق RC).

التفصيل: `Ops/R21/prod-readiness/isolated-rehearsal.json`.

---

## 6) تخزين المستندات و`PROD-READINESS-01`

الإنتاج بلا `FileStorage__DocumentsRootPath` ⟹ الجذر يسقط داخل `publish` المستبدَل في كلّ نشر = فقدان صامت. أُثبِت الإغلاق على النسخة المعزولة: دورة حياة كاملة (رفع/بيانات/تنزيل/إصدار v2/أرشفة/استرجاع، بصمات مطابقة)، والـblobs خارج `publish` نجت من استبدال الحزمة (`REDEPLOY_NO_LOSS = YES`). العلاج **إعداد فقط**: إضافة `FileStorage__DocumentsRootPath=/var/lib/reporting/documents` إلى `/etc/reporting-api.env`.
- `PROD_READINESS_01 = PROVEN CLOSED on isolated copy — NOT yet applied to production` ⟹ **حاجز إعداد مفتوح يُغلَق عند التنفيذ**.
- `DOCUMENT_STORAGE_REHEARSAL = PASS`.

التفصيل: `Ops/R21/prod-readiness/isolated-rehearsal.json` (قسم prodReadiness01Proof).

---

## 7) بوّابات البناء والاختبار (رموز خروج حقيقيّة) — PASS

| البوّابة | النتيجة | رمز الخروج |
|---|---|---|
| بناء الخلفيّة (Release) | 0 خطأ، 4 تحذيرات | 0 |
| اختبارات الوحدة | 359/359 | 0 |
| **التكامل (متسلسل، موثوق)** | **2011/2011** | **0** |
| دخان مستهدف (Project360/ExecutionBridge/Workstream/Documents/Portfolio) | 91/91 | 0 |
| اختبارات الواجهة (vitest) | 588/588 (50 ملفّ) | 0 |

**درس تشغيليّ:** التشغيل المتوازي أظهر فشل 13 اختبار `RepeatableNumericValidation` (مجموعة `PfeNumericIsolated`) بسبب **سباق تهيئة** بين المجموعات المعزولة والمجموعة الثقيلة `Integration` — **قطعة أثر في المِنصّة لا عيب منتج**: الاختبارات نفسها 13/13 ناجحة منفردة، والتشغيل المتسلسل 2011/2011 برمز 0. المقياس الموثوق هو المتسلسل (`parallelizeTestCollections=false`).

التفصيل: `Ops/R21/prod-readiness/build-and-test-gates.json`.

---

## 8) مصفوفة الـ115 والانحدار القائم على المخاطر

الـ115 = 90 (حزم R1) + 25 (`UAT-R2`) **مكتبة حالات قابلة للتنفيذ لا نتائج**، تحملها كرّاسة R2.1 نموذجًا فارغًا. **عدم تنفيذها لا يمنع النشر** (حسب `FINAL-ACCEPTANCE-GATE.md:149`)، ويمنع فقط ادّعاء `141/141 PASS`. سطح تغيير R2.1 مغطّى بالبوّابات الآليّة (تكامل/وحدة هذه الدورة) + 26/26 من `UAT-R21` بأدلّة حقيقيّة.
- `LEGACY_115_EXECUTED = 0` (غير مانع) · `RISK_BASED_REGRESSION = الحزمة الآليّة المتسلسلة على النسخة المعزولة`.

التفصيل: `Ops/R21/prod-readiness/legacy-matrix-and-regression.json`.

---

## 9) `BASELINE-DEFECT-02` — غير مانع

`EmployeeProfileScopeTests.Profile_Summary_Reflects_Submitted_Kpi`: قطعة أثر تجهيز اختبار (صنف `BASELINE-DEFECT-01` نفسه) لا عيب منتج. **ناجح** منفردًا على قاعدة نظيفة وضمن التشغيل المتسلسل 2011/2011. **لم يُجرَ أيّ تعديل شيفرة** (تصليح التجهيز تغيير شيفرة يُؤجَّل لتذكرة عزل مستقلّة). `BASELINE_DEFECT_02 = OPEN (test-fixture only) — NON-BLOCKING`.

التفصيل: `Ops/R21/prod-readiness/baseline-defect-02.json`.

---

## 10) الدخان المعزول — `ISOLATED_SMOKE = PASS`

صحّة 200 · مسار محميّ بلا مصادقة 401 · دخول خاطئ 401 · SignalR negotiate بلا مصادقة 401 · CORS يرفض أصلًا أجنبيًّا ويقبل أصل الإنتاج فقط · قوائم/Project360 overview/Client360 بمصادقة 200 · اتّصال خارجيّ واحد (postgres) · لا SMTP · صندوق البريد 0 غير مُرسَل · لا أخطاء حقيقيّة في السجلّ · لا طلبات localhost/TEST/RC. (تقديم SPA ثابت مسؤوليّة nginx لا الـAPI؛ سلامة `dist` مُتحقَّقة منفصلةً.)

التفصيل: `Ops/R21/prod-readiness/isolated-smoke.json`.

---

## 11) خطّة النشر (20 خطوة، لم تُنفَّذ) والتراجع — `ROLLBACK_READY = YES`

خطّة تنفيذيّة كاملة من الإعلان ⟵ نسخة طازجة ⟵ جسر (dry ثمّ apply) ⟵ إيقاف الخدمة (بدء التوقّف) ⟵ نشر الحزمتَين ⟵ تعديل env (documents root) ⟵ تشغيل (MigrateAsync ×10) ⟵ صحّة 200 (نهاية التوقّف) ⟵ دخان عبر nginx ⟵ توثيق. **نقطة اللاعودة:** بدء الخطوة 13 (كتابة الهجرات). **تراجع مستوى 1:** إعادة الحزمة القديمة (متوافق بنيويًّا لأنّ الهجرات إضافيّة بحتة). **مستوى 2:** استعادة قاعدة كاملة (مرجعيّة). `EXPECTED_DOWNTIME ≈ 2–5 دقائق`.

التفصيل: `Ops/R21/prod-readiness/deployment-and-rollback-plan.json`.

---

## 12) الحواجز المفتوحة قبل التنفيذ — `OPEN_BLOCKERS`

1. **`PROD-READINESS-01`** (إعداد): إضافة `FileStorage__DocumentsRootPath` — مُثبَت الإغلاق على النسخة المعزولة، لم يُطبَّق على الإنتاج بعد.
2. **مفاتيح DataProtection لـwww-data** غير موجودة في النسخة الاحتياطيّة ⟹ التفعيل يُبطِل الجلسات ما لم تُهيَّأ حلقة مفاتيح دائمة قبل إعادة التشغيل.
3. **تصريح نشر صريح جديد** — غير موجود (هذه التذكرة ليست تصريحًا).

---

## 13) القرار

| المقدار | القيمة |
|---|---|
| `PRODUCTION_READINESS` | **CONDITIONAL_GO** |
| `PRODUCTION_DEPLOY_AUTHORIZED` | **NO** |
| `NEXT_REQUIRED_ACTION` | مراجعة هذا التقرير، ثمّ (عند القرار) إصدار تصريح نشر صريح منفصل مع إغلاق الحاجزَين الإعداديَّين (documents root + مفاتيح DataProtection) أثناء نافذة الصيانة حسب خطّة النشر. |

**التبرير:** المرشّح مبنيّ ومختبَر ومُجرَّب معزولًا بالكامل (بناء/اختبار PASS، تكامل 2011/2011، جسر الهجرات مُثبَت بلا فقدان، دخان معزول PASS، نسخة/استعادة مؤكَّدة). يتبقّى حاجزان **إعداديّان** يُغلقان عند التنفيذ، وتصريح نشر صريح. لذلك القرار **مشروط** لا `GO` مطلق ولا `NO_GO`.

---

## 14) فحص بصريّ (Visual QA) ومسح الأسرار

- **مسح أسرار الحزمة/التقرير:** لم تُطبَع أيّ أسرار؛ Staging بلا ملفّ بيئة أو قيم سرّيّة؛ سرّ env يُحفظ كمجموع تحقّق فقط.
- **Visual QA:** يُجرى على مخرجات DOCX/PDF (اتّجاه RTL، سلامة الجداول، لا نصّ مبتور) قبل التسليم.

---

## 15) صفحة الاعتماد (فارغة عمدًا)

> **تنبيه:** الجاهزيّة ليست تصريح نشر. هذه الصفحة تُملأ فقط عند إصدار تصريح نشر صريح منفصل.

| الحقل | القيمة |
|---|---|
| اسم المُصرِّح (مالك المنتج) | ____________________ |
| القرار (نشر / تأجيل) | ____________________ |
| التاريخ والوقت | ____________________ |
| نطاق التصريح | ____________________ |
| التوقيع | ____________________ |
