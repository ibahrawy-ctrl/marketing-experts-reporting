# نشر Hotfix حارس نشر القوالب على الإنتاج ومصالحة القوالب الثلاثة — تقرير الإغلاق

**التذكرة:** `REPORT_TEMPLATE_PUBLICATION_GUARD_HOTFIX_R1`
**التاريخ:** 31 أغسطس 2026
**المرشّح المنشور:** `d25dc69` (`fix/report-template-publication-guard-r1`) — **نفس القطعة المُثبَتة على TEST وRC**
**الحالة السابقة على الإنتاج:** `045cb0d`
**العناوين:** إنتاج `reports.emarketingacademy.net` · TEST `test.emarketingacademy.net` · RC منفذ 5092

---

## 1) الحكم

```
REPORT_TEMPLATE_PUBLICATION_GUARD_HOTFIX_PRODUCTION_PASS
PRODUCTION_SHA=d25dc69
STARTUP_PUBLICATION_MUTATION_ELIMINATED
AFFECTED_TEMPLATES_RECONCILED=3
PUBLISHED_VERSIONS=80
POST_RESTART_STABILITY_PASS
E0_READY_FOR_SEPARATE_ACTIVATION_GATE
```

**لم يُفعَّل Phase 2 ولم يُبدأ E0.** الحجب قائم بانتظار تذكرة مستقلّة تبدأ بمجموعة Pilot صغيرة.

---

## 2) المرحلة 0 — النَسَب وحالة الفروع

| البند | القيمة |
|---|---|
| `HOTFIX_RUNTIME_SHA` | `d25dc69` |
| `HOTFIX_DOCUMENTATION_HEAD` | `a1c397f` |
| محلّيّ = بعيد | `LOCAL = REMOTE = a1c397f` (`Everything up-to-date`) |
| `d25dc69` سلف للفرع البعيد | نعم |
| دلتا التشغيل مقابل `develop` | **3 ملفّات** (`TemplateSeeder.cs` + ملفّا اختبار) |
| `origin/develop` وقتها | `2b37e39` بدلتا تشغيل **فارغة** فوق `045cb0d` |

أمر الدفع الرسميّ المستعمل:
`GIT_SSH_COMMAND="ssh -i ~/.ssh/github_ed25519 -o IdentitiesOnly=yes -o BatchMode=yes"`

## 3) المرحلة 1 — خطّ الأساس المقيس قبل أيّ لمسة

| المقياس | القيمة |
|---|---|
| `report_template_versions` الكلّيّ | **107** |
| المنشور | **77** (كان 81 قبل عطب الحارس) |
| `report_submissions` | **330** |
| الهجرات المطبَّقة | **49** |
| `max(UpdatedAtUtc)` | `2026-08-31 06:39:09.825766+00` |
| بصمة `versions` CSV | `54dc81f2e99d060b0c69cd31901a6324` |
| بصمة `submissions` CSV | `1d7806dba3b8e324edaf94fb1245d97f` |
| عائلات متعدّدة النشر | 16 |
| أخطاء 5xx حقيقيّة | 0 |

**الضرر المرصود (مقارنة `tplguard_before` السليمة بالإنتاج):**

| القالب | الأساس السليم | الإنتاج قبل الإصلاح | الفعّال قبل | الفعّال المطلوب |
|---|---|---|---|---|
| تقرير المديرشن الأسبوعي | v8=t · v9=t | v8=t · v9=**f** | v8 ✗ | v9 |
| تقرير فريق الفيديو | v7=t · v8=t | v7=t · v8=**f** | v7 ✗ | v8 |
| تقرير كاتب المحتوى الأسبوعي | v8=t · v9=t | v8=t · v9=**f** | v8 ✗ | v9 |
| تقرير فريق التصميم | v7=t · v8=t | v7=**f** · v8=t | v8 ✓ | v8 (سليم — **مستثنى من المصالحة**) |

أربعة إصدارات أُلغي نشرها (81→77)، لكنّ **ثلاثة قوالب فقط** تضرّر فيها الإصدار الفعّال. لذلك استُهدفت ثلاث عائلات لا أربع.

## 4) المرحلة 2 — النسخ الاحتياطيّ الثلاثيّ

`TS=20260831T140007Z` — نسخة كاملة `-Fc` + نسخة مخطَّط + نسخة بيانات، متحقَّق منها بـ`pg_restore -l`: **507 مدخل TOC · 84 TABLE DATA · `report_template_versions` حاضر**. زائدًا نسخة `publish` عند `045cb0d` (بصمة `ebf2b84e…`) ونسخة `/etc/reporting-api.env`. `PENDING_MIGRATIONS=0` مُثبَت بدلتا Migrations فارغة.

## 5) المرحلة 3 — النشر

| البند | القيمة |
|---|---|
| بصمة مجموعة الـDLL | `f75f926c490440176d133feacd1e4d01` — **STAGING = RC = TEST** |
| نَسَب الـDLL الحيّ | `1.0.0+d25dc696…` |
| `MainPID` | 1815908 · `NRestarts=0` |
| الإقلاع | 2026-08-31 14:02:11 UTC |
| `/health` | 200 |
| الهجرات | 49 (بلا تغيير) |
| `VERSIONS_DIFF` / `SUBMISSIONS_DIFF` | **0 / 0** |
| عبارات `UPDATE` أثناء الإقلاع | **0** |

## 6) المرحلة 4 — Smoke

مجسّات 200/401/404 كما هو متوقّع · `/health` العامّ 200 · 41 قالبًا · 107/77 · 330 تسليمًا · 1534 سجلّ تدقيق · 5 حقول `workItems` · 5 حقول `schemaVersion=2`.

## 7) المرحلة 5 — Dry-run الإنتاج

الخطّة: `plan-prod.json` (`md5=e7bdf86fb32fb72920cf1d9043d7d52c`) مولَّدة من الأساس السليم `tplguard_before` (107|81) وهدفها `reporting_prod`، **مُقلَّصة إلى ثلاث عائلات** بحذف «تقرير فريق التصميم».

| الفحص | النتيجة |
|---|---|
| `--verify-only` | خروج 5 · `refusals=0` · ثلاث عائلات `NOT_COMPLIANT` (متوقّع) |
| Dry-run | `DRY_RUN_OK` · `refusals=0` |
| `WOULD_PUBLISH` | **3** — المديرشن v9 · الفيديو v8 · كاتب المحتوى v9 |
| `TOTAL` | 107 |
| `PUBLISHED` | 77 → **80** |
| `TARGET_FAMILIES` | 3 |
| `HISTORICAL_RELINKS` / `DELETIONS` | 0 / 0 |
| كتابة أثناء Dry-run | **لا شيء** — `maxUpd` و`audit=1534` بلا تغيير |

**مطابق للنتيجة المتوقّعة حرفًا بحرف.**

### تعارض موثَّق وحلّه

بند المرحلة 6 «لكلّ عائلة مستهدفة: إصدار واحد منشور فقط» **يتعارض حسابيًّا** مع `PUBLISHED=80`:
- الأداة **لا تُلغي نشر أيّ إصدار إطلاقًا** (النشر فعل توسيعيّ عبر `PublishVersionAsync` حصرًا).
- 80 = 77 + 3 لا تتحقّق إلّا بإصدارَين منشورَين لكلّ عائلة مستهدفة.
- وهذه **بالضبط حالة الأساس السليم قبل العطب** (v8+v9 · v7+v8 · v8+v9)، و16 عائلة أخرى على الإنتاج تحمل أصلًا أكثر من إصدار منشور ⟹ «إصدار واحد منشور» لم يكن ثابتًا قطّ.

**التفسير المعتمد:** «**إصدار فعّال واحد لا لبس فيه**» — عقد زمن التشغيل يختار الأعلى `VersionNumber` بين المنشور (`SubmissionService.cs:69-72` · `ReportTemplateService.cs:359` · `UnifiedReportStatusService.cs:264`). نُفِّذ الرقم الصريح `80`.

## 8) المرحلة 6 — التطبيق

```
== وضع التشغيل: APPLY ==
  استُعيد PublishedAtUtc الأصليّ لـ«تقرير المديرشن الأسبوعي» v9: 2026-08-31T06:12:37.8388000Z
  استُعيد PublishedAtUtc الأصليّ لـ«تقرير فريق الفيديو» v8: 2026-08-31T06:12:37.3260720Z
  استُعيد PublishedAtUtc الأصليّ لـ«تقرير كاتب المحتوى الأسبوعي» v9: 2026-08-30T16:43:02.0804590Z
== الحكم: APPLIED_AND_VERIFIED ==
```

| الفحص | النتيجة |
|---|---|
| المنشور | 77 → **80** |
| الكلّيّ | 107 → 107 (**لا حذف ولا إنشاء**) |
| صفوف تغيّرت | **3 فقط** — `IsPublished: f→t`، لا عمود آخر |
| المعرّفات المتغيّرة | `a28354c2…` (المديرشن v9) · `f32ea97c…` (الفيديو v8) · `ed4796e4…` (كاتب المحتوى v9) |
| `report_submissions` | 330 · **CSV متطابق بايتًا ببايت** ⟹ `HISTORICAL_RELINKS=0` |
| سجلّ التدقيق | 1534 → **1537** = ثلاثة مداخل `TemplateVersionReconciled` بالضبط |
| إشعارات/بريد | **0** منذ التطبيق |
| Dry-run الثاني | `DRY_RUN_NOTHING_TO_DO` · `CHANGES_REQUIRED=0` |

**الإصدارات الفعّالة بعد المصالحة** (العنوان · الإصدار · عدد الحقول · `schemaVersion=2` · `workItems`):

```
تقرير المديرشن الأسبوعي     | 9 | 5 | t | t
تقرير فريق التصميم          | 8 | 5 | t | t
تقرير فريق الفيديو          | 8 | 5 | t | t
تقرير كاتب المحتوى الأسبوعي | 9 | 5 | t | t
```

القوالب الأربعة كلّها استعادت `schemaVersion=2` و`workItems`.

## 9) المرحلة 7 — إقلاع التحقّق الواحد

| البند | القيمة |
|---|---|
| وقت الإقلاع | 2026-08-31 **14:12:57** UTC |
| `MainPID` / `NRestarts` | 1818304 / **0** (لا حلقة إعادة تشغيل) |
| `ActiveState` | `active (running)` · `/health=200` |
| المنشور بعد الإقلاع | **80** — **لا ارتداد إلى 77** |
| الكلّيّ / التسليمات / التدقيق | 107 / 330 / 1537 |
| `max(UpdatedAtUtc)` | `14:11:39.097166` — **أسبق من الإقلاع 14:12:57** |
| `VERSIONS_DIFF` / `SUBMISSIONS_DIFF` | **0 / 0** |
| عبارات `UPDATE` في سجلّ الإقلاع | **0** |
| `INSERT INTO report_template*` | **0** |
| هجرات مطبَّقة | 0 |
| أخطاء | 0 |

**البرهان الحاسم:** آخر كتابة على `report_template_versions` سابقة للإقلاع، ومع ذلك بقي المنشور 80. الحارس القديم (`045cb0d`) كان سيفرض «فائزًا» بترتيب GUID ويُعيد الحالة إلى 77.

**تشخيص الحارس الجديد — قراءة فقط، تسعة أسطر** (ثلاثة حرّاس × ثلاث عائلات):

```
warn: TemplateSeeder[0]
      TemplateSeeder[ProjectFirst]: القالب «تقرير كاتب المحتوى الأسبوعي» (5e6ad325…) يملك 2 إصدارًا
      منشورًا [v9, v8]. زمن التشغيل سيستعمل v9. لم تُجرَ أيّ كتابة؛ المصالحة — إن لزمت — تمرّ بالمسار
      الرسميّ للنشر.
```

نفس النمط لـ`TaxonomyV3` و`TaxonomyV4` وللعائلات الثلاث. كلّ سطر يُقرّ بالفائز الصحيح **بلا أيّ كتابة**.

## 10) المرحلة 8 — المراقبة

نافذة **32 دقيقة** من `14:14:19Z` إلى `14:46:21Z` بلقطة كلّ دقيقتين.

| المقياس | النتيجة |
|---|---|
| لقطات `/health` | **16/16 = 200** |
| `MainPID` | 1818304 ثابت — قيمة واحدة فقط طوال النافذة |
| `NRestarts` | 0 (لا إعادة تشغيل غير مقصودة) |
| `TOTAL` / `PUBLISHED` / `SUBMISSIONS` / `AUDIT` | 107 / **80** / 330 / 1537 — ثابتة في كلّ لقطة |
| `max(UpdatedAtUtc)` | `14:11:39.097166` بلا تغيير طوال النافذة |
| `VERSIONS_DIFF_WINDOW` / `SUBMISSIONS_DIFF_WINDOW` | **0 / 0** |
| تحذيرات `TemplateSeeder` غير متوقّعة | **0** |
| `UPDATE report_template_versions` | **0** |
| `Unhandled exception` | **0** |
| 5xx داخل النافذة | **0** (54 طلبًا) |

**تحليل الـ5xx على مستوى اليوم كلّه:** ستّة إجماليًّا، **كلّها** `502` على `POST /hubs/notifications/negotiate` أثناء إعادات تشغيل مخطَّطة — اثنان `06:39:03/05` (إقلاع `045cb0d`، سابق للتذكرة وجزء من خطّ الأساس) وأربعة `14:02:11/14` (إقلاع نشر الهوتفكس، المرحلة 3). عابرة ويُشفى منها SignalR ذاتيًّا بإعادة الاتصال. **صفر 5xx عند إقلاع التحقّق `14:12:57` وصفر داخل نافذة المراقبة.**

الملفّ: `evidence/prod-d25dc69/08-MONITOR.txt`.

## 11) الأدلّة

`Ops/HOTFIX/REPORT-TEMPLATE-PUBLICATION-GUARD-R1-20260831/evidence/prod-d25dc69/`

| الملفّ | المحتوى |
|---|---|
| `01-BASELINE.txt` | خطّ الأساس قبل أيّ لمسة |
| `02-MATRIX-BEFORE.txt` | مصفوفة النشر الكاملة قبل الإصلاح |
| `05-DRYRUN.txt` | نتيجة `--verify-only` وDry-run وإثبات انعدام الكتابة |
| `06-APPLY.txt` | فروق الصفوف ومداخل التدقيق والحالة الفعّالة بعد التطبيق |
| `07-RESTART.txt` | أدلّة إقلاع التحقّق كاملة مع أسطر التشخيص التسعة |
| `08-MONITOR.txt` | نافذة المراقبة |
| `plan-prod.json` | خطّة المصالحة (3 عائلات) |
| `verify-prod.json` · `dryrun-prod.json` · `apply-prod.json` · `dryrun2-prod.json` | تقارير الأداة الأربعة |

## 12) ما لم يُلمَس

- `main` — لم يُمسّ.
- أعلام Phase 2 و`perm` — لم تُعدَّل.
- لا حسابات إنتاج جديدة · لا تعديل على تقرير تاريخيّ · لا حذف إصدار.
- «تقرير فريق التصميم» — استُثني عمدًا لأنّ إصداره الفعّال كان سليمًا.
- بقايا شجرة العمل الرئيسيّة (`TemplateSeeder.cs` غير الملتزَم + ملفّا الاختبار غير المتعقَّبَين + تعديل `CLAUDE.md`) — لم تُلمَس.
- R22B — لم تُمسّ.

## 13) الخطوة التالية

E0 يُفتح **كتذكرة مستقلّة** تبدأ بقرار منح الصلاحيّات والأعلام لمجموعة **Pilot صغيرة**، لا تشغيلًا عامًّا مباشرًا. معايير رفع الحجب في `03-E0-UNBLOCK-CRITERIA.md` — الشروط 2 و3 و4 و5 و6 صارت **مستوفاة** بهذا التقرير؛ يبقى الشرط 7 (نافذة المراقبة) والشرط 8 (الدمج) ثمّ قرار المنتج.
