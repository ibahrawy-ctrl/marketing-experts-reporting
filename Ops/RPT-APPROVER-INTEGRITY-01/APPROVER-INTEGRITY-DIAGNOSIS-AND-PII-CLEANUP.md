# RPT-APPROVER-INTEGRITY-01 — تشخيص سلامة المعتمِدين وتنظيف مخلَّفات الـPII

- **التذكرة:** `RPT-APPROVER-INTEGRITY-01` (مستقلّة تمامًا عن R22B)
- **التاريخ:** 7 سبتمبر 2026
- **الطبيعة:** تشخيص للقراءة فقط + بوّابة تنظيف أدلّة. **صفر كتابة على بيانات الإنتاج.**
- **حالة R22B:** `REMAINS_CLOSED` — لم يُعَد فتحها ولم يُعَد نشرها ولم تُمسّ الكناري المغلقة.

> **إخفاء الهويّات:** كلّ المعرّفات في هذا التقرير مقصوصة إلى 8 محارف، والأسماء إلى 3 محارف + `***`. لا بريد كامل ولا توكِن ولا كوكيز ولا أسرار.

---

## 1) الحالة الابتدائيّة (`START_STATE_GATE`)

| البند | القيمة المقيسة حيًّا | المتوقَّع | الحكم |
|---|---|---|---|
| `origin/develop` | `069e0ba55b1525161e731961b584a994ad378590` | `069e0ba` | ✅ مطابق |
| `origin/main` | `508509ad8474b321c80cbdd48eb84ecb54bee212` | `508509ad…` | ✅ مطابق |
| `origin/release/r22b-production-20260906` | `069e0ba55b1525161e731961b584a994ad378590` | `069e0ba` | ✅ مطابق |
| SHA تطبيق الإنتاج (SourceLink) | `c5e0202d0a528a1a45856790716e449b812f0184` | `c5e0202` | ✅ مطابق |
| `/health` الإنتاج | `200 ok` | 200 | ✅ |
| خدمة `reporting-api` | `active (running)` منذ 2026-09-06 17:48:13 UTC | active | ✅ |
| الهجرات المعلّقة | `0` | 0 | ✅ |
| `git diff c5e0202 069e0ba -- reporting-backend reporting-frontend/src` | **فارغ** | — | ✅ `develop` متقدّم بالتوثيق فقط |

```text
START_STATE_GATE      = PASS
CONCURRENT_WRITER     = NONE (worktree معزول /private/tmp/rpt-approver-wt · git status نظيف 0 مدخل)
REMOTE_REFS_STABLE    = YES (قياسان متباعدان بنفس النتيجة)
PRODUCTION_IDENTITY   = c5e0202 (مطابق للمُصرَّح)
WRITES_PERFORMED      = 0
```

**بيئة العمل:** `worktree` معزول على `069e0ba` خارج الشجرة المشتركة. لم تُلتقط أيّ ملفّات غير مُتتبَّعة من الشجرة الأصليّة. كلّ استعلامات قاعدة الإنتاج `SELECT` فقط عبر `sudo -u postgres psql -f`.

---

## 2) المراجع البعيدة وهويّة الإنتاج

```text
develop                              = 069e0ba55b1525161e731961b584a994ad378590
main                                 = 508509ad8474b321c80cbdd48eb84ecb54bee212
release/r22b-production-20260906     = 069e0ba55b1525161e731961b584a994ad378590
release/production-canonical-r1      = bd84115c…  (سابق، لم يُمسّ)
release/rc-canonical-r1              = a86ad3b8…  (سابق، لم يُمسّ)
release/r22a-from-897c9b18           = 4b8902ee…  (سابق، لم يُمسّ)
PRODUCTION_APPLICATION_SHA           = c5e0202d0a528a1a45856790716e449b812f0184
PENDING_MIGRATIONS                   = 0
```

ملاحظة مُثبَتة: قاعدة الإنتاج تسجّل **49** هجرة بينما شجرة `c5e0202` تحوي **47** ملفّ هجرة. المقارنة بالمجموعات أثبتت أنّ الفارق هجرتان **مطبَّقتان تاريخيًّا وغير موجودتين في الشجرة** (`20260622144900_KpiTemplateAssignmentsPhaseT1`, `20260626135944_AddReportViewGrants`) — أي `PENDING = 0` ولا يوجد حاجب.

---

## 3) الحالات الإحدى عشرة (`SELECT` فقط)

**تعريف الجماعة الحاكم:** `Status='Submitted'` **و**(المعتمِد `NULL` **أو** معطَّل **أو** غير موجود). النتيجة **11 صفًّا بالضبط**.

| # | التسليم | الفترة | القالب | الفريق | المُقدِّم | نشط؟ | مدير المُقدِّم | تاريخ التقديم | العمر (يوم) | محذوف إداريًّا؟ |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `52455d79` | W28 | `e1a84946` | `a7ef8832` SEO | `0e30b66a` | ✅ | `f4e25122` | 2026-07-09 | 59 | لا |
| 2 | `416de42c` | W27 | `3059612d` | `87611efc` B2C | `8284241a` | ✅ | `9141ee82` | 2026-07-12 | 57 | **نعم** |
| 3 | `4c883e36` | W26 | `3059612d` | `87611efc` B2C | `8284241a` | ✅ | `9141ee82` | 2026-07-12 | 56 | **نعم** |
| 4 | `2ada052d` | W25 | `3059612d` | `87611efc` B2C | `8284241a` | ✅ | `9141ee82` | 2026-07-12 | 56 | **نعم** |
| 5 | `e840df55` | W23 | `3059612d` | `87611efc` B2C | `8284241a` | ✅ | `9141ee82` | 2026-07-13 | 56 | **نعم** |
| 6 | `c25673e1` | W31 | `46e100e3` | `a7ef8832` SEO | `0b2a60f1` | ✅ | `0e30b66a` | 2026-07-26 | 43 | **نعم** |
| 7 | `ea04ce56` | W31 | `32f263b9` | `87fbcea9` Ninjas | `50884a37` | ✅ | `356916a7` | 2026-07-30 | 39 | لا |
| 8 | `65179e5c` | W31 | `e9734e7f` | `87fbcea9` Ninjas | `f7c2f8bb` | ❌ معطَّل | `356916a7` | 2026-07-30 | 39 | لا |
| 9 | `e72a7916` | W33 | `32f263b9` | `698c5e0e` 777 | `aedbfba9` | ❌ معطَّل | `8be4ba0c` | 2026-08-30 | 8 | لا |
| 10 | `a167a5bf` | W34 | `32f263b9` | `698c5e0e` 777 | `aedbfba9` | ❌ معطَّل | `8be4ba0c` | 2026-08-30 | 8 | لا |
| 11 | `902bb0e9` | W35 | `32f263b9` | `698c5e0e` 777 | `aedbfba9` | ❌ معطَّل | `8be4ba0c` | 2026-08-30 | 8 | لا |

كلّ الأحد عشر بلا مشروع (`ProjectId = NONE`) ⇒ **لا يوجد بُعد «مالك مشروع/مدير عميل» في أيّ منها**، وهذا ينفي فرضيّة `PROJECT_ASSIGNMENT_MISMATCH` نفيًا قاطعًا.

### 3-أ) خطوات الاعتماد المرتبطة (`approval_steps`)

| التسليم | المستوى | معتمِد الخطوة | حالته | حالة الخطوة | تاريخ القرار |
|---|---|---|---|---|---|
| `416de42c`,`4c883e36`,`2ada052d`,`e840df55` | L1 | `9141ee82` | ✅ نشط | `CancelledByAdministrativeDeletion` | 2026-07-14 |
| `c25673e1` | L1 | `0e30b66a` | ✅ نشط | `CancelledByAdministrativeDeletion` | 2026-07-30 |
| `52455d79` | L1 | `6a00ccc0` | ⛔ **غير موجود** (حُذف نهائيًّا) | `Pending` | — |
| `ea04ce56`, `65179e5c` | L1 | `356916a7` | ❌ معطَّل | `Pending` | — |
| `902bb0e9`, `e72a7916`, `a167a5bf` | L1 | `8be4ba0c` | ❌ معطَّل | `Pending` | — |

### 3-ب) الأثر التدقيقيّ (`audit_logs`)

```text
2026-07-09 18:53  submission.submitted     52455d79  by 0e30b66a
2026-07-12/13     submission.submitted     416de42c · 4c883e36 · 2ada052d · e840df55  by 8284241a
2026-07-14 18:36  submission.admin_deleted (الأربعة أعلاه)                            by 49607be5 (Admin)
2026-07-26 05:20  submission.submitted     c25673e1  by 0b2a60f1
2026-07-30 07:51  submission.admin_deleted c25673e1                                   by 49607be5 (Admin)
2026-07-30 10:28  submission.submitted     ea04ce56  by 50884a37
2026-07-30 13:39  submission.submitted     65179e5c  by f7c2f8bb
2026-08-30        submission.submitted     902bb0e9 · e72a7916 · a167a5bf             by aedbfba9
```

**اكتشاف تدقيقيّ حاسم:** لا يوجد في مفردات `audit_logs` كلّها (42 فعلًا مميّزًا · 1607 صفًّا) **أيّ فعل من نوع `user.deactivated` أو ما يعادله**. ⇒ تعطيل المستخدمين يجري **بلا أثر تدقيقيّ إطلاقًا**، ولا يمكن تحديد لحظة تعطيل `356916a7` أو `8be4ba0c` من السجلّ. هذا بذاته عيب حوكمة (يُوثَّق في §8).

### 3-ج) تمييز مهمّ: صفوف `Returned` ليست عيبًا

- `RETURNED_TOTAL = 15` · `RETURNED_NULL_APPROVER = 15`
- **بالتصميم:** في `SubmissionService.DecideAsync` حالة `Returned` تضبط `CurrentApproverId = null` عمدًا (إعادة الملكيّة للمُقدِّم لتعديله).
- ⇒ هذه الخمسة عشر **مستبعدة من جماعة العطب** ولا تحتاج معالجة. المجموع «الخام» للسلامة المفتوحة هو 26 صفًّا، لكن الجماعة الفعليّة **11**.

---

## 4) سبب تعطّل كلّ حالة

| المجموعة | العدد | التصنيف | الشرح |
|---|---|---|---|
| `416de42c`,`4c883e36`,`2ada052d`,`e840df55`,`c25673e1` | **5** | `EXPECTED_HISTORICAL_STATE` | حُذفت إداريًّا (`IsDeleted=true`) وخطواتها `CancelledByAdministrativeDeletion` ومعتمِدو خطواتها **نشطون**. ليست عالقة تشغيليًّا؛ ظهورها في الاستعلام سببه أنّ الحذف الإداريّ يُفرِغ `CurrentApproverId` دون تغيير `Status`. |
| `52455d79` | **1** | `DATA_INTEGRITY_DEFECT` | المعتمِد `6a00ccc0` **حُذف حذفًا صلبًا** من `AspNetUsers` وبقيت إشارته معلّقة في `report_submissions.CurrentApproverId` و`approval_steps.ApproverId`. مرجع يتيم فعليّ. |
| `ea04ce56`,`65179e5c` | **2** | `INACTIVE_APPROVER` + `STALE_TEAM_LEADER` + `ROUTING_SNAPSHOT_STALE` | المعتمِد `356916a7` (قائدة Ninjas) عُطِّلت **بعد** التقديم. |
| `902bb0e9`,`e72a7916`,`a167a5bf` | **3** | `INACTIVE_APPROVER` + `STALE_TEAM_LEADER` + `ROUTING_SNAPSHOT_STALE` | المعتمِد `8be4ba0c` (قائدة 777) عُطِّلت **بعد** التقديم. |

**العالق تشغيليًّا فعلًا = 6** (الحالات 1، 7–11). ومن هذه الستّة **اثنتان فقط** لهما مُقدِّم نشط (`52455d79`, `ea04ce56`)؛ الأربع الباقية مُقدِّموها معطَّلون أيضًا.

### 4-أ) نفي فرضيّة «منطق الاختيار عند التقديم هو السبب»

- الحارس `IsActiveUserAsync(tl, ct)` داخل `ResolveFirstApproverAsync` **شُحن في `67d4b20` بتاريخ 2026-07-06** وهو حاضر في `045cb0d` و`d25dc69` و`c5e0202` و`069e0ba` (تحقّق بـ`git show <sha>:<path> | grep -c`، النتيجة `IsActiveUserAsync=6 · ResolveFirstApprover=4 · ApproverOverride=2` في الأربعة).
- كلّ التقديمات العالقة تمّت **بعد** 2026-07-06 (2026-07-09 · 2026-07-30 · 2026-08-30).
- ⇒ المعتمِدون كانوا **نشطين لحظة التقديم**، والاختيار كان صحيحًا آنذاك.
- **السبب الجذريّ الحقيقيّ:** لقطة التوجيه (`CurrentApproverId` + `approval_steps.ApproverId`) تُلتقط عند التقديم و**لا يُعاد توجيهها أبدًا** عند تعطيل المستخدم أو حذفه لاحقًا. أي `ROUTING_SNAPSHOT_STALE` لا `ROLE_SCOPE_MISMATCH`.

### 4-ب) خطورة إضافيّة: الحالات العالقة **غير مرئيّة لأحد**

`SubmissionService.GetPendingApprovalsAsync` سطر 825 يرشّح بـ`Where(s => s.CurrentApproverId == userId)`. بما أنّ المعتمِد معطَّل أو محذوف، **لا يرى هذه التسليمات أيّ مستخدم في النظام** — لا بديل ولا مدير ولا لوحة إداريّة. هذا يفسّر بقاءها 8–59 يومًا بلا تصعيد.

---

## 5) مصفوفة المعتمِد الحاليّ مقابل المرشَّح

| # | التسليم | الحالة | سبب التعطل | المعتمِد الحالي | المعتمِد المرشّح | أساس الترشيح | درجة الثقة | الإجراء المقترح |
|---|---|---|---|---|---|---|---|---|
| 1 | `52455d79` | Submitted · غير محذوف | معتمِد محذوف حذفًا صلبًا (`6a00ccc0`) | ⛔ مرجع يتيم | `f4e25122` (المدير العام، نشط) | `ManagerId` المُقدِّم `0e30b66a` = `f4e25122` (مُثبَت في `AspNetUsers` وفي `user.org.changed` بتاريخ 2026-07-14)؛ ومنطق `ResolveFirstApproverAsync` يتخطّى قائد الفريق لأنّه المُقدِّم نفسه (`tl != submitterId`) ثمّ يستقرّ على المدير النشط | **PROVEN** | «إرجاع» إداريّ ← يُعيد المُقدِّمة النشطة التقديم فيلتقط التوجيه معتمِدًا صحيحًا تلقائيًّا |
| 2 | `416de42c` | Submitted · **محذوف** | حذف إداريّ (لا عطب) | فارغ بالتصميم | — | خطوة L1 معتمِدها `9141ee82` **نشط** وحالتها `CancelledByAdministrativeDeletion` | **PROVEN (حالة تاريخيّة)** | **لا إجراء**؛ إن أُريدت الاستعادة فعبر `ArchiveService.RestoreReportAsync` بقرار المالك |
| 3 | `4c883e36` | Submitted · **محذوف** | حذف إداريّ | فارغ بالتصميم | — | نفس أعلاه | **PROVEN (حالة تاريخيّة)** | لا إجراء |
| 4 | `2ada052d` | Submitted · **محذوف** | حذف إداريّ | فارغ بالتصميم | — | نفس أعلاه | **PROVEN (حالة تاريخيّة)** | لا إجراء |
| 5 | `e840df55` | Submitted · **محذوف** | حذف إداريّ | فارغ بالتصميم | — | نفس أعلاه | **PROVEN (حالة تاريخيّة)** | لا إجراء |
| 6 | `c25673e1` | Submitted · **محذوف** | حذف إداريّ | فارغ بالتصميم | — | خطوة L1 معتمِدها `0e30b66a` **نشطة** | **PROVEN (حالة تاريخيّة)** | لا إجراء |
| 7 | `ea04ce56` | Submitted · عالق | معتمِد معطَّل `356916a7` + قيادة Ninjas معطَّلة | `356916a7` (معطَّل) | ⚠️ غير محسوم — الاحتياطيّ التقنيّ `f4e25122` | لا يوجد قائد نشط لفريق Ninjas؛ `ManagerId` المُقدِّم = نفس المعطَّل؛ المسار الوحيد المتبقّي في الكود هو «أوّل مدير عامّ نشط» و**هو واحد فقط** (`f4e25122`) ⇒ حتميّ تقنيًّا لكنّه **ليس إثباتًا تنظيميًّا** لملكيّة بود السوشيال | **AMBIGUOUS** | موقوف على قرار قيادة الفريق (§6) |
| 8 | `65179e5c` | Submitted · عالق | معتمِد معطَّل `356916a7` + **المُقدِّم نفسه معطَّل** | `356916a7` (معطَّل) | ⚠️ غير محسوم | نفس أعلاه، ويزيد أنّ المُقدِّم `f7c2f8bb` معطَّل ⇒ لا يمكن مسار «إرجاع وإعادة تقديم» | **AMBIGUOUS** | موقوف على قرار المالك |
| 9 | `e72a7916` | Submitted · عالق | معتمِد معطَّل `8be4ba0c` (قائدة 777) + المُقدِّم معطَّل | `8be4ba0c` (معطَّل) | ⚠️ غير محسوم | لا قائد نشط لفريق 777؛ `ManagerId` المُقدِّم = نفس المعطَّل | **AMBIGUOUS** | موقوف على قرار قيادة 777 |
| 10 | `a167a5bf` | Submitted · عالق | نفس أعلاه | `8be4ba0c` (معطَّل) | ⚠️ غير محسوم | نفس أعلاه | **AMBIGUOUS** | موقوف على قرار قيادة 777 |
| 11 | `902bb0e9` | Submitted · عالق | نفس أعلاه | `8be4ba0c` (معطَّل) | ⚠️ غير محسوم | نفس أعلاه | **AMBIGUOUS** | موقوف على قرار قيادة 777 |

```text
PROVEN_REASSIGNMENTS    = 1   (52455d79)
EXPECTED_HISTORICAL     = 5   (لا تحتاج معالجة)
AMBIGUOUS_REASSIGNMENTS = 5   (موقوفة على قرار تنظيميّ)
NO_VALID_APPROVER       = 0   (يوجد دائمًا احتياطيّ تقنيّ، لكنّه غير مُثبَت تنظيميًّا)
```

**تحفّظ صريح مُلزِم:** «أوّل مدير عامّ نشط» في الكود يُرتَّب بـ`Id` لا بالسلطة التنظيميّة. صادف أنّه **واحد فقط** في الإنتاج (`ACTIVE_GM = f4e25122`) فصار حتميًّا، لكنّ هذا **مصادفة بيانات لا ضمانة تصميم**؛ لا يجوز الاستناد إليه كإثبات صحّة تنظيميّة.

---

## 6) تحليل الفريق 777

### 6-أ) الهويّة والغرض

| البند | القيمة |
|---|---|
| المعرّف | `698c5e0e` |
| الاسم | «سوشيال — البود الأول - 777» |
| الإدارة | `4dc72160` (تنفيذ مشاريع التسويق والويب) |
| `Team.IsActive` | **`true`** ← الفريق نشط |
| `Team.TeamLeaderId` | `8be4ba0c` — **`IsActive = false`** ← المرجع البائت |
| الأعضاء الأساسيّون | 5 (منهم 3 نشطون) |
| العضويّات الثانويّة | 2 (`50884a37` منذ 2026-07-21 · `74419e7c` منذ 2026-07-14) |

### 6-ب) الأعضاء

| العضو | نشط؟ | المسمّى | `ManagerId` | الأدوار |
|---|---|---|---|---|
| `d352528f` سمر*** | ✅ | كاتب محتوى | `8be4ba0c` | Employee |
| `74419e7c` شيم*** | ✅ | **مشرف سوشيال** | `8be4ba0c` | **Employee فقط** |
| `50a2fa2c` محم*** | ✅ | مصمم جرافيك | `8be4ba0c` | Employee |
| `aedbfba9` أحم*** | ❌ | محرر فيديو | `8be4ba0c` | — |
| `8be4ba0c` بسن*** | ❌ | قائد فريق السوشيال (القائد المسجَّل) | `f4e25122` | TeamLeader |

**الأثر المباشر:** الأعضاء الثلاثة النشطون **كلّهم** مديرهم المباشر معطَّل ⇒ أيّ تقديم جديد منهم اليوم سيتخطّى القائد (معطَّل) ثمّ المدير (نفسه، معطَّل) ثمّ يهبط إلى الاحتياطيّ العامّ.

### 6-ج) الفريق الشقيق يعاني نفس العطب

| الفريق | القائد المسجَّل | نشط؟ | أعضاء نشطون |
|---|---|---|---|
| `698c5e0e` — «777» | `8be4ba0c` | ❌ | 3 من 5 |
| `87fbcea9` — «Ninjas 🥷» | `356916a7` | ❌ | 1 من 4 |

⇒ **بودَا السوشيال كلاهما بلا قائد نشط.** المشكلة ليست فريقًا واحدًا بل وحدة تنظيميّة كاملة.

### 6-د) هل يوجد قائد نشط بديل؟

القادة النشطون في الإدارة `4dc72160`:

| المرشّح | المسمّى | الدور | ملاءمته لبود سوشيال |
|---|---|---|---|
| `f6380cb1` | قائد فريق الويب | TeamLeader | ⛔ تخصّص مختلف |
| `0e30b66a` | قائدة فريق SEO | TeamLeader | ⛔ تخصّص مختلف |
| `722b5313` | قائد الأداء | Manager | ⛔ نطاق مختلف |
| `74419e7c` | **مشرف سوشيال** (داخل 777) | **Employee فقط** | ⚠️ المسمّى الوظيفيّ يوحي بالسلطة لكنّه **لا يحمل دور `TeamLeader`** |

⇒ **لا يوجد مرشّح مُثبَت.** حالة `74419e7c` هي بالضبط «تعارض بين المسمّى والسلطة» الذي تصنّفه قواعد الأمان حاجبًا.

### 6-ه) مصدر الحقيقة الحاكم للقيادة

القياس أثبت أنّ النظام يستعمل **مصدرين مستقلّين لا يتزامنان**:

| المصدر | الاستعمال | الحالة في 777 |
|---|---|---|
| `Teams.TeamLeaderId` | توجيه اعتماد التقارير (`ResolveFirstApproverAsync`) · الإجازات · نطاق العرض | `8be4ba0c` معطَّل |
| `AspNetUsers.ManagerId` | المسار الاحتياطيّ الثاني للاعتماد · التصعيد (`ResolveNextApproverAsync`) · Employee 360 | `8be4ba0c` معطَّل لدى الخمسة |
| `user_team_memberships` | العضويّات الثانويّة فقط | لا تحمل قيادة إطلاقًا |
| `position_scopes` / `report_view_grants` | نطاق الاطّلاع لا الاعتماد | خارج مسار التوجيه |

⇒ لا يوجد مصدر حقيقة **واحد**؛ تغيير القيادة يتطلّب تعديلين متزامنين على الأقلّ (`Teams.TeamLeaderId` **و**`ManagerId` لخمسة مستخدمين) وإلّا نشأ تعارض جديد.

### 6-و) الحكم

```text
TEAM_777_CORRECT_LEADER = UNKNOWN  ⇒  NOT_PROVEN
TEAM_777_CHANGE_IMPACT  = PARTIAL
```

**لم يُغيَّر شيء.** السلطة التنظيميّة غير معروفة ⇒ شرط التوقّف «سلطة تنظيميّة غير معروفة» مُفعَّل ⇒ قرار المالك مطلوب.

---

## 7) تحليل أثر العلاقات التنظيميّة

أثر تغيير `Teams.TeamLeaderId` للفريق 777 (لو صُرِّح به لاحقًا):

| السطح | الأثر | درجة الإثبات |
|---|---|---|
| **توجيه التقارير** | كلّ تقديم **جديد** سيلتقط القائد الجديد فورًا (`ResolveFirstApproverAsync`) | **PROVEN** (الكود مقروء) |
| **التسليمات العالقة الثلاثة** | **لا تتأثّر إطلاقًا** — لقطتها مُخزَّنة ولا يُعاد حسابها | **PROVEN** |
| **الإجازات/الأذونات** | `LeaveRequestService` يستعمل نفس مرجع قائد الفريق؛ توجد له آليّة معالجة عالقين (`RemediateTeamLeaderStuckRequestsAsync`) | **PROVEN** |
| **الخصم من دفتر الأرصدة** | يقع عند اعتماد قائد الفريق ⇒ تغيير القائد ينقل مسؤوليّة الخصم | **PROVEN** |
| **Employee 360** | يعرض المدير المباشر من `ManagerId` لا من `TeamLeaderId` ⇒ **لن يتحدّث** ما لم يُعدَّل `ManagerId` للخمسة | **PROVEN** |
| **Project 360** | كلّ الحالات الإحدى عشرة بلا `ProjectId` ⇒ لا أثر | **PROVEN** |
| **KPI** | منتقي KPI غير تراكميّ ويعتمد الإسناد لا القيادة | **PARTIAL** (لم يُقَس تنفيذيًّا) |
| **الحوكمة/التصعيد** | `ResolveNextApproverAsync` يتصاعد عبر `ManagerId` للمعتمِد الحالي | **PROVEN** |
| **لوحات المعلومات والعدّادات** | `GetPendingApprovalsAsync` يرشّح بـ`CurrentApproverId` ⇒ العدّادات لن تتغيّر للحالات القديمة | **PROVEN** |
| **الإشعارات** | لم يُقَس؛ عمود المستلم في `notifications` بتسمية مختلفة عن المفترض | **UNKNOWN** |
| **نطاق الوصول** | `position_scopes` مستقلّ عن قيادة الفريق | **PARTIAL** |

**الخلاصة:** تغيير القيادة **وحده لا يُصلح أيًّا من الحالات العالقة**، وقد يُنشئ تعارضًا جديدًا بين `TeamLeaderId` و`ManagerId` إن لم يُنفَّذ التغييران معًا.

---

## 8) تحليل خطر التكرار (`RECURRENCE_RISK`)

### 8-أ) الأسئلة الإلزاميّة وأجوبتها المُثبَتة من الكود

| السؤال | الجواب المُثبَت | المرجع |
|---|---|---|
| ماذا لو كان المعتمِد معطَّلًا **عند التقديم**؟ | يُتخطّى بسلامة ويُنتقل للتالي | `ResolveFirstApproverAsync` سطر 466-502 |
| ماذا لو `ManagerId = NULL`؟ | يُتخطّى ثمّ يُجرَّب أوّل مدير عامّ نشط ثمّ Admin/CEO | نفس المرجع |
| ماذا لو عُطِّلت قيادة الفريق؟ | يُتخطّى القائد بشرط `IsActiveUserAsync(tl)` | نفس المرجع |
| هل يوجد Fallback؟ | نعم، سلسلة من 5 مستويات | نفس المرجع |
| هل الـFallback مطابق للعقد التشغيليّ؟ | **لا** — «أوّل مدير عامّ نشط مرتَّبًا بـ`Id`» اختيار عشوائيّ تقنيًّا لا تنظيميًّا | نفس المرجع |
| هل يُمنَع التقديم بلا معتمِد صالح؟ | **لا** — التسليم يُغلَق تلقائيًّا (`Status = Closed`) دون أيّ رسالة للمستخدم | `SubmissionService` سطر 404-410 |
| هل تُعرض رسالة صحيحة للمستخدم؟ | **لا** في مسار الإغلاق التلقائيّ (نعم فقط في مسار `ReportApproverOverrideUserId` غير الصالح) | سطر 357-419 |
| هل يوجد نجاح كاذب؟ | **نعم** — التقرير يُصبح `Closed` بلا أيّ مراجعة بشريّة والمستخدم يظنّه معتمَدًا | سطر 404-410 |

### 8-ب) ناقلات التكرار الثلاثة (كلّها حيّة في `c5e0202` وفي `069e0ba`)

| # | الناقل | الموضع | الأثر |
|---|---|---|---|
| **V1** | **تعطيل مستخدم بلا أيّ حارس** — `user.IsActive = req.IsActive;` لا يفحص الاعتمادات المعلّقة ولا الفرق المقودة ولا المرؤوسين، **ولا يكتب سجلًّا تدقيقيًّا** | `DirectoryService.UpdateUserAsync` سطر 626 | ينتج عالقين جددًا في أيّ لحظة، وبلا أثر يُتتبَّع (مُؤكَّد بغياب `user.deactivated` من مفردات التدقيق) |
| **V2** | **حذف صلب لا ينظّف مراجع التوجيه** — `DeleteUserAsync` ينظّف `RefreshTokens` و`Teams.TeamLeaderId` و`Departments.ManagerId` و`Users.ManagerId`، **ولا يمسّ** `report_submissions.CurrentApproverId` ولا `approval_steps.ApproverId` | `DirectoryService.DeleteUserAsync` سطر 848-855 | مراجع يتيمة (`6a00ccc0` دليل حيّ) |
| **V3** | **`ValidateOrgAsync` يفحص الوجود لا النشاط** — `!await _db.Users.AnyAsync(u => u.Id == mid)` | `DirectoryService` سطر 1540-1549 (والقائد سطر 1147) | يسمح بإسناد مدير/قائد **معطَّل** إداريًّا |

### 8-ج) اختبار «هل يتكرّر؟» دون كتابة

محاكاة منطقيّة على منطق `c5e0202` (بلا إنشاء أيّ سجلّ إنتاج):

- **تقديم جديد اليوم من عضو 777 نشط:** القائد `8be4ba0c` معطَّل ⇒ يُتخطّى · `ManagerId` = نفسه معطَّل ⇒ يُتخطّى · غير `Senior` ⇒ أوّل مدير عامّ نشط = `f4e25122` ⇒ **يُوجَّه بنجاح**. ⇒ التقديم الجديد **لا يعلق** حاليًّا.
- **لكن:** لو عُطِّل `f4e25122` (وهو **المدير العامّ النشط الوحيد**) لسقط المسار إلى أوّل Admin/CEO نشط (`249be4c0` بترتيب الـ`Id`) — أي إلى حساب إداريّ تقنيّ لا سلطة اعتماد وظيفيّة له.
- **الناقل الحقيقيّ للتكرار ليس التقديم بل التعطيل:** أيّ تعطيل لمستخدم لديه اعتمادات `Pending` يُنتج فورًا عالقين جددًا **غير مرئيّين لأحد** (§4-ب)، ولا حارس يمنع ذلك (V1).

```text
RECURRENCE_RISK = YES
```

### 8-د) الإصلاح المقترح (منفصل — **لم يُنفَّذ ولم يُنشر**)

1. **حارس تعطيل:** منع/تحذير عند تعطيل مستخدم لديه `approval_steps` بحالة `Pending`، مع إلزام إعادة توجيهها.
2. **أثر تدقيقيّ للتعطيل:** إضافة `user.deactivated` / `user.reactivated` إلى `audit_logs`.
3. **تنظيف الحذف الصلب:** توسيع `DeleteUserAsync` ليعالج `CurrentApproverId` و`ApproverId` (إعادة توجيه لا حذف).
4. **`ValidateOrgAsync` يفحص `IsActive`** للمدير وقائد الفريق.
5. **آليّة معالجة عالقين للتقارير** على غرار `LeaveRequestService.RemediateTeamLeaderStuckRequestsAsync` (سطر 697-752) — وهي **السابقة الوحيدة الموجودة في النظام ولا نظير لها للتقارير**.
6. **إزالة النجاح الكاذب:** منع الإغلاق التلقائيّ الصامت أو على الأقلّ إشعار المستخدم والإدارة.
7. **رؤية إداريّة:** لوحة «تسليمات بلا معتمِد صالح» — لأنّ العالق حاليًّا غير مرئيّ لأحد.

كلّ بند يحتاج اختبارات ووحدة عمل مستقلّة **وتصريحًا جديدًا**.

---

## 9) خطة المعالجة والتراجع (**غير مُطبَّقة**)

### 9-أ) المسارات الرسميّة المتاحة (بلا SQL خام)

| المسار | الصلاحيّة | الموضع | الاستعمال |
|---|---|---|---|
| قرار إداريّ على أيّ تسليم | Admin | `SubmissionService.DecideAsync` سطر 619-621: `if (!isCurrentApprover && !_currentUser.IsInRole(Roles.Admin)) return Failure(...)` | اعتماد/إرجاع/تصعيد التسليمات العالقة |
| استعادة تسليم محذوف | Admin/CEO/GM + سبب 10–500 حرفًا | `ArchiveService.RestoreReportAsync` سطر 239-306 | الخمسة المحذوفة إن أراد المالك |

⇒ **لا حاجة إطلاقًا لكتابة SQL خام**، وهو شرط توقّف لو لزم.

### 9-ب) الخطّة لكلّ حالة

| التسليم | الإجراء | آليّة التنفيذ | خطوة جديدة أم إعادة توجيه؟ | أثر Audit | إشعار؟ | أثر على العدّادات | التراجع |
|---|---|---|---|---|---|---|---|
| `52455d79` | **إرجاع إداريّ** ثمّ إعادة تقديم من المُقدِّمة النشطة | `DecideAsync(Returned)` بحساب Admin | `CurrentApproverId → null`، ثمّ **خطوة L2 جديدة** عند إعادة التقديم بمعتمِد `f4e25122` | `submission.returned` ثمّ `submission.submitted` | نعم للمُقدِّمة ثمّ للمعتمِد الجديد | +1 في قائمة `f4e25122` · −1 من العالق | التسليم يعود لـ`Draft`/`Returned`؛ الخطوة L1 اليتيمة تبقى سجلًّا تاريخيًّا |
| `416de42c`,`4c883e36`,`2ada052d`,`e840df55`,`c25673e1` | **لا إجراء** (حالة تاريخيّة صحيحة) | — | — | — | — | — | — |
| `ea04ce56`,`65179e5c`,`902bb0e9`,`e72a7916`,`a167a5bf` | **موقوفة** حتّى قرار المالك في قيادة البودين | بعد القرار: تعيين قائد نشط ثمّ اعتماد/إرجاع إداريّ | يُحدَّد بعد القرار | يُحدَّد بعد القرار | يُحدَّد بعد القرار | يُحدَّد بعد القرار | يُحدَّد بعد القرار |

### 9-ج) الاختبارات التشغيليّة المطلوبة قبل أيّ تطبيق

1. إعادة قياس البصمات (§10) والتأكّد من عدم تغيّرها.
2. نسخة احتياطيّة ثلاثيّة لقاعدة الإنتاج.
3. تنفيذ الإجراء على **حالة واحدة** (`52455d79`) ثمّ التحقّق قبل أيّ توسّع.
4. التحقّق من ظهور التسليم في قائمة `f4e25122` (`GetPendingApprovalsAsync`).
5. التحقّق من كتابة `submission.returned`/`submission.submitted` في `audit_logs`.
6. إعادة قياس `FP_STUCK_11_MD5` وتوثيق الدلتا المتوقَّعة حصرًا.

**التحديث الجماعيّ الأعمى ممنوع منعًا باتًّا.**

```text
REMEDIATION_READY = NO
```
السبب: 5 من 11 غير مُثبَتة، وقيادة الفريقين غير محسومة، وشرط المستخدم صريح: «إن لم يكن المعتمِد الصحيح مُثبَتًا لكل الحالات ⇒ `REMEDIATION_READY = NO`».

---

## 10) البصمات والضوابط قبل أيّ تغيير

مأخوذة حيًّا من قاعدة الإنتاج بـ`SELECT` فقط بتاريخ **2026-09-07 14:32:10 UTC**:

```text
FP_SUBMISSIONS_TOTAL     = 350
FP_APPROVAL_STEPS_TOTAL  = 402
FP_STUCK_11_MD5          = 3f9b19c6639a29f97ddbb2e8f7318466
FP_STEPS_OF_11_MD5       = 588a60e2e427180fe4ec02ca01f98679
FP_TEAMS_MD5             = c1cc5bd069a6362114a99b42c0f93fe4
FP_USERS_ACTIVE          = 28
FP_USERS_INACTIVE        = 6
FP_AUDIT_ROWS            = 1607
```

توزيع الحالات المرجعيّ:
```text
STATUS_DIST = ApprovedByDirectManager:35 · Closed:219 · Draft:9 · Returned:15 · Submitted:71
OPEN_TOTAL             = 121
NULL_APPROVER_OPEN     = 20   (منها 15 Returned بالتصميم + 5 محذوفة إداريًّا)
INACTIVE_APPROVER_OPEN = 5
ORPHAN_APPROVER_OPEN   = 1
```

أيّ تغيير مستقبليّ يجب أن يُقارَن بهذه البصمات بالضبط، وأيّ دلتا غير متوقَّعة = شرط توقّف.

---

## 11) جرد اللقطات الأصليّة والمموّهة

### 11-أ) الأصليّة (`RAW`)

| البند | القياس |
|---|---|
| العدد على القرص | **47** (21 في `journey-shots/` + 26 في `vis-shots/`) |
| الموضع | `/private/tmp/prod-verify/{journey-shots,vis-shots}` — **خارج Git تمامًا** |
| البيان الحاكم | `Ops/R22B/PRODUCTION-RELEASE-20260906/evidence/production-authenticated-cycle/screenshot-inventory-sha256.txt` (47 مدخلًا) |
| إعادة حساب SHA-256 حيًّا | `MANIFEST=47 · ACTUAL=47 · SHA256_MATCH = EXACT_47/47` |
| متتبَّعة في Git؟ | **لا** — `git ls-files` في الشجرة الرئيسة وفي الـworktree أعاد **0** مسارًا مطابقًا (المطابقة الوحيدة `Ops/R22B/EXECUTION/regroup-prod-verify.json` نتيجة كاذبة لتطابق السلسلة النصّيّة، وهي ملفّ JSON لا لقطة) |

```text
RAW_FILE_COUNT           = 47
RAW_FILES_TRACKED_BY_GIT = 0
MANIFEST_REFERENCES_VALID = YES  (47/47 مطابقة بايتيّة بالبصمة)
```

### 11-ب) المموّهة (`REDACTED`)

بحث شامل في `/private/tmp` و`~/Documents` (عمق 6) عن أيّ مجلّد أو ملفّ بأنماط `*redact*` · `*masked*` · `*blur*` · `*مموه*`:

```text
النتيجة: لا شيء إطلاقًا.
REDACTED_COUNTERPARTS = 0/47
REDACTED_VISUAL_QA    = NOT_RUN (مستحيل — لا يوجد ما يُفحَص)
```

### 11-ج) بوّابة الحذف

| الشرط المطلوب | القيمة المقيسة | الحكم |
|---|---|---|
| `RAW_FILE_COUNT = 47` | 47 | ✅ |
| `RAW_FILES_TRACKED_BY_GIT = 0` | 0 | ✅ |
| `REDACTED_COUNTERPARTS = 47/47` | **0/47** | ⛔ **فشل** |
| `REDACTED_VISUAL_QA = PASS` | `NOT_RUN` | ⛔ **فشل** |
| `MANIFEST_REFERENCES_VALID = YES` | YES | ✅ |
| `NO_SESSION_OR_CREDENTIAL_DATA_REQUIRED = YES` | YES | ✅ |

**⇒ البوّابة فشلت في شرطين ⇒ لم يُحذف أيّ ملفّ، عملًا بالقاعدة الصريحة: «إذا فشل أيّ شرط: لا تحذف شيئًا وأصدر قائمة النواقص».**

### 11-د) قائمة النواقص الحاجبة

1. **لا توجد نسخ مموّهة إطلاقًا** — البيان نفسه ينصّ على أنّ الملفّات أُبقيت خارج Git *لأنّها تحوي PII إنتاجيًّا غير مموَّه*، ولم تُنشأ نسخ مموّهة قطّ.
2. **لا توجد سجلّات تمويه** (`masking log`) لأنّ عمليّة التمويه لم تُنفَّذ.
3. ⇒ الحذف الآن يعني **فقدان الدليل التشغيليّ نهائيًّا** لا تنظيفه.

**الطريق للأمام (يحتاج تصريحًا):** إنشاء 47 نسخة مموّهة → فحص بصريّ 47/47 → تحديث البيان → ثمّ حذف الأصول.

---

## 12) نتيجة تنظيف الـPII

```text
RAW_SCREENSHOTS_DELETED     = 0/47
RECOVERY_STATUS             = N/A (لم يُحذف شيء)
REDACTED_EVIDENCE_PRESERVED = NO  (غير موجود أصلًا)
PII_INVENTORY               = PASS
PII_CLEANUP                 = BLOCKED
```

### 12-أ) فحص مخلَّفات الحسّاسة (بلا طباعة أيّ محتوى)

| الفحص | النتيجة |
|---|---|
| ملفّات `storage-state` / `cookies` / `auth.json` تحت `/private/tmp` | **0** |
| لقطات PNG إضافيّة **خارج** المجلّدين المُبيَّنين | **4** — `./PROD-chromium-desktop-logi…` · `./PROD-chromium-mobile390-lo…` · `./PROD-webkit-desktop-login.…` · `./PROD-webkit-mobile390-logi…` (شاشات تسجيل دخول إنتاجيّة، **غير مُدرَجة في البيان الحاكم**) |
| سكربتات `.mjs` تحوي كلمات مفتاحيّة حسّاسة (`password`/`token`/`bearer`/`authorization`) | **8 ملفّات** — `journey-a-employee.mjs`(3) · `diag-archive-guard.mjs`(3) · `diag-p360.mjs`(2) · `diag-leader-closed2.mjs`(2) · `diag-leader-closed.mjs`(2) · `cycles.mjs`(2) · `cleanup-feasibility.mjs`(2) · `discover-leader.mjs`(1) |
| ملفّات هويّة جلسة | `session-identity-leader.json` · `session-identity-employee.json` (لم تُقرأ ولم تُطبَع) |

```text
UNEXPECTED_SENSITIVE_ARTIFACTS = 12  (4 لقطات دخول غير مُبيَّنة + 8 سكربتات تحوي بيانات اعتماد محتملة)
SENSITIVE_RESIDUALS            = 12
```

**لم يُوسَّع الحذف تلقائيًّا** عملًا بالقاعدة: «إذا وُجدت بقايا حسّاسة إضافيّة لا توسّع الحذف تلقائيًّا — اعرضها وانتظر». **لم يُقرأ ولم يُطبَع محتوى أيّ منها**؛ سُجِّل العدد والمسارات المقنَّعة فقط.

---

## 13) ما حُذف وما بقي

| البند | الحالة |
|---|---|
| اللقطات الأصليّة الـ47 | **باقية كما هي** — بصماتها مطابقة للبيان 47/47 |
| النسخ المموّهة | **غير موجودة** (لم تُنشأ قطّ) |
| البيان الحاكم `screenshot-inventory-sha256.txt` | **باقٍ ولم يُمسّ** |
| سجلّات التمويه | غير موجودة |
| اللقطات الأربع الإضافيّة | **باقية** — معروضة للقرار |
| السكربتات الثمانية | **باقية** — معروضة للقرار |
| **ما حُذف** | **لا شيء إطلاقًا** |
| بيانات الإنتاج | **لم تُمسّ** — `PRODUCTION_DATA_WRITES = 0` |

---

## 14) ما يحتاج قرار المالك

| # | القرار المطلوب | لماذا هو حاجب |
|---|---|---|
| **D1** | **من هو القائد الصحيح لفريق «777» (`698c5e0e`)؟** | لا يوجد قائد نشط ولا مرشّح مُثبَت. `74419e7c` مسمّاه «مشرف سوشيال» لكنّه بدور `Employee` فقط ⇒ تعارض مسمّى/سلطة |
| **D2** | **من هو القائد الصحيح لفريق «Ninjas» (`87fbcea9`)؟** | نفس العطب؛ عضو نشط واحد فقط |
| **D3** | هل تُعتمَد التسليمات الخمس العالقة من المدير العامّ `f4e25122` كحلّ استثنائيّ، أم تُنتظر D1/D2؟ | مُقدِّمو 4 منها معطَّلون ⇒ مسار «إرجاع وإعادة تقديم» مستحيل لها |
| **D4** | هل يُعتمَد الإرجاع الإداريّ لـ`52455d79` (الحالة الوحيدة `PROVEN`)؟ | يحتاج تصريح كتابة إنتاج جديدًا |
| **D5** | هل يُبدَأ تذكرة إصلاح منفصلة لناقلات التكرار V1/V2/V3 + آليّة معالجة العالقين؟ | `RECURRENCE_RISK = YES` |
| **D6** | هل تُنشَأ 47 نسخة مموّهة تمهيدًا للحذف، أم يُبقى على الأصول كما هي؟ | بوّابة §8 فاشلة؛ الحذف الآن = فقدان دليل |
| **D7** | ما مصير اللقطات الأربع الإضافيّة والسكربتات الثمانية؟ | مخلَّفات حسّاسة خارج البيان الحاكم |
| **D8** | هل تُصحَّح الحالة الشاذّة: تسليمات `Status='Submitted'` مع `IsDeleted=true`؟ | 5 صفوف؛ تشوّش كلّ استعلامات السلامة |

---

## 15) الحكم النهائيّ

```text
R22B_RELEASE_STATUS          = REMAINS_CLOSED
APPROVER_INTEGRITY_DIAGNOSIS = PASS
AFFECTED_SUBMISSIONS         = 11  (منها 6 عالقة تشغيليًّا · 5 حالة تاريخيّة متوقَّعة)
PROVEN_REASSIGNMENTS         = 1
AMBIGUOUS_REASSIGNMENTS      = 5
TEAM_777_CORRECT_LEADER      = NOT_PROVEN
RECURRENCE_RISK              = YES
REMEDIATION_READY            = NO
PRODUCTION_DATA_WRITES       = 0
PII_INVENTORY                = PASS
RAW_SCREENSHOTS_DELETED      = 0/47
REDACTED_EVIDENCE_PRESERVED  = NO
SENSITIVE_RESIDUALS          = 12
PII_CLEANUP                  = BLOCKED
NEXT_AUTHORIZATION_REQUIRED  = YES
```

**شروط التوقّف المُفعَّلة:** «سلطة تنظيميّة غير معروفة» (D1/D2) · «تعارض في مرشّحي المعتمِد» (§5) · «دليل مموّه مفقود» (§11).

**التصريحات المطلوبة للمتابعة:** D1–D8 أعلاه. لا كتابة على الإنتاج ولا حذف لأيّ ملفّ قبلها.
