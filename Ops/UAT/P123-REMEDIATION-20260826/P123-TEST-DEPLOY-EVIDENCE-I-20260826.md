# P123 — دليل المرحلة I: بناء المرشّح ونشره على TEST وحده

**التاريخ:** 26 أغسطس 2026 · **النطاق:** TEST حصرًا (`test.emarketingacademy.net`). **RC والإنتاج لم يُمسّا بأيّ كتابة ولا إعادة تشغيل** — والبرهان في §6.

## 1) النَسَب — سبعة التزامات صغيرة على المرشّح المصرَّح به

الأساس `9595c01937ca714deb80ebe6fc02c959f5c400d6` كما نصّ أمر المعالجة حرفيًّا. الفرع `feature/p123-remediation-20260826` على شجرة عمل معزولة، وفرع الأدلّة `evidence/p123-remediation-20260826` عند الرأس نفسه (**محلّيّان، لم يُدفع أيّ منهما**).

| # | SHA | الموضوع |
|---|---|---|
| 1 | `cc302ef` | الهجرة + تفرّد أسماء الإدارات والفرق + حارس بدء التشغيل |
| 2 | `f31a51a` | مسار منح الصلاحيّات الدقيقة + رفض التكرار بـ409 عربيّة + اختباراهما |
| 3 | `8aba930` | حجب واقعة الحضور عن موضوعها ما دامت سابقةً للإرسال |
| 4 | `72e39c1` | إيقاف التمرير الأفقيّ عند 390px |
| 5 | `efa11c9` | كبح نداء ملخّص الحوكمة لمن لا يملك مفتاحه |
| 6 | `0ffaa8d` | توليد أسماء الإدارات في الطُعم — **مرشّح الكود النهائيّ** |
| 7 | `4c452e8` | شجرة أدلّة المراحل C–H — **رأس الفرع والمنشور** |

`git merge-base --is-ancestor` أعطى `ANCESTOR_OK` للسبعة ولـ`9595c01` نفسه مقابل الرأس. و`git diff 0ffaa8d 4c452e8 -- reporting-backend reporting-frontend` **فارغ** ⟹ الشجرة المنشورة مطابقة لمرشّح الكود بايتًا ببايت، والالتزام السابع توثيقيّ بحت.

## 2) هويّة البيئات الثلاث — مقيسة من الخادم قبل أيّ لمسة

| البيئة | الخدمة | PID | المنفذ | مسار الخلفيّة | جذر الواجهة | القاعدة | البريد | التذكيرات |
|---|---|---|---|---|---|---|---|---|
| **TEST** | `khubara-reporting-test` | 1556576 | 5091 | `/opt/reporting-test/publish` | `/opt/reporting-test/frontend/dist` | `reporting_test_uat` | **`DryRun`** | **`false`** |
| RC | `khubara-reporting-rc` | 1556575 | 5092 | `/opt/reporting-rc/publish` | `/opt/reporting-rc/frontend/dist` | `reporting_rc` | DryRun | false |
| PROD | `reporting-api` | 1556574 | 5090 | `/opt/reporting/publish` | `/opt/reporting/reporting-frontend/dist` | `reporting_prod` | Enabled | true |

المكبحان المطلوبان على TEST (`EmailNotifications__Mode=DryRun` و`Reminders__Enabled=false`) كانا مضبوطَين أصلًا ولم يُغيَّرا. لا تقاطع بين المسارات ولا القواعد ولا المنافذ ولا أسماء الخدمات.

## 3) النسخة الاحتياطيّة الثلاثيّة — قبل أيّ كتابة، ومُتحقَّق منها

الطابع `20260826-094802`:

| المكوّن | الموضع | الإثبات |
|---|---|---|
| القاعدة | `/root/db-backups/reporting_test_uat-p123remed-20260826-094802.dump` | `pg_restore -l` نجح: **503 مدخلة TOC** منها **84 مدخلة بيانات جداول**، ورأس الأرشيف يذكر `dbname: reporting_test_uat` · md5 `a200a851d48dc7603c6f674b839bae83` |
| الخلفيّة | `/opt/reporting-test/publish-backup-p123remed-20260826-094802` | `Reporting.Api.dll` md5 `da5857cf83ed5224bbb3405475781906` = بصمة خطّ الأساس حرفيًّا |
| الواجهة | `/opt/reporting-test/frontend/dist-backup-p123remed-20260826-094802` | `index.html` md5 `e24b30189dac343f902409133347627c` = بصمة خطّ الأساس حرفيًّا |

التحقّق ليس «وجود ملفّ»: الـdump قُرِئ فعلًا بـ`pg_restore`، والنسختان الملفّيّتان قُورنتا ببصمة ما قبل النشر فتطابقتا.

## 4) الحارس اشتغل فعلًا — والتكرار القائم كان طُعمًا اصطناعيًّا يتيمًا

فحص ما قبل النشر على `reporting_test_uat` (قراءة فقط) وجد ما يمنع الهجرة:

```
dept_dup_groups=1  → UAT-P123-DEPT-ALPHA ×5
team_dup_groups=1  → UAT-P123-TEAM-A1    ×4
```

القاعدة `reporting_test_uat` **مخصَّصة للـUAT بالكامل** — كلّ أسمائها إمّا `UAT-P123-*` أو «… UAT» أو «إدارة اختبار P123»، ولا إدارة حقيقيّة واحدة فيها. ثمّ جُرِد كلّ صفّ مكرَّر مقابل **الجداول التسعة** التي تحمل `DepartmentId`/`TeamId` (`AspNetUsers`, `teams`, `job_roles`, `attendance_incidents`, `report_submissions`, `kpi_evaluations`, `governance_items`, `risks`, `position_scopes`):

| الصفّ | التبعيّات | القرار |
|---|---|---|
| إدارة `ALPHA` `04627d73…` | 11 مستخدمًا · 5 فرق · 12 واقعة · 3 تسليمات · 3 تقييمات | **أُبقيت** |
| إدارات `UATP123A` · `UATDUP-025313` · `UAT3E6DCA` · `UATE5198A` | **صفر في التسعة كلّها** | حُذفت |
| فريق `59a14216…` | 4 مستخدمين · 12 واقعة · 3 تسليمات · 3 تقييمات | **أُبقي** |
| فرق `e72ca62d…` · `b88e148d…` · `8e94d84b…` | **صفر في التسعة كلّها** | حُذفت |

الحذف نُفِّذ بمعاملة واحدة بحراسة مزدوجة: شرط الاسم الحرفيّ `UAT-P123-…` **مع** تسعة شروط `NOT EXISTS`، فلو تعلّق بصفٍّ شيءٌ ما لَما مسّه الحذف أصلًا. الأرقام قبل/بعد:

```
departments 15 → 11   (DELETE 4)
teams       16 → 13   (DELETE 3)
users       35 → 35   ·  attendance 17 → 17  ·  submissions 16 → 16   (بلا تغيير)
dept_dup_groups 1 → 0 ·  team_dup_groups 1 → 0
```

هذا هو **الحدّ الأدنى الذي يقتضيه الإصلاح** حرفيًّا، وهو داخل التصريح الممنوح (`REMOVAL OF VERIFIED UAT-P123 SYNTHETIC FIXTURES FROM TEST ONLY WHEN REQUIRED BY THE FIX`). لم يُحذف صفّ واحد يتعذّر إثبات اصطناعيّته.

## 5) البناء والنشر — مطابقة بصمة حرفيّة

البناء من **الشجرة المعزولة** لا من شجرة التطوير، بعد `rm -rf bin obj`، وبـ`-p:SourceRevisionId=4c452e8… -p:ContinuousIntegrationBuild=true`.

| المكوّن | البصمة المبنيّة محلّيًّا | البصمة على TEST بعد النشر | مطابقة |
|---|---|---|---|
| `Reporting.Api.dll` | `9240e16ae36ed477e61db043ed3d98a2` | `9240e16ae36ed477e61db043ed3d98a2` | ✅ |
| `index.html` | `d6374c89f098a2129beb543fb3278221` | `d6374c89f098a2129beb543fb3278221` | ✅ |
| حزمة الواجهة | `index-U6ZkcPeV.js` | `index-U6ZkcPeV.js` | ✅ |

**النَسَب داخل الثنائيّة المنشورة نفسها** (قراءة بايتات `/opt/reporting-test/publish/Reporting.Api.dll` على الخادم، فكّ UTF-16LE):

```
1.0.0+4c452e8040946b772671e35dfd1bf0e19b528bd8
```

⟵ يطابق رأس الفرع حرفيًّا. **لا يوجد `GET /api/version` في هذا المستودع** (`/api/version` يردّ 404، والبحث في `Reporting.Api` لا يُظهر أيّ متحكّم إصدار)، فالإثبات المكافئ وأقوى هو قراءة المراجعة المضمَّنة من الملفّ المنشور ذاته لا من ردّ HTTP يمكن أن يُخدَع بالتخزين المؤقّت.

الواجهة بُنِيت بـ`VITE_API_BASE_URL=https://test.emarketingacademy.net/api`؛ والحزمة تحوي هذا العنوان **وصفر ظهور لـ`localhost:5090`** ⟹ لا سقوط إلى الاحتياطيّ.

## 6) ما بعد النشر — TEST أخضر، وRC والإنتاج بلا أيّ أثر

**TEST:**
- الخدمة `active`، PID جديد `1562758`، `Now listening on: http://127.0.0.1:5091`.
- `/health` → **200** · `{"status":"ok","service":"reporting-api"}`.
- الهجرات **44 ⟵ 45**، وآخرها `20260826073223_P123DirectoryNameUniqueness` مسجَّلة في `__EFMigrationsHistory`.
- الفهرسان قائمان وفريدان فعلًا:
  - `CREATE UNIQUE INDEX "IX_departments_NameAr" ON public.departments USING btree ("NameAr")`
  - `CREATE UNIQUE INDEX "IX_teams_DepartmentId_NameAr" ON public.teams USING btree ("DepartmentId", "NameAr")`

**RC والإنتاج — قياس بعد النشر مقابل خطّ الأساس:**

| المؤشّر | الإنتاج قبل | الإنتاج بعد | RC قبل | RC بعد |
|---|---|---|---|---|
| `Reporting.Api.dll` md5 | `ddf9598c…1975` | `ddf9598c…1975` | `ddf9598c…1975` | `ddf9598c…1975` |
| mtime الثنائيّة | 2026-08-18 15:02:08 | نفسه | 2026-08-18 15:02:08 | نفسه |
| `index.html` md5 | `e419f48d…fc9c` | نفسه | `abeee208…e0c6` | نفسه |
| `MainPID` | 1556574 | **1556574** | 1556575 | **1556575** |
| زمن التفعيل | 08-26 06:22:54 UTC | نفسه | 08-26 06:22:54 UTC | نفسه |
| الهجرات | 42 | 42 | 42 | 42 |
| المستخدمون | 34 | 34 | 49 | 49 |

ثبات `MainPID` وزمن التفعيل يعني أنّ العمليّتين **لم تُعادا تشغيلًا أصلًا**، وثبات الـmd5 والـmtime يعني أنّه **لم تُكتب فيهما بايتة**. لا استدلال بالنيّة هنا: العمليّة نفسها هي العمليّة، والملفّ نفسه هو الملفّ.

## 7) الأسرار

لم تُطبع كلمة مرور ولا رمز وصول ولا سلسلة اتّصال في أيّ خطوة. قراءة إعدادات البيئات اقتصرت على `Database=<name>` وأعلام البريد/التذكيرات عبر `grep` انتقائيّ، بلا `source` وبلا طباعة الملفّ. سجلّ الخدمة المقروء صُفّي على أسطر التشخيص فقط ولم يُستخرج منه أيّ تجزئة كلمة مرور.
