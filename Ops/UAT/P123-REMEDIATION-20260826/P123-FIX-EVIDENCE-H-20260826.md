# P123 — دليل المرحلة H: الانحدار الكامل بالأرقام المقيسة

**التاريخ:** 26 أغسطس 2026 · **النطاق:** تشغيل حزم الاختبار محلّيًّا على قواعد بيانات معزولة فقط — لم يُمسّ TEST ولا RC ولا الإنتاج.
**الفرع:** `feature/p123-remediation-20260826` على شجرة عمل معزولة، أساسها `9595c01937ca714deb80ebe6fc02c959f5c400d6`.

## 1) جدول الأرقام الفعليّة

| الحزمة | الأمر / الفلتر | القاعدة | النتيجة |
|---|---|---|---|
| بناء الخلفيّة | `dotnet build Reporting.sln -c Release` | — | **0 خطأ · 4 تحذيرات** (نفس الأربعة المعروفة `CS8604`) |
| وحدويّ خلفيّ | `dotnet test tests/Reporting.UnitTests` | — | **556/556** · 0 فشل |
| تكامل مُوجَّه (13 صنفًا: Directory · Attendance · Permissions · HR Ops) | فلتر صريح بأسماء الأصناف | `reporting_p123_remed_iso` + `…_p2` | **208/208** |
| المجموعة المرجعيّة التسعيّة | فلتر تسعة أصناف حرفيًّا (كما في تذكرة 25 أغسطس) | معزولة | **120/120** — مطابق للمرجع رقمًا برقم |
| التسعيّة + `PermissionGrantPathTests` الجديد | نفس الفلتر + الصنف الجديد | معزولة | **134/134** (14 اختبارًا جديدًا) |
| **تكامل كامل — جولة 1** | بلا فلتر | **قاعدتان جديدتان تمامًا** | **2174/2174 · 0 فشل** |
| تكامل كامل — جولة 2 على نفس القاعدة | بلا فلتر | مأهولة بعد الجولة 1 | 2173/2174 — فشل واحد (§3) |
| `RepeatableNumericValidationIntegrationTests` | فلتر الصنف | `reporting_p123_pfe_fresh` جديدة | **13/13** (§2) |
| TypeScript | `npx tsc --noEmit` | — | **نظيف** · `exit 0` |
| Vitest | `npx vitest run` | — | **61 ملفّ اختبار · 734 اختبارًا · كلّها نجحت** · `exit 0` |
| Playwright | `npx playwright test` | — | **47/47** (42 قائمة + 3 من F + 2 من G) |
| الهجرات | عدّ ملفّات `Migrations/*.cs` | — | 44 ⟵ **45** (واحدة جديدة، **إضافيّة بحتة**) |

**اشتقاق الجديد من خطّ الأساس بلا فجوة:** وحدويّ `548 + 8 = 556` (الثمانية = `AttendanceDraftPrivacyTests`)، وتكامل `2151 + 23 = 2174` (23 = `DirectoryNameUniquenessTests` 9 + `PermissionGrantPathTests` 14). لا اختبار قائم سقط من التغطية ولا عدد بلا مصدر.

## 2) الأربعة الفاشلة في `reporting_pfe_iso` — تلوّث بيئة لا انحدار

الجولة الأولى أظهرت 4 فشل في `RepeatableNumericValidationIntegrationTests` برسالة:

```
Npgsql.PostgresException : 42701: column "BelowTargetThreshold" of relation "kpi_template_versions" already exists
```

هذا تصادم معرّفات هجرات في قاعدة `reporting_pfe_iso` القديمة (نفس النمط الموثَّق `42P07`/`42701`). البرهان قاطع وغير جدليّ: **نفس الثنائيّة، نفس الفلتر، قاعدة جديدة ⟹ 13/13 نجحت**. الصنف خارج فرقنا كلّيًّا.

## 3) الفشل الواحد المتبقّي — سابق للتذكرة، مُثبَت بالإقصاء

`ProjectRepeatableGridTests.SeoRollup_Aggregation_Unchanged_ReadsTopLevelNumbers` — `Expected: 10 · Actual: 30.00`.

الرقم نفسه يشرح الطبيعة: **ثلاثة أضعاف** القيمة المتوقَّعة، أي أنّ التأكيد يقرأ تجميع SEO على مستوى القالب بلا حصر نطاقه بطُعمه، فيلتقط تسليمات أصناف أخرى في القاعدة المشتركة. ليس عيب تفرّد ولا صلاحيّات.

**التجربة الحاسمة — إقصاء اختباراتي الثلاثة الجديدة تمامًا:**

| الجولة | الفلتر | القاعدة | النتيجة |
|---|---|---|---|
| 1 | `!~DirectoryNameUniquenessTests & !~PermissionGrantPathTests & !~AttendanceDraftPrivacyTests` | جديدة تمامًا | **2151/2151 · 0 فشل** |
| 2 | نفس الفلتر | نفس القاعدة بعد الجولة 1 | 2150/2151 — **نفس الفشل الواحد بعينه** |

⟹ العيب يتكرّر حرفيًّا **بلا وجود أيّ اختبار من إنتاج هذه التذكرة**، ورقم الجولة الأولى **2151** يطابق «إجمالي مجموعة التكامل في الثنائيّة = 2,151» المسجَّل في تذكرة 25 أغسطس حرفيًّا. كما أنّ `git diff` **لم يمسّ** `ProjectRepeatableGridTests.cs` ولا مسار التجميع.

**ملاحظة قياس دقيقة:** تشغيل الصنف **منفردًا** مرّتين على قاعدة جديدة أعطى `16/16` ثمّ `16/16` — أي أنّه **لا يسمّم نفسه**، بل يُسمَّم من أصناف أخرى. لذلك التوصيف الصحيح هو **عيب عزل عابر للأصناف**، لا عيب تكرار داخليّ. لا يُصلَح ضمن هذه التذكرة (خارج نطاقها المصرَّح به)، ويُسجَّل عيبًا قائمًا مستقلًّا.

## 4) الفشل الستّة في الجولة الأولى — عيوب طُعم كشفها القيد الجديد، وعولجت في الاختبار لا في المنتج

القيد الجديد (`DEF-P123-001`) جعل `departments.NameAr` فريدًا. القاعدة المشتركة **دائمة وتتراكم**، فكلّ اختبار كان يُنشئ إدارة باسم **حرفيّ ثابت** بينما يُولِّد `Code` عشوائيًّا صار ينجح في أوّل جولة ويصطدم بنفسه في كلّ جولة تالية:

| الموضع | الاسم الثابت | العلاج |
|---|---|---|
| `DirectoryTests.cs` — `CreateDepartment_AsAdmin_CreatesDepartment` | `"إدارة جديدة"` | الاسم يُولَّد كما يُولَّد `Code`، والتأكيد يقارن بالمتغيّر نفسه |
| `DirectoryTests.cs` — `UpdateDepartment_AsAdmin_ChangesNameAndActive` | `"إدارة معدّلة"` | نفسه |
| `PositionsTests.cs:377` (17 اختبارًا) | `$"إدارة {tag}"` والوسم حرفيّ (`P1X`…`P17X`) | لاحقة جولة على `NameAr` كما على `Code` |
| `ReportTemplateAssignmentTests.cs:300,310` (5 اختبارات) | `$"إدارة {tag}"` والوسم حرفيّ (`DI_DEPT`…) | نفسه |

**لم يُخفَّف أيّ تأكيد ولم يُلمس كود المنتج:** التأكيدات كانت على **هويّة الصفّ ونجاح العمليّة**، لا على نصّ الاسم — والاسم صار متغيّرًا يُقارَن بنفسه، فبقيت قوّة التأكيد كما هي. ورفضُ التكرار له اختباره المستقلّ المخصَّص `DirectoryNameUniquenessTests` (9 اختبارات).

اسم الفريق تُرك بوسمه عمدًا: تفرّده مقيَّد بـ`(DepartmentId, NameAr)` وإدارته مُنشأة لتوّها في كلّ نداء، فلا تصادم — وتغييره كان سيكون ضجيجًا بلا سبب.

## 5) فحص الأسرار

فحص على `git diff` كاملًا **وعلى كلّ ملفّ جديد** (باستثناء الأدلّة الثنائيّة) بأنماط: كلمة مرور · `secret` · `api_key` · `Bearer <token>` · `Host=…Password` · مفاتيح خاصّة.

النتيجة الوحيدة: `PermissionGrantPathTests.cs:37 → private const string TestPassword = "Passw0rd#1";` — وهي **كلمة الطُعم المحلّيّة القائمة في المستودع أصلًا** التي يستعملها `TestAuth` في عشرات ملفّات الاختبار المُلتزَمة على `HEAD` (مثل `DirectoryTests.cs` بأربعة مواضع). لا سرّ جديد، ولا سلسلة اتّصال، ولا رمز وصول، ولا تجزئة كلمة مرور في أيّ مخرَج.

## 6) الملفّات — كلّها داخل النطاق

**14 معدَّلًا:** `DirectoryController.cs` · `AttendanceAccess.cs` · `DirectoryModels.cs` · `IDirectoryService.cs` · `OrgConfigurations.cs` · `AppDbContextModelSnapshot.cs` (مولَّد) · `AttendanceService.cs` · `DirectoryService.cs` · `DirectoryTests.cs` · `PositionsTests.cs` · `ReportTemplateAssignmentTests.cs` · `dashboard.tsx` · `ui.tsx` · `RoleDashboards.tsx`.

**7 جديدًا:** هجرة `P123DirectoryNameUniqueness` (+`Designer`) · `DirectoryNameUniquenessTests.cs` · `PermissionGrantPathTests.cs` · `AttendanceDraftPrivacyTests.cs` · `dashboard-overflow.spec.ts` · `governance-summary-gate.spec.ts`.

صفر ملفّ خارج نطاق العيوب الخمسة والسيناريوهات المحجوبة. صفر ملفّ إعداد بيئة. صفر مسّ لشجرة الأدلّة القديمة.

## 7) الهجرة الجديدة — إضافيّة بحتة وبحارس بدء تشغيل

`20260826073223_P123DirectoryNameUniqueness`: فهرسان فريدان (`IX_departments_NameAr` و`IX_teams_DepartmentId_NameAr`) يسبقهما حارس `DO $$ … RAISE EXCEPTION` يفحص التكرارات القائمة ويرفع رسالة قابلة للتنفيذ تذكر **العدد والاسم المتضارب**. **لا يحذف ولا يدمج ولا يعدّل صفًّا واحدًا** — معالجة البيانات قرار تشغيليّ صريح خارج الهجرة. `Down` يُسقِط الفهرسين فقط.
