diff --git a/reporting-backend/src/Reporting.Application/Attendance/AttendanceAccess.cs b/reporting-backend/src/Reporting.Application/Attendance/AttendanceAccess.cs
index 0247cf9..ab30b10 100644
--- a/reporting-backend/src/Reporting.Application/Attendance/AttendanceAccess.cs
+++ b/reporting-backend/src/Reporting.Application/Attendance/AttendanceAccess.cs
@@ -1,5 +1,7 @@
+using System.Linq.Expressions;
 using Reporting.Application.Common;
 using Reporting.Application.Security;
+using Reporting.Domain.Entities.Attendance;
 using Reporting.Domain.Enums;
 
 namespace Reporting.Application.Attendance;
@@ -48,9 +50,22 @@ public static class AttendanceAccess
     /// الحالات السابقة للإرسال: الواقعة فيها **ملكُ المُبلِّغ وحده** ولم تصر بلاغًا رسميًّا بعد.
     /// <c>Cancelled</c> لا تُبلَغ إلّا من <c>Draft</c> (انظر جدول <c>AttendanceTransitions</c>)
     /// فهي مسودّة عدَل عنها صاحبها ⇒ تبقى كأن لم تكن بالنسبة إلى الموظّف موضوعها.
+    ///
+    /// <para>
+    /// هذه المصفوفة هي **مصدر الحقيقة الوحيد** لحدّ ما قبل الإرسال: تستهلكها قاعدة العنصر المفرد
+    /// <see cref="CanViewIncident"/> وقاعدة الاستعلام <see cref="VisibleIncidentPredicate"/> معًا،
+    /// فلا يمكن أن ينحرف السطحان عن بعضهما بتعديل أحدهما وحده.
+    /// </para>
     /// </summary>
-    private static bool IsPreSubmission(AttendanceIncidentStatus status) =>
-        status is AttendanceIncidentStatus.Draft or AttendanceIncidentStatus.Cancelled;
+    public static readonly AttendanceIncidentStatus[] PreSubmissionStatuses =
+    {
+        AttendanceIncidentStatus.Draft,
+        AttendanceIncidentStatus.Cancelled
+    };
+
+    /// <summary>هل الواقعة ما زالت سابقةً للإرسال؟ — مشتقّة من <see cref="PreSubmissionStatuses"/>.</summary>
+    public static bool IsPreSubmission(AttendanceIncidentStatus status) =>
+        Array.IndexOf(PreSubmissionStatuses, status) >= 0;
 
     /// <summary>
     /// حقّ الاطّلاع على واقعة بعينها: صاحبها **بعد إرسالها**، أو مُبلِّغها، أو مشرف تشغيليّ
@@ -76,4 +91,43 @@ public static class AttendanceAccess
                && ctx.HasAnyRole(Roles.TeamLeader, Roles.Manager)
                && FieldVisibilityRules.IsSupervisoryRelation(ctx.Relation);
     }
+
+    /// <summary>
+    /// DEF-P123-RC-001 — نظير <see cref="CanViewIncident"/> على مستوى **الاستعلام**، بترتيب الفروع نفسه
+    /// وبالمصفوفة نفسها لحدّ ما قبل الإرسال.
+    ///
+    /// <para>
+    /// كان سطح القائمة يبني نطاقه من الملكيّة/النطاق وحدها فيُدرِج مسودّةً لصاحبها بينما يردّ سطحُ
+    /// التفاصيل عليها <c>404</c>. العلاج ليس إخفاءً بعد الجلب: الشرط يدخل الاستعلام نفسه فيسبق
+    /// <c>Count</c> و<c>Skip/Take</c> والإسقاط والتسلسل معًا، فلا يتسرّب وجود المسودّة من عدّاد ولا صفحة.
+    /// </para>
+    ///
+    /// <para>
+    /// الثابت المحروس: <b>القائمة ⊆ التفاصيل</b> — كلّ صفّ يعود هنا يفتحه <see cref="CanViewIncident"/>
+    /// للمُشاهِد نفسه. الفروع كلّها قابلة للترجمة إلى SQL (لا تقييم على العميل ولا تحميل كامل للجدول).
+    /// </para>
+    /// </summary>
+    /// <param name="viewerUserId">المُشاهِد.</param>
+    /// <param name="canReviewOrEscalate">يحمل <c>Attendance.Review</c> أو <c>Attendance.Escalate</c> صراحةً.</param>
+    /// <param name="isOperationalSupervisor">قائد فريق أو مدير — الشرط الدوريّ لفرع الإشراف.</param>
+    /// <param name="seesAllSubjects">نطاق شركة/حوكمة (يقابل <c>SubjectRelation.Company</c>).</param>
+    /// <param name="scopedSubjectUserIds">مَن يقع داخل نطاق المُشاهِد من شجرة <c>ManagerId</c>.</param>
+    public static Expression<Func<AttendanceIncident, bool>> VisibleIncidentPredicate(
+        Guid viewerUserId,
+        bool canReviewOrEscalate,
+        bool isOperationalSupervisor,
+        bool seesAllSubjects,
+        IReadOnlyCollection<Guid> scopedSubjectUserIds)
+    {
+        // متغيّر محلّيّ لا حقل ساكن: يضمن أن يُقوَّم كمعامل استعلام (‎= ANY‎) لا كتعبير غير مترجَم.
+        var preSubmission = PreSubmissionStatuses;
+        var scoped = scopedSubjectUserIds as IList<Guid> ?? scopedSubjectUserIds.ToList();
+
+        return i => i.SubjectUserId == viewerUserId
+            // فصل الواجبات: الموضوع يُقيَّم بصفته موضوعًا فقط، ولا ترفعه مفاتيح المراجعة فوق واقعته.
+            ? !preSubmission.Contains(i.Status)
+            : i.ReportedByUserId == viewerUserId
+              || canReviewOrEscalate
+              || (isOperationalSupervisor && (seesAllSubjects || scoped.Contains(i.SubjectUserId)));
+    }
 }
diff --git a/reporting-backend/src/Reporting.Infrastructure/Services/AttendanceService.cs b/reporting-backend/src/Reporting.Infrastructure/Services/AttendanceService.cs
index 5b48470..ab1b881 100644
--- a/reporting-backend/src/Reporting.Infrastructure/Services/AttendanceService.cs
+++ b/reporting-backend/src/Reporting.Infrastructure/Services/AttendanceService.cs
@@ -760,24 +760,34 @@ public class AttendanceService : IAttendanceService
     /// <summary>
     /// استعلام الوقائع محصورًا بنطاق المستخدم — يُطبَّق **قبل** أيّ مرشِّح من العميل،
     /// فلا يستطيع مرشِّح مُلفَّق توسيع النطاق.
+    ///
+    /// <para>
+    /// DEF-P123-RC-001: القاعدة نفسها التي يحتكم إليها سطح التفاصيل
+    /// (<see cref="AttendanceAccess.CanViewIncident"/>) تدخل هنا **داخل الاستعلام** عبر
+    /// <see cref="AttendanceAccess.VisibleIncidentPredicate"/>، فتسبق <c>Count</c> و<c>Skip/Take</c>
+    /// والإسقاط والتسلسل معًا. الثابت المحروس: <b>القائمة ⊆ التفاصيل</b>.
+    /// </para>
     /// </summary>
     private async Task<IQueryable<AttendanceIncident>> BuildScopedQueryAsync(CancellationToken ct)
     {
-        var query = _db.AttendanceIncidents.AsNoTracking();
-
-        var canReview = _currentUser.HasPermission(AppPermissions.AttendanceReview);
-        var canEscalate = _currentUser.HasPermission(AppPermissions.AttendanceEscalate);
-        if (canReview || canEscalate) return query;
-
-        var scope = await _scope.ResolveAsync(ct);
-        if (scope.SeesAll) return query;
-
-        var visibleUsers = scope.UserIds.ToList();
-        var me = ActorId;
+        var canReviewOrEscalate = _currentUser.HasPermission(AppPermissions.AttendanceReview)
+                                  || _currentUser.HasPermission(AppPermissions.AttendanceEscalate);
+        var isOperationalSupervisor = _currentUser.IsInRole(Roles.TeamLeader)
+                                      || _currentUser.IsInRole(Roles.Manager);
+
+        // حلّ النطاق مكلف؛ حامل مفتاح المراجعة/التصعيد لا يحتاجه (كما في السلوك السابق تمامًا).
+        IReadOnlyCollection<Guid> scopedSubjectUserIds = Array.Empty<Guid>();
+        var seesAllSubjects = false;
+        if (!canReviewOrEscalate)
+        {
+            var scope = await _scope.ResolveAsync(ct);
+            scopedSubjectUserIds = scope.UserIds.ToList();
+            seesAllSubjects = scope.SeesAll;
+        }
 
-        // صاحب الواقعة، أو مُبلِّغها، أو من هو داخل نطاق المُشاهِد — وما عدا ذلك غير موجود بالنسبة له.
-        return query.Where(i =>
-            i.SubjectUserId == me || i.ReportedByUserId == me || visibleUsers.Contains(i.SubjectUserId));
+        return _db.AttendanceIncidents.AsNoTracking().Where(
+            AttendanceAccess.VisibleIncidentPredicate(
+                ActorId, canReviewOrEscalate, isOperationalSupervisor, seesAllSubjects, scopedSubjectUserIds));
     }
 
     private IQueryable<AttendanceIncident> ApplyNeedsMyActionFilter(IQueryable<AttendanceIncident> query)
