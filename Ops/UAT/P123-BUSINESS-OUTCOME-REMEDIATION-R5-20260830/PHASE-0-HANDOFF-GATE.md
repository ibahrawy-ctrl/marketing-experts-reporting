# PHASE_0_HANDOFF_GATE — حزمة R5

**التاريخ:** 30 أغسطس 2026 · **الحالة:** `PASS` (حُدِّثت — انظر §0)

## 0) تحديث حالة العقد — رُفِع الحجب

| الطور | الحكم | الأساس |
|---|---|---|
| **الأوّل** (30 أغسطس، قبل الاعتماد) | `R5_BUSINESS_OUTCOME_BLOCKED — MISSING_APPROVED_DEC_01_CONTRACT` | `DEC-01` مفتوح بلا توقيع؛ 9 بنود في الوثيقة مقابل 18 في التكليف |
| **الحاليّ** (30 أغسطس، بعد الاعتماد) | **`PASS` — الحجب مرفوع** | مالك المنتج اعتمد `DEC-01` بنصّ حرفيّ **ذي 18 بندًا** |

**النصّ المعتمد محفوظ حرفيًّا في:** `DEC-01-APPROVED-CONTRACT.md` (نفس المجلّد) — وهو **المرجع الوحيد**.
**النسخة المفتوحة القديمة** في `P123-OPEN-PRODUCT-DECISIONS.md` (9 بنود بلا توقيع) **مُلغاة ومستبدَلة**. تُحفَظ للتاريخ ولا يُبنى عليها.

**أثر الاعتماد على تحليل §5 أدناه:** التعارضات الثلاثة المرصودة **زالت كلّها**:
- تعارض العدد (18 مقابل 9 مقابل 8) ⇐ زال؛ النصّ المعتمد 18 بندًا صريحًا.
- تعارض الحالة (مفتوح مقابل معتمد) ⇐ زال؛ الاعتماد صدر.
- البنود الثلاثة الفارغة ⇐ حُسِمت صراحةً: التواتر بأولويّة رباعيّة (بند 5) · `Missing` ليست `Zero` (بند 10) · حدّ التغطية **80%** من `AdjustedExpected` (بند 13).

**تصريح التنفيذ الممنوح:** تنفيذ R5 كاملًا من الشجرة النظيفة بلا موافقات مرحليّة، مع التوقّف عند حدود أربعة فقط (تعارض حقيقيّ · بيانات حيّة · بيئة مشتركة · قرار منتج جديد). ممنوع دخول TEST وطلب بيانات الدخول قبل اكتمال الكود والاختبارات وبناء مرشّح صالح.

> **يُقرأ ما يلي بوصفه سجلًّا تاريخيًّا لحالة ما قبل الاعتماد.** أقسام §1–§4 (نقطة البدء والسلسلة والعزل وسلامة الحزمة) تبقى **سارية ومقيسة**؛ §5–§6 (التعارض والبدائل) **تجاوزها الاعتماد**؛ §7–§8 (الملفّات والتقدير) يُعاد إصدارهما في مصفوفة القبول.

---

## 1) نقطة البداية — مقيسة

| العنصر | القيمة المقيسة |
|---|---|
| شجرة التنفيذ الجديدة | `/Users/ibrahimelbahrawi/p123-remediation-r5` |
| الفرع | `fix/p123-business-outcome-remediation-r5` |
| HEAD | `66c9f359c80df7e6ed252d35bf1702b931291409` |
| حالة الشجرة | **نظيفة** — `git status --short` = 0 سطر |
| الأساس | `16983d2` مُثبَت سلفًا لـ`66c9f35` (`merge-base --is-ancestor` = YES) |

## 2) سلسلة R1–R4 — محفوظة ومتسلسلة

```
d86c0eb (حزمة التشخيص)
  └─ 3ee3889  R1
      └─ cde7ed3  R2
          └─ 86abfcf  R3
              └─ 66c9f35  R4   ← نقطة بدء R5 النظيفة
                  └─ 0c967f9  ← محاولة R5 السابقة (غير موثوقة، معزولة)
```

كلّ التزام أبٌ مباشر للذي يليه — لا تراجع ولا إعادة كتابة. `66c9f35` يعدّل 5 ملفّات واجهة فقط (+134/−15).

## 3) المحاولة السابقة لـR5 — `UNTRUSTED_PARTIAL_WORK`

الشجرة الأصليّة `/Users/ibrahimelbahrawi/p123-remediation-r1` **لم تكن نظيفة** عند `66c9f35`. وجدت طبقتين:

| الطبقة | الحجم المقيس |
|---|---|
| التزام `0c967f9` فوق `66c9f35` | 10 ملفّات · +158/−19 |
| تعديلات غير مودعة فوق `0c967f9` | 10 ملفّات · +660/−22 |
| **الإجمالي مقابل `66c9f35`** | **15 ملفًّا · +805/−28** |
| ملفّ تقرير غير متتبَّع | `Ops/UAT/P123-BUSINESS-OUTCOME-REMEDIATION-R1-20260829/P123-IMPLEMENTATION-REPORT.md` |

**طريقة العزل المطبَّقة (بلا أيّ مساس):**
- الشجرة الأصليّة **تُركت كما هي حرفيًّا** — تُحُقّق بعد العزل: HEAD ما زال `0c967f9` و11 مسارًا متغيّرًا. لا `reset` ولا `checkout --` ولا `stash` ولا حذف.
- نُسِخت لقطة للقراءة فقط إلى `/Users/ibrahimelbahrawi/p123-r5-untrusted-snapshot-20260830/`:
  - `0001-fix-p123-r5-KPI.patch` (الالتزام)
  - `UNTRUSTED-R5-worktree-vs-66c9f35.patch` (الفرق الكلّيّ)
  - `UNTRUSTED-R5-IMPLEMENTATION-REPORT.md`
- **لم يُنقَل أيّ سطر منها إلى شجرة R5.** أيّ نقل لاحق يتطلّب مراجعة ملفًّا ملفًّا مقابل عقد R5.

## 4) حزمة التشخيص — سليمة

16 ملفًّا متتبَّعًا عند `66c9f35`؛ `git diff 66c9f35 -- <الحزمة>` فارغ ⇒ **غير معدَّلة**.
بصمات العقد: `P123-OPEN-PRODUCT-DECISIONS.md` = `801882dff9da8e33a0a77ea92fa0a4d6` · `P123-TARGET-BEHAVIOR-AND-ACCEPTANCE.md` = `19bd8478292c03219e0848ba875d4066`.

## 5) التعارض الحاكم — سبب `BLOCKED`

### 5.1 القرار التجاري المتأثّر
`DEC-01` — معادلة تجميع الأداء الربعيّ ومستوى تحديد تواتر التقييم.

### 5.2 السلوك الحالي المقيس (على `66c9f35`)
| القياس | النتيجة |
|---|---|
| حالة `DEC-01` | **`مفتوح`** — `P123-OPEN-PRODUCT-DECISIONS.md:4,11,50`: «الخمسة كلّها **مفتوحة**. لا قرار مُتَّخَذ» · «القرار المعتمد: `لم يُتَّخذ بعد.` — التاريخ: ____ — التوقيع: ____» |
| نسخة موقَّعة في مكان آخر | **لا توجد** — البحث عن `DEC-01` في `Ops/` كلّه يعيد **6 ملفّات، جميعها داخل حزمة التشخيص نفسها**، ولا واحد منها يحمل توقيعًا |
| عدد بنود `DEC-01` | **9 بنود** في الجدول (`:36-44`) — بينما نصّ الوثيقة نفسها `:47` يقول «لكلّ بند من **الثمانية**» |
| ما ورد في التكليف | «**ثمانية عشر** بندًا **معتمدًا**» |
| تثبيت التواتر في الواجهة | `KpiPage.tsx:146` — `cadence: 'WeeklyPulse'` مثبَّت حرفيًّا |
| السابقة المرشَّحة لإعادة الاستعمال | `ReportCadencePolicy.cs:16` — `ExpectedCadence(jobRoleCode)` تعيد `Daily` للمبيعات و`Weekly` لغيرهم. **لا `Quarterly` فيها، ومستواها المسمّى الوظيفيّ لا الموظّف** |
| سقوط الفترة في 360 | `Employee360Service.cs:131,133,315` — `LastCompletedWeek()` احتياطيًّا |

### 5.3 لماذا يستحيل التنفيذ الصحيح دون حسم
ثلاثة تعارضات مستقلّة، كلّ واحد كافٍ وحده:

1. **عدد البنود لا يطابق:** التكليف يقول 18 بندًا معتمدًا · الجدول فيه 9 · نصّ الوثيقة يقول 8. لا أستطيع «استخراج البنود الثمانية عشر إلى مصفوفة قبول» (§7/1) لأنّها غير موجودة. أيّ مصفوفة أبنيها ستكون اختراعًا.
2. **الحالة لا تطابق:** التكليف يصفه «معتمدًا»؛ المستودع يصفه «مفتوحًا بلا توقيع». والوثيقة تنصّ حاكميًّا `:3`: «لا يجوز للوكيل ولا للمطوّر أن يختار نيابةً عن مالك المنتج، **ولا أن يستنتج القرار من الكود القائم — فالكود القائم هو موضع الخلل لا مرجعه**».
3. **البنود الثلاثة الحاكمة لـR5 فارغة فعليًّا:**
   - **البند 9** (مستوى تحديد التواتر: المسمّى الوظيفيّ · إعداد الموظّف · مزيج) ⟸ يحكم §6.2 كاملةً ويحدّد **المقام** في حساب الاكتمال. السابقة القائمة على مستوى المسمّى؛ التكليف يطلب مستوى الموظّف ⇒ الفارق نفسه هو البند المفتوح.
   - **البند 2** (الفترات الناقصة: تُتجاهَل · تُحتسَب صفرًا · تمنع الإصدار) ⟸ يحكم «نسبة التغطية» في §6.3. مثال الوثيقة `:37`: «موظّف بأسبوع واحد ممتاز: **100% أم 11%؟**» — رقمان مشروعان، والاختيار قرار عمل.
   - **البند 4** (حدّ أدنى للتغطية) ⟸ يحكم حالة «تغطية غير كافية» في §6.5، ويمسّ **7 موظّفين بصفر تغطية** مباشرةً.

### 5.4 ما ليس محجوبًا
`DEC-01` يحجب `R6` **مطلقًا** و`R5` **جزئيًّا** فقط (`:11`). الشريحة غير المحجوبة قابلة للتنفيذ الآن:

| بند التكليف | الحالة | السبب |
|---|---|---|
| §6.1 الفترة — فتح KPI على `Q3-2026` تلقائيًّا ومسار الفترات التاريخيّة | **غير محجوب** | اشتقاق تقويميّ بحت، لا معادلة فيه |
| §6.4 النطاق والصلاحيّات خادميًّا · لا GUID | **غير محجوب** | أمنيّ لا ماليّ |
| §6.5 التمييز بين الفراغ والمنع والخطأ والقالب/الدورة المفقودة | **غير محجوب عدا حالة «تغطية غير كافية»** | الحالات الأخرى لا تتطلّب عتبة |
| §6.2 نوع وتواتر التقييم | **محجوب** | البند 9 |
| §6.3 الاكتمال (المتوقَّع/المكتمل/النسبة) | **محجوب** | البندان 2 و4 |

## 6) البدائل وأثر كلّ منها

| # | البديل | الأثر |
|---|---|---|
| **أ** | يوقّع مالك المنتج `DEC-01` (البنود 2 و4 و9 كحدّ أدنى لفتح R5؛ والتسعة كلّها لفتح R6 لاحقًا) ثمّ تُنفَّذ R5 كاملة | **المسار الوحيد المؤدّي إلى `R5_BUSINESS_OUTCOME_PASS`**. يفتح R6 أيضًا. تكلفة: قرار عمل واحد |
| **ب** | تنفيذ الشريحة غير المحجوبة فقط (§6.1 + §6.4 + §6.5 جزئيًّا) وتأجيل §6.2/§6.3 | يسلّم قيمة حقيقيّة (الشاشة تفتح على الربع الصحيح بنطاق محكوم) لكنّ الحكم النهائيّ يبقى `R5_BUSINESS_OUTCOME_BLOCKED` لا `PASS`، لأنّ الاكتمال والتواتر غير مثبتين |
| **ج** | استنتاج البنود من `ReportCadencePolicy` والكود القائم | **مرفوض** — يخالف `:3` من الوثيقة ونصّ التكليف «لا تخترع معادلة أو سلوكًا غير موجود فيه»، ويُنتِج أرقام تغطية تُقرأ ماليًّا وهي غير معتمدة |
| **د** | اعتماد «18 بندًا» الواردة في التكليف بوصفها مصدرًا أحدث | غير ممكن — النصّ الكامل لهذه البنود غير متاح لي في أيّ ملفّ |

**التوصية المبنيّة على الأدلّة: (أ)**، وإن تعذّر التوقيع الآن فـ**(ب)** بشرط تسمية الحكم `BLOCKED` صراحةً لا `PASS`.

## 7) الملفّات المتوقَّع تعديلها في R5 (مقيسة على `66c9f35`)

**غير محجوبة (§6.1/§6.4/§6.5):**
- `reporting-backend/src/Reporting.Application/Periods/PeriodModels.cs` · `CanonicalPeriodService.cs` — اشتقاق الربع
- `reporting-backend/src/Reporting.Api/Controllers/KpiAnalyticsController.cs` — حدود الفترة والنطاق
- `reporting-backend/src/Reporting.Infrastructure/Services/Employee360Service.cs:131,133,315` — سقوط `LastCompletedWeek()`
- `reporting-frontend/src/pages/KpiPage.tsx` (35,910 B) · `KpiOverview.tsx` (17,332 B) · `components/KpiFilterBar.tsx` · `lib/useKpi.ts` · `pages/IndividualKpiPage.tsx` · `pages/EmployeeProfilePage.tsx`
- اختبارات: `CanonicalPeriodServiceTests.cs` · `KpiTruthContractAndSecurityTests.cs` · `KpiOverview.test.tsx`

**محجوبة حتّى توقيع `DEC-01` (§6.2/§6.3):**
- `reporting-backend/src/Reporting.Application/Common/ReportCadencePolicy.cs` (البند 9)
- `reporting-backend/src/Reporting.Infrastructure/Services/KpiCalculationService.cs` + `Kpi/KpiCalculationModels.cs` + `Kpi/IKpiCalculationService.cs` (البندان 2 و4)

**هجرة/تغيير بيانات:** لا شيء في الشريحة غير المحجوبة — الربع يُشتقّ عند القراءة. أيّ تخزين للربع أو ملء رجعيّ لـ77 تقييمًا = Backfill ⇒ **تصريح مستقلّ**.

## 8) التقدير المحدَّث (بعد القياس)

| النشاط | التقدير |
|---|---|
| بوابة الاستلام والعزل | **مكتمل** |
| خطّ أساس أخضر (بناء خلفيّ + `tsc` + اختبارات واجهة قبل أيّ تعديل) | 30–50 دقيقة |
| §6.1 الفترة (خادم + واجهة + اختبارات) | 2–3 ساعات |
| §6.4 النطاق + §6.5 الحالات الصريحة | 1.5–2.5 ساعة |
| انحدار R1–R4 + توثيق + أدلّة | 1.5–2 ساعة |
| **مجموع الشريحة غير المحجوبة (البديل ب)** | **5.5–8 ساعات** |
| §6.2 + §6.3 بعد توقيع `DEC-01` | +3.5–5 ساعات |
| **الإجمالي للبديل (أ)** | **9–13 ساعة** |

لا يشمل: انتظار قرار المنتج · النشر على TEST · UAT · بيانات الدخول.

## 9) الحوكمة
لم يُمسّ الإنتاج ولا RC ولا TEST. لم تُغيَّر أعلام ولا `perm`. لم يُطلب أيّ اعتماد دخول. لم يُكتب سطر كود منتج. لا Push ولا Merge ولا Tag.
