# تقرير إغلاق دمج R5 في `develop` — المخرجات العشرة

**التاريخ:** 31 أغسطس 2026 · **النطاق:** TEST وحدها + دمج `develop`.
**RC والإنتاج لم يُمسّا إطلاقًا.**

---

## 1) نَسَب `4b8902ee` إلى `develop` وإلى المرشّح النهائيّ

الوضع **قبل** التنفيذ (بوابة المرحلة 1، الموثّقة كاملة في `ANCESTRY-GATE-REPORT.md`):

```
4b8902ee -> origin/develop (812ae903) : NO
4b8902ee -> المرشّح (ab2e0cf)          : NO
```

⟹ فشلت بوابة `DEV-01` على مستوى الـSHA. **لم يُستعمل أيّ Cherry-pick ولا حلّ تلقائيّ**؛ بل استُخرِج فرق وظيفيّ كامل أثبت أنّ المرشّح **يحتوي كلّ ما هو منشور**:

- 8 من 9 ملفّات فرع الإصدار **متطابقة بايتًا** (مقارنة `blob OID`).
- الملفّ التاسع `reporting-frontend/src/types/api.ts` **مجموعة عليا صارمة**: كلّ الأسطر الـ30 المضافة في الإصدار موجودة حرفيًّا.
- صفر مسار مفقود · صفر هجرة مفقودة.
- **البرهان الحاسم:** `git merge-tree --write-tree HEAD 4b8902ee` أنتج `ad619f0d…` وهو **شجرة HEAD نفسها** ⟹ دمج خطّ الأساس المنشور عمليّة **بلا أثر على المحتوى**.

بعد اعتماد مالك المنتج للخيار (أ) — استعادة النَسَب طوبولوجيًّا — نُفِّذ `git merge --no-ff 4b8902ee`:

```
PRE_HEAD  = ab2e0cfe997b48cc10109cf420a7d238d6ca25d5
PRE_TREE  = ad619f0dcd67fd6dc84b55a264bc2b667ad63afb
النتيجة   = Merge made by the 'ort' strategy      ← صفر تعارض
POST_HEAD = 993317aa5c671c86650a6dd6ce2b4a588e8dbf7e
POST_TREE = ad619f0dcd67fd6dc84b55a264bc2b667ad63afb   ← مطابق للشجرة السابقة
الأبوان   = ab2e0cf 4b8902ee
```

`git diff PRE_TREE POST_TREE` = **فارغ**. بلا `-s ours` · بلا Cherry-pick · بلا Rebase · بلا Force.

**الوضع النهائيّ:**

| العلاقة | النتيجة |
|---|---|
| `4b8902ee -> FINAL_SHA` | **YES** |
| `812ae903 -> FINAL_SHA` | **YES** |
| `93c8cb6 -> FINAL_SHA` | **YES** |
| `4b8902ee -> origin/develop` | **YES** |
| `812ae903 -> origin/develop` | **YES** |
| `93c8cb6 -> origin/develop` | **YES** |

---

## 2) الـSHA النهائيّ على فرع R5

```
الفرع     = fix/p123-business-outcome-remediation-r5
FINAL_SHA = cc336a53263efa27a78e420da782bf2e62afc503
المحلّيّ = البعيد (دفع عاديّ بلا Force):  cc336a5 == origin/fix/p123-…-r5
```

سلسلة البناء: `ab2e0cf` → `993317a` (دمج `4b8902ee`) → `9c7b82a` (دمج `origin/develop=812ae903`)
→ `0067b0e` (بوابات الشجرة + تصحيح `DEV-02`) → `cc336a5` (أدلّة UAT النهائيّة).
**كلّ ما بعد `9c7b82a` توثيقيّ تحت `Ops/` — دلتا التشغيل بينهما فارغة.**

مزامنة `origin/develop` تمّت بشروط `DEV-04` مقيسة بعد `fetch`:
37 ملفًّا كلّها `A` وكلّها تحت `Ops/` · `outside_Ops = 0` · `deletions = 0` · صفر هجرة · صفر تعارض.

---

## 3) الـSHA المنشور على TEST وبصماته

**المنشور حاليًّا على TEST هو الـSHA النهائيّ نفسه** الذي دخل `develop`:

| المؤشّر | القيمة |
|---|---|
| ختم SourceLink مقروءًا من `Reporting.Api.dll` على الخادم | `1.0.0+cc336a53263efa27a78e420da782bf2e62afc503` |
| `PKG_ALL` (86 ملفًّا) | `8b286d81e60d5a27` — **Local == Staging == Live** |
| `DLLS` (37 مكتبة في الجذر) | `44d438162aca0f73` — **Local == Staging == Live** |
| `FE_PKG` (حزمة الواجهة) | `f1f7556b1c1c3071` (بلا تغيير) |
| `/health` | `200` · `{"status":"ok","service":"reporting-api"}` |
| `NRestarts` | `0` · صفر خطأ حقيقيّ في السجلّ بعد الإقلاع |
| عدد الهجرات في `reporting_test_uat` | `47` (بلا هجرة جديدة عند الإقلاع) |
| خطّ أساس البيانات بعد إعادة النشر | **متطابق** مع ما قبل النشر (10 عدّادات + 4 بصمات MD5) |

**نسخة احتياطيّة ثلاثيّة قبل اللمس:** `/opt/reporting-test/backup-20260831T060500`
(قاعدة البيانات + `publish.tar.gz` + `frontend-dist.tar.gz`)، إضافةً إلى نسخة الجولة السابقة `…T023800`.

**فرق الحزمة عن جولة UAT (`0067b0e`) — معزول ومُفسَّر:** مقارنة ملفًّا بملفّ مع الحزمة الحيّة السابقة
أظهرت **8 ملفّات مختلفة فقط** هي مكتبات المشروع الأربع وملفّات `pdb` المقابلة، و**78 ملفًّا من التبعيّات متطابقة بايتًا**.
والسبب هو ختم الإصدار وحده: بناءان من **نفس الشجرة ونفس الجذر** لا يختلفان إلّا في الملفّات الثمانية نفسها،
وأحجام المكتبات الأربع **متطابقة تمامًا** (`delta = 0` بايت لكلّ منها)، والختم داخل الأربع جميعًا هو `…cc336a5…`.
ويعضده أنّ `git diff --stat 0067b0e cc336a5 -- reporting-backend reporting-frontend` = **فارغ**.

---

## 4) نتائج الاختبارات كاملة على الـSHA النهائيّ

| البوابة | النتيجة |
|---|---|
| `dotnet build -c Debug` | **0 Error(s)** · 5 Warning(s) |
| `dotnet build -c Release` | **0 Error(s)** · 5 Warning(s) |
| `npx tsc --noEmit` | **نظيف** · مخرجات صفريّة |
| `vite build` (بـ`VITE_API_BASE_URL` الصحيح) | **نجح** |
| الوحدويّة `Reporting.UnitTests` | **610 / 610** |
| الواجهة `vitest run` (المُبلِّغ الافتراضيّ) | **826 / 826** في **71** ملفًّا |
| التكامليّة — الجولة 1 | 2277 / 2278 (فشل عابر واحد) |
| التكامليّة — عزل الفشل | **9 / 9** لكامل `DirectoryNameUniquenessTests` منفردًا |
| التكامليّة — الجولة 2 الكاملة | **2278 / 2278** · 14 د 55 ث |

**عزل القواعد:** ستّ قواعد جديدة `reporting_fin9c7b82a_{main,cal,pfe,kpi,dec,p2}` مُهجَّرة **تسلسليًّا**
عبر 5 مراحل تمهيد قبل الجولة المتوازية، وكلّ واحدة أظهرت **84 جدولًا**، مع `Classification='Supplementary'` على `main` (`UPDATE 5`).

**تحليل الفشل العابر:** `LeadingOrTrailingWhitespace_DoesNotBypassUniqueness` توقّع رمزًا مُعالَجًا فورد الخام `23505`
(انتهاك تفرّد في PostgreSQL) ⟹ سبق الاستثناءُ طبقةَ التحويل بسبب تسابق في الجولة المتوازية.
ليس انحدارًا: نجح منفردًا 9/9، ونجح في الجولة الكاملة الثانية، وشجرة التشغيل **مطابقة بايتًا** لشجرة سجّلت 2278/2278 سابقًا.

**الهجرات:** 47 ملفّ هجرة · صفر هجرة محذوفة أو معدَّلة · المضافتان **إضافيّتان بحتًا**
(`AddSubmissionFieldValueJsonGinIndex`: فهرس GIN واحد؛ `R5_DecOneCadenceEffectivityAndEmploymentWindow`:
4 أعمدة `nullable: true` + فهرس) و`Down()` عكسيّة كاملة في الحالتين. صفر `DROP TABLE` · صفر `NOT NULL` بلا افتراضيّ · صفر تعديل بيانات.
الفارق مع `reporting_prod` (47 صفًّا) مفسَّر بالكامل بصفَّي جسر النَسَب التاريخيَّين الغائبَين عن `4b8902ee` نفسه.

**الأسرار والملفّات المولَّدة:** صفر تطابق لأنماط (JWT · Bearer · سلسلة اتصال بكلمة مرور · مفاتيح خاصّة)
في كامل الفرق وفي كامل الشجرة · صفر ملفّ `bin/`·`obj/`·`node_modules/`·`dist/`·`.env`·`.pem`·`.key`.

---

## 5) نتائج UAT الحرج

**30 بندًا · 30 PASS · 0 FAIL · 0 NA** — التفصيل الكامل والأدلّة في
`final-candidate-9c7b82a/FINAL-TEST-UAT-EVIDENCE.md` ومجلّد `uat-evidence/` (37 لقطة + 13 ملفّ نتائج).

أبرز البنود التي اشترطها التكليف:

| البند | النتيجة المقيسة |
|---|---|
| فتح الربع الجاري تلقائيًّا | PASS — `2026-Q3` بقالبَين |
| استقلال Weekly Pulse عن Official Quarterly لنفس الموظّف | PASS — «الأسبوع 36 — 2026» بـ7 قوالب مقابل `2026-Q3` بقالبَين، بلا تسرّب |
| سلّم الأولويّة مستقلًّا لكلّ مسار | PASS (`U18` · `U18b` · `U18v`) |
| لا قائمة `cadence` تقنيّة في الواجهة | PASS (`U3`) |
| `U6` — غياب الإعداد النشط | PASS — زرّان معطَّلان · **`CREATE_POSTS = 0`** |
| `U10` — مصادَق غير مخوّل | PASS — `403` على ستّة أسطح إداريّة مقابل `401` بلا جلسة · `UNCHANGED_VIA_API = true` |
| التصدير الماليّ — الوجه الموجب | PASS — `2026-Q2` = صفّان من مفتاحَين أسبوعيَّين |
| التصدير الماليّ — الوجه السالب | PASS — `2026-Q3` = صفر صفّ · CSV بترويسة فقط · بلا "Weekly" |
| `U16`/`U17` بالمسار الصحيح | PASS — تبويب «التقييمات» ثمّ اختيار القالب قبل اختبار الزرّ |

**سبب استعمال تفاعل DOM مضبوط بدل مُحدِّدات Playwright المنتظِرة (`OBS-M-01`):**
عقدة زرّ الإنشاء يُعاد تصييرها في React فتُعلَّق `isDisabled`/`click`/`waitFor visible` رغم ظهور الزرّ؛
فاستُعمِل `page.evaluate` للقراءة والنقر. هذا التفافٌ على عدم استقرار الأداة لا على منطق التطبيق.

**ربط الرفض المقصود `403` بالطلب المتوقَّع:** `CONSOLE_ERRORS = 1` · `NET_403 = 1` على
`PATCH /api/directory/users/11f07eba…/employment-window` · `DELTA_MS = 3` · `NET_5xx = 0`.

---

## 6) أثر البيانات والتنظيف

| المؤشّر | القيمة |
|---|---|
| إزالة إسنادات الحساب التركيبيّ | `REMOVED_ASSIGNMENTS = 9` · `200` |
| حذف الحساب التركيبيّ | `DELETE_USER = 200` |
| خطّ الأساس: قبل النشر ≡ بعد النشر ≡ بعد UAT ≡ بعد إعادة النشر | **`IDENTICAL = YES`** |
| تدوير اعتماد المشرف بعد الانتهاء | `FP 263010318229AF25 → 05E1072C926CBC01` · `ROLES_EQUAL = True` · 48 مستخدمًا |
| ملفّات الأسرار المحلّيّة | حُذِفت (`.pw` · `.synthpw` · `.conn.local`) والأنفاق أُغلِقت |
| رمز JWT داخل ملفّ أدلّة الشبكة | استُبدِل بـ`<REDACTED-JWT>` · المتبقّي `0` |

**لم تُغيَّر أيّ بيانات تنظيميّة دائمة، ولم يُلمَس أيّ عَلَم ميزة، ولم تُمنَح أيّ `perm` لصناعة نتيجة خضراء.**

---

## 7) الـSHA النهائيّ لـ`origin/develop`

```
origin/develop  =  ad5df1e96cc72f4ad9215c9385e4a131bff1125c
الأبوان         =  812ae90364d1e1d2cb11f9f035c5d28c024025cd   (develop السابق)
                   cc336a53263efa27a78e420da782bf2e62afc503   (فرع R5 النهائيّ)
شجرة الدمج      =  fc2cffd41af6d5773fea8084b4adaecf5617020a
شجرة المرشّح    =  fc2cffd41af6d5773fea8084b4adaecf5617020a   ← مطابقة
git diff cc336a5 ad5df1e  =  فارغ
```

⟹ **ناتج الدمج مطابق وظيفيًّا وبايتيًّا للمرشّح المنشور على TEST** — لا شيء دخل `develop` لم يخضع لـUAT.

`origin/develop` يحتوي الآن: `4b8902ee` · `812ae903` · `93c8cb6` · `36a6a5b` · `ff1e337` · `32e5fec` · كامل R1–R5
· **72 ملفًّا** تحت `Ops/R22A` (أحدث توثيق R22A) · **159 ملفًّا** تحت `Ops/UAT/P123-BUSINESS-OUTCOME-REMEDIATION-R5-20260830`.

---

## 8) طريقة الدمج

**Push رسميّ مباشر بلا Force** — لا يوجد `gh` ولا مسار PR محميّ مُفعَّل في هذه البيئة، والمسار الرسميّ المستعمل
في هذا المستودع تاريخيًّا هو الدفع المباشر إلى `develop` (كما في `812ae90` و`15886d9` و`32e5fec`).

الخطوات المنفَّذة حرفيًّا:
1. فرع دمج مؤقّت من أحدث `origin/develop`: `merge/r5-to-develop-20260831`.
2. `git merge --no-ff cc336a5` — **Merge Commit، لا Squash ولا Rebase** — صفر تعارض.
3. إثبات تطابق شجرة الدمج بشجرة المرشّح (§7).
4. إعادة `fetch` والتأكّد أنّ رأس `develop` **لم يتغيّر** منذ القياس (`812ae903`).
5. `git push origin HEAD:develop` — دفع عاديّ: `812ae90..ad5df1e`.

**بلا Force · بلا Rebase · بلا حذف فروع · فرع R5 باقٍ كما هو على البعيد.**

---

## 9) إثبات عدم المساس بـRC والإنتاج

| المؤشّر | RC | الإنتاج |
|---|---|---|
| الخدمة | `khubara-reporting-rc` = `active` | `reporting-api` = `active` |
| `ActiveEnterTimestamp` | `Sun 2026-08-30 15:47:15 UTC` | `Sun 2026-08-30 16:40:58 UTC` |
| `mtime` لمجلّد `publish` | `2026-08-30 15:40:40 UTC` | `2026-08-30 15:35:57 UTC` |

كلّ الطوابع **سابقة ليوم 31 أغسطس**، أي قبل أيّ عمل في هذه الجولة ⟹ لم تُعَد تشغيل أيّ خدمة ولم تُلمَس أيّ حزمة.
كلّ النشر جرى حصرًا على `khubara-reporting-test` (المنفذ 5091) وقاعدة `reporting_test_uat`.
`origin/main` = `508509a` لم يُمسّ · وسم الإصدار `r22a-prod-20260830` لم يُمسّ.

---

## 10) الحكم النهائيّ

```
R5_MERGED_TO_DEVELOP
DEPLOYED_BASELINE_ANCESTRY_RESTORED
FINAL_TEST_CANDIDATE_PASS
AWAITING_RC_DEPLOYMENT_APPROVAL
```

**توقّف مقصود هنا.** لا نشر RC ولا نشر إنتاج ولا بدء R6 بلا تصريح صريح جديد ولكلّ عمليّة على حدة.
