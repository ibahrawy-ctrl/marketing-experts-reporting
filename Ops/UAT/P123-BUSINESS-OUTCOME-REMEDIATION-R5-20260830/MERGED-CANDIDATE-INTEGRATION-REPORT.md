# تقرير المرشّح الموحّد — دمج R1–R5 مع أحدث `origin/develop` وإثبات ثبات نتيجة الأعمال على TEST

- **التاريخ:** 30 أغسطس 2026
- **الفرع:** `fix/p123-business-outcome-remediation-r5`
- **الطبيعة:** مرحلة تكامل وتحقّق — لا ميزات جديدة ولا إعادة تصميم ولا تعديل على عقد `DEC-01`.
- **الأدلّة الخام:** `Ops/UAT/P123-BUSINESS-OUTCOME-REMEDIATION-R5-20260830/merged-candidate-5b211db/` (27 ملفّ سجلّ + 7 لقطات للجولة المدموجة).

---

## 1) SHA قبل الدمج وبعده

| البند | القيمة |
|---|---|
| رأس الفرع قبل الدمج | `93c8cb6c487a2e4efcb47dea0f6f8c0f6d4acaf3` (آخر التزام أدلّة R5) |
| رأس الفرع بعد الدمج | **`5b211db61982011d576e8279654e6db40fcd08e7`** |
| الأب الأوّل | `93c8cb6c487a2e4efcb47dea0f6f8c0f6d4acaf3` |
| الأب الثاني | `ff1e337e358b956a62c1dc195eaf440bc920a7b9` |
| نوع الدمج | `git merge --no-ff` — **دمج لا Rebase**، لحفظ أثر التدقيق |
| نصّ الالتزام | `merge: ادمج أحدث origin/develop (ff1e337) في مرشّح R5 لإنتاج مرشّح موحّد قابل للدفع` |
| حالة الشجرة | نظيفة (`git status --porcelain` = فارغ) قبل الدمج وبعده |
| علاقة الفرع بـ`origin/develop` | متقدّم 16 · متأخّر **0** |

**R1–R4 لم يُعكس أيّ منها** — الأربعة أسلاف مباشرون للرأس المدموج، وكذلك `c9667ec` المنشور على TEST في جولة R5:

```
3ee3889 → cde7ed3 → 86abfcf → 66c9f35 → … → c9667ec → f538ec8 → 93c8cb6 → 5b211db
```

---

## 2) رأس `origin/develop` المقيس وقت التنفيذ

`git fetch` نُفِّذ بمفتاح GitHub الصحيح (`GIT_SSH_COMMAND='ssh -i ~/.ssh/github_ed25519 -o IdentitiesOnly=yes'` — المفتاح الافتراضيّ يُرجِع `Permission denied (publickey)`).

- **`origin/develop` = `ff1e337e358b956a62c1dc195eaf440bc920a7b9`** — مطابق تمامًا للقيمة الواردة في التكليف، لا تغيّر بين القياس والتنفيذ.
- الالتزامان اللاحقان اللذان كان الفرع متأخّرًا عنهما: `36a6a5b` و`ff1e337`.

### تصحيح مقيس لمعطًى في التكليف
التكليف وصف الالتزامَين بأنّهما «توثيق/أدلّة R22A». **القياس أثبت خلاف ذلك جزئيًّا** ولم أبنِ على الفرضيّة:

- `ff1e337` — توثيق وأدلّة فقط (مطابق للوصف).
- `36a6a5b` — **يعدّل كودًا إنتاجيًّا**: `reporting-backend/src/Reporting.Infrastructure/Services/ReportTemplateService.cs` (9 أسطر) + 80 سطر اختبارات.

**التغيير نفسه:** كان الحارس يرفض إنشاء مسودة إن وُجد **أيّ** إصدار غير منشور؛ صار يرفض فقط إن كان **أحدث** إصدار غير منشور — أي تحرير القوالب المجمَّدة بمسودة بذر أدنى من الإصدار المنشور.

**إثبات انعدام التماس مع أسطح R5** (KPI / Employee / Attendance / Auth / Navigation / Finance Export):

| الدليل | القياس |
|---|---|
| تقاطع الملفّات بين R1–R5 و`36a6a5b` | **0** ملفّ |
| ورود كلمة `kpi` داخل `ReportTemplateService.cs` | **0** |
| خدمة قوالب KPI منفصلة | `KpiTemplateService.cs:239` برمز مختلف `kpi_version.draft_exists.conflict` مقابل `version.draft_exists.conflict` |
| هل مسّت R5 أيًّا من الملفّين؟ | **لا** |

⟸ الخدمتان منفصلتان تمامًا برمزَي خطأ مختلفَين، والدمج آمن. سُجِّل هذا التصحيح في نصّ التزام الدمج نفسه.

---

## 3) نتيجة تحليل التعارض الفعليّة

قبل أيّ كتابة، نُفِّذ **تجفيف غير مُتلِف** لا يمسّ الشجرة العاملة:

```
git merge-tree --write-tree --name-only HEAD origin/develop
```

- رمز الخروج **0**، والمخرَج **معرّف الشجرة وحده** بلا أيّ قسم تعارض.
- **عدد التعارضات = 0.** لم يلزم أيّ حلّ يدويّ، ولم يُختَر أيّ طرف تلقائيًّا.
- لم يُسجَّل أيّ `CONFLICT` في مخرَج `git merge --no-ff` الفعليّ.

---

## 4) بوّابات البناء والاختبار على الشجرة المدموجة — بالأرقام

| البوّابة | الأمر | النتيجة |
|---|---|---|
| بناء خلفيّ Debug | `dotnet build Reporting.sln -c Debug` | **0 Error · 5 Warning** (15.75s) |
| بناء خلفيّ Release | `dotnet build Reporting.sln -c Release` | **0 Error · 5 Warning** (8.53s) |
| اختبارات وحدويّة | `dotnet test Reporting.UnitTests -c Release` | **Failed 0 · Passed 610 · Skipped 0 · Total 610** |
| اختبارات تكامل | `dotnet test Reporting.IntegrationTests -c Release` | **Failed 0 · Passed 2278 · Skipped 0 · Total 2278** — 8 د 23 ث |
| فحص أنواع الواجهة | `npx tsc --noEmit` | **0 سطر مخرَج** |
| اختبارات الواجهة | `npx vitest run` (المُبلِّغ الافتراضيّ) | **Test Files 71 passed (71) · Tests 826 passed (826)** — 14.27s |
| بناء الواجهة | `VITE_API_BASE_URL=https://test.emarketingacademy.net/api npm run build` | نجح · `index-DPf3EPx4.js` · `index-Cz7lCSe8.css` · ورود `localhost:5090` في الحزمة = **0** |

**عزل قاعدة اختبارات التكامل (منع سباق الهجرات المعروف):** أُنشئت **ستّ قواعد جديدة** `reporting_mrg5b211db_{main,cal,pfe,kpi,dec,p2}` وسُقيت **تسلسليًّا** قبل أيّ تشغيل متوازٍ، ثمّ قيس عدد الجداول: **84 جدولًا في كلّ قاعدة من الستّ**، ثمّ طُبِّقت عبارة `Classification='Supplementary'` على القاعدة الرئيسة (`UPDATE 5`) وإلّا تفشل 5 اختبارات Rollup. لم تُستعمل `reporting_test` الملوَّثة إطلاقًا.

> **اعتماد على سطر العدد لا على رمز الخروج:** رمز خروج `vitest` عاد فارغًا كما في الجولات السابقة؛ الحكم مبنيّ على سطر العدد `Tests 826 passed (826)` كما نصّ التصريح.

### تصحيح مقيس لبصمة حزمة الواجهة
تقرير إغلاق R5 يذكر `index-BEPw02yj.js` / `6de99bfccf274bd2`. أعدتُ البناء مرّتين (`npx vite build` و`npm run build`) فأنتجتا **`index-DPf3EPx4.js`** في الحالتين، وقراءة الحزمة **المنشورة فعلًا** على TEST وقتها أظهرت أنّها كانت `index-DPf3EPx4.js` أيضًا ⟹ الرقم المذكور في تقرير الإغلاق **بائت من جولة بناء أسبق**، والبناء المدموج يعيد إنتاج البايتات المنشورة والمُختبَرة بالضبط. لا انحراف فعليّ.

**فحص ما قبل الالتزام/الدفع:** لا أسرار ولا كلمات مرور ولا سلاسل اتّصال في أيّ ملفّ ملتزَم، ولا ملفّات مولَّدة (`bin/` `obj/` `dist/` `node_modules/`) داخل الالتزام.

---

## 5) قائمة الهجرات وأثر البيانات

- **عدد ملفّات الهجرات على الشجرة المدموجة = 47** في `reporting-backend/src/Reporting.Infrastructure/Persistence/Migrations/`.
- **عدد الهجرات المطبَّقة على قاعدة TEST بعد النشر = 47** ⟸ **لا هجرة جديدة، ولا تغيّر في العدد قبل النشر وبعده.**
- الهجرة الوحيدة الآتية من R5 هي `20260829214324_R5_DecOneCadenceEffectivityAndEmploymentWindow`، و`Up()` فيها **إضافيّ بحت**:

```
AddColumn<DateOnly>  EffectiveFrom · EffectiveTo · ExitDate · HireDate
CreateIndex          IX_kpi_template_assignments_ScopeType_ScopeId_EffectiveFrom_Ef~
```

- الاستدعاءات الأربعة `DropColumn` موجودة في `Down()` **فقط** — تراجع EF الطبيعيّ، لا يُنفَّذ عند الإقلاع.
- **لا `Sql(...)` ولا Backfill ولا `DropTable` ولا `DropDatabase`** في أيّ هجرة.
- **أثر البيانات على TEST = صفر:** لا تحويل ولا إعادة كتابة لأيّ صفّ قائم.

---

## 6) SHA والبصمات المنشورة على TEST

النشر تمّ من **الشجرة الملتزَمة نفسها** (`5b211db`) بعد `rm -rf bin obj` وبوسم SourceLink:

```
-p:SourceRevisionId=5b211db61982011d576e8279654e6db40fcd08e7 -p:ContinuousIntegrationBuild=true
```

| البند | القيمة المقيسة |
|---|---|
| وسم الإصدار داخل `Reporting.Api.dll` على TEST | **`1.0.0+5b211db61982011d576e8279654e6db40fcd08e7`** |
| مطابقة الوسم لـSHA الملتزَم | **مطابق حرفًا بحرف** |
| عدد ملفّات `publish` | 48 |
| بصمة `publish` (51 ملفّ `*.dll`/`*.json`، قائمة مطبَّعة) | محلّيًّا وعلى الخادم = **`316d4a76cf99e16f13d0214892fb17f6`** · `diff` = **متطابق تمامًا** |
| بصمة `dist` الواجهة (7 ملفّات) | محلّيًّا وعلى الخادم = **`f6e054600def6c5343bbed85c2cf4564`** · `diff` = **متطابق تمامًا** |
| حزمة الواجهة المنشورة | `index-DPf3EPx4.js` · `index-Cz7lCSe8.css` |
| الهجرات المطبَّقة | **47** |
| `/health` محلّيًّا | **200** |
| `/health` عموميًّا (`test.emarketingacademy.net`) | **200** |
| النسخ الاحتياطيّ الثلاثيّ | `BACKUP_TS=20260830T165241Z` |
| زمن تعديل `Reporting.Api.dll` على TEST | 2026-08-30 16:52:23 UTC |
| إقلاع `khubara-reporting-test.service` | 2026-08-30 16:53:16 UTC · `active` |

> عند أوّل فحص بعد إعادة التشغيل عاد `/health` بـ`000` — ليس إخفاقًا بل استمرار عمل الإقلاع؛ إعادة الفحص بعد 8 ثوانٍ أعادت `200` مع `{"status":"ok","service":"reporting-api"}`.

---

## 7) نتيجة كلّ سيناريو UAT حرِج على حدة (على SHA المدموج المنشور)

الحصيلة: **27 سجلًّا موثوقًا · 27 PASS · 0 FAIL · 0 NOT EXECUTED**.

| # | المعرّف | السيناريو | النتيجة | الرقم المقيس |
|---|---|---|---|---|
| 1 | `U1` | فتح `/app/kpi` بلا نقرة | PASS | الصفحة تُفتح مباشرة · عدد القوائم المنسدلة = 2 |
| 2 | `U1b` | مرشّح قراءة مستقلّ لكلّ مسار في النظرة الشاملة | PASS | «تلقائي / نبض أسبوعيّ / تقييم ربعيّ رسميّ» متاحة = true |
| 3 | `U15` | **المساران معًا لنفس الموظّف** | PASS | نبض الأسبوع = true · التقييم الربعيّ الرسميّ = true |
| 4 | `U2` | مصدر الحسم شرح لا خيار | PASS | **لا منتقي تواتر = true** · نصّ «يحدّده النظام» = true |
| 5 | `U3` | الإجراءان المستحقّان متاحان معًا | PASS | زرّ النبض متاح = true · زرّ الربعيّ متاح = true |
| 6 | `U18` | العدّادات تختلف باختلاف المسار | PASS | النبض 0/35 · الربعيّ 0/35 · تلقائي 0/35 — كلٌّ من مصدره |
| 7 | `U18b` | **الافتتاح على الربع الجاري** والنداء يحمل المسار صراحةً | PASS | `?periodType=CurrentQuarter&cadence=Quarterly` و`&cadence=WeeklyPulse` |
| 8 | `U13` | التغطية والأهليّة معلنتان | PASS | نصّ التغطية حاضر = true · مؤهّلون = 0/35 |
| 9 | `U18v` | لقطتان لنفس الموظّف بعدّادات مختلفة | PASS | نصّ مسار النبض ≠ نصّ المسار الربعيّ = true |
| 10 | `U14` | **Drill-down يعرض الفترات المصدريّة** | PASS | مفاتيح الأسابيع 28…40 · عددها 13 |
| 11 | `U19` | **الوجه السالب للتصدير**: النبض المعتمَد لا يدخل التصدير المالي | PASS | 4 نبضات معتمَدة داخل الربع 3 (W34:65 · W33:65 · W32:45 · W31:85) · **صفوف التصدير = 0** |
| 12 | `U19b` | **الوجه الموجب للتصدير** | PASS | الربع 2 = **صفّان** · القالب «مؤشرات مندوب المبيعات» · الدرجات 77 و33 |
| 13 | `U19c` | الملفّ المُصدَّر ذاته يطابق الشاشة | PASS | CSV الربع 3 = **1 سطر** (ترويسة) · CSV الربع 2 = **3 أسطر** · ورود «Weekly» = false |
| 14 | `U19d` | نصّ الشاشة يطابق مصدر البيانات | PASS | يعلن «المسار الربعيّ الرسميّ وحده» = true · بقايا النصّ القديم = false |
| 15 | `U19e` | **التجميع ثنائيّ المرحلة وعتبة التغطية 80%** | PASS | النبض: منجَز 4 / مقام 13 · **تغطية 30.77% ⇒ `InsufficientCoverage`** · الربعيّ: منجَز 0 / مقام 1 · **تغطية 0% ⇒ `NoData`** · النبضات الأربع لم تُضِف شيئًا إلى بسط المسار الربعيّ |
| 16 | `U7` | التلاعب من خارج الواجهة يُرفض برمز مسمًّى | PASS | 5 محاولات ⇒ `kpi_eval.period_type_not_supported` ×3 · `kpi_eval.period_format_invalid` · `calendar.cycle_not_open` |
| 17 | `U20` | جرد القوالب: لا تكرار ولا فقد | PASS | 9 قوالب · ربعيّ 2 · نبض 7 · مكرَّرات 0 · مطابق لخطّ الأساس |
| 18 | `U6` | **حالات مسمّاة عند غياب الإعداد + زرّ معطَّل + صفر طلب إنشاء** | PASS | بعد `Employee/Exclude` على القوالب التسعة: مسارات الخادم `WeeklyPulse:0 · Quarterly:0` · «غير مُهيّأ» معلَنة لكلّ مسار · زرّا الإجراءين موجودان ومعطَّلان · **`POST /api/kpi-evaluations` = 0** · لم يُعطَّل أيّ قالب عامّ (9 منشورة من 9) |
| 19 | `U6b` | الفرق من الاستثناء لا من عطل عرض | PASS | نفس الحساب قبل الاستثناء: `WeeklyPulse:7 · Quarterly:2` وخيارات القالب 37 |
| 20 | `U10` | **مستخدم مسجَّل غير مخوَّل يُمنَع بـ403 لا 401** | PASS | من داخل جلسة المتصفّح بدور `Employee`: `PATCH employment-window` ⇒ **403** · وستّة أسطح إداريّة ⇒ **403 ×6** · نفس النداء بلا هويّة ⇒ **401** (المقارنة الحاسمة) |
| 21 | `U10b` | **البيانات لم تتغيّر** بعد كلّ المحاولات | PASS | القراءة المباشرة من قاعدة TEST = `2026-01-04 \| 2026-06-30` بلا تغيير رغم استهداف `2001-01-01/2001-12-31` · سطوح التعديل الظاهرة = `[]` |
| 22 | `U16` | مسار نبض الأسبوع يفتح على الدورة الجارية بقوالبه وحدها ويمنع الإنشاء المكرّر | PASS | الدورة = «الأسبوع 36 — 2026» · قوالب المسار = **7** · زرّ الإنشاء معطَّل قبل اختيار القالب = true · حارس مسمًّى: «يوجد تقييم قائم … لن يُنشأ تقييم جديد» · **`POST` إنشاء = 0** |
| 23 | `U16b` | قوالب النبض وحدها في مسار النبض | PASS | 7 قوالب نبض · **صفر قالب ربعيّ** |
| 24 | `U17` | المسار الربعيّ الرسميّ يفتح على **الربع الجاري** بقوالبه وحدها ويمنع الإنشاء المكرّر | PASS | الفترة = **`2026-Q3`** · قوالب المسار = **2** · الزرّ معطَّل قبل الاختيار = true · حارس مسمًّى بالنتيجة 92 · **`POST` إنشاء = 0** |
| 25 | `U17b` | قوالب المسار الربعيّ وحدها | PASS | «مؤشرات مشتري الإعلانات · مؤشرات مندوب المبيعات» · **صفر قالب نبض** |
| 26 | `U16c` | **لا خلط**: حارسا المسارين يُبلّغان رقمين مستقلَّين | PASS | النبض «لم تُحتسب / مسودة» · الربعيّ «92 / قيد المراجعة» · تسرُّب رقم الربعيّ إلى حارس النبض = **false** · العكس = **false** |
| 27 | `U4/U5` | التقييمان قائمان معًا لنفس الموظّف | PASS | الأسبوع 36 في القائمة = true · الربع الجاري في القائمة = true |

### أخطاء الـConsole
- `s15` (U16/U17/U16c/U4/U5): **`CONSOLE_ERRORS = 0`**
- `s1` (U1/U1b/U15/U2/U3): **`CONSOLE_ERRORS = 0`**
- `s12b` (U10/U10b): **`CONSOLE_ERRORS = 1`** — ونصّها الملتقط حرفيًّا:
  `Failed to load resource: the server responded with a status of 403 (Forbidden)`
  ⟸ هذا **سطر المتصفّح لاستجابة الـ403 المقصودة التي يؤكّدها السيناريو نفسه**، لا خطأ تطبيق. ⟹ **صفر أخطاء Console غير مفسَّرة.**

### انحراف منهجيّ مُعلَن في `U16`/`U17`
في جولة R5 كان `U16`/`U17` يُثبَتان بـ`POST` إنشاء ناجح (200). على البناء المدموج **لم يُنفَّذ `POST` إنشاء جديد عمدًا**، لسببين مقيسين:

1. الواجهة على المرشّح المدموج تستدعي `Lookup` (قراءة محضة لا تكتب) عند اختيار القالب، فتستبدل زرّ «إنشاء تقييم» بزرّ «فتح التقييم القائم» وتُصدر حارسًا مسمًّى ⟹ **لا يمكن أصلًا إصدار `POST` إنشاء لفترة لها تقييم قائم**.
2. إنشاء تقييم على قالب/فترة بلا تقييم قائم كان سيترك **بيانات دائمة** على TEST: `AdminDeleteAsync` **حذف ناعم فقط** (`IsDeleted=true` + حدث مراجعة)، أي أنّ الصفّ يبقى ولا يمكن استرجاع خطّ الأساس.

⟸ التزامًا بشرط «لا بيانات دائمة على TEST»، أُثبت المساران عبر: فتح كلّ مسار على فترته الجارية الصحيحة، وحصر قوالبه في مساره، وتعطيل الزرّ قبل اختيار القالب، والحارس المسمًّى ضدّ التكرار، و**صفر طلب إنشاء**. مسار الكتابة نفسه (`CreateOrGetAsync`) مغطًّى ببوّابة التكامل **2278/2278** على قواعد معزولة.

---

## 8) أثر البيانات على TEST والتنظيف — قبل/بعد

الحساب الاصطناعيّ `synthetic.uat@p123.r5.uat.test` (`c4c62773-9b7a-4e23-aa75-9a231eff5e78`، الأدوار `["Employee"]`، الصلاحيات `[]`) كان لازمًا لسيناريوَي `U6` و`U10`، ونُظِّف بالكامل بعد الانتهاء.

| البند | قبل الجولة | بعد التنظيف | الحكم |
|---|---|---|---|
| `ASSIGN_MD5` | `d41d8cd98f00b204e9800998ecf8427e` | `d41d8cd98f00b204e9800998ecf8427e` | ✅ |
| `EVAL_MD5` | `ad458e767b9e0e87ef7c277569a66feb` | `ad458e767b9e0e87ef7c277569a66feb` | ✅ |
| `TEMPLATE_MD5` | `870383b4ec6bd6096b408b38889929a2` | `870383b4ec6bd6096b408b38889929a2` | ✅ |
| `USERS_MD5` | `e4e8e25f3f358fad550319deb0582f4b` | `e4e8e25f3f358fad550319deb0582f4b` | ✅ |
| `kpi_evaluations` | 21 | 21 | ✅ |
| `kpi_results` | 17 | 17 | ✅ |
| `kpi_review_events` | 7 | 7 | ✅ |
| `kpi_template_assignments` | 0 | 0 | ✅ |
| `kpi_template_versions` | 9 | 9 | ✅ |
| `kpi_templates` | 9 | 9 | ✅ |
| `userroles` | 52 | 52 | ✅ |
| `users` | 48 | 48 | ✅ |

```
diff baseline-pre.txt baseline-post.txt  ⇒  لا فرق (IDENTICAL)
```

- حذف الإسنادات الاصطناعيّة التسعة: **9 × HTTP 200**؛ حذف المستخدم: **HTTP 200**.
- تحقّق مباشر من القاعدة: `SYNTH_ROWS = 0` (لا صفّ ينتهي بـ`@p123.r5.uat.test`) · `SYNTH_ASSIGN = 0`.
- **التزامات محفوظة:** لم يُعطَّل أيّ قالب عامّ (9 منشورة من 9)، ولم تُغيَّر صلاحيات ولا أعلام ميزات، ولم يُمسّ أيّ حساب حقيقيّ.
- `U16`/`U17` على البناء المدموج: `kpi_evaluations` بقيت **21** و`kpi_results` بقيت **17** بعد التشغيل ⟹ **صفر كتابة**.
- **الأسرار:** كلمة مرور مدير TEST الجديدة لم تُطبع ولا تُنسخ في أيّ سجلّ أو تقرير؛ مصدرها الوحيد `~/.secrets/p123-r5-test-admin.txt`، وحُمِّلت إلى ملفّ مؤقّت `0600` طُبع منه العدد فقط (`PW_LOADED_LEN=29 MODE=0o600`). ملفّا `/tmp/r5uat/.pw` و`.synthpw` **حُذِفا** بعد الانتهاء (تحقّق: `No such file or directory`).

---

## 9) اسم الفرع البعيد وSHA بعد الدفع

| البند | القيمة |
|---|---|
| اسم الفرع البعيد | **`origin/fix/p123-business-outcome-remediation-r5`** (فرع جديد — `* [new branch]`) |
| SHA البعيد | **`df1a07521247cb081805d68bb4d224db9ad4ab61`** |
| SHA المحلّيّ | **`df1a07521247cb081805d68bb4d224db9ad4ab61`** |
| المطابقة | ✅ **متطابقان** (`git ls-remote` مقابل `git rev-parse HEAD`) |
| التزام الدمج المبنيّ والمنشور والمُختبَر | `5b211db61982011d576e8279654e6db40fcd08e7` = `HEAD~1` |
| الفارق بين المدفوع والمُختبَر | التزام **توثيق وأدلّة فقط** (`df1a075` = هذا التقرير + مجلّد الأدلّة) — **لا سطر كود واحد** |
| نوع الدفع | عاديّ · **بلا `--force`** · بلا إعادة كتابة تاريخ |
| `origin/main` بعد الدفع | `508509ad8474b321c80cbdd48eb84ecb54bee212` — **لم يُمسّ** |

---

## 10) إثبات أنّ RC والإنتاج لم يُمسّا

| البيئة | الخدمة | الوسم المقيس الآن | `mtime` الـDLL | إقلاع الخدمة |
|---|---|---|---|---|
| الإنتاج | `reporting-api` (5090) | `1.0.0+4b8902eec9b67513d115dbc49c20af0dd62de8b8` | 15:35:57 UTC | 16:40:58 UTC |
| RC | `khubara-reporting-rc` (5092) | `1.0.0+4b8902eec9b67513d115dbc49c20af0dd62de8b8` | 15:40:40 UTC | 15:47:15 UTC |
| TEST | `khubara-reporting-test` (5091) | `1.0.0+5b211db6…` (المرشّح المدموج) | 16:52:23 UTC | 16:53:16 UTC |

- **أوّل اتّصال لهذه الجلسة بالخادم كان 16:52 UTC**، أي **بعد** آخر تعديل على RC (15:40) والإنتاج (15:35) بأكثر من ساعة ⟹ لا يمكن أن تكون هذه الجلسة قد أحدثتهما.
- كلّ عمليّات هذه الجلسة على الخادم اقتصرت على `/opt/reporting-test/**` وقاعدة `reporting_test_uat`.
- الوسمان على RC والإنتاج **لم يتغيّرا** بين بداية الجلسة ونهايتها.

---

## 11) العيوب المفتوحة والانحرافات

| # | البند | الحالة |
|---|---|---|
| `DEV-01` | **انحراف بيئيّ عن التكليف (غير ناتج عن هذه الجلسة):** التكليف ينصّ «RC والإنتاج ما زالا على `897c9b18`». القياس يُظهر أنّ **الاثنين يشغّلان `4b8902eec9b67513d115dbc49c20af0dd62de8b8`** (فرع `release/r22a-from-897c9b18`، وليس سليلًا لـ`origin/main` ولا لـ`origin/develop`)، ونُشِرا اليوم نفسه قبل بدء عملي. **مفتوح — يحتاج قرار مالك المنتج**، ولا يحجب دفع فرع المهمّة. |
| `DEV-02` | بصمة حزمة الواجهة المذكورة في `R5-CLOSURE-REPORT.md` (`index-BEPw02yj.js` / `6de99bfccf274bd2`) **بائتة** من جولة بناء أسبق؛ الصحيح المنشور والمُختبَر هو `index-DPf3EPx4.js`. **مسجَّل — لا أثر وظيفيّ.** |
| `DEV-03` | معطى التكليف «الالتزامان اللاحقان توثيق/أدلّة R22A» **غير دقيق**: `36a6a5b` يعدّل `ReportTemplateService.cs`. قيس التماس وثبت أنّه **صفر** مع أسطح R5. **مسجَّل ومُغلَق قياسًا.** |
| `OBS-M-01` | لَقّاطات Playwright الانتظاريّة (`isDisabled`/`click`) تعلَق على زرّ «إنشاء تقييم» رغم أنّه ظاهر في الـDOM (`w=97.48 h=36 visibility=visible`) — إعادة تصيير React متكرّرة تُبدِّل العقدة. عولج في أدوات الاختبار بقراءة/نقر مباشرَين على الـDOM. **ملاحظة على أدوات القياس لا على المنتج.** |
| `DEV-04` | **تقدَّم `origin/develop` بعد لحظة القياس وبعد الدفع:** صار `32e5feca6ad0d81de7f1327a45a9af4694c1afb9` بدل `ff1e337`. الالتزام الجديد `32e5fec` **توثيقيّ بحت ومقيس**: 32 ملفًّا، 1963 إضافة، **0 حذف**، **0 ملفّ خارج `Ops/`**، **0 ملفّ كود** (`.cs`/`.ts`/`.tsx`/`.csproj`/`.sln`). تجفيف `merge-tree` مقابل الرأس الجديد يُعيد شجرة نظيفة **بلا تعارض**. لم أُعِد الدمج ولم أُعِد النشر — المرشّح المدفوع يبقى هو **المبنيّ والمنشور والمُختبَر** فعلًا. **يُعرَض على بوّابة الدمج.** |
| — | **لا عيب وظيفيّ مفتوح** على المرشّح المدموج. |

---

## 12) الحكم النهائيّ

> **`MERGED_CANDIDATE_TEST_UAT_PASS — FEATURE_BRANCH_PUSHED — AWAITING DEVELOP_MERGE_APPROVAL`**

مبنيًّا على: 0 تعارض · 0 خطأ بناء في Debug وRelease · 610/610 وحدويّ · 2278/2278 تكامل على ستّ قواعد معزولة مسقيّة تسلسليًّا · 0 خطأ أنواع · 826/826 واجهة · 47 هجرة بلا تغيير وأثر بيانات صفر · بصمة `publish` و`dist` مطابقة حرفيًّا بين البناء المحلّيّ والمنشور على TEST · **27/27 سيناريو UAT حرِج PASS على SHA المدموج المنشور** · صفر أخطاء Console غير مفسَّرة · خطّ أساس TEST مستعاد بالكامل (4 بصمات MD5 + 10 عدّادات متطابقة) · RC والإنتاج لم يُمسّا.

---

## 13) بوّابة مستقلّة — قرار الدمج في `develop`

**لم يُدمَج شيء في `origin/develop`، ولم يُحذف أيّ فرع، ولم يُنشر شيء على RC أو الإنتاج، ولم تُبدأ R6.**

ما يحتاج قرارًا صريحًا من مالك المنتج قبل أيّ خطوة تالية:

1. **دمج `fix/p123-business-outcome-remediation-r5` في `develop`** — بأيّ آليّة (PR أم دمج مباشر)، مع ملاحظة أنّ `origin/develop` تقدّم إلى `32e5fec` (توثيق بحت، بلا تعارض — `DEV-04`).
2. **الانحراف `DEV-01`:** RC والإنتاج يشغّلان `4b8902ee` لا `897c9b18` كما ورد في التكليف. يلزم قرار: هل هذا وضع مقصود ومعتمَد؟ وهل يُبنى مسار RC للمرشّح الحاليّ فوقه؟
3. **أيّ نشر على RC أو الإنتاج** — غير مصرَّح به إطلاقًا حتّى الآن.
