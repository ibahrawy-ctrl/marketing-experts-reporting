# تقرير أدلّة UAT — المساران المتزامنان على TEST (`DEC-01` / `OBS-R5-01`)

**التاريخ:** 30 أغسطس 2026 · **البيئة:** TEST وحدها (`test.emarketingacademy.net`) · **RC والإنتاج لم يُمسّا إطلاقًا.**
**الحكم:** `R5_BUSINESS_OUTCOME_TEST_UAT_PASS` — 29 من 30 سيناريو مجتاز، والوحيد غير المجتاز (`U6`) **غير قابل لإعادة الإنتاج بيئيًّا** لا عيبًا في الكود (§5).

---

## 1) الشرط 1 — إثبات SHA والحزمة المنشورة

| القياس | القيمة |
|---|---|
| **SHA المنشور على TEST** | `c9667ec21c48a8a1a11cd1006e24a4d5a043294d` |
| الختم المقروء من التجميعة الحيّة | `1.0.0+c9667ec21c48a8a1a11cd1006e24a4d5a043294d` |
| بصمة الحزمة (86 ملفًّا) | `265668b44673d8d5` — **متطابقة** محلّيًّا ← مرحليًّا ← حيًّا |
| بصمة الـDLLs | `6e4ac1b3c2b4296e` — متطابقة في المواضع الثلاثة |
| بصمة الواجهة الحيّة | `f1f7556b1c1c3071` (من `440bcfa`؛ شجرة `reporting-frontend` **لم تتغيّر** بين `440bcfa` و`c9667ec` ⇒ نشر خلفيّة فقط) |
| الهجرات | **47** — بلا زيادة ولا هجرة جديدة |
| `/health` | `{"status":"ok","service":"reporting-api"}` · الخدمة `active` · صفر أسطر خطأ في السجلّ |

**سلسلة النشر على TEST خلال هذه الجولة:** `d8666f5` → `6ff0c8f` → `440bcfa` → **`c9667ec`** (النهائيّ).

**بوّابات محلّيّة على `c9667ec` قبل النشر:** وحدوي **610/610** · تكامل **2276/2276** على قاعدة نظيفة (`r5reg_174729`) · واجهة **826/826** (71 ملفًّا، على `440bcfa` الذي لم تتغيّر واجهته بعده).

---

## 2) نتائج السيناريوهات U1–U20 (30 معرّفًا · 29 PASS)

الأدلّة الخام: `ALL-RESULTS.json` · 28 لقطة في `shots/` · سجلّات الشبكة `net-s*.json`. **صفر أخطاء وحدة تحكّم في كلّ الجولات.**

### أ) المساران متزامنان ومرئيّان معًا
| # | السيناريو | النتيجة | الدليل المقيس |
|---|---|---|---|
| U1 | فتح `/app/kpi` بلا نقرة | PASS | قائمتان منسدلتان لا واحدة · `U1-kpi-landing.png` |
| U1b | مرشّح قراءة مستقلّ لكلّ مسار | PASS | «تلقائي / نبض أسبوعيّ / تقييم ربعيّ رسميّ» متاحة |
| U15 | **المساران معًا لنفس الموظّف** | PASS | نبض الأسبوع=`true` · الربعيّ الرسميّ=`true` · `U15-dual-track.png` |
| U2 | لا منتقي تواتر تقنيّ | PASS | «يحدّده النظام» معروض شرحًا لا خيارًا (`DEC-01 §4`) |
| U3 | الإجراءان المستحقّان متاحان معًا | PASS | زرّ النبض متاح · زرّ الربعيّ متاح |
| U4/U5 | التقييمان قائمان معًا | PASS | الأسبوع 36 والربع الجاري كلاهما في القائمة · `U4U5-both-listed.png` |

### ب) سلّم الأولويّة داخل كلّ مسار مستقلًّا (`DEC-01 §2`)
| # | السيناريو | النتيجة | الدليل المقيس |
|---|---|---|---|
| U16 | تسجيل نبض الأسبوع | PASS | `HTTP=200` · `Weekly` · `2026-W36` · قوالب المسار=**7** |
| U16b | قوالب النبض وحدها في مسار النبض | PASS | النبض العام · SEO · الفيديو · المصمم · المودريشن · كاتب المحتوى · مبيعات B2C |
| U17 | التقييم الربعيّ الرسميّ لنفس الموظّف | PASS | `HTTP=200` · `Quarterly` · `2026-Q3` · قوالب المسار=**2** |
| U17b | قوالب المسار الربعيّ وحدها | PASS | مؤشرات مشتري الإعلانات · مؤشرات مندوب المبيعات |

⇒ **غياب أحد المسارين لا يخفي الآخر، ولا يبتلع أحدهما قوالب الآخر** (`DEC-01 §3`).

### ج) لا خلط في العدّادات ولا في التغطية (`DEC-01 §5`) — **البند الحاسم**
| # | السيناريو | النتيجة | الدليل المقيس |
|---|---|---|---|
| U18 | العدّادات تختلف باختلاف المسار | PASS | `U18-weekly.png` / `U18-quarterly.png` / `U18-auto.png` |
| U18b | النداء يحمل المسار صراحةً | PASS | `…&cadence=Quarterly` مقابل `…&cadence=WeeklyPulse` |
| U18v | لقطتان لنفس الموظّف بعدّادين مختلفين | PASS | نصّ مسار النبض ≠ نصّ المسار الربعيّ · الاسم حاضر في كلتيهما |
| U13 | التغطية والأهليّة معلنتان لا مخفيّتان | PASS | نصّ التغطية/الأهليّة حاضر |
| U14 | التنقيب يعرض الفترات المصدريّة | PASS | 13 مفتاح أسبوع (28…40) بمآل كلٍّ منها · `U14-drilldown.png` |
| **U19e** | **عدّادان مستقلّان لنفس الموظّف ونفس الربع** | **PASS** | انظر الجدول أدناه · `U19e-two-independent-counters.png` |

**إثبات عدم اختلاط العدّادات — نفس الموظّف («موظف تنفيذ UAT») ونفس الفترة (`2026-Q3`):**

| المقياس | مسار نبض الأسبوع | المسار الربعيّ الرسميّ |
|---|---|---|
| منجَز (بسط) | **4** | **0** |
| متوقَّع (مقام) | **13** | **1** |
| ناقص | 9 | 1 |
| التغطية | **30.77%** | **0%** |
| جودة البيانات | `InsufficientCoverage` | `NoData` |
| حالة الرحلة | `InProgress` | `NotStarted` |

النبضات الأربع المعتمَدة (`2026-W31:85` · `W32:45` · `W33:65` · `W34:65`) **لم تُضِف شيئًا** إلى بسط المسار الربعيّ ولا إلى مقامه. لا بسط مشترك ولا مقام مشترك.

**والانفصال قائم على مستوى الشركة أيضًا** (`2026-Q3`، 35 موظّفًا): النبض `14/455 = 3.08%` (`InsufficientCoverage`) مقابل الربعيّ `0/35 = 0%` (`NoData`) — رقمان مختلفان تمامًا. المتوسّطان يظهران `—` في المسارين لأنّ **كليهما دون عتبة الكفاية**، وهو السلوك الصحيح: لا يُختلق رقم من تغطية ناقصة.

### د) التصدير المالي يستهلك المسار الربعيّ وحده (`DEC-01 §5`) — بند القبول «التصدير لا يستهلك النبض»
| # | السيناريو | النتيجة | الدليل المقيس |
|---|---|---|---|
| **U19** | **الوجه السالب:** نبض معتمَد داخل الربع لا يدخل التصدير | PASS | الربع 3: **4 نبضات معتمَدة** داخل الربع ⇒ **0 صفّ** في التصدير · لا مفتاح أسبوعيّ على الشاشة · `U19-q3-pulse-excluded.png` |
| **U19b** | **الوجه الموجب:** تقييم المسار الربعيّ يظهر فعلًا | PASS | الربع 2: **2 صفّ**، القالب «مؤشرات مندوب المبيعات» (ربعيّ)، الدرجتان 77 و33 · `U19-q2-quarterly-included.png` |
| U19c | الملفّ المُصدَّر يطابق الشاشة | PASS | CSV الربع 3 = **ترويسة فقط** (سطر واحد) · CSV الربع 2 = ترويسة + صفّان · `Weekly` في CSV = `false` |
| U19d | نصّ الشاشة يطابق مصدره ولا يناقضه | PASS | يعلن «المسار الربعيّ الرسميّ وحده»=`true` · النصّ المتناقض القديم=`false` |

**الوجهان معًا ضروريّان:** الوجه السالب وحده تُرضيه شاشة فارغة دائمًا. الوجه الموجب يثبت أنّ المصدر المالي **موجود ويعمل**، والسالب يثبت أنّه **لا يشمل النبض**.

### هـ) نافذة الخدمة وسلطة الخادم والتدقيق
| # | السيناريو | النتيجة | الدليل المقيس |
|---|---|---|---|
| U8 | نافذة الخدمة معلَنة نصًّا لا فراغًا | PASS | «التحاق:» · «انتهاء الخدمة:» · حالة معلَنة · `U8-employment-window.png` |
| U9 | التحرير من داخل الصفّ بلا انتقال | PASS | `PATCH …/employment-window = 200` · القيمة تُقرأ محدَّثة · `U9-saved.png` |
| U9b | النافذة تقصّ المقام فعلًا ولا تُخفي نقصًا | PASS | مقام النبض `13 ← 7` · مقام الربعيّ `1 ← 1` (**لم يتأثّر** ⇒ لا خلط) |
| U9c | إرجاع النافذة إلى أصلها | PASS | المقام عاد إلى `13` |
| U10 | الخادم سلطة المنع لا إخفاء الزرّ | PASS | `PATCH` بلا هويّة ⇒ **401**، لا نجاح صامت |
| U11 | تدقيق بالقيمة قبل/بعد والفاعل والوقت | PASS | `HTTP=200` · الحدث والقيمة الجديدة حاضران · `U11-audit.png` |
| U12 | لا إعادة كتابة للتاريخ | PASS | توقيع (المعرّف\|النوع\|المفتاح\|الحالة\|الدرجة) لكلّ التقييمات **متطابق قبل/بعد** |
| **U7** | **التلاعب من خارج الواجهة يُرفض برمز مسمًّى** | PASS | 5 محاولات، كلّها **400** برمز مسمًّى · `U7-server-authority.png` |

**تفصيل U7 — خمس محاولات تلاعب مباشرة بالـAPI تجاوزًا للواجهة:**

| المحاولة | HTTP | الرمز المسمّى |
|---|---|---|
| قالب أسبوعيّ + `PeriodType=Quarterly` | 400 | `kpi_eval.period_type_not_supported` |
| قالب ربعيّ + `PeriodType=Weekly` | 400 | `kpi_eval.period_type_not_supported` |
| قالب ربعيّ + مفتاح أسبوعيّ `2026-W20` | 400 | `kpi_eval.period_format_invalid` |
| قالب أسبوعيّ + `PeriodType=Monthly` | 400 | `kpi_eval.period_type_not_supported` |
| قالب ربعيّ + ربع لم يبدأ `2027-Q1` | 400 | `calendar.cycle_not_open` |

⇒ التواتر خاصّية قالب يفرضها الخادم، لا خيارًا يملكه العميل.

### و) حوكمة القوالب المبذورة (`DEC-01 §6`)
| # | السيناريو | النتيجة |
|---|---|---|
| **U20** | جرد القوالب: لا تكرار ولا فقد والمساران قائمان | PASS |

**حالة القوالب على TEST — قبل التدخّل وبعده (متطابقتان):**

| التواتر | العدد | الحالة | النسخ لكلّ قالب | مكرَّر |
|---|---|---|---|---|
| `Quarterly` | **2** (مؤشرات مندوب المبيعات · مؤشرات مشتري الإعلانات) | `Published` | 1 | **0** |
| `WeeklyPulse` | **7** (النبض العام · SEO · الفيديو · المصمم · المودريشن · كاتب المحتوى · مبيعات B2C) | `Published` | 1 | **0** |

**لم تلزم أيّ كتابة مصالحة على TEST،** والسبب موثَّق: القالبان الربعيّان المبذوران **عامّان** (`JobRoleId` فارغ) بأثر هشاشة `OrgSeeder.SeedJobRolesAsync` على قاعدة مأهولة (خطّ الأساس §3). وهذه هي **أسوأ حالة** للعيب المعالَج لا أضعفها: قالب ربعيّ عامّ كان — قبل الإصلاح — يبتلع المسار الأسبوعيّ لكلّ موظّف بلا استثناء. ومع ذلك بقيت قوالب النبض السبعة كاملة ومرئيّة (U16b) ⇒ **لا يخفي أحد المسارين الآخر**.

---

## 3) العيب الحاكم `DEF-R5-003` وعلاجه على مرحلتين

**العَرَض المقيس على TEST قبل الإصلاح (`d8666f5`):** شاشة التصدير المالي للربع 3 كانت تعرض صفّ **«2026-W29 · النبض الأسبوعي العام · ٨٧٫٥ · معتمد»** — أي أنّ مؤشّرًا تشغيليًّا غير رسميّ كان يظهر كمصدر ماليّ، بمخالفة مباشرة لـ`DEC-01 §5`.

**المرحلة 1 — `6ff0c8f`:** قصر `BuildFinanceExportAsync` على `PeriodType == Quarterly` ومفتاح ربعيّ تامّ. أزال العَرَض، لكنّه أحدث عيبًا أدقّ.

**المرحلة 2 — `c9667ec` (`OBS-R5-02`):** `KpiCalculationService.BaseEvaluationsQuery` يميّز المسار **بتواتر القالب** ويطابق الفترة بمدى الربع، بينما صار التصدير يميّزه بـ`PeriodType` ومفتاح تامّ. فاختلفت المجموعة التي يراها «المتوسّط الرسميّ» عن التي يراها «التصدير المالي» **على البيانات نفسها** — وهو خلط من نوع آخر يخالف العقد ذاته، إذ يوجب `DEC-01 §5` أن يستهلك الرقمان المجموعة نفسها. **الدليل الحيّ:** السجلّ `Quarterly | 2026-W20 | Approved | 77` من قالب ربعيّ لكنّه مفتاح بدورة (بيانات سابقة لـ`DEC-01`) كان يدخل الرقم الرسميّ ويختفي ماليًّا. بعد `c9667ec` يظهر في تصدير الربع 2 (U19b) بينما يبقى النبض المعتمَد خارجه (U19).

**تغطية اختباريّة:** الاختبار 23 في `KpiFinanceExportTests` يثبت الوجهين معًا في اختبار واحد (السجلّ الربعيّ المفتاح بدورة يظهر · النبض المعتمَد داخل الربع نفسه لا يظهر).

**عيب نصّيّ مصاحب (`440bcfa`):** الحالة الفارغة كانت تقول «لا توجد تقييمات **أسبوعية** معتمَدة» فتُبلغ مستخدم المالية أنّ الصفر سببه نقص **نبض** — نقيض المصدر الفعليّ. صُحِّحت وتُقاس الآن في U19d.

---

## 4) أثر التنفيذ على بيانات TEST (إفصاح كامل)

1. **تقييم ربعيّ للموظّف الاختباريّ (`2026-Q3`) انتقل من `Draft`/بلا درجة إلى `UnderReview`/`92`.** تغيير ناتج عن UAT، مقصود وموثَّق، على حساب اختباريّ لا على بيانات عمل حقيقيّة.
2. **إنشاء نبض `2026-W36` وتقييم ربعيّ `2026-Q3`** ضمن U16/U17.
3. **نافذة خدمة عُدِّلت ثمّ أُرجِعت إلى قيمتها الأصليّة** (U9 → U9c، مثبَت بعودة المقام إلى 13).
4. **لا بريد حقيقيّ**: مكبح `EmailNotifications__Mode` قائم على TEST طوال التنفيذ.
5. **لا تغيير صلاحيّات ولا أدوار ولا أعلام ميزات.** ولا كتابة مصالحة على القوالب.
6. نسخة احتياطيّة ثلاثيّة سابقة للتنفيذ: `BACKUP_TS = 20260830T113625Z`.

---

## 5) العيوب والملاحظات المفتوحة

| الرمز | الوصف | الأثر | الحالة |
|---|---|---|---|
| `U6-ENV` | **`U6` غير قابل لإعادة الإنتاج على TEST**: فُحِص **35 موظّفًا**، لا أحد بلا مسار ولا بمسار فارغ، لأنّ القوالب التسعة كلّها **عامّة** فيطابقها كلّ موظّف. لم أفتعل الشرط بإسناد `Exclude` لأنّه تغيير تنظيميّ على بيانات مشتركة خارج نطاق التصريح. | لا أثر وظيفيّ؛ المسار مغطّى وحدويًّا وتكامليًّا. | **مفتوح — بيئيّ لا برمجيّ** |
| `OBS-R5-03` | **فصل المهامّ يمنع اعتماد المُقيِّم لتقييمه**: `EnsureCanReview` ردّ **403** («لا يمكن لمُدخِل التقييم مراجعته») عند محاولة اعتماد التقييم الربعيّ بحساب المدير نفسه. | **ضابط صحيح لا عيب.** لكنّه حصر الوجه الموجب لـU19 في بيانات معتمَدة سابقًا بدل تقييم مُنشأ في الجلسة. لم أتجاوز الحارس ولا عدّلت `KpiReviewerOverrideUserId` لصناعة لقطة خضراء. | **موثَّق — لا يحتاج علاجًا** |
| `OBS-R5-04` | **مقارنة المتوسّطين على مستوى الشركة غير حاسمة** حين يكون المساران دون عتبة الكفاية: كلاهما يعرض `—`، ومقارنة `—` بـ`—` لا تُثبت انفصالًا. | لا أثر؛ الانفصال مُثبَت بالبسط والمقام (U19e) وبأرقام الشركة (`14/455` مقابل `0/35`). | **موثَّق — تحسين توكيد لا عيب منتج** |
| `OBS-R5-05` | **`U10` مُثبَت عند حدّ الخادم فقط** (`401` بلا هويّة). إخفاء الزرّ في الواجهة مشروط بـ`canEditBasic` (Admin/CeoSupport/HR) ولم يُختبَر بحساب دور آخر لأنّ التصريح يغطّي حساب مدير TEST وحده. | منخفض: سلطة المنع خادميّة وهي المُثبَتة. | **مفتوح — يحتاج تصريح حساب إضافيّ** |
| `OBS-R5-06` | **سجلّات ربعيّة سابقة لـ`DEC-01` مفتاحة بدورة** (`Quarterly | 2026-W20`) موجودة فعلًا على TEST. | لا أثر بعد `c9667ec`: تُعامَل كعضو في الربع في **الرقمين معًا**. لا ملء رجعيّ ولا تعديل مفاتيح. | **مغلق بالتصميم** |

**العيوب المغلقة في هذه الجولة:** `DEF-R5-003` (مرحلتان: `6ff0c8f` + `c9667ec`) · العيب النصّيّ في الحالة الفارغة (`440bcfa`) · `OBS-R5-02` (`c9667ec`).

---

## 6) ما لم يُفعَل عمدًا (حدود التصريح)

- **لا Push** إلى أيّ فرع بعيد. الالتزامات الأربعة محلّيّة على `fix/p123-business-outcome-remediation-r5`.
- **لا نشر RC ولا إنتاج**، ولا مسّ لهما بأيّ صورة.
- **لا هجرة جديدة** (47 ثابتة) ولا Backfill ولا كتابة على قاعدة حيّة خارج ما اقتضاه UAT المصرَّح به.
- **لا إضعاف لأيّ اختبار قائم** لتمرير نتيجة.
