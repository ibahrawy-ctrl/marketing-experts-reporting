# بوابة النَسَب والتعارض (المرحلة 1) — تقرير توقّف مقيس

**التذكرة:** `P123-BUSINESS-OUTCOME-REMEDIATION-R5` — مرحلة الدمج إلى `develop`
**التاريخ:** 30 أغسطس 2026
**الحكم:** `R5_DEVELOP_MERGE_BLOCKED — DEPLOYED_BASELINE_NOT_IN_CANDIDATE_ANCESTRY (على مستوى SHA فقط)`
**النطاق المنفَّذ:** المرحلة 1 وحدها. لم يُنفَّذ أيّ دمج ولا دفع ولا نشر. RC والإنتاج لم يُمسّا.

---

## 1) القياس الأوّليّ بعد `git fetch --all --prune`

| المرجع | SHA |
|---|---|
| `origin/develop` | `812ae90364d1e1d2cb11f9f035c5d28c024025cd` |
| `origin/main` | `508509ad8474b321c80cbdd48eb84ecb54bee212` (لم يُمسّ) |
| رأس فرع R5 محلّيًّا | `ab2e0cfe997b48cc10109cf420a7d238d6ca25d5` |
| `origin/fix/p123-business-outcome-remediation-r5` | `ab2e0cfe997b48cc10109cf420a7d238d6ca25d5` (مطابق) |
| خطّ الأساس المنشور على RC والإنتاج | `4b8902eec9b67513d115dbc49c20af0dd62de8b8` |

`4b8902ee` موجود حصرًا على `release/r22a-from-897c9b18` (محلّيًّا وعلى `origin`)، وهذا **قرار موثَّق** لا حادث:
`Ops/R22A/PROMOTION/R22A-FINAL-CLOSURE-REPORT.md` §9-ج ينصّ صراحةً على أنّ `origin/release/r22a-from-897c9b18 = 4b8902ee`
وأنّ `origin/develop` تقدّم إلى `32e5fec` **بمسار منفصل**، أي أنّ SHA المنشور لم يدخل `develop` طوبولوجيًّا قطّ.

## 2) نتيجة `merge-base --is-ancestor` (البوابة الإلزاميّة)

```
4b8902ee -> origin/develop   : NO   ← فشل
4b8902ee -> HEAD (مرشّح R5)   : NO   ← فشل البوابة الإلزاميّة
4b8902ee -> origin/main      : NO
origin/main -> origin/develop: YES
32e5fec  -> origin/develop   : YES
897c9b18 -> HEAD             : YES
897c9b18 -> origin/develop   : YES
```

**السلف المشترك:** `merge-base(4b8902ee, HEAD) = merge-base(4b8902ee, origin/develop) = 897c9b187ab4216213b4f453ec65948cd06dff27`.

⟹ البوابة **فشلت على مستوى SHA**. وقفتُ فورًا قبل أيّ دمج، ونفّذتُ الفرق الوظيفيّ الكامل المطلوب أدناه.
لم أستعمل Cherry-pick ولا Rebase ولا Force ولا أيّ حلّ آليّ لتجاوز الفشل.

## 3) الفرق الوظيفيّ الكامل `4b8902ee → مرشّح R5` (لا عدّ ملفّات فقط)

### 3-أ) ما الذي يضيفه فرع الإصدار أصلًا فوق السلف المشترك؟

`git rev-list 897c9b18..4b8902ee` = **ثلاثة التزامات** فقط:

| SHA | العنوان | `git cherry HEAD` |
|---|---|---|
| `e6d4ad1` | `feat(r22a): بنود عمل متعدّدة داخل المشروع الواحد واكتشاف تقارير المشروع — بلا فهرس GIN` | `+` (لا نظير patch-id تامّ) |
| `e510f5a` | `fix(p360-r2): امنع انهيار محرّر التسليم على قيمة إجابة غير نصّيّة` | `-` (نظير موجود على المرشّح) |
| `4b8902e` | `fix(r22a): حرِّر القوالب المجمَّدة بمسودات بذر أدنى من الإصدار المنشور` | `-` (نظير موجود على المرشّح) |

اثنان من الثلاثة لهما نظير مطابق بـ`patch-id` على المرشّح. الثالث (`e6d4ad1`) لا نظير تامّ له
لأنّه طُبِّق على قاعدة مختلفة فاختلف سياق الـdiff — **لا لأنّ محتواه ناقص**، والدليل في 3-ب.

### 3-ب) مقارنة على مستوى البصمة (blob OID) للملفّات التسعة التي مسّها فرع الإصدار

```
SAME   reporting-backend/src/Reporting.Application/Clients/ProjectReportSliceModels.cs
SAME   reporting-backend/src/Reporting.Infrastructure/Services/ProjectService.cs
SAME   reporting-backend/src/Reporting.Infrastructure/Services/ReportTemplateService.cs
SAME   reporting-backend/src/Reporting.Infrastructure/Services/SubmissionService.cs
SAME   reporting-frontend/src/pages/ProjectMultiWorkItems.test.tsx
SAME   reporting-frontend/src/pages/ProjectReportSlicePage.test.tsx
SAME   reporting-frontend/src/pages/ProjectReportSlicePage.tsx
SAME   reporting-frontend/src/pages/SubmissionsPage.tsx
DIFF   reporting-frontend/src/types/api.ts   (968abdc9… → 7a589c2d…)
```

**ثمانية من تسعة متطابقة بايتًا ببايت.** والملفّ التاسع:

```
git diff --numstat 4b8902ee HEAD -- reporting-frontend/src/types/api.ts  ⟹  47   0
```

**47 إضافة · صفر حذف** ⟹ مجموعة فائقة صارمة (strict superset). وللتحقّق المباشر لا الاستنتاج:
الأسطر الثلاثون التي أضافها فرع الإصدار إلى `api.ts` فُحِصت واحدًا واحدًا مقابل نسخة المرشّح ⟹ **`MISSING_COUNT = 0`**.

### 3-ج) الحذوفات في الفرق الكامل — أين تقع بالضبط؟

```
git diff --numstat 4b8902ee HEAD -- reporting-backend/src reporting-frontend/src
⟹ add=14773  del=398  files=72
```

الملفّات الحاملة للحذف (31 ملفًّا) تقاطعها مع الملفّات التسعة التي مسّها فرع الإصدار:

```
comm -12 <ملفّات فرع الإصدار> <الملفّات الحاملة للحذف>  ⟹  (فارغ)
```

**صفر تقاطع.** كلّ الحذوفات الـ398 تقع في ملفّات لم يمسّها فرع الإصدار إطلاقًا
(`KpiCalculationService.cs` 89 · `KpiPage.tsx` 53 · `KpiOverview.test.tsx` 51 · `KpiOverview.tsx` 35 …)،
أي أنّها إعادة تشكيل R5 لأسطحها الخاصّة فوق السلف المشترك `897c9b18`، لا إزالة لسلوك منشور.

### 3-د) الهجرات والملفّات المفقودة

- الهجرات: `4b8902ee` = **91** ملفّ `.cs` · المرشّح = **95** · `comm -23` ⟹ **صفر هجرة مفقودة**. فرع الإصدار لم يُضِف أيّ هجرة.
- الشجرة كاملةً: `git ls-tree -r --name-only 4b8902ee` مقابل `HEAD` ⟹ **صفر مسار موجود في المنشور وغائب عن المرشّح**.

### 3-هـ) البرهان الحاسم — تجربة دمج غير مدمِّرة

```
git merge-tree --write-tree --name-only HEAD 4b8902ee   ⟹  ad619f0dcd67fd6dc84b55a264bc2b667ad63afb
git rev-parse HEAD^{tree}                               ⟹  ad619f0dcd67fd6dc84b55a264bc2b667ad63afb
git diff --stat HEAD ad619f0d…                          ⟹  (فارغ تمامًا)
```

**شجرة نتيجة دمج خطّ الأساس المنشور في المرشّح مطابقة تمامًا لشجرة المرشّح الحاليّة.**
صفر تعارض · صفر تغيير محتوى · صفر ملفّ.

⟹ **الاحتواء الوظيفيّ مثبَت بالقياس المباشر لا بالاستنتاج:** المرشّح يحتوي **100%** من محتوى `4b8902ee` المنشور،
ولا يعيد أيّ جزء منه إلى إصدار أقدم. خطر «التراجع الصامت» الذي حذّر منه مالك المنتج **غير قائم على مستوى المحتوى**.
الفجوة الباقية **طوبولوجيّة بحتة**: لا يوجد مسار آباء يصل `4b8902ee` بالمرشّح.

## 4) حركة `origin/develop` (DEV-04) — إعادة قياس بعد `fetch`

`32e5fec..812ae903` = التزامان:

- `15886d9 docs(r22a): أغلِق الحوكمة بمراقبة إنتاج 15 دقيقة وتعطيل حساب RC المؤقّت وتثبيت نَسَب الإصدار`
- `812ae90 docs(r22a): سجِّل تنظيف أدوات الصيانة والملفّات المؤقّتة بعد إغلاق الحوكمة`

ستّة ملفّات **كلّها تحت `Ops/`** · **صفر ملفّ خارج `Ops/`** · صفر ملفّ تنفيذيّ · صفر هجرة.
تجربة الدمج: `git merge-tree --write-tree --name-only HEAD origin/develop` ⟹ `c1477627…` بلا قسم تعارض ⟹ **صفر تعارض**.

⟹ شرط DEV-04 مستوفًى، لكنّ التنفيذ موقوف لأنّ بوابة النَسَب أسبق ترتيبًا وفشلت.

## 5) إثبات أنّ RC والإنتاج لم يُمسّا

| البيئة | `/health` | بصمة `Reporting.Api.dll` | زمن الكتابة (UTC) | `NRestarts` | الحالة |
|---|---|---|---|---|---|
| الإنتاج (5090) | `200` | `3234ff855e599a50…` | `2026-08-30 15:35:57` | `0` | `active` |
| RC (5092) | `200` | `3234ff855e599a50…` **مطابق للإنتاج** | `2026-08-30 15:40:40` | `0` | `active` |
| TEST (5091) | `200` | `4d35418077b19a2f…` (مرشّح R5 المدموج) | `2026-08-30 16:52:23` | `0` | `active` |

أزمنة كتابة الإنتاج وRC **تسبق** كامل عمل هذه المرحلة، والبصمتان متطابقتان بينهما ⟹ لم تُلمَس أيّ منهما.
كلّ نداءات الخادم في هذه المرحلة كانت قراءة فقط (`curl /health` · `systemctl show` · `md5sum` · `stat`).

## 6) الخيار العلاجيّ المقيس (يحتاج تصريحًا صريحًا — لم يُنفَّذ)

سدّ الفجوة الطوبولوجيّة ممكن بعمليّة **واحدة** غير مدمِّرة، وليست أيًّا من الممنوعات:

```
git merge --no-ff 4b8902ee        (على فرع R5)
```

خصائصها **مقيسة مسبقًا** لا متوقَّعة:

1. ليست Cherry-pick ولا Rebase ولا Force Push — بل التزام دمج عاديّ بأبوَين.
2. شجرة النتيجة **مطابقة بايتًا ببايت** لشجرة `HEAD` الحاليّة (`ad619f0d…`) ⟹ **صفر تغيير في أيّ ملفّ**،
   أي أنّ كلّ ما قيس على TEST يبقى صالحًا حرفيًّا لأنّ الشجرة نفسها لم تتغيّر.
3. صفر تعارض.
4. بعدها يصبح `merge-base --is-ancestor 4b8902ee HEAD` ⟹ **YES**، فتُستوفى البوابة الإلزاميّة نصًّا لا تأويلًا،
   ويصل `4b8902ee` إلى `develop` ضمن نَسَب المرشّح.

**لم أنفّذها.** قرار DEV-01 يوجب التوقّف عند فشل النَسَب وتقديم الفرق الوظيفيّ، وتغيير طوبولوجيا الفرع
خارج ما صُرِّح به (تحديث الفرع بأحدث `origin/develop` فقط).

## 7) الخيارات المعروضة على مالك المنتج

| # | الخيار | الأثر على المحتوى | نتيجة البوابة |
|---|---|---|---|
| أ | تصريح بـ`git merge --no-ff 4b8902ee` ثمّ استئناف المراحل 2→6 | **صفر** (الشجرة مطابقة) | البوابة تمرّ نصًّا |
| ب | قبول الاحتواء الوظيفيّ المقيس (§3) بديلًا عن النَسَب الحرفيّ، مع توثيق الاستثناء | صفر | البوابة تُستثنى بقرار موثَّق |
| ج | الإبقاء على الحجب | — | `R5_DEVELOP_MERGE_BLOCKED` |

## 8) الحكم

```
R5_DEVELOP_MERGE_BLOCKED — DEPLOYED_BASELINE_NOT_IN_CANDIDATE_ANCESTRY
  · فشل النَسَب: على مستوى SHA فقط (لا مسار آباء من 4b8902ee إلى المرشّح ولا إلى origin/develop)
  · الاحتواء الوظيفيّ: مثبَت — شجرة دمج خطّ الأساس المنشور في المرشّح مطابقة تمامًا لشجرة المرشّح
  · ما قد يُفقَد: لا شيء — صفر مسار مفقود · صفر هجرة مفقودة · صفر حذف في ملفّات فرع الإصدار
  · RC والإنتاج: لم يُمسّا (بصمة مطابقة بينهما وزمن كتابة يسبق المرحلة · NRestarts=0)
```

المراحل 2 إلى 6 (تحديث الفرع · بوابات الشجرة النهائيّة · نشر TEST · UAT الحرج · الدمج إلى `develop`)
**موقوفة** بانتظار قرار مالك المنتج بين الخيارات أ/ب/ج.

---

## 9) ملحق التنفيذ — قرار مالك المنتج: `OPTION A APPROVED — RESTORE TOPOLOGICAL ANCESTRY`

اختار مالك المنتج الخيار (أ): استعادة النَسَب الطوبولوجيّ بدل تسجيل استثناء حوكميّ دائم.
نُفِّذ `git merge --no-ff 4b8902ee` على فرع R5 (بلا `-s ours` · بلا Cherry-pick · بلا Rebase · بلا Force):

```
PRE_HEAD  = ab2e0cfe997b48cc10109cf420a7d238d6ca25d5
PRE_TREE  = ad619f0dcd67fd6dc84b55a264bc2b667ad63afb
النتيجة   = Auto-merging reporting-frontend/src/types/api.ts
            Merge made by the 'ort' strategy.   ← صفر تعارض
POST_HEAD = 993317aa5c671c86650a6dd6ce2b4a588e8dbf7e
POST_TREE = ad619f0dcd67fd6dc84b55a264bc2b667ad63afb
الأبوان   = ab2e0cf 4b8902ee
```

**إثبات الشرطين الملزمَين:**

```
git diff PRE_TREE POST_TREE           ⟹  فارغ تمامًا   (TREE_EQUAL = YES)
git merge-base --is-ancestor 4b8902ee HEAD  ⟹  YES
```

⟹ صفر تغيير في أيّ ملفّ، فكلّ ما قيس سابقًا على هذه الشجرة يبقى صالحًا حرفيًّا،
و`4b8902ee` صار **سلفًا فعليًّا** للمرشّح فانطبقت البوابة الإلزاميّة نصًّا لا تأويلًا.

علاقات النَسَب بعد الدمج:

```
4b8902ee -> HEAD : YES   (خطّ الأساس المنشور على RC والإنتاج)
93c8cb6  -> HEAD : YES   (مرشّح R5 قبل التكامل)
ff1e337  -> HEAD : YES   (R22A على develop)
897c9b18 -> HEAD : YES   (السلف المشترك)
32e5fec  -> HEAD : NO    (يُستوفى بمزامنة origin/develop في المرحلة 2)
```
