# دليل نشر TEST — PROJECT360-PROJECT-SCOPED-REPORT-NAVIGATION-FIX-R1

> كلّ رقم هنا **مقيس وقت التنفيذ**. لا رقم منقول من تقرير سابق.

## 1) النَسَب

| المؤشّر | القيمة |
|---|---|
| قاعدة الإصلاح (قرار المالك) | `d937788` |
| `origin/develop` قبل الدفع | `8479d37` (دمج P123) |
| المرشّح بعد إعادة الأساس | **`63b7d42f2d0cc54899b22cc919045389c23b2ec7`** |
| أب المرشّح | `8479d37` = رأس `origin/develop` بالضبط |
| نوع الدفع | **تقديم سريع** `8479d37..63b7d42` — بلا `force`، بلا دمج، بلا وسم |
| `origin/develop` بعد الدفع | `63b7d42` |
| `origin/main` | **لم يُمسّ** |

**دلتا `d937788..origin/develop`** = 8 التزامات · 19 ملفًّا · **صفر تغيير في `src/`** (توثيق `Ops/` + ملفَّا اختبار: `ProjectRepeatableGridTests.cs`, `SecurityPenetrationTests.cs`) ⟹ نشر المرشّح لا يغيّر سلوك TEST إلّا بتغييرات هذه التذكرة.

### تباين موثَّق (ليس من عمل هذه التذكرة)
تقرير `Ops/UAT/P123-REMEDIATION-20260826/P123-TEST-DEPLOY-EVIDENCE-I-20260826.md` يسجّل أنّ TEST يحمل `4c452e8…` بـ`md5 = 9240e16a…`. القياس المباشر للثنائيّة قبل نشرنا أعطى `1.0.0+d937788…` بـ`md5 = 81eed683…` و`mtime = 2026-08-26 10:30:10 UTC` ⟹ **أُعيد نشر TEST على `d937788` بعد ذلك التقرير**. لم نُصحِّح ذلك التقرير ولم نبنِ عليه؛ خطّ الأساس المعتمد هنا هو المقيس.

## 2) الـRegression على المرشّح `63b7d42` (بعد إعادة الأساس)

| القياس | النتيجة | رمز الخروج |
|---|---|---|
| `dotnet build Reporting.sln` | 0 أخطاء · 4 تحذيرات (سابقة في مشروع الاختبار) | 0 |
| اختبارات وحدويّة | **556 / 556** | 0 |
| اختبارات تكامل (قاعدة نظيفة `p360rc_180332`) | **2183 / 2183** · 11د29ث | 0 |
| منها اختبارات الشريحة الجديدة | **9 / 9** | 0 |
| `tsc --noEmit -p tsconfig.app.json` | نظيف | 0 |
| `vitest run` | **748 / 748** في 64 ملفًّا | 0 |
| `vite build` | نجح | 0 |

## 3) خطّ الأساس قبل النشر (البيئات الثلاث)

| البيئة | `MainPID` | زمن التفعيل UTC | `Reporting.Api.dll` md5 | `index.html` md5 | Health | هجرات | مستخدمون |
|---|---|---|---|---|---|---|---|
| TEST | 1565350 | 08-26 11:04:44 | `81eed683…b1f2` | `f7842579…8f50` | 200 | 45 | 35 |
| RC | 1579500 | 08-26 13:34:46 | `7f77bfbf…7414` | `4df3e00d…8f08` | 200 | 47 | 49 |
| PROD | 1556574 | 08-26 06:22:54 | `ddf9598c…1975` | `e419f48d…fc9c` | 200 | 42 | 34 |

## 4) النسخ الاحتياطيّ الثلاثيّ (TS = `20260826-151801`)

| النسخة | المسار | إثبات |
|---|---|---|
| الخلفيّة | `/opt/reporting-test/publish-backup-p360navfix-20260826-151801` | `Reporting.Api.dll` md5 `81eed683…b1f2` = خطّ الأساس حرفيًّا |
| الواجهة | `/opt/reporting-test/frontend/dist-backup-p360navfix-20260826-151801` | `index.html` md5 `f7842579…8f50` = خطّ الأساس حرفيًّا |
| القاعدة | `/root/db-backups/reporting_test_uat-p360navfix-20260826-151801.dump` | `pg_dump -Fc` · 790,971 بايت |

## 5) البناء — من الشجرة المعزولة

- `rm -rf bin obj` لكلّ مشاريع `src/` ثمّ `dotnet publish -c Release -p:SourceRevisionId=63b7d42f… -p:ContinuousIntegrationBuild=true` ⟹ 48 ملفًّا.
- `appsettings.json` المحلّيّ والخادميّ **متطابقان** (`d51e726f…a4c9`) ⟹ لا إعداد خاصّ بالبيئة دُهِس.
- الواجهة: `VITE_API_BASE_URL=https://test.emarketingacademy.net/api` ⟹ الحزمة تحوي العنوان **مرّة واحدة** و**صفر** ظهور لـ`localhost:5090` (لا سقوط إلى الاحتياطيّ).

### مطابقة البصمة محلّيًّا ⟷ على TEST

| المكوّن | محلّيًّا | على TEST بعد النشر | مطابقة |
|---|---|---|---|
| `Reporting.Api.dll` | `06d09ba2e39f9468c509be8f97927df2` | `06d09ba2e39f9468c509be8f97927df2` | ✅ |
| `index.html` | `2fdc9c5b6078444937799ca1de3670f5` | `2fdc9c5b6078444937799ca1de3670f5` | ✅ |
| حزمة الواجهة | `index-Dyz6sh2e.js` | `index-Dyz6sh2e.js` | ✅ |

**النَسَب المضمَّن داخل الثنائيّة المنشورة** (قراءة بايتات الملفّ على الخادم وفكّ UTF-16LE):

```
1.0.0+63b7d42f2d0cc54899b22cc919045389c23b2ec7
```

## 6) ما بعد النشر

**TEST:** خدمة `active` · `MainPID` جديد **1589037** · تفعيل `2026-08-26 15:18:56 UTC` · `Now listening on: http://127.0.0.1:5091` · `/health` = **200**.
سجلّ الإقلاع: `No migrations were applied. The database is already up to date.` — والعدّ بقي **45 = 45** (المرشّح يحمل 45 ملفّ هجرة، وكلّها مطبَّقة سلفًا) ⟹ **لا هجرة نُفِّذت ولا سكيما تغيّرت**.

**RC والإنتاج — قياس بعد النشر:**

| المؤشّر | RC قبل | RC بعد | PROD قبل | PROD بعد |
|---|---|---|---|---|
| `MainPID` | 1579500 | **1579500** | 1556574 | **1556574** |
| زمن التفعيل | 08-26 13:34:46 | نفسه | 08-26 06:22:54 | نفسه |
| `Reporting.Api.dll` md5 | `7f77bfbf…7414` | نفسه | `ddf9598c…1975` | نفسه |
| `index.html` md5 | `4df3e00d…8f08` | نفسه | `e419f48d…fc9c` | نفسه |
| Health | 200 | 200 | 200 | 200 |

ثبات `MainPID` وزمن التفعيل ⟹ العمليّتان **لم تُعادا تشغيلًا**؛ وثبات الـmd5 ⟹ **لم تُكتب فيهما بايتة**.

## 7) الأسرار

مسح الشيفرة المتغيّرة (`password|secret|api[_-]?key|bearer|BEGIN (RSA|OPENSSH|PRIVATE)|Pwd=`) = **صفر مطابقة**؛ الضجيج الوحيد `CancellationToken`. لم تُطبَع سلسلة اتّصال ولا كلمة مرور ولا رمز وصول في أيّ خطوة؛ قراءة إعدادات البيئات اقتصرت على `md5` و`grep` انتقائيّ بلا `source` وبلا طباعة ملفّ.
