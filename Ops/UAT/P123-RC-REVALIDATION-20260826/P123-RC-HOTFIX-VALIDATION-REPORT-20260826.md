# إعادة التحقّق الكامل على RC بعد إصلاح `DEF-P123-RC-001`

| الحقل | القيمة |
|---|---|
| `HOTFIX_SHA` | `59f483ebd86211a793bd96a5b2a602fda123d36f` |
| `DEVELOP_MERGE_SHA` = `RC_CANDIDATE_SHA` | `897c9b187ab4216213b4f453ec65948cd06dff27` |
| المنشور فعلًا على RC | `1.0.0+897c9b187ab4216213b4f453ec65948cd06dff27` (`Reporting.Api.dll` md5 `46e0a3a169c9ef2285aef3b8e9d7fe13`) |
| حزمة الواجهة على RC | `index-gPMunv-Q.js` md5 `62c3bb1998b2004ccb2ada157943887f` |
| البيئة | RC — `rc-report.emarketingacademy.net` / `127.0.0.1:5092` / `reporting_rc` (47 هجرة) |
| التاريخ | 26 أغسطس 2026 |
| الإنتاج | **لم يُمسّ في أيّ خطوة من هذه المرحلة** |

---

## 0) تصحيح مقدّمات التكليف — مقيس لا مفترَض

التكليف بُني على مقدّمات صارت بائتة قبل بدء التنفيذ. القياس:

| المقدّمة في التكليف | المقيس فعلًا | الأثر |
|---|---|---|
| `origin/develop` = `8479d374…` | `897c9b18…` (متقدّم بثلاث دفعات) | المرشّح الحاكم تغيّر |
| العيب قابل لإعادة الإنتاج على RC | **لا** — الإصلاح منشور ويعمل | المراحل 2–6 كانت ستُنتج إصلاحًا مكرّرًا متشعّبًا فوق إصلاح صحيح |
| لا هوتفكس آخر على المسار نفسه | **يوجد** `59f483e` مدموج ومدفوع | ⇒ لم تُعَد كتابته |
| المصدر الحاكم `Ops/UAT/P123-REMEDIATION-20260826/` | غير موجود؛ الفعليّ `Ops/UAT/P123-RC001-HOTFIX-20260826/` | صُحّح المرجع |

`8479d374` سلف مباشر لـ`897c9b18` (`git merge-base --is-ancestor` ⇒ نعم)، والـ`SHA`ان محلّيًّا = بُعديًّا على GitHub (`git ls-remote`). لذلك **لم تُعَد كتابة الإصلاح**، وانتقل العمل إلى ما لم يكن مُنجَزًا: إعادة تحقّق كاملة على RC للمرشّح الجديد.

---

## 1) بوابات المرشّح — تحقّق مستقلّ على شجرة عمل معزولة نظيفة

شجرة العمل: `.claude/worktrees/p123-rc001-merge-20260826` على `897c9b18` (`git status --porcelain` = 0 سطر).

| البوّابة | الأمر | المقيس | الحكم |
|---|---|---|---|
| بناء الخلفيّة Release | `dotnet build Reporting.sln -c Release` | `0 Warning(s)` · `0 Error(s)` | PASS |
| اختبارات الوحدة | `Reporting.UnitTests` | `Failed: 0, Passed: 556, Total: 556` | PASS |
| اختبارات حجب الحضور | `AttendanceListVisibilityTests` | `Failed: 0, Passed: 14, Total: 14` | PASS |
| التكامل الكامل — جولة 2 | خمس قواعد معزولة مهاجَرة | `Failed: 0, Passed: 2197, Total: 2197` — 11د 31ث | PASS |
| التكامل الكامل — جولة 3 | نفس القواعد المأهولة بمخرجات الجولة 2 | `Failed: 0, Passed: 2197, Total: 2197` — 11د 09ث · **مطابقة تامّة ⇒ صفر تلوّث بين الجولات** | PASS |
| TypeScript | `npx tsc -b --force` | صفر مخرجات، رمز خروج 0 | PASS |
| Vitest | `npx vitest run` | `64 passed (64)` ملفًّا · `748 passed (748)` اختبارًا | PASS |
| Playwright | `npx playwright test` | `47 passed` | PASS |
| مسح الأسرار | `grep -inE` على الفرق | 0 مطابقة | PASS |
| ملفّات خارج النطاق | `git status --porcelain` | 0 | PASS |

### جولة التكامل الأولى — لماذا أُبطِلت ولم تُخفَ

الجولة الأولى على القواعد الخمس **الفارغة حديثًا** أعطت `Failed: 9, Passed: 2188, Total: 2197`. الأخطاء التسعة كلّها وقعت في أوّل أربع ثوانٍ وكلّها من عائلة واحدة:

```
42P07: relation "leave_request_events" already exists
42P07: relation "email_outbox" already exists
42701: column "Classification" of relation "report_templates" already exists
23505: duplicate key value violates unique constraint "pg_type_typname_nsp_index"
```

هذا **سباق هجرة**: مجموعات اختبار متوازية استدعت `MigrateAsync()` معًا على قاعدة غير مهاجَرة بعد، فتصادمت على إنشاء الكيانات نفسها. ليس عيبًا في المنتج ولا في المرشّح — ولذلك لم تُحتسَب الجولة الأولى قياسًا، وأُعيد القياس على جولتين تاليتين متطابقتين. الدليل الخام محفوظ كما هو في `/tmp/mc897-integration-round1.log` بلا تجميل.

**تصحيح مطلوب على القاعدة النظيفة:** بلا تنفيذ `UPDATE report_templates SET "Classification"='Supplementary'` لخمسة قوالب تفشل خمسة اختبارات Rollup. طُبِّق على القواعد الخمس (`UPDATE 5` لكلٍّ منها) قبل الجولتين 2 و3.

---

## 2) إعادة التحقّق الحيّ على RC — 99 سيناريو

المصفوفة الكاملة: `P123-RC-REVALIDATION-SCENARIO-MATRIX-20260826.csv` (لكلّ صفّ: المعرّف، الوصف، المتوقَّع، المقيس، الحالة، الدليل، `CandidateSHA`، البيئة، الطابع الزمنيّ).

| الحزمة | العدد | النتيجة | الدليل |
|---|---|---|---|
| (أ) المِشدّ الموجَّه للعيب `DEF-P123-RC-001` | 10 | **10 PASS** | `evidence/rc001-targeted-probe.py` · `rc001-targeted-results.json` |
| (ب) الحزمة العامّة (مصادقة، منع افتراضيّ، دليل، صلاحيات، حضور، 360، قوائم، KPI، أرصدة، إجازات، طلبات، لوحات، حوكمة، تدقيق) | 69 | **67 PASS · 2 SUPERSEDED** | `evidence/phase910-run2.log` |
| (ب-مكرّر) البديلان عن الـSUPERSEDED + رفض الموظّف للتدقيق | 3 | **3 PASS** | `evidence/phase9-extra-run2.txt` |
| (ج) تغطية `63b7d42` — عزل شريحة تقارير المشروع | 8 | **8 PASS** | `evidence/p360-slice-probe.py` · `p360-slice-results.json` |
| (د) الواجهة والمصالحة | 9 | **9 PASS** | `screenshots/ui-log.json` · `evidence/rc-snapshot-*.txt` |
| **الإجماليّ** | **99** | **97 PASS · 2 SUPERSEDED · 0 FAIL** | — |

### (أ) العيب نفسه — أُعيد اختباره من الطرف إلى الطرف

| المعرّف | ما يُثبِته | المقيس |
|---|---|---|
| RC001-S1 | المسودّة لا تدخل `items` للموضوع | `status=Draft · seen=False` |
| RC001-S2 | العدّاد نفسه لا يُفشي الوجود | `before=1 after=1` |
| RC001-S3 | التفاصيل تردّ 404 للموضوع | `HTTP 404` |
| RC001-S4 | المُبلِّغ لا يفقد مسودّته | `seen=True` |
| RC001-S5 | غير المرتبط لا يكتشفها | `detail=404 inList=False` |
| RC001-S6 | الترقيم بحجم 100 لا يُسرِّبها | `seen=False totalCount=1` |
| RC001-S7 | الملغاة السابقة للإرسال تبقى محجوبة | `cancel=200 status=Cancelled inList=False detail=404` |
| RC001-S8 | بعد الإرسال يراها الموضوع | `status=AwaitingEmployee inList=True detail=200` |
| RC001-S9 | المراجع بمفتاح `Attendance.Review` الصريح يراها | `inList=True detail=200` |
| RC001-S10 | الثابت «القائمة ⊆ التفاصيل» على كلّ الصفات | `drift=0` |

#### خطآن في المِشدّ كانا سيمنحان نجاحًا زائفًا — كُشفا وصُحّحا

1. **حقل الإنشاء الخاطئ.** أرسل المِشدّ `typeId` بينما اسم الحقل في العقد `incidentTypeId`، فارتبط بـ`Guid.Empty` وردّ الخادم `404 نوع الحادثة غير معروف`. النتيجة: `CREATED_INCIDENTS=[]` مع `PASS=7` — سبع فحوص حجب «نجحت» لأنّ شيئًا لم يُنشَأ أصلًا. أُضيف `raise SystemExit` عند فشل الإنشاء حتّى يستحيل تكرار ذلك.
2. **عقد الإلغاء الخاطئ.** نادى المِشدّ `POST /api/attendance/{id}/cancel` وهو مسار غير موجود (`404`)، فكان RC001-S7 يفحص **مسودّة** لا **ملغاة**. العقد الصحيح `DELETE /api/attendance/{id}?concurrencyStamp=N`. بعد التصحيح صار المقيس `cancel=200 status=Cancelled` — أي أنّ الحالة الملغاة السابقة للإرسال اختُبِرت فعلًا.

10/10 الأولى كانت زائفة جزئيًّا؛ **10/10 المعتمَدة هنا هي بعد التصحيحَين**.

### (ب) السيناريوهان المستبدَلان — عيبان في المِشدّ لا في المنتج

| المعرّف | سبب الفشل | البديل | نتيجة البديل |
|---|---|---|---|
| `S-KPI-AGG` | نداء بلا معامل `granularity` ⇒ `400` تحقّق مدخلات | `S-KPI-AGG2` | `200` مع تجميع صحيح |
| `S-AUDIT-PERM` | مسار سجلّ تدقيق خاطئ ⇒ `404` | `S-AUDIT-PERM2` | `200`؛ والعدّ المباشر على القاعدة: `user.permissions.changed = 13` قيدًا مع الفاعل والكيان |

### (ج) تغطية `63b7d42` — بقرار صريح من مالك المنتج بإبقائه داخل المرشّح

المسار الجديد `GET /api/projects/{id}/reports/{submissionId}`. اختُبِر على **بيانات RC الحقيقيّة قراءةً فقط**: تسليم واحد (`2a7a831c…`) يحمل أربعة مشاريع.

| المعرّف | المقيس |
|---|---|
| P360-S1 | المسار مفعَّل ويطلب مصادقة — `HTTP 401` لا 404 توجيه |
| P360-S2 | صفة مخوَّلة تقرأ الشريحة — `200 · projectId مطابق · fields=1` |
| P360-S3 | **صفر تسريب**: المشاريع الشقيقة الثلاثة في التسليم نفسه غائبة تمامًا عن الحمولة |
| P360-S4 | عدد العناصر = عناصر هذا المشروع فقط (`entries=1`) ⇒ الحمولة الكاملة للتسليم لا تُرسَل |
| P360-S5 | مشروع حقيقيّ لا يخصّ التسليم ⇒ `404` |
| P360-S6 | أربعة فاعلين خارج نطاق المشروع ⇒ `404` للجميع |
| P360-S7 | المعرّف الوهميّ والمشروع المحجوب: **نفس الرمز ونفس الرسالة** ⇒ لا قناة اكتشاف |
| P360-S8 | تسليم غير موجود ⇒ `404` لا 500 |

وحزمة RC المنشورة نفسها تحمل المسار: `grep '/app/projects/:projectId/reports/:reportId'` في `index-gPMunv-Q.js` ⇒ 1.

### (د) الواجهة على RC — 390 / 768 / 1440

الحزمة المنشورة تحمل `VITE_API_BASE_URL=https://rc-report.emarketingacademy.net/api` مخبوزًا، ونجينكس أمامها يفرض `auth_basic`. فبدل تجاوز الحارس أو تسريب كلمة مروره، اعتُرِض كلّ طلب إلى ذلك الأصل **داخل المتصفّح** ولُبّي محلّيًّا: `/api` و`/hubs` إلى خلفيّة RC الحيّة عبر نفق SSH، وما عداه من نسخة `dist` المطابقة حرفيًّا. النتيجة: نفس الأصل (لا CORS) ونفس الحزمة ونفس قاعدة البيانات، بلا لمس أيّ إعداد على الخادم.

| القياس | النتيجة |
|---|---|
| اللقطات | 39 (4 صفات + مجهول × 3 مسارات × 3 عروض) |
| الدخول | `admin, hr, manager, employee` — الأربع وصلت `/app` |
| أخطاء الـConsole | **0** (لا حقيقيّة ولا ضجيج وسيط) |
| الفيض الأفقي | **0 انتهاك** |
| اتّجاه RTL | `dir="rtl"` في 39/39 |
| تدرّج الشريط الجانبيّ | admin=8 · manager=7 · hr=7 · employee=6 |

**ملاحظة صادقة:** `OBS-P123-RC-004` (أخطاء SignalR في الـConsole) لم تظهر إطلاقًا في هذه الجولة — صفر خطأ. كانت أثرًا لوسيط الجولة السابقة لا للمنتج، وهذا يؤكّد إغلاقها السابق كـ«غير عيب».

---

## 3) مصالحة بيانات RC — إثبات أنّ الجولة لم تمسّ صفًّا حقيقيًّا

أربع لقطات متتابعة بالسكربت نفسه (`evidence/rc-snapshot.sh`، قراءة فقط):

| اللقطة | مستخدمون حقيقيّون | اصطناعيّون | وقائع حضور | `perm` على حقيقيّين |
|---|---|---|---|---|
| BEFORE (قبل الجولة) | 49 | 5 | 5 | 0 |
| BASELINE (بعد تنظيف ما قبل الجولة) | 49 | 0 | 0 | 0 |
| AFTER-TESTS (بعد كلّ السيناريوهات) | 49 | 5 | 4 | **0** |
| AFTER-CLEANUP (النهائيّة) | 49 | 0 | 0 | 0 |

**البصمات الخمس للبيانات الحقيقيّة متطابقة حرفيًّا في اللقطات الأربع:**

```
md5_users_nonsynth        d2e39bb254f05ddf8168d10f20cdb59d
md5_departments_nonsynth  a2e0fd896804c41d4445c40acd6f925c
md5_teams_nonsynth        963bb33b84a6ace6dadc45d74bfe7aee
md5_submissions           d7206b3ed1eb86341dd42a769f6d7c79
md5_perm_claims_nonsynth  EMPTY
```

- كلّ ما أُنشئ كان تحت نطاق `@p123.rc.test` أو بادئة `RC-P123-`، وكلّه أُزيل.
- منح الصلاحيات جرى على مستخدم اصطناعيّ واحد ثمّ سُحِب في الجولة نفسها (`S-PERM-REVOKE-READBACK` ⇒ `perms=[]`)، و`userclaims_perm_nonsynth = 0` في كلّ لقطة ⇒ **لم يُمنح أيّ مستخدم حقيقيّ أيّ مفتاح**.
- 39 تسليمًا و41 قالبًا و47 هجرة بلا تغيّر.

---

## 4) حالة العيوب

| المعرّف | الشدّة | الحالة قبل | الحالة بعد | الدليل |
|---|---|---|---|---|
| `DEF-P123-RC-001` | P2 — إفشاء معلومات / خرق نطاق | مفتوح، يمنع الترقية | **مغلق — مُتحقَّق منه على RC الحيّ** | RC001-S1…S10 + 14 اختبار تكامل |
| `OBS-P123-RC-002` | ملاحظة | مغلقة (ليست عيبًا) | تبقى مغلقة — `S-GOV-DENY` يثبت الحجب خادميًّا | `phase910-run2.log` |
| `OBS-P123-RC-003` | ملاحظة | مغلقة (ليست عيبًا) | تبقى مغلقة — `S-PERM-DENY-*` منع افتراضيّ للأربع صفات | `phase910-run2.log` |
| `OBS-P123-RC-004` | ملاحظة | مغلقة (أثر وسيط) | تبقى مغلقة — **0 خطأ Console** في هذه الجولة | `screenshots/ui-log.json` |

**عيوب P0/P1/P2 مفتوحة: صفر. سيناريوهات أمنيّة محجوبة (BLOCKED): صفر. FAIL: صفر.**

---

## 5) حكم بوّابة RC

- `0 FAIL` ✔
- `0 BLOCKED security scenarios` ✔
- `0 open P0/P1/P2 defects` ✔

```
P123_RC_HOTFIX_VALIDATION_PASS_PRODUCTION_GATE_OPEN
```

بوّابة الإنتاج **مفتوحة داخليًّا**. لا يعني ذلك أنّ نشرًا وقع: لم يُكتَب على الإنتاج شيء حتّى لحظة كتابة هذا التقرير.
