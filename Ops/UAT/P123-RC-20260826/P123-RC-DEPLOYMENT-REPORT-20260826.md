# تقرير نشر مرشّح RC (المرحلتان 6 و8)

- **`DEVELOP_MERGE_SHA` = `RC_CANDIDATE_SHA` = `8479d374238b71731996ad73d20d1485701d2053`**
- المرشّح مشتقّ **حرفيًّا** من قيد الدمج على `origin/develop` — لا فرع وسيط ولا إعادة بناء من شجرة أخرى.
- **سلسلة النَسَب:** `9595c01` (الأساس السابق) → … 15 قيدًا … → `d937788` (شيفرة المنتج المثبَّتة) → … → `328d59e` (رأس فرع العلاج) ⟶ دُمِج مع `736b5c5` (develop السابق) ⟶ **`8479d374`**.
- **لا وسم إنتاج ولا وسم إصدار أُنشئ إطلاقًا.**

## 1) بوّابة هويّة RC قبل أيّ كتابة (المرحلة 6)

| المحور | المتوقَّع لـRC | **المقيس على الخادم** | مختلف عن الإنتاج؟ |
|---|---|---|---|
| الخدمة | `khubara-reporting-rc` | `khubara-reporting-rc` | نعم (الإنتاج `reporting-api`) |
| المنفذ | 5092 | 5092 | نعم (الإنتاج 5090) |
| القاعدة | `reporting_rc` | `reporting_rc` | نعم (الإنتاج `reporting_prod`) |
| البيئة | `ReleaseCandidate` | `ASPNETCORE_ENVIRONMENT=ReleaseCandidate` | نعم |
| الجذر | `/opt/reporting-rc` | `/opt/reporting-rc` | نعم (الإنتاج `/opt/reporting`) |
| البريد | `DryRun` | `EmailNotifications__Mode=DryRun` | نعم (الإنتاج `Enabled`) |
| التذكيرات | `false` | `Reminders__Enabled=false` | نعم |

**التوقّف الصارم:** لم يظهر في أيّ أمر تنفيذ هدفُ كتابةٍ إلى `reporting_prod` أو المنفذ 5090 أو `/opt/reporting/publish`.

### نسخ احتياطيّة كاملة قابلة للاستعادة قبل النشر

- قاعدة RC: `pg_dump` كامل (`evidence/rc-repr.sql.gz` استُعمل أيضًا لبروفة الهجرة — 297,662 بايت).
- حزمة `publish` وحزمة `dist` السابقتان: مؤرشفتان على الخادم قبل الاستبدال.
- ملفّ البيئة: `/etc/khubara-reporting-rc.env.bak-p123rc-20260826` (md5 `099fbcf4289f649d96139084f0653e27`).

### بصمة RC قبل النشر

`evidence/snapshot-rc-BEFORE.txt` — 49 مستخدمًا، 6 إدارات، 11 فريقًا، 39 تسليمًا، 41 قالبًا، 42 هجرة، وخمس بصمات md5.

## 2) النشر (المرحلة 8)

| الإثبات | المتوقَّع | **المقيس** | الحالة |
|---|---|---|---|
| `/health` | 200 | **200** | PASS |
| الإصدار المضمَّن في الثنائيّة | `1.0.0+<CANDIDATE_SHA>` | **`1.0.0+8479d374238b71731996ad73d20d1485701d2053`** | PASS |
| بصمة الخلفيّة المنشورة | مطابقة للمبنيّة | `md5 7f77bfbf434ed733f3295c12f4bd7414` · `Reporting.Api.dll` · mtime `2026-08-26 13:13:49 UTC` | PASS |
| بصمة حزمة الواجهة | مطابقة للمبنيّة محلّيًّا | `index-BjkQX4K1.js` · `md5 6731d83545be4bdc264512c1789172a5` · `index.html md5 4df3e00d7bf8cd04db45c99efdbf8f08` | PASS |
| عدد الهجرات المتوقَّع | 42 → 47 | **47** (5 هجرات مطبَّقة، صفر معلَّقة) | PASS |
| حلقة إعادة تشغيل | لا | `NRestarts=0` · `SubState=running` · `MainPID=1579500` · `ActiveEnterTimestamp=Wed 2026-08-26 13:34:46 UTC` | PASS |
| أخطاء إقلاع غير مفسَّرة | صفر | **صفر** | PASS |

### تعديل بيئيّ لازم على RC (موثَّق)

كانت أعلام `Phase2` غائبة عن ملفّ بيئة RC، وقيمتها الافتراضيّة في الشيفرة `false` ⟹ كانت مسارات P1/P2/P3 تردّ 404 ويستحيل التحقّق من المرشّح. أُضيفت بعد نسخ احتياطيّ:

```
Phase2__Employee360Enabled=true
Phase2__AttendanceEnabled=true
Phase2__HrOperationsEnabled=true
Phase2__EmployeeChecklistEnabled=true
```

- النسخة الاحتياطيّة: `/etc/khubara-reporting-rc.env.bak-p123rc-20260826`.
- **الوضع الحاليّ: الأعلام الأربعة ما تزال مفعَّلة على RC عمدًا**، لأنّ تعطيلها يُبطِل قدرة RC على تمثيل المرشّح. للتراجع: استعادة ملفّ النسخة الاحتياطيّة ثمّ `systemctl restart khubara-reporting-rc`.
- TEST كانت أصلًا تحمل الأعلام الأربعة = true. **الإنتاج لا يحملها ولم تُضَف إليه.**

## 3) إعادة إثبات أنّ الإنتاج لم يُمسّ (بعد النشر)

| المحور | قبل | بعد | الحكم |
|---|---|---|---|
| md5 `/opt/reporting/publish/Reporting.Api.dll` | `ddf9598c0bf00f821a0aefe0c6cc1975` | `ddf9598c0bf00f821a0aefe0c6cc1975` | مطابق |
| mtime الثنائيّة | `2026-08-18 15:02:08 +0000` | `2026-08-18 15:02:08 +0000` | مطابق |
| `MainPID` | `1556574` | `1556574` | مطابق — لا إعادة تشغيل |
| `ActiveEnterTimestamp` | `Wed 2026-08-26 06:22:54 UTC` | `Wed 2026-08-26 06:22:54 UTC` | مطابق |
| `NRestarts` | — | `0` | لا حلقة |
| هجرات `reporting_prod` | 42 | **42** | مطابق — **لم تُطبَّق أيّ هجرة على الإنتاج** |
| مستخدمو الإنتاج | 34 | **34** | مطابق |
| حزمة واجهة الإنتاج | `index-CMjXSPXr.js` | `index-CMjXSPXr.js` | مطابق |
| مستخدمون بنطاق `p123`/`rc.test` في الإنتاج | — | **0** | لا تلوّث |
| إدارات `RC-P123-%` في الإنتاج | — | **0** | لا تلوّث |
| مطالبات `perm` في الإنتاج | — | **0** | لا منح |

**فارق وحيد مُفسَّر:** عدد `report_submissions` في الإنتاج تحرّك (307 ← 309 ← 308) خلال أربعين دقيقة. الإنتاج نظام حيّ يستعمله الموظّفون الآن، وكلّ الصفوف أُنشئت بأختام `CreatedAtUtc` تخصّ حركة مستخدمين حقيقيّين (آخرها `2026-08-26 13:27:31+00`). **لم يُصادَق على الإنتاج ولا مرّة واحدة في هذه الجولة، ولم تُكتب فيه أيّ صفّ.**

الدليل: `evidence/prod-BEFORE.txt` · `evidence/prod-AFTER.txt`
