# مذكّرة إغلاق — نشر مرشّح P123 على RC والتحقّق منه

**الحكم النهائيّ:**

```
P123_RC_DEPLOYMENT_AND_VALIDATION_FAILED_NOT_READY_FOR_PRODUCTION
```

## لماذا لم يُمنح حكم النجاح

كلّ بوّابات النجاح تحقّقت **ما عدا اثنتين**، وكلتاهما تسقط بالعيب نفسه:

- «صفر عيب مفتوح من فئة P0/P1/P2» ⟵ **`DEF-P123-RC-001` مفتوح بشدّة P2**.
- «صفر تسريب أو خرق نطاق» ⟵ العيب نفسه **تسريب معلومات وخرق نطاق مقيس على RC الحيّة**.

بقيّة البوّابات (الدفع والدمج بلا فقد ولا Force · تطابق `origin/develop` مع قيد الدمج · خضرة كلّ الحزم على قيد الدمج · نجاح الهجرة بلا حذف بيانات حقيقيّة · تطابق RC المنشورة مع `RC_CANDIDATE_SHA` حرفيًّا · نجاح كلّ الرحلات الجوهريّة · قياس السماح/المنع عبر مستخدمين اصطناعيّين · تطابق بيانات RC غير الاصطناعيّة قبل/بعد · عدم مسّ الإنتاج) **تحقّقت كلّها بأدلّة مقيسة**.

## العيب المفتوح

**`DEF-P123-RC-001` — P2 — إفشاء معلومات / خرق نطاق**

`GET /api/attendance` تُدرِج واقعة حضور في حالة `Draft` لموضوعها **قبل الإرسال**، بينما `GET /api/attendance/{id}` يردّ عليها `404` بصورة صحيحة للمستخدم نفسه. أي إنّ سطح القائمة يناقض سطح التفاصيل ويناقض العقد المرجعيّ.

- **السبب الجذريّ:** `AttendanceService.BuildScopedQueryAsync` (الأسطر 764-781) تبني نطاق القائمة بشرط الملكيّة/النطاق فقط، وتُسقِط استثناء ما قبل الإرسال الذي يفرضه `AttendanceAccess.CanViewIncident:70` (`if (ctx.IsSelf) return !IsPreSubmission(status);`) والذي يطبّقه `Employee360Service` صراحةً عند 496-497 و687-688.
- **فجوة تغطية موثَّقة:** لا اختبار تكامل يمسّ هذا المسار ⇒ 2174/2174 خضراء في جولتين رغم وجود العيب.
- **إثبات معزول:** `evidence/repro-draft-leak.json` (أُنشئت المسودّة بمستخدم اصطناعيّ وحُذفت فورًا بعد الالتقاط).

## ما نُظِّف وما بقي

**نُظِّف:**
- كلّ المستخدمين والإدارات والفرق والوقائع الاصطناعيّة على RC (نطاق `@p123.rc.test` وبادئة `RC-P123-`) — في جولتَي تنظيف، والثانية بعد التقاط اللقطات.
- كلّ مطالبات `perm` الاصطناعيّة (`userclaims_perm_total = 0`).
- خادم المعاينة المحلّيّ (المنفذ 4174) أُوقِف والمنفذ حرّ.
- الرموز المؤقّتة (JWT الاصطناعيّة) — عمرها انتهى ولم تُخزَّن.
- المجلّدات المؤقّتة: `/tmp/p123-rc` و`/tmp/rc-ui-verify` محلّيًّا · `/tmp/p123-rc` و`/tmp/p123-shot` و`/tmp/stage-p123rc` و`/tmp/dist.tgz` و`/tmp/publish.tgz` على الخادم.
- الرابط الرمزيّ `node_modules` داخل شجرة الدمج المعزولة (الهدف الأصليّ سليم).
- المدخل المؤقّت `p123-rc-verify` من `.claude/launch.json`.
- السكربتان المؤقّتان `p123-shots.mjs` و`p123-probe404.mjs` من `reporting-frontend/`.

**بقي عمدًا:**
- **أعلام `Phase2` الأربعة مفعَّلة على RC** — بلا تفعيلها تردّ مسارات P1/P2/P3 بـ404 ويستحيل أن تمثّل RC المرشّح. النسخة الاحتياطيّة للتراجع: `/etc/khubara-reporting-rc.env.bak-p123rc-20260826`.
- **حزمة المرشّح `8479d374` منشورة على RC** — لم يُتراجَع عنها لأنّ RC هي بيئة التحقّق المقصودة.
- **قيود التدقيق على RC** (`user.permissions.changed`، `user.org.changed`) — لم تُحذف لأنّها أدلّة لازمة، تمامًا كما ينصّ التكليف.
- شجرة العمل المعزولة `.claude/worktrees/p123-merge-20260826` — الدمج مدفوع سلفًا، فحذفها اختياريّ ويحتاج قرار المستخدم.
- بقايا جولة سابقة لا تخصّ هذه الجولة: `/tmp/p123-rem`، `/tmp/p123-remed`، `/tmp/p123-perm-catalog.md`، وخادم معاينة قديم على المنفذ 4399 — تُرِكت كما هي.

## حالة الفروع والوسوم

| المرجع | القيمة | التغيّر |
|---|---|---|
| `refs/heads/feature/p123-remediation-20260826` | `328d59e2287ca2b8c9a8d818d582f0d1dd95dd19` | **جديد — دُفِع بلا Force** |
| `refs/heads/develop` | `8479d374238b71731996ad73d20d1485701d2053` | **تقدّم بقيد دمج ذي أبوَين، دفع عاديّ** |
| `refs/heads/main` | `508509ad8474b321c80cbdd48eb84ecb54bee212` | لم يتغيّر |
| `refs/heads/develop-canonical` | `a86ad3b88ba176ffeaf56d624596909cabef28c3` | لم يتغيّر |
| الوسوم | لا وسم جديد | **صفر وسم أُنشئ — لا وسم إصدار ولا وسم إنتاج** |
| فرع إصدار | لا يوجد | **لم يُنشَأ** |

## الإنتاج

**لم يُنشَر على الإنتاج، ولم يُصرَّح بذلك، ولم يُصادَق عليه ولا مرّة.** كلّ بصماته مطابقة قبل/بعد: الثنائيّة نفسها (`ddf9598c…`)، والـPID نفسه (`1556574`)، وطابع الإقلاع نفسه، و`NRestarts=0`، و42 هجرة كما هي، و34 مستخدمًا كما هم، وصفر مستخدم اصطناعيّ وصفر مطالبة `perm`.

## الخطوة التالية المقترحة (تحتاج تصريحًا صريحًا جديدًا)

1. علاج `DEF-P123-RC-001` بإضافة استثناء ما قبل الإرسال في `BuildScopedQueryAsync` بما يطابق `AttendanceAccess.CanViewIncident`.
2. إضافة اختبار تكامل يغطّي `GET /api/attendance` بصفة الموضوع على واقعة `Draft`/`Reported` — لسدّ فجوة التغطية التي أخفت العيب.
3. إعادة بناء مرشّح ونشره على RC وإعادة السيناريو `S-ATT-DRAFT-HIDDEN` وحده مع جولة انحدار موجَّهة.
4. عندئذٍ فقط يُعاد النظر في بوّابة الإنتاج.
