# R22B — مصفوفة الجرد النهائيّة وبوابة ما قبل الكتابة

**الغرض:** إثبات `ALL_ACTIVE_ASSIGNED_TEMPLATES_DISCOVERED = YES` و`UNCLASSIFIED_TEMPLATES = 0` قبل أيّ كتابة، استجابةً للبند §4 من تفويض مالك المنتج.
**المصدر:** استعلام قراءة فقط `Ops/R22B/tools/inventory-final.sql` على القواعد الثلاث (31 أغسطس 2026).

## 1) الأعداد المقيسة

| القاعدة | قوالب كلّيّة | نشطة | فيها `ProjectRepeatableSection` (الإصدار المنشور الفعّال) | نشطة بلا قسم مشاريع في أيّ إصدار |
|---|---|---|---|---|
| `reporting_prod` | 41 | 28 | 11 (منها 1 معطَّل) | 18 |
| `reporting_rc` | 41 | 29 | 11 (منها 1 معطَّل) | 19 |
| `reporting_test_uat` | 35 | 29 | 9 (كلّها نشطة) | 20 |

القوالب التي **لا تملك قسم مشاريع في أيّ إصدار** خارج نطاق R22B قطعيًّا: لا حقل قابل للتحويل أصلًا ⟹ `EXCLUDE_NO_PROJECT_SECTION`.

## 2) الاتّحاد الكامل للقوالب ذات قسم المشاريع عبر البيئات الثلاث = **12**

`prod(11) ∪ rc(11) ∪ test(9) = 12` عنوانًا متمايزًا. لا يوجد عنوان في أيّ بيئة خارج هذه القائمة.

| # | القالب | prod | rc | test | القرار | السبب المقيس |
|---|---|---|---|---|---|---|
| 1 | تقرير فريق الفيديو | v7 sv1 | v7 sv1 | v4 sv1 | **`INCLUDE`** | 6 حقول تصف مخرَجًا واحدًا؛ بصمة `ConfigJson` **`13f1904d…` متطابقة في البيئات الثلاث**. |
| 2 | تقرير فريق التصميم | v7 sv1 | v7 sv1 | v4 sv1 | **`INCLUDE`** | 5 حقول تصف تصميمًا واحدًا؛ بصمة `cab376e7…` متطابقة في الثلاث. |
| 3 | تقرير المديرشن الأسبوعي | v8 sv1 | v8 sv1 | v4 sv1 | **`INCLUDE`** | 5 حقول تصف نشاطًا واحدًا؛ بصمة `315c06d2…` متطابقة في الثلاث. |
| 4 | تقرير متابعة مقالات SEO الأسبوعي | v5 sv1 (11 حقلًا، `articles_grid`) | v5 sv1 (نفس بصمة الإنتاج `5604bff1…`) | v1 sv1 (3 حقول، `articles`) | **`INCLUDE`** (وضع `grid`) | الشبكة تحمل المخرَج المتكرّر بأعمدة متطابقة الدلالة في الثلاث (تفصيل §4 أدناه). |
| 5 | تقرير كاتب المحتوى الأسبوعي | **v9 sv2 `workItems`** `bc3a5087…` | v8 sv1 `554bd2e1…` | v4 sv1 `554bd2e1…` | **`ALREADY_COMPLIANT` على الإنتاج · `INCLUDE` على RC وTEST** | الإنتاج مطابق فعلًا للعقد؛ RC وTEST متأخّرتان على sv1 بنفس مفاتيح الإنتاج الخمسة ⟹ تُحاذَيان. |
| 6 | 🔍 تقرير فريق SEO | v5 sv1 (14) | v5 sv1 (14) | v1 sv1 (2) | **`EXCLUDE_NOT_APPLICABLE`** | قرار مالك المنتج §1: إجماليّات فريق لا مخرَج متكرّر. |
| 7 | 🏫 تقرير تشغيل الأكاديمية | v5 sv1 (7) | v5 sv1 (7) | — | **`EXCLUDE`** | حقول سرديّة لحالة العميل/المشروع + شبكة `actions`؛ لا مخرَج متكرّر داخل المشروع. |
| 8 | 💻 تقرير فريق الويب | v2 sv1 (9) | v2 sv1 (9) | — | **`EXCLUDE`** | الوحدة المتكرّرة **هي المشروع نفسه** (`maxProjects=10`). |
| 9 | 📈 تقرير النمو والأداء — Media Buyer | v6 sv1 (39) | v5 sv1 (11) | v1 sv1 (2) | **`EXCLUDE`** | التكرار مُخدَّم أصلًا بستّ شبكات؛ باقي الحقول تحليل مفرد. |
| 10 | 🤝 تقرير إدارة الحسابات العملاء | v4 sv1 (18) | v5 sv1 (18) | — | **`EXCLUDE`** | حالة/مخاطر/علاقة على مستوى المشروع؛ لا مخرَج متكرّر. |
| 11 | 🤝 تقرير مدير الحسابات | v5 sv1 (12) — **معطَّل** | v5 sv1 (12) — **معطَّل** | v1 sv1 (15) — نشط | **`EXCLUDE`** | أربع شبكات + سرد على مستوى العميل. معطَّل في الإنتاج وRC؛ نشاطه على TEST لا يغيّر الدلالة. |
| 12 | **UAT R2 — بنود العمل المتعدّدة (مؤقّت)** | — | — | **v2 sv2 `workItems`** `3ac3e653…` | **`EXCLUDE_OUT_OF_SCOPE`** | **هذا هو «القالب الإضافيّ المكتشَف» في §4.** موجود في `reporting_test_uat` **فقط**، أُنشئ ضمن قبول R22A كقالب اختبار مؤقّت. مقيس: نشط، منشور v2، `schemaVersion=2` مع `workItems` (4 حقول بند) ⟹ **مطابق للعقد المستهدف سلفًا**، وله إسناد نشط واحد ومسودّة مفتوحة واحدة (`268f70a4-5cc5-43d0-b74c-03f2dc67882e` · `p360r2.emp@r2uat.test` · `2026-W35`). لا وجود له في الإنتاج ولا RC ⟹ لا أثر إنتاجيّ، ولا يحتاج تحويلًا، ولا يُمسّ. |

**`keywords_grid`** (داخل قوالب SEO): خارج نطاق التحويل بقرار §1، يبقى كما هو بلا تعديل.

## 3) نتيجة البوابة

| المؤشّر | القيمة |
|---|---|
| `ALL_ACTIVE_ASSIGNED_TEMPLATES_DISCOVERED` | **YES** (استعلام شامل على `report_templates` بلا مرشِّح عنوان في القواعد الثلاث) |
| القوالب ذات قسم مشاريع (اتّحاد البيئات) | **12** |
| قوالب مصنَّفة صراحةً | **12** |
| `UNCLASSIFIED_TEMPLATES` | **0** |
| قوالب للتحويل | **4** (`INCLUDE`) + محاذاة «كاتب المحتوى» على RC/TEST |

**البوابة مفتوحة.** يجوز الانتقال إلى التنفيذ على TEST.

## 4) القالب البائت على TEST (استجابةً لـ§3) — بلا نسخ معرّفات وبلا إصدار وسيط

- **لم يُنسَخ أيّ `TemplateId` أو `VersionId` من الإنتاج إلى TEST.** الأداة تكتشف القالب بالعنوان **داخل بيئته**، وتوقف التنفيذ إن لم يكن التطابق فريدًا.
- **بصمة مصدر الإنتاج (مرجع دون اتّصال):** `5604bff161dd62842edaaf5e8ae14621` (v5، 11 حقل مشروع، شبكة `articles_grid`).
- **بصمة TEST السابقة:** `d2e9bd4b7c45bdf357c1b163053fd537` (v1، 3 حقول مشروع، شبكة `articles`).
- **الاكتشاف الحاسم:** شبكة TEST `articles` (التسمية «مقالات المشروع») أعمدتها **`["عنوان المقال","الكلمة المفتاحية","الحالة","المراجع","تاريخ التسليم","ملاحظات"]` — مطابقة حرفيًّا لأعمدة `articles_grid` في الإنتاج** ⟹ خريطة التحويل المعتمدة في §2 تنطبق على قالب TEST بذاته.
- **النتيجة:** لا بذر من الإنتاج، ولا إصدار وسيط، ولا إصدار غير منشور مُهمَل. يُنشأ إصدار الهدف مباشرةً على قالب TEST نفسه.
- **فرق بيئيّ موثَّق:** حقول المشروع الباقية بعد التحويل على TEST = `[published, project_notes]` مقابل 10 في الإنتاج. هذا فرق **خارج عقد بنود العمل** ولا يؤثّر على تمثيليّة الرحلة لعقد `workItems`.
- **إتاحة المراجع على TEST:** المجالات الثلاثة عشر لـ`catalogDomain` (`video_type`, `edit_type`, `video_duration`, `video_status`, `design_type`, `design_status`, `design_tool`, `activity_type`, `interaction_result`, `response_time`, `content_type`, `content_goal`, `work_status`) **متطابقة العدد النشط والبصمة `md5` في البيئات الثلاث** ⟹ `CATALOG_DOMAINS_AVAILABLE_ON_TEST = YES`. حقول بند مقال SEO الستّة **لا تحمل `catalogDomain` إطلاقًا** ⟹ لا اعتماد خارجيّ فيها.
