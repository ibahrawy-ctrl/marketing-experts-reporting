# R22B — التقرير النهائيّ الشامل: تحويل حقول المشروع إلى بنود عمل متعدّدة

**التاريخ:** 31 أغسطس 2026 · **النطاق:** TEST → RC → الإنتاج (متّصل) · **الأدوات:** `R22BWorkItemsPublisher` لتفعيل القوالب و`R22BDraftRegrouper` لتصحيح تجميع المسودّة الإنتاجيّة (كلتاهما EF Core الرسميّ، معاملة واحدة، لا SQL خام، لا هجرات، لا تعديل مباشر لـ`ConfigJson`).

## 0) لوحة القرار

> **حالة التذكرة: `R22B_RELEASE_STATUS = RELEASED`.**
> مسار التذكرة كان: إغلاق أوّل مرفوض ⟸ `PARTIAL_WITH_PRODUCTION_DRAFT_BLOCKER` ⟸ **تصحيح التجميع مكتمل ومقيس (§8)** ⟹ `RELEASED`.
> تفعيل القوالب الأربعة مقبول ومغلَق. مسودّة الإنتاج `bba208cd` **جُمِّعت في 4 بطاقات مشروع بتوزيع `3/1/2/5` بلا فقد قيمة واحدة وبلا إرسال**.

| العَلَم المطلوب (§9) | النتيجة |
|---|---|
| `VIDEO_MULTI_WORK_ITEMS` | **PASS** |
| `DESIGN_MULTI_WORK_ITEMS` | **PASS** |
| `MODERATION_MULTI_WORK_ITEMS` | **PASS** |
| `SEO_ARTICLE_MULTI_WORK_ITEMS` | **PASS** |
| `CONTENT_CURRENT_DRAFT_MIGRATION` | **PASS** — `PROJECT_ENTRY_COUNT=4` · `UNIQUE_PROJECT_ID_COUNT=4` · `TOTAL_WORK_ITEM_COUNT=11` · `WORK_ITEMS_PER_PROJECT=[3,1,2,5]` (§8.3) |
| `PROJECT_DUPLICATION_REMOVED` | **YES** — `DUPLICATE_PROJECT_IDS = 0` |
| `NO_PROJECT_DUPLICATION` | **PASS** |
| `CONTENT_DRAFT_STRUCTURALLY_SENDABLE` | **YES** — `DUPLICATE_PROJECT_VALIDATION = PASS` وتكافؤ بنيويّ مع حمولة TEST المُرسَلة بنجاح (§8.5) |
| `CURRENT_DRAFT_VALUES_PRESERVED` | **YES** — 53/53 زوجًا دلاليًّا · مفقود 0 · يتيم 0 · مضاف 0 · مجهول 0 · ملخّصات منقولة 0 |
| `HISTORICAL_SUBMISSIONS_MUTATED` | **0** |
| `PRODUCTION_EMAILS_TRIGGERED` | **0** — `email_outbox` 0→0 و`email_notifications` 756→756 في نافذة التصحيح |
| `UNCLASSIFIED_TEMPLATES` | **0** |
| `OPERATIONAL_VISUAL_ACCEPTANCE` | **PASS** (للقوالب الأربعة + بروفة التجميع الثمانيّة على TEST) |
| `RC_LEGACY_DRAFT_MIGRATED` | **NO** |
| `RC_LEGACY_DRAFT_PRESERVED_UNCHANGED` | **YES** |
| `RC_LEGACY_DRAFT_EXCEPTION` | **DOCUMENTED** — استثناء **غير حاجب** للإنتاج، وليس ترحيلًا ناجحًا |

## 1) بوابة ما قبل الكتابة (§4)

الجرد الشامل على القواعد الثلاث بلا مرشِّح عنوان: `prod` 41 قالبًا (28 نشطًا) · `rc` 41 (29) · `test` 35 (29). اتّحاد القوالب ذات `ProjectRepeatableSection` عبر البيئات = **12**، كلّها مصنَّفة صراحةً ⟹ `ALL_ACTIVE_ASSIGNED_TEMPLATES_DISCOVERED = YES` و`UNCLASSIFIED_TEMPLATES = 0`.

**القالب الإضافيّ المكتشَف** = `UAT R2 — بنود العمل المتعدّدة (مؤقّت)`: موجود في `reporting_test_uat` وحدها، منشور v2 بـ`schemaVersion=2` و`workItems` ⟹ **مطابق للعقد سلفًا**، لا أثر إنتاجيّ ⟹ `EXCLUDE_OUT_OF_SCOPE`، ولم يُمسّ.
اكتشاف ثانٍ مسجَّل: `🤝 تقرير مدير الحسابات` معطَّل على الإنتاج وRC ونشط على TEST — `EXCLUDE` بلا تغيير.

التفاصيل الكاملة: `Ops/R22B/INVENTORY/R22B-FINAL-INVENTORY-MATRIX.md`.

## 2) ما نُفِّذ فعلًا في كلّ بيئة

| القالب | TEST | RC | الإنتاج |
|---|---|---|---|
| تقرير فريق الفيديو | v4 → **v5** | v7 → **v8** | v7 → **v8** |
| تقرير فريق التصميم | v4 → **v5** | v7 → **v8** | v7 → **v8** |
| تقرير المديرشن الأسبوعي | v4 → **v5** | v8 → **v9** | v8 → **v9** |
| تقرير متابعة مقالات SEO الأسبوعي | v1 → **v2** (وضع `grid`, `articles`) | v5 → **v6** (`articles_grid`) | v5 → **v6** (`articles_grid`) |
| تقرير كاتب المحتوى الأسبوعي | v4 → **v6** (محاذاة) | v8 → **v10** (محاذاة) | **v9 — `ALREADY_COMPLIANT`، لا إصدار جديد** |

الحالة النهائيّة المقيسة على الإنتاج: الخمسة كلّها `schemaVersion=2`، حقول المشروع = 0 (عدا SEO = 10 حقلًا خارج نطاق التحويل)، وحقول بند العمل = 6/5/5/6/5. عدد الهجرات ثابت **47 → 47**. `/health = 200`.
على RC: الخمسة `schemaVersion=2`، الهجرات ثابتة **49 → 49**، `/health = 200`.

**حقول بند مقال SEO (§2)** أُنشئت بالمفاتيح والأنواع المعتمدة حرفيًّا — `article_title`/`target_keyword`/`article_status`/`reviewer`/`delivery_date` بنوع `ShortText` و`notes` بنوع `LongText`، كلّها `required=false`، والتسميات العربيّة المرئيّة كما هي بلا تغيير. **لم يُرقَّ أيّ حقل** إلى `Date` أو `Select` أو `User/Lookup`.

## 3) القالب البائت على TEST (§3)

لم يُنسَخ أيّ `TemplateId` أو `VersionId` من الإنتاج إلى TEST؛ الأداة تكتشف القالب بعنوانه **داخل بيئته** وتتوقّف إن لم يكن التطابق فريدًا. بصمة مصدر الإنتاج المرجعيّة `5604bff161dd62842edaaf5e8ae14621`، وبصمة TEST السابقة `d2e9bd4b7c45bdf357c1b163053fd537`. أعمدة شبكة TEST `articles` مطابقة حرفيًّا لأعمدة `articles_grid` في الإنتاج ⟹ نفس خريطة التحويل. **لا إصدار وسيط ولا إصدار غير منشور مُهمَل.** المجالات الثلاثة عشر لـ`catalogDomain` متطابقة العدد النشط والبصمة في البيئات الثلاث ⟹ `CATALOG_DOMAINS_AVAILABLE_ON_TEST = YES`؛ وحقول بند مقال SEO الستّة لا تحمل `catalogDomain` إطلاقًا.

## 4) المسودّات الحاليّة (§5)

### 4.1 بروفة TEST على شكل حمولة الإنتاج (§5.7) — نُفِّذت قبل أيّ لمسة للإنتاج
بُذِرت على TEST مسودّة `d07d6c4e-f7eb-45d4-a681-1810fdb60cfc` عبر الـAPI الرسميّ بنسخة **مُجرَّدة من الهويّة** من شكل حمولة الإنتاج نفسها: 11 مدخلًا على 4 مشاريع بالتوزيع نفسه (3/1/2/5) و53 قيمة غير فارغة، مع الإبقاء على رموز التصنيفات والأعداد كما هي وتجريد النصوص الحرّة وحدها. بعد التحويل: المعرّف ثابت، `Draft→Draft`، الفترة والمُرسِل بلا تغيير، 11→11 مدخلًا، 53/53 زوجًا متطابقًا، بصمة `eaba34ef2729c0e714ecd97171b3c422` قبل = بعد، وكلّ `answers` على مستوى المشروع صارت `{}` مع بند عمل واحد بالضبط لكلّ مدخل.

### 4.2 مسودّة التصميم الفارغة على الإنتاج — `739c42fb-aad1-435c-a009-3cc26c5963a8`
لقطة ما قبل الكتابة محفوظة. أُعيد ربطها بالآليّة الرسميّة v7 → **v8**. المعرّف والمستخدم والفترة `2026-W35` والحالة `Draft` كما هي، و`SubmittedAtUtc`/`ClosedAtUtc` تبقيان `NULL`. بصمة الحمولة `d41d8cd9` قبل = بعد (فارغة). الإصدار الجديد يحمل `workItems` ⟹ زرّ **«+ إضافة بند عمل»** متاح.

### 4.3 مسودّة كاتب المحتوى المأهولة على الإنتاج — `bba208cd-9fb1-4c70-881e-abd91c8c57fe` (البوّابة الأهمّ)

| المؤشّر | القيمة المقيسة |
|---|---|
| نسخة كاملة + بصمة قبل | محفوظة (`/tmp/r22b-prod-bba208cd.json`, `capturedAtUtc` مسجَّل) |
| Dry Run مفتاحًا-بمفتاح | `Ops/R22B/EVIDENCE/PROD-DRAFT-BBA208CD-DRYRUN.md` (53 سطرًا، كلّ مفتاح قديم وقيمته وموضعه الجديد) |
| مدخلات المشروع قبل / بعد | 11 / 11 |
| أزواج (مدخل، مفتاح، قيمة) قبل / بعد | **53 / 53** |
| قيم مفقودة · يتيمة · مكرّرة · مفاتيح مجهولة | **0 · 0 · 0 · 0** |
| بصمة القيم القانونيّة قبل / بعد | `00bc5f564121c7c60297356c32b11aee` / **متطابقة** |
| بصمة حمولة الأداة قبل / بعد | `48ba89cb…` / **متطابقة** |
| `projectId` لكلّ مدخل | **متطابقة بالترتيب نفسه** |
| بنود العمل لكلّ مدخل | **1 لكلّ مدخل** (11 مدخلًا) |
| `answers` على مستوى المشروع بعد التحويل | **فارغة كلّها** |
| ملخّصات التقرير العامّة | **4 حقول غير-PRS متطابقة حرفيًّا قبل/بعد — لم يدخل أيٌّ منها بند العمل** |
| المعرّف · المُرسِل · الفترة · الحالة | ثابتة (`bba208cd…` · `d352528f…` · `2026-W35` · `Draft`) |
| ربط الإصدار | v8 → **v9** (الإصدار الفعّال المطابق سلفًا) |

⟹ لا فقد قيمة، **لكنّ هذه المرحلة الأولى وحدها كانت `FAIL`**: الحفاظ 1:1 على 11 مدخلًا أبقى المشروع مكرَّرًا (4 معرّفات فريدة فقط) ⟹ لم تبلغ النموذج التشغيليّ المطلوب «مشروع واحد ببنود عمل متعدّدة». **صُحِّح ذلك بالكامل في §8**، وبعده صارت الحمولة 4 بطاقات بتوزيع `3/1/2/5` ⟹ `CONTENT_CURRENT_DRAFT_MIGRATION = PASS`.

### 4.4 مسودّة RC — `f116a7ef-93b1-4670-aebe-09334c557448` · **لم تُرحَّل عمدًا**
عالقة على **v5** بينما الإصدار الفعّال v8، ومفاتيحها (`issues`, `next_week`, `pieces_grid`, `return_reason`, `project_status`) لا مقابل لها في الإصدار الخليفة. حكمت الأداة تلقائيًّا: *«محظورة — لا تُمسّ: حقول تحمل قيمًا ولا مقابل لها في الإصدار الخليفة»*. تطبيقًا لقاعدة §5 «**إن لم تتحقّق المكافئة الدلاليّة الكاملة فتوقّف ولا تُرحّل**» تُركت كما هي بالضبط: `UpdatedAtUtc` ما زال `2026-07-08 21:01:08` وبصمتها `8ffb1c02` قبل = بعد. هذه حالة **سابقة لـR22B** (المسودّة كانت عالقة على v5 قبل التذكرة) ولم تتغيّر بها.

⟹ `RC_LEGACY_DRAFT_MIGRATED = NO` · `RC_LEGACY_DRAFT_PRESERVED_UNCHANGED = YES` · `RC_LEGACY_DRAFT_EXCEPTION = DOCUMENTED`. تُوصَف صراحةً بأنّها **استثناء موثَّق غير حاجب للإنتاج**، **لا ترحيل ناجح**.

## 5) القبول البصريّ التشغيليّ (§7) — على TEST بحسابات موظّفين حقيقيّين

رحلة كاملة عبر **الواجهة المنشورة فعلًا على TEST** (تُقدَّم محلّيًّا بلا تعديل أيّ ملفّ من الحزمة، والـAPI عبر نفق SSH).

| القالب | التسليم | بنود بعد إعادة التحميل | البند 1 ثابت | البند 2 تغيّر | النتيجة |
|---|---|---|---|---|---|
| تقرير فريق الفيديو | `644c8a2b…` | 2 | نعم | نعم | **PASS** |
| تقرير فريق التصميم | `6b575ad0…` | 2 | نعم | نعم | **PASS** |
| تقرير المديرشن الأسبوعي | `683ffcdc…` | 2 | نعم | نعم | **PASS** |
| تقرير متابعة مقالات SEO الأسبوعي | `12e09523…` | 2 | نعم | نعم | **PASS** |

- **مشروع واحد وبندَان مختلفان** في كلّ حالة، والحفظ ثمّ إعادة التحميل ثمّ تعديل البند الثاني وحده — البند الأوّل لم يتغيّر حرفيًّا.
- **قالب متابعة المقالات يحمل مقالَين مستقلَّين داخل المشروع نفسه**، بستّة حقول مملوءة لكلٍّ منهما.
- **Project 360:** التقرير يظهر في تقارير المشروع، والشريحة المقيَّدة بالمشروع تعرض **البندَين معًا**.
- **الحجب عبر المشاريع:** فتح التسليم من مشروع آخر يُرجِع `404 project.not_found` ولا يعرض أيّ بند.
- **المسودّة المحوَّلة لكاتب المحتوى:** 11 بند عمل (واحد لكلّ مشروع) و11 زرّ «+ إضافة بند عمل»، والضغط على أوّلها رفع العدّ إلى 12 ⟹ **إضافة بند ثانٍ داخل المشروع نفسه متاحة فعلًا**.
- **أخطاء 5xx = 0** و**أخطاء كونسول التطبيق = 0**. (الاستجابة `404` الوحيدة المسجَّلة هي **نتيجة اختبار الحجب المقصود** نفسه.)

الأدلّة: `Ops/R22B/screenshots/R22B-{V,D,M,S}-0{1,2,3}-*.png` و`R22B-E0{1..5}-*.png`، ونتائج مقيسة في `Ops/R22B/EXECUTION/`.

**ملاحظة صريحة على موضع الإثبات البصريّ للمسودّة الإنتاجيّة:** الإثبات البصريّ لبند العمل الأوّل وإضافة بند ثانٍ نُفِّذ على **نسخة TEST المطابقة لشكل حمولة الإنتاج** (هي بعينها ما فرضته §5.7)، لا على الإنتاج مباشرةً، لأنّ ذلك يستلزم دخولًا بحساب موظّف إنتاجيّ حقيقيّ أو إنشاء اعتماد على الإنتاج — وكلاهما كتابة على الإنتاج تخالف §8. المكافئة على الإنتاج مُثبَتة بنيويًّا بالقياس (53/53، بصمة متطابقة، بند عمل واحد لكلّ مدخل، الإصدار الفعّال يحمل `workItems`).

## 6) حدود التنفيذ (§6) — كلّها محترمة

| القيد | الالتزام |
|---|---|
| API/خدمات النظام الرسميّة وEF الرسميّ فقط | ✅ الأداة تعمل عبر `AppDbContext` وكيانات النطاق؛ الحسابات عبر `UserManager` الرسميّ |
| لا SQL خام | ✅ (الاستعلامات الخام الوحيدة قراءة فقط: `SELECT current_database()` والجرد) |
| لا تعديل مباشر لـ`ConfigJson` في القاعدة | ✅ التغيير يتمّ بإنشاء **إصدار قالب جديد** بالآليّة الرسميّة |
| لا هجرة | ✅ عدد الهجرات ثابت: TEST 46→46 · RC 49→49 · الإنتاج 47→47 |
| لا تغيير كود | ✅ لم يُعدَّل أيّ ملفّ في `reporting-backend/` أو `reporting-frontend/` |
| لا مساس بتقرير Submitted/Approved/Closed | ✅ بصمة التقارير التاريخيّة مُتحقَّق منها داخل المعاملة قبل الإقرار |
| لا حذف إصدارات أو مسودّات تاريخيّة | ✅ لا حذف إطلاقًا |
| لا كتابة على الإنتاج قبل نجاح TEST وRC | ✅ الترتيب: TEST (05:50) → RC (06:09) → الإنتاج (06:12) |

## 7) الإنتاج والبريد (§8)

| المؤشّر | قبل | بعد | الدلالة |
|---|---|---|---|
| `email_outbox` | **0** | **0** | لا رسالة صادرة |
| `email_notifications` | 742 | 754 | +12، **كلّها خارج R22B** |
| تسليمات غير مسودّة | 27/208/15/68 | **بلا تغيير** | لا تقرير جديد ولا إرسال للاعتماد |
| الهجرات | 47 | 47 | لا هجرة |

**إثبات أنّ الزيادة ليست من R22B:** الاثنتا عشرة رسالة كلّها `EntityType = ReportReminder` من مهامّ التذكير المجدولة (`report-weekly-due` ×2، `report-overdue` ×7، `report-review-overdue-manager` ×2، `report-review-overdue-teamleader` ×1)، أُنشئت بين **06:11:08 و06:11:21** بينما بدأ تنفيذ R22B الساعة **06:12:37.96Z**. عدد ما أُنشئ داخل نافذة التنفيذ = **0**، وعدد المرتبط بأيّ مسودّة = **0**.
⟹ **`PRODUCTION_EMAILS_TRIGGERED_BY_R22B = 0`.**

**عدم مساس التقارير التاريخيّة:** أحدث تعديل لأيّ تسليم غير مسودّة على الإنتاج = `2026-08-30 12:22:59`، أي **قبل يوم التنفيذ**. وبصمات التقارير التاريخيّة لكلّ قالب (12/11/5/8/9 تقريرًا) متطابقة قبل/بعد داخل المعاملة نفسها ⟹ `HISTORICAL_SUBMISSIONS_MUTATED = 0`.

## 8) التصحيح المُنفَّذ: تجميع مسودّة الإنتاج `bba208cd` حسب `projectId` — **مُغلَق**

**القياس الحاكم:** `OLD_PROJECT_ENTRIES = 11` · `UNIQUE_PROJECT_IDS = 4` · التوزيع `a151fd07`×3، `3258848e`×1، `2c1e6a2f`×2، `7c0531a3`×5. بعد ترحيل §4.3 بقيت الحمولة على **11 مدخل مشروع**، و`SubmissionService.cs:1633-1637` يرفض تكرار `projectId` عند الإرسال.

**التقييم المصحَّح (يلغي التصنيف السابق «اكتشاف مستقلّ لا يحجب»):** الحارس **ليس عيبًا في الكود**؛ هو يكشف أنّ ترحيل البيانات لم يبلغ النموذج التشغيليّ المطلوب. كلّ مدخل قديم يمثّل **بند عمل مستقلًّا لا بطاقة مشروع مستقلّة** ⟹ الواجب إنشاء بطاقة واحدة لكلّ `projectId` تحوي بنود العمل الخاصّة به.

**الهدف المقيس للتصحيح:**

| المؤشّر | المطلوب |
|---|---|
| `PROJECT_ENTRY_COUNT` | 4 |
| `UNIQUE_PROJECT_ID_COUNT` | 4 |
| `DUPLICATE_PROJECT_IDS` | 0 |
| `TOTAL_WORK_ITEM_COUNT` | 11 |
| `WORK_ITEMS_PER_PROJECT` | `[3, 1, 2, 5]` |

**قواعد التحويل الملزِمة:** حفظ ترتيب أوّل ظهور لكلّ مشروع؛ حفظ ترتيب المدخلات القديمة داخل كلّ مشروع؛ عدم نقل ملخّصات التقرير العامّة إلى بنود العمل؛ ثبات المعرّف والمُرسِل والفترة والحالة ومعرّفات المشاريع وكلّ القيم والملاحظات؛ عدم إسقاط مدخل المشروع المفرد `3258848e`؛ لا مساس بأيّ تقرير Submitted/Approved/Closed؛ EF الرسميّ وخدمات النظام فقط بلا SQL خام.

**معيار السلامة الحاكم:** مقارنة رباعيّات مرتّبة — قبل `(projectId, oldEntryOrdinal, fieldKey, value)` وبعد `(projectId, workItemOrdinal, fieldKey, value)`. **البصمة البايتيّة للحمولة قد تختلف مشروعًا بسبب إعادة الهيكلة، وليست معيارًا.**

### 8.1 أداة التصحيح
`Ops/R22B/tools/R22BDraftRegrouper/` — تنفيذيّ .NET 8 يرجع إلى `Reporting.Infrastructure` ويعمل عبر `AppDbContext` وكيانات النطاق الرسميّة **بلا SQL خام** (الاستعلام الخام الوحيد `SELECT current_database()` للتحقّق من الهويّة، قراءة فقط). حرّاسه: قفل `pg_advisory_xact_lock`؛ رفض أيّ حالة غير `Draft` أو أيّ `SubmittedAtUtc`/`ClosedAtUtc` غير فارغ؛ `--expect-db` و`--expect-payload-md5` (حارس تزامن يمنع الكتابة فوق تعديل مستخدم)؛ معاملة واحدة مع تراجع كامل إن أخفق أيّ فحص. بصمة الحزمة المنشورة على الخادم `df190c26d0bc6c15c0639144e14954d4` — **هي نفسها في TEST والإنتاج**.

### 8.2 بروفة TEST قبل أيّ لمسة للإنتاج — البوّابات الثماني

نُفِّذت على نسخة TEST المطابقة لشكل حمولة الإنتاج (`d07d6c4e-f7eb-45d4-a681-1810fdb60cfc`) عبر **الواجهة المنشورة فعلًا على TEST** (تُقدَّم محلّيًّا بلا تعديل أيّ ملفّ من الحزمة، والـAPI عبر نفق SSH).

| # | البوّابة | النتيجة المقيسة | الحكم |
|---|---|---|---|
| 1 | أربع بطاقات مشاريع فريدة | 4 منتقيات مشروع + 4 أزرار «+ إضافة بند عمل» | **PASS** |
| 2 | توزيع بنود العمل `3/1/2/5` | 11 عنوان «بند عمل n» بنمط 1‑2‑3 / 1 / 1‑2 / 1‑2‑3‑4‑5 | **PASS** |
| 3 | الحفظ ثمّ إعادة التحميل | `saveStatus=200` · `saveReloadIdentical=true` · مطابق بعد إعادة تحميل المتصفّح | **PASS** |
| 4 | تعديل بند واحد بلا مساس بالبقيّة | البطاقة 3 / البند 1 / المفتاح `count`: `changedPairCount=1` · `editPersisted=true` · `othersUntouched=true` | **PASS** |
| 5 | اجتياز حارس تكرار المشروع في الخادم | لا رسالة «لا يمكن تكرار نفس المشروع» إطلاقًا | **PASS** |
| 6 | **إرسال نسخة TEST بنجاح** | `Status=Submitted` · `SubmittedAtUtc=2026-08-31T08:02:01.196631Z` · `CurrentApproverId=2e12a40e…` | **PASS** |
| 7 | ظهور كلّ البنود في Project 360 | الشرائح الأربع `HTTP 200` تعرض **3 / 1 / 2 / 5** بندًا بالضبط | **PASS** |
| 8 | صفر 5xx وصفر أخطاء كونسول | `serverErrors5xx=[]` · `consoleErrors=[]` في الرحلتَين | **PASS** |

الأدلّة: `Ops/R22B/EXECUTION/regroup-test-{dryrun,apply,api-journey,ui-journey,p360}.json` و`Ops/R22B/screenshots-regroup/G0{1..4}-*.png`.

**ملاحظة مقيسة على البوّابة 5:** أوّل محاولة إرسال على TEST رجعت `400 submission.repeatable_section_invalid` برسالة **«مشروع خارج نطاق صلاحيتك» ×2** — وهي رسالة **رؤية** لا **تكرار**؛ أي أنّ حارس التكرار كان قد اجتيز فعلًا. سببها أنّ حساب الكاتب المؤقّت على TEST يرى مشروعَين فقط من مشاريع التجهيز الأربعة. عُولِج بإنشاء مشروعَي TEST داخل نطاقه وتبديل المعرّفَين الخارجَين عن النطاق، مع إثبات `VALUES_UNCHANGED_BY_PID_SWAP = True`. لا علاقة لهذا بالإنتاج: مشاريع الإنتاج الأربعة اختارها المُرسِل نفسه داخل مسودّته، والتجميع **لم يُضِف ولم يُغيّر أيّ `projectId`**.

### 8.3 التطبيق على الإنتاج — إعادة القياس ثمّ النسخ ثمّ الكتابة بلا إرسال

**إعادة القياس قبل الكتابة (حارس «لا تكتب فوق تعديل المستخدم»):** `UpdatedAtUtc = 2026-08-27 07:42:14.114975+00` و`prsPayloadMd5 = cbb720c8cb922a0b8a50d6bbaf2047f7` — **مطابقان تمامًا** لقياس ما قبل النسخة الاحتياطيّة ⟹ لم يمسّها المستخدم.

**النسخ الاحتياطيّ الثلاثيّ (2026‑08‑31T08:07:48Z):**

| النسخة | المسار | البصمة |
|---|---|---|
| حمولة القسم المتكرّر كاملة | `/root/r22b-backups/20260831T080748Z/prs-payload.json` (3597 B) | `1f5c2f9fd6a3110c8d63bbd5a7c62338` |
| كلّ قيم حقول المسودّة | `…/all-field-values.txt` | `05f23f2d12cc20ab68f418d6281fb9f0` |
| صفّ التسليم كاملًا | `…/submission-row.txt` | `43fc85de31e4e2119ac2b20018187192` |
| نسخة كاملة لقاعدة الإنتاج | `/var/lib/postgresql/r22b-backups/reporting_prod-20260831T080748Z.dump` (1 507 637 B) | `d8f4efbbefe6591d0741e43fd1ab4724` |

**التشغيل الجافّ ثمّ التطبيق** (`08:08:12Z` ثمّ `08:08:24Z`، الأخير بـ`--apply --expect-payload-md5 cbb720c8…`):

| المؤشّر | المطلوب | المقيس (جافّ) | المقيس (تطبيق) | التحقّق المستقلّ من القاعدة |
|---|---|---|---|---|
| `PROJECT_ENTRY_COUNT` | 4 | 4 | 4 | **4** |
| `UNIQUE_PROJECT_ID_COUNT` | 4 | 4 | 4 | **4** |
| `DUPLICATE_PROJECT_IDS` | 0 | 0 | 0 | **0** |
| `TOTAL_WORK_ITEM_COUNT` | 11 | 11 | 11 | **11** |
| `WORK_ITEMS_PER_PROJECT` | `[3,1,2,5]` | `[3,1,2,5]` | `[3,1,2,5]` | **`[3,1,2,5]`** |

**ترتيب أوّل الظهور محفوظ:** `a151fd07…` → `3258848e…` → `2c1e6a2f…` → `7c0531a3…`. **المدخل المفرد `3258848e` لم يُسقَط** (بطاقة كاملة ببند عمل واحد).

**إثبات السلامة الدلاليّة بالرباعيّات المرتّبة** — قبل `(projectId, oldEntryOrdinal, fieldKey, value)` وبعد `(projectId, workItemOrdinal, fieldKey, value)`:

| `SEMANTIC_VALUE_PAIRS_BEFORE` | `AFTER` | `MISSING_VALUES` | `ORPHAN_VALUES` | `ADDED_VALUES` | `UNKNOWN_KEYS` | `GENERAL_SUMMARIES_MOVED` |
|---|---|---|---|---|---|---|
| **53** | **53** | **0** | **0** | **0** | **0** | **0** |

بصمة القيم الدلاليّة المرتّبة `97aecf6d2a3c0651dff5e812a8cd2ad9` **قبل = بعد**. وعدّ القيم داخل بنود العمل مباشرةً من القاعدة = **53**. وبصمة الحقول العامّة غير-PRS الأربعة `4cbe42ac9e7a94e6ed8cd22114bef71a` **قبل = بعد** ⟹ لم ينتقل أيّ ملخّص عامّ إلى بند عمل، و`answers` على مستوى البطاقة **فارغة في البطاقات الأربع كلّها**.

**البصمة البايتيّة اختلفت مشروعًا كما هو متوقَّع** (`cbb720c8…` → `b24a194a…` بمُسلسِل الأداة، و`25686d0c…` بالتمثيل القانونيّ لـ`jsonb` في postgres). هذا فارق ترتيب مفاتيح وتمثيل لا فارق بنيويّ، و**المعيار الحاكم هو تطابق القيم الدلاليّة المرتّبة** كما نصّ التوجيه.

**لا إرسال:** `Status = Draft` · `SubmittedAtUtc IS NULL` · `ClosedAtUtc IS NULL` · `CurrentApproverId` فارغ. `UpdatedAtUtc` الجديد `2026-08-31 08:08:24.450267+00`. المعرّف والمُرسِل (`d352528f…`) والفترة (`2026-W35`) وربط الإصدار (`ed4796e4…` = v9) بلا تغيير.

### 8.4 التحقّق بعد الحفظ وإعادة بناء النموذج

أُعيدت قراءة المسودّة عبر **نفس مسار EF والنموذج الرسميّ** (تشغيل جافّ ثانٍ، `applied=false`): أعاد بناء **4 بطاقات · 4 معرّفات فريدة · 0 تكرار · 11 بند عمل · `[3,1,2,5]` · 53/53 · كلّ الفروق أصفار**، وبصمة دلاليّة `97aecf6d…` مطابقة ⟹ الحمولة المخزَّنة تُفكَّك بالنموذج الرسميّ إلى النموذج التشغيليّ المطلوب بالضبط، والعمليّة **مستقرّة عند التكرار** (لا تُغيّر شيئًا عند إعادة التشغيل).

المشاريع الأربعة موجودة وحالتها `Active` على الإنتاج: `ادارة السوشيال ميديا` · `إنشاء وتجهيز منصات السوشيال ميديا` · `سوشيال ميديا` (عميلان مختلفان).

### 8.5 إثبات قابليّة الإرسال — بلا إرسال فعليّ

المسودّة الإنتاجيّة **لم تُرسَل**. أُثبِتت قابليّة إرسالها بمسارَين مستقلَّين معًا (`Ops/R22B/EXECUTION/regroup-prod-sendable-proof.json`، والسكربت `Ops/R22B/EXECUTION/r22b-sendable-proof.py` — قراءة فقط):

1. **إعادة تشغيل منطق التحقّق نفسه للقراءة فقط**، منقولًا حرفيًّا عن `SubmissionService.cs:1613‑1637` (حارس `HashSet` لتكرار `projectId`) و`1675‑1738` (`MinItems`، و`MaxItems` **يُطبَّق فقط إذا كان > 0**، والحقول المطلوبة `content_type`/`content_goal`/`work_status`/`count`، والحقل الرقميّ `count`، و`UniqueBy` الفارغة). النتيجة على حمولة الإنتاج: **صفر خطأ في المسارَين**.
2. **التكافؤ البنيويّ مع حمولة TEST المُرسَلة بنجاح فعلًا** (`Status=Submitted` عند `08:02:01Z`): شكل البطاقة متطابق `answers:dict · projectId:str · workItems:list`، وشكل البند متطابق `answers:dict`، و`schemaVersion = 2` في الطرفَين، وإعدادات القالب متطابقة حرفيًّا، والتوزيع `[3,1,2,5]` نفسه.

⟹ **`DUPLICATE_PROJECT_VALIDATION = PASS`** · **`SUBMISSION_STRUCTURALLY_SENDABLE = YES`**.

### 8.6 البريد والتزامن في نافذة التصحيح

| المؤشّر | قبل (08:07:48Z) | بعد (08:09Z) | الدلالة |
|---|---|---|---|
| `email_outbox` | **0** | **0** | لا رسالة صادرة |
| `email_notifications` | **756** | **756** | **صفر زيادة** |
| رسائل مرتبطة بالمسودّة (`EntityId=bba208cd…`) | — | **0** | لا إشعار عنها إطلاقًا |
| تسليمات غير مسودّة تغيّرت | — | **0** | لا مساس بأيّ Submitted/Approved/Closed |

الرسالتان الوحيدتان اليوم `leave-request-approved` عند `07:36:38` و`07:36:48` — **قبل نافذة التصحيح** ونوعهما `LeaveRequest` لا علاقة له بالتقارير. وضع البريد الإنتاجيّ `EmailNotifications__Mode=Enabled` ولم يُمسّ. مسودّة أخرى (`8b43e647…`, `2026-W36`, مُرسِل آخر) تغيّرت عند `08:07:13` — **نشاط مستخدم إنتاجيّ عاديّ سابق لكتابتي وغير متعلّق بها**.

**الحالة النهائيّة للحاجب: مُغلَق.** `CONTENT_CURRENT_DRAFT_MIGRATION = PASS` · `PROJECT_DUPLICATION_REMOVED = YES` · `CONTENT_DRAFT_STRUCTURALLY_SENDABLE = YES` · `CURRENT_DRAFT_VALUES_PRESERVED = YES`.

## 9) التنظيف

- **حسابات TEST المؤقّتة السبعة معطَّلة** (`IsActive=false` + قفل دائم + إزالة كلمة المرور): `p360r2.admin@r2uat.test`, `p360r2.emp@r2uat.test`, `p360nav.emp@navuat.test`, `emp1@uat.local`, `emp2@uat.local`, `employee@uat.local`, `r22a.e2e.writer@r22uat.test`. أُعيد تفعيل حسابَين منها مؤقّتًا لبروفة §8.2 (`r22a.e2e.writer@r22uat.test` و`p360r2.admin@r2uat.test`) ثمّ **أُعيد تعطيلهما بالأداة الرسميّة بعد انتهائها** — مُتحقَّق منه: `IsActive=f` · `locked=t` · `no_password=t` للاثنَين.
- **إسنادات TEST المؤقّتة الأربعة أُزيلت** عبر الـAPI الرسميّ (`297eb646`, `d6e51494`, `b29d3d48`, `8b156f70`).
- المُقدِّم المحلّيّ (المنفذ 4432) ونفق SSH أُوقِفا (المنفذ حرّ والعمليّة منتهية)، ونسخة الواجهة المحلّيّة المؤقّتة وكلّ السكربتات والملفّات المؤقّتة (بما فيها ملفّات كلمات المرور والرموز) حُذِفت من الجهاز المحلّيّ ومن `/tmp` على الخادم، ومدخل `r22b-test-uat` أُزيل من `.claude/launch.json`.
- **مشروعا TEST المؤقّتان مُحتفَظ بهما عمدًا** على بيئة الاختبار وحدها: `R22B-REGROUP-PRJ-1` (`44388186-3e2a-4825-8b92-a8307abdc941`) و`R22B-REGROUP-PRJ-2` (`1d3ae941-05a2-43c9-a37d-8e32fbac5796`) — أُنشئا لإصلاح نطاق رؤية حساب الكاتب في البوّابة 5، ولا وجود لهما على RC ولا على الإنتاج. تُركا كما هما لأنّ حذفهما كتابةً إضافيّة بلا فائدة تشغيليّة، وهما موسومان باسم صريح يدلّ على مصدرهما.
- **النسخ الاحتياطيّة قبل الكتابة** — لتفعيل القوالب: `/var/backups/r22b/r22b-prod-before.dump` (md5 `18821f6dc8a61827414ec0e249e6e72f`، ونسخة ثانية مطابقة) و`r22b-rc-before.dump` (md5 `56033910cad4ab545d5587d1ed69c139`). ولتصحيح التجميع: النسخ الأربع المدرَجة في §8.3. **كلّها محفوظة ولم تُحذف.**

## 10) الملفّات المرجعيّة

| الغرض | المسار |
|---|---|
| مصفوفة الجرد وبوّابة ما قبل الكتابة | `Ops/R22B/INVENTORY/R22B-FINAL-INVENTORY-MATRIX.md` |
| Dry Run مفتاحًا-بمفتاح للمسودّة الإنتاجيّة | `Ops/R22B/EVIDENCE/PROD-DRAFT-BBA208CD-DRYRUN.md` |
| تقارير التنفيذ (تجريبيّ + تطبيق) للبيئات الثلاث | `Ops/R22B/EXECUTION/report-{test,rc,prod}-*.json` |
| لقطات القبول البصريّ | `Ops/R22B/screenshots/` |
| الخطط المعتمدة | `Ops/R22B/tools/plan-{test,rc,prod}.json` |
| أداة التحويل (تفعيل القوالب) | `Ops/R22B/tools/R22BWorkItemsPublisher/` |
| **أداة تصحيح التجميع** | `Ops/R22B/tools/R22BDraftRegrouper/` |
| إثبات قابليّة الإرسال (قراءة فقط) | `Ops/R22B/tools/r22b-sendable-proof.py` |
| رحلتا Playwright لبروفة التجميع | `Ops/R22B/tools/regroup-journeys/r22b-regroup-{journey,p360}.mjs` |
| نتائج تصحيح التجميع (TEST والإنتاج) | `Ops/R22B/EXECUTION/regroup-{test,prod}-*.json` |
| لقطات بروفة التجميع | `Ops/R22B/screenshots-regroup/` |
| أداة اعتماد TEST (محظورة على الإنتاج بحارس صريح) | `Ops/R22B/tools/R22BAccountTool/` |
