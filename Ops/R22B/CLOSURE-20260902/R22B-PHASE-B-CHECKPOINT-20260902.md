# R22B — Checkpoint المرحلة B: الإصلاحات والاختبارات والضوابط السالبة

**التاريخ:** 2 سبتمبر 2026
**الفرع:** `fix/r22b-reporting-visual-operational-closure-20260902`
**الأساس:** `865c0edde96163325d00af248d0a3b8df96878dd`
**شجرة العمل:** `/private/tmp/r22b-closure-wUIoo4tt/repo` (معزولة)
**الشجرة المشتركة:** `SHARED_WORKTREE_TOUCHED = NO` · **الالتزامات: 0** · **الدفعات: 0**

---

## 1) الإصلاحات المُنفَّذة — خمسة عيوب شديدة

| المعرّف | السبب الجذريّ المُثبَت | الإصلاح | الملفّات |
|---|---|---|---|
| **VIS-01** | `<option>` لا يقبل أبناءً ⟹ لا `<bdi>`؛ فيُعيد خوارزم Unicode ترتيب المقاطع اللاتينيّة داخل السلسلة العربيّة. اللقطة `E02-…emp-video` أظهرت `R22B UAT — مشروع الفيديو (مؤقّت) — 2B UAT` | `bidiIsolate` بمحرفَي FSI (U+2068) وPDI (U+2069) + `projectOptionLabel` موحّدة، مطبَّقة على 3 مواضع نداء | `lib/format.ts` · `pages/SubmissionsPage.tsx` |
| **VIS-02** | مساحة عمل المشروع لم تحمل تبويب تقارير إطلاقًا (8 تبويبات، لا واحد منها للتقارير) رغم وجود النقطة `GET /projects/{id}/reports` والمكوّن `LinkedReportsCard` | تبويب تاسع كسول + مكوّن `ProjectReportsTab` | `pages/Project360Page.tsx` · `components/project360/ProjectReportsTab.tsx` |
| **VIS-02ب** | `LinkedReportRow` تسعة حقول لا تكفي لاتّخاذ قرار: لا اسم قالب يميّز السيو من التصميم، ولا عدد بنود عمل، ولا آخر قرار اعتماد، ولا سبب إرجاع | ستّة حقول اختياريّة الموضع (توافق خلفيّ تامّ مع المُنشِئين القائمين) + خمسة أعمدة مشروطة بـ`showDecisionColumns` | `Clients/ClientModels.cs` · `Services/ProjectService.cs` · `types/api.ts` · `pages/ClientDetailPage.tsx` |
| **VIS-03** | `main.tsx` كان ينشئ `new QueryClient()` بلا خيارات ⟹ افتراض `retry: 3` بتراجع أُسّيّ. على 404/403 يبقى `isPending` صحيحًا ~7 ثوانٍ، و`isLoading = isPending && isFetching` ⟹ دوّار بلا نهاية بدل حالة الخطأ | `shouldRetryQuery`: صفر إعادة على 4xx، محاولتان على 5xx/الشبكة | `lib/api.ts` · `main.tsx` |
| **VIS-04** | التأكيد الوحيد `toast.success` عابر — يختفي فلا يبقى أثر يثبت الحفظ. (تصحيح لادّعاء التدقيق الأصليّ: الـToast **كان** موجودًا؛ العيب أنّه غير باقٍ) | مؤشّر ثابت `data-testid="draft-saved-indicator"` بوقت آخر حفظ ناجح | `pages/SubmissionsPage.tsx` |
| **VIS-05** | «الحالة» و«تاريخ التسليم» كانا **عمودَي `SGrid`** لا حقلَين خاطئَي النوع؛ وأعمدة الجدول نصّ حرّ بطبيعتها ⟹ لا تحقّق ولا تجميع ولا مقارنة عبر الأسابيع | إصدار محكوم: `SSelectCat("work_status", …)` + `SDate("delivery_date", …)` بصفّ لكلّ مقال، بحارس خمول `IsSeoArticlesGoverned`. **`work_status` مستعارة بقرار مالك المنتج** — الكتالوج الرسميّ (22 نطاقًا) لا يحوي `seo_status` واستحداث نطاق محظور | `Persistence/TemplateSeeder.cs` |

**عدد الملفّات المعدَّلة: 15 · المُنشأة: 4 · الهجرات: 0 · الكتابة الخام على SQL: 0**
(الثلاثة الإضافيّة اختباريّة بحتة: `ReportsTests.cs` · `PfeNumericIsolatedFactory.cs` · `RepeatableNumericValidationIntegrationTests.cs` — انظر §2ب و§2ج.)

---

## 2) بوّابات البناء والتحقّق

| البوّابة | النتيجة | البرهان |
|---|---|---|
| `dotnet build Reporting.sln` | **0 أخطاء** | 5 تحذيرات `CS8604` سابقة، كلّها في ملفّات اختبار خارج نطاق التعديل |
| `npx tsc --noEmit` | **0 أخطاء** | رمز خروج 0 |
| **الواجهة — اختبارات الوحدة** | **857/857** (75 ملفًّا) | خطّ الأساس 844 + 13 اختبارًا جديدًا. **صفر انحدار** |
| `npx eslint .` | **`LINT_DELTA = 0`** | 43 مشكلة (26 خطأ · 17 تحذير) **قبل وبعد بالتساوي**، قيست على شجرة عمل مرجعيّة مستقلّة على `BASE_SHA`. الفروق كلّها إزاحة أرقام أسطر وحشو مسافات. **اللينت يفشل أصلًا في خطّ الأساس** في ملفّات لم تُمسّ (`ActionResultToast`, `DailyCalendarPicker`, `PresentationProfileReport`) |
| **الخادم — VIS-02ب + VIS-05 + TaxonomyV4** | **30/30** | قاعدة `createdb` معزولة نظيفة |
| **الخادم — الحزمة الكاملة** | **2311/2311 · 618/618** | تكامل على **ثلاث** قواعد نظيفة منفصلة (`TEST_DB_CONNECTION` · `_PFE` · `_PFE_NUM`) + وحدة. التفصيل في §2ب |

### 2ب) تصحيح حاكم — الحزمة الكاملة كشفت سقوطَين، وهذا المستند كان يدّعي `REGRESSIONS = 0` قبل قياسها

النسخة الأولى من هذا الـCheckpoint أعلنت `REGRESSIONS = 0` استنادًا إلى اختبارات النطاق وحدها (30/30) **قبل** تشغيل الحزمة الكاملة. التشغيل الكامل أعطى **2309 ناجحًا و2 ساقطَين**. أُبقي هذا السطر عمدًا: الادّعاء كان سابقًا لدليله، والتصحيح جزء من السجلّ لا يُمحى.

| السقوط | الحكم بعد القياس | البرهان |
|---|---|---|
| `ReportsTests.SeoRollup_Employee_MergesTwoTemplatesWithAutoNetKeywords` (`Expected 8, Actual 0`) | **انحدار حقيقيّ منّي** | خطّ الأساس `865c0edd` بقاعدة نظيفة: ناجح. السبب: v5 من VIS-05 صار يفرض أربعة حقول فرعيّة إلزاميّة، بينما الاختبار يرسل `answers:{}` الفارغ ⟹ `PUT /values` يُرفَض 400. والمعينة `SubmitSeoArticlesAsync` **لم تكن تتحقّق من نجاح الحفظ إطلاقًا**، فمرّ الرفض صامتًا وظهر العطل بعيدًا عن سببه كأصفار في التجميع |
| `RepeatableNumericValidationIntegrationTests.AboveMax_Rejected400_AboveMaxCode` | **ليس انحدارًا — عيب اختباريّ كامن (`TEST-ISO-01`)** | انظر أدناه |

**إصلاح الانحدار:** معينة `ArticlesSectionValueAsync` تبني عنصر PRS صالحًا للقالب المحكوم، و**قيمة الحالة تُقرأ من لقطة خيارات القالب المنشور نفسه** لا تُكتَب حرفيًّا (فلا يكسرها تغيّر مفردات الكتالوج). وأُضيف `Assert.True(saved.IsSuccessStatusCode, …)` إلى **كلتا** معينتَي تسليم SEO: رفضٌ صامت للحفظ لن يتنكّر بعد اليوم في هيئة تسليم أخضر.

### 2ج) `TEST-ISO-01` — مصنعان «معزولان» يتقاسمان قاعدة واحدة

`ProjectFirstIsolatedFactory` و`PfeNumericIsolatedFactory` **كلاهما** كان يقرأ `TEST_DB_CONNECTION_PFE` ويؤول إلى `reporting_pfe_iso`، رغم أنّ توثيق كلٍّ منهما ينصّ على قاعدة «منفصلة… لا تمسّ المشتركة». مجموعتان تعملان بالتوازي على قاعدة واحدة:

- **على قاعدة مهاجَرة سلفًا** يمرّ التصادم صامتًا في الغالب (ويظهر أحيانًا كسقوط مفرد متقطّع — وهو بالضبط ما رأيناه في `AboveMax`).
- **على قاعدة نظيفة** تتصادم `MigrateAsync` المتزامنة: `23505 pg_type_typname_nsp_index` و`42701 column … already exists`.

**البرهان التجريبيّ (المصنفان وحدهما، 21 اختبارًا، قاعدة PFE نظيفة):**

| الشجرة | قبل الإصلاح | بعد الإصلاح |
|---|---|---|
| شجرة العمل (تغييراتي) | **13 ساقطًا** | **21/21** |
| خطّ الأساس `865c0edd` | **10 ساقطة** | — (لم يُمسّ) |

سقوط خطّ الأساس بنفس الرمز يُثبت أنّ العيب **سابق لتغييراتي**، وأنّ الفرق 13/10 توقيت سباق لا فرق سلوك. الأثر الحاكم: **قياس حزمة الخادم على قواعد نظيفة كان متعذّرًا أصلًا** — وهو بروتوكول القياس المُلزِم. الإصلاح اختباريّ بحت: مفتاح `TEST_DB_CONNECTION_PFE_NUM` وقاعدة `reporting_pfe_num_iso` خاصّان بتذكرة التحقّق الرقميّ (تُنشَآن تلقائيًّا عند أوّل هجرة). **صفر مساس بكود الإنتاج.**

### تصحيح عقد كان يجب تحديثه
`Project360Page.test.tsx` كان يثبّت «التبويبات الثمانية فقط». صار **تسعة** بترتيب مقصود: التقارير تلي «تحديثات التنفيذ» وتسبق «القرارات والحوكمة» لأنّها **مُدخَل** القرار لا نتيجته.

---

## 3) الاختبارات الجديدة (13 واجهة + 10 خادم)

- `lib/format.test.ts` (+4) — VIS-01: تغليف FSI/PDI بالضبط · عزل الطرفين على حدة · لا فاصل معلّق بلا عميل · **قابليّة التجريد** (المحارف لا تلوّث البحث والنسخ).
- `lib/queryRetryPolicy.test.ts` (+4, جديد) — VIS-03: صفر إعادة على ستّة أكواد 4xx · محاولتان على 5xx · إعادة على انقطاع الشبكة بلا استجابة · حدّ أقصى لا يُتجاوَز.
- `pages/DraftSavePersistentConfirmation.test.tsx` (+3, جديد) — VIS-04: غياب المؤشّر قبل الحفظ · ظهوره وبقاؤه بعد نجاح · **غيابه عند الفشل** (النصف السالب: مؤشّر يظهر بلا حفظ أسوأ من لا مؤشّر).
- `pages/Project360Page.test.tsx` (+2) — VIS-02: كسلٌ (لا نداء قبل الفتح، ونداء واحد عنده) · أعمدة القرار المُثراة. **المطابقة على `textContent` حرفيًّا** لأنّ مطابِق Testing Library الافتراضيّ يطبّع المسافات فيبتلع `\n` ⟹ ادّعاء «الأسطر محفوظة» كان سينجح حتّى لو دُمِجت.
- `SeoArticlesGovernedTemplateTests.cs` (5, جديد) — VIS-05: القائمة محكومة بنطاق `work_status` · التاريخ مكتوب النوع · **لا `Grid` حرّ باقٍ** (نفي العيب لا إثبات الإصلاح فقط) · الإصدار المحكوم هو الأحدث والوحيد المنشور والأقدم يبقى مقروءًا · **خمول: إصدار محكوم واحد بالضبط**.
- `ProjectLinkedReportsDecisionFieldsTests.cs` (5, جديد) — VIS-02ب: **العدّ مقصور على المشروع** (3 و2 داخل تسليم واحد؛ عدّاد كسول كان سيعيد 5 لكليهما) · v1 القديم = بند واحد لا صفر · اسم القالب حاضر · حقول القرار خالية قبل أيّ قرار · المواضع التسعة الأصليّة سليمة.

---

## 4) الضوابط السالبة — 5/5 اجتازت

كلّ ضابط: حقن العيب الأصليّ → إثبات سقوط الاختبار → استعادة **مطابقة بالبصمة SHA-256**.

| # | العيب المحقون | النتيجة | بصمة الاستعادة |
|---|---|---|---|
| NEG-1 | `bidiIsolate` تُعيد النصّ خامًا | **3 سقطت** | `3c7ef7c5…` مطابقة |
| NEG-2 | `shouldRetryQuery` تعيد المحاولة 3 مرّات بلا نظر للحالة (العيب الأصليّ حرفيًّا) | **3 سقطت** | `ecca80ce…` مطابقة |
| NEG-3 | حذف `setSavedAtUtc` ⟹ Toast عابر فقط | **1 سقط** | `b50bca94…` مطابقة |
| NEG-4 | حذف تبويب التقارير من Project 360 (العيب الأصليّ حرفيًّا) | **3 سقطت** | `cceffa84…` مطابقة |
| NEG-5 | تعطيل `UpgradeSeoArticlesTemplateAsync` — على **قاعدة نظيفة جديدة** لا على قاعدة تحمل الإصدار المحكوم سلفًا | **5 سقطت** | `266a3891…` مطابقة · القاعدة حُذِفت |

---

## 5) ما لم يُصلَح عمدًا

- **خلط الأرقام العربيّة/الهنديّة (#9):** `toLocaleString('ar-EG')` يظهر **44 مرّة في 14 ملفًّا**، أكثرها في صفحات KPI والمبيعات والرواتب — **خارج نطاق R22B صراحةً**. إصلاح جزئيّ كان سينتج تنافرًا أسوأ من الحالة الراهنة. **`DEFERRED_OUT_OF_SCOPE`**.
- **`EVID-01`** (أسطح الخطأ/الفراغ/الفشل لم تُلتقَط أصلًا في بوّابة 1 سبتمبر): يُغلَق في المرحلة C بالتقاط فعليّ لسطح الخطأ.

---

## 6) الحالة والخطوة التالية

`PHASE_B_STATUS = COMPLETE` · `NEGATIVE_CONTROLS = 5/5` · `LINT_DELTA = 0`

`REGRESSIONS_INTRODUCED = 1` · `REGRESSIONS_REMAINING = 0` (انحدار `SeoRollup` اكتُشِف وأُصلِح — §2ب)
`PRE_EXISTING_DEFECTS_FOUND = 1` (`TEST-ISO-01`، أُصلِح اختباريًّا — §2ج)

**البوّابة النهائيّة للمرحلة B:** خادم **2311/2311** تكامل + **618/618** وحدة على ثلاث قواعد نظيفة · واجهة **857/857** · `tsc` 0 · `LINT_DELTA` 0 · بناء الحلّ 0 أخطاء 0 تحذيرات.

**التالي — المرحلة C:** البناء ببصمة أثر، لقطة خطّ أساس TEST، النشر على TEST **فقط**، ثمّ الـUAT التشغيليّ والبصريّ الكامل بما فيه سطح الخطأ.
