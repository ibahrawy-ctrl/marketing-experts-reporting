# R22B — نقطة تفتيش المرحلة C3: الـUAT التشغيليّ والبصريّ على TEST

**التاريخ:** 2 سبتمبر 2026 · **الفرع:** `fix/r22b-reporting-visual-operational-closure-20260902` · **المرشَّح:** `dc47c0be`
**البيئة:** TEST وحدها. `RC_TOUCHED = NO` · `PROD_TOUCHED = NO` · `MIGRATIONS_RUN = 0` · `RAW_SQL_WRITE = 0`.

---

## 1) على أيّ شيء نُفِّذ الـUAT بالضبط (وحدود ما يُثبته)

بوّابةٌ تخاطب `localhost` لا تُثبت شيئًا عن حزمة المتصفّح، ونجاح `curl` لا يُثبت شيئًا عن الواجهة.
لذلك نُفِّذت الرحلات على **بايتات `/opt/reporting-test/frontend/dist` المنشورة نفسها**، تحت **اسم الأصل الحقيقيّ** `https://test.emarketingacademy.net` في نظر المتصفّح.

| العنصر | القيمة |
|---|---|
| الأصل في نظر المتصفّح | `https://test.emarketingacademy.net` عبر `--host-resolver-rules=MAP … 127.0.0.1:8443` |
| البايتات المقدَّمة | نسخة `rsync` حرفيّة من `/opt/reporting-test/frontend/dist` (الأصل المنشور) |
| الأصل الخلفيّ | نفق SSH `-L 15091:127.0.0.1:5091` ⟵ `khubara-reporting-test.service` |
| الحزمة | `index-CpDO9pG-.js` · بيان `bb487f62…6f125b` (مطابق تمامًا لِما في C2) |

**سبب الالتفاف على nginx:** الموقع العامّ لـTEST خلف `auth_basic` (يُرجِع 401 على `/`، و`/health` وحده 200)، وتجزئات `htpasswd` غير قابلة للعكس. الالتفاف يمسّ **طبقة الحافّة فقط**؛ لا حزمة الواجهة ولا الخادم ولا القاعدة.

**تصريح حدود:** ما لم يُقَس في هذه المرحلة: طبقة nginx نفسها (رؤوس، ضغط، تخزين مؤقّت، شهادة)، والأداء تحت حمل، والمتصفّحات غير Chromium.

---

## 2) بيانات الـUAT: لماذا حسابات جديدة، ومسار إنشائها

تقارير R22C الخمسة كانت `Closed` — وحالة الإغلاق **نهائيّة** (الإرجاع يُرفَض بـ403 لغير المعتمِد و**409** للمشرف)، والدورة التالية مغلقة (`calendar.cycle_not_open`). ⟹ لا سبيل لإعادة تشغيل الرحلة على نفس الحسابات.

`Ops/R22B/tools/r22b-closure-uat-provision.py` أنشأ **5 موظّفين جددًا** عبر واجهات المنتج الرسميّة حصرًا، **يرثون** من نظرائهم في R22C: المسمّى الوظيفيّ (`PATCH /api/directory/users/{id}/job-role` — وهو ما يقود الاستحقاق، لا الدور الأمنيّ)، والإدارة والفريق والمدير والأدوار. العميل والمشروعات **أُعيد استعمالها كما هي** (لا كيانات جديدة).

| التخصّص | الحساب | القالب | المشروع |
|---|---|---|---|
| content | `r22bc-content-c1@r22uat.test` | تقرير كاتب المحتوى الأسبوعي | R22C — مشروع المحتوى |
| design | `r22bc-design-c1@r22uat.test` | تقرير فريق التصميم | R22C — مشروع التصميم |
| video | `r22bc-video-c1@r22uat.test` | تقرير فريق الفيديو | R22C — مشروع الفيديو |
| moderation | `r22bc-moderation-c1@r22uat.test` | تقرير المديرشن الأسبوعي | R22C — مشروع المديرشن |
| seo | `r22bc-seo-c1@r22uat.test` | تقرير متابعة مقالات SEO الأسبوعي | R22C — مشروع SEO |

المراجِع: `r22c-lead@r22uat.test` · مدير الحساب: `r22c-am@r22uat.test` · المشروع خارج النطاق: `ef4a0e14…`.

**دَين تنظيف مسجَّل للمرحلة D:** الحسابات الخمسة الجديدة تُعطَّل، **و7 حسابات R22C تُعاد إلى `isActive=false`** كما كانت قبل هذه الجلسة (الحالة السابقة محفوظة حرفيًّا في `/tmp/r22b-uat/reactivated.json`). `HARD_DELETE = FORBIDDEN` ⟹ التنظيف بالتعطيل لا بالحذف.

---

## 3) الرحلة التشغيليّة للموظّف — 4 تخصّصات ببنود عمل متعدّدة

`emp.json` · لقطات `emp-<slug>-01…08`.

| القياس | content | design | video | moderation |
|---|---|---|---|---|
| VIS-01 عزل ثنائيّ الاتّجاه في خيار المشروع (FSI `U+2068` + PDI `U+2069`) | ✓/✓ | ✓/✓ | ✓/✓ | ✓/✓ |
| بندا عمل مضافان | 2 | 2 | 2 | 2 |
| **الأسطر محفوظة بعد إعادة التحميل** | ✓ | ✓ | ✓ | ✓ |
| **البندان متمايزان (لا ابتلاع)** | ✓ | ✓ | ✓ | ✓ |
| VIS-04 مؤشّر الحفظ: قبل ✗ / بعد ✓ / يبقى ✓ | ✗→✓→✓ | ✗→✓→✓ | ✗→✓→✓ | ✗→✓→✓ |
| تعديل مستقلّ: البند 1 لم يُمَسّ / البند 2 تغيّر | ✓/✓ | ✓/✓ | ✓/✓ | ✓/✓ |
| أُرسل للاعتماد | ✓ | ✓ | ✓ | ✓ |
| **أخطاء Console / إخفاقات API** | 0 / 0 | 0 / 0 | 0 / 0 | 0 / 0 |

قيمة حاكمة حرفيّة من `content` بعد إعادة التحميل (لاحظ `\n` الحقيقيّة — التطبيع بالمسافات كان سيبتلعها ويمرّر الادّعاء زورًا):

```
البند 1: "البند الأوّل — حملة «إطلاق المنتج».\nسطر ثانٍ يثبت تعدّد الأسطر.\nسطر ثالث."
البند 2: "البند الثاني — حملة «عروض نهاية الأسبوع».\nمهمّة مختلفة كلّيًّا.\nسطر ثالث مختلف."
بعد التعديل المستقلّ:
البند 1: (كما هو، حرفيًّا)
البند 2: "البند الثاني بعد تعديل مستقلّ.\nالسطر الثاني تغيّر وحده.\nالسطر الثالث كما هو."
```

**تصحيح لقراءة خاطئة محتملة:** الحقل `unexpectedTemplates` في `emp.json` غير فارغ (29 عنصرًا). هذا **ليس** إشارة استحقاق: المنتقي المقروء هو **مُرشِّح** صفحة «تقاريري» ويعرض كلّ القوالب بطبيعته، لا قائمة استحقاق الموظّف. `TEMPLATE_ENTITLEMENT = NOT_MEASURED_HERE`.

**تصحيح ثانٍ:** المُدخَلة `emp.seo` في `emp.json` تحمل `ERROR` — وهي **بائتة ومُتجاوَزة**: القالب المحكوم لا يحتوي بنود عمل أصلًا (بطاقة المشروع نفسها هي صفّ المقال)، فسقط المُعبِّئ العامّ. الرحلة الصحيحة نُفِّذت في `PART=seo` وحالتها `DONE` (§4).

---

## 4) رحلة SEO والقالب المحكوم (VIS-05) على الحزمة المنشورة

`seo.json` · لقطات `seo-01…04`.

| القياس | النتيجة |
|---|---|
| بطاقتا مقال على نفس التقرير | 2 |
| «حالة المقال» قائمة منسدلة وقيمها من الكتالوج | ✓ — `["Draft","Published"]` مأخوذة حرفيًّا من `execution_taxonomy_values` نطاق `work_status` |
| «تاريخ التسليم» حقل تاريخ حقيقيّ (`input[type=date]`) | ✓ |
| حقول حالة نصّ حرّ | **0** |
| الصفّان متمايزان بعد إعادة التحميل | ✓ |
| أُرسل للاعتماد | ✓ |
| أخطاء Console / إخفاقات API | 0 / 0 |

**قراءة سالبة كاذبة يجب عدم تصديقها:** `vis05.statusIsSelect = false` في `seo.json` **خطأ في الاستدلال لا في المنتج** — مُلتقِط التسمية أمسك نصّ الخيار النائب «اختر…» بدل التسمية «حالة المقال». الأدلّة الموجبة الثلاثة المستقلّة: (أ) `statusValuesFromCatalog = ["Draft","Published"]` أي وُجد `<select>` قيمه من الكتالوج؛ (ب) لقطة `seo-01-governed-fields.png` تُظهر «حالة المقال *» منسدلة و«تاريخ التسليم *» حقل تاريخ بأيقونة تقويم؛ (ج) إثبات قاعدة TEST في نقطة تفتيش C2 (إصدار واحد منشور = v3، `catalogDomain` مربوط، لا شبكة نصّ حرّ داخل القسم المتكرّر).

**تمييز صادق للأسطر المتعدّدة في SEO:** حقل `ملاحظات` (نصّ طويل/`textarea`) حفظ `\n` الحقيقيّة عبر إعادة التحميل. أمّا `عنوان المقال` و`الكلمة المفتاحية` و`المراجع` و`رابط المنشورة` فهي حقول سطر واحد (`<input>`) وابتلاعها للأسطر **سلوك HTML صحيح لا عيب**. ⟹ `SEO_MULTILINE_SCOPE = NOTES_FIELD_ONLY_BY_DESIGN`.

---

## 5) طابور المراجِع والاعتماد — تصحيح جوهريّ

`review.json` · لقطات `review-01…03`.

**العيب في البوّابة نفسها:** الجولة السابقة قصدت مسار `/app/reviews` — وهو **غير موجود في التطبيق إطلاقًا**، فأعطى طابورًا فارغًا وكاد يُقرأ «لا شيء للاعتماد». الطابور الحقيقيّ هو تبويب **«بانتظار اعتمادي»** داخل `/app/submissions`، والاعتماد يقع في صفحة التقرير تحت ترويسة **«إجراء الاعتماد»**. صُحِّحت البوّابة وأُعيد التنفيذ.

**عيب ثانٍ صُحِّح:** الطابور قد يُقاس وهو في حالة تحميل، فتُقرأ صفر صفوفٍ على أنّها فراغ. أُضيف انتظارٌ صريح لظهور صفٍّ **أو** لنصّ الفراغ الحرفيّ «لا توجد تقارير بانتظار اعتمادك».

| القياس | النتيجة |
|---|---|
| الطابور قبل الاعتماد | **5** تقارير (content · design · video · moderation · seo)، كلّها `مُرسَل` 2026-W36 |
| اعتُمدت | **5/5** (2 في التنفيذ الأوّل + 3 بعد التصحيح) |
| الطابور بعد الاعتماد | **0** |
| بطاقة «إجراء الاعتماد» ظاهرة للمراجِع | 5/5 |
| الأسطر المتعدّدة تصل سطح المراجِع محفوظةً | **4/4** حيث ينطبق (SEO خارج النطاق بالتصميم — §4) |
| أخطاء Console / إخفاقات API | 0 / 0 |

لقطة `review-02-reviewer-sees-multiline.png` تُظهر على شاشة المراجِع: مسار اعتماد مكتمل، بطاقة مشروع واحدة تحوي **بندَي عمل مستقلَّين** («بند 1» و«بند 2») بقيم مختلفة، وملاحظات كلّ بند **بثلاثة أسطر مرئيّة منفصلة**، وأزرار «اعتماد / إعادة للتعديل / تصعيد».

---

## 6) Project 360 — VIS-02 · VIS-02ب · VIS-03

`p360.json` · لقطات `p360-01…03`.

| القياس | النتيجة |
|---|---|
| **VIS-02** نداءات تقارير المشروع **قبل** فتح التبويب | **0** |
| **VIS-02** نداءات **بعد** فتح «التقارير المرتبطة» | **1** (تحميل كسول فعليّ، لا استباقيّ) |
| **VIS-02ب** أعمدة الجدول | القالب · مُقدِّم التقرير · الدورية · الفترة · الحالة · **تاريخ الإرسال** · **آخر تحديث** · **بنود العمل** · **آخر قرار اعتماد** · **سبب الإرجاع** |
| الشريحة مقصورة على المشروع | ✓ (عدد بنود العمل مختلف بين المشروعات على نفس الصفحة) |
| **VIS-03** زمن الوصول إلى سطح خطأ مستقرّ | **1 529 مللي ثانية** (السقف 20 000) |
| **VIS-03** إعادات المحاولة | **0** — 3 نداءات 404 فقط (`/projects/{id}` · `/summary` · `/reports`)، لا 12 |

النصّ المعروض عند تجاوز النطاق: «تعذّر عرض المشروع — قد يكون المشروع خارج نطاق صلاحيتك أو غير موجود.» + «إعادة المحاولة». **الرسالة لا تُفرِّق بين «غير موجود» و«خارج صلاحيتك» — وهذا مقصود** (منع تعداد الموارد)، ومتّسق مع قاعدة «404 لا 403».

---

## 7) البوّابة البصريّة — مقاسان × 6 أسطح

`visual.json` · لقطات `visual-{desktop,mobile}-*`.

| السطح | 1440×1080 | 390×844 |
|---|---|---|
| `/app` · `/app/submissions` · `/app/projects/{id}` · `/app/projects/{id}/360` | لا تجاوز أفقيّ (`scrollW = clientW = 1440`) · `dir=rtl` | لا تجاوز أفقيّ (`scrollW = clientW = 390`) · `dir=rtl` |
| **سطح الخطأ** (مشروع خارج النطاق) | «تعذّر عرض المشروع» + «إعادة المحاولة» | مطابق |
| **سطح الفراغ** (معرّف أصفار) | مطابق للخطأ | مطابق |
| **سطح التحميل** | «يتم تحميل بيانات المشروع…» + دوّارة، داخل هيكل التطبيق | مطابق |

**عيب في البوّابة صُحِّح (يستحقّ التسجيل):** الالتقاط الأوّل لسطح التحميل استعمل خنق الشبكة العامّ `Network.emulateNetworkConditions`، فجوّع **تحميل الخطوط** أيضًا وأسقط `page.screenshot` بـ«waiting for fonts to load» — أي أنّ السطح لم يُلتقَط أصلًا. التصحيح: اعتراض `**/api/projects/**` وحده بتأخير 9 ثوانٍ. محاولة وسطى بتأخير `**/api/**` كلّه التقطت **دوّارة إقلاع الغلاف** («جارٍ التحميل…») لا سطح تحميل الصفحة المقصود، فضُيِّق النطاق حتّى صار السطح الملتقَط هو المقصود فعلًا.

**تصريح حدود:** لم تُقَس أوضاع التباين العالي ولا الوضع الداكن ولا القارئ الشاشيّ في هذه المرحلة.

---

## 8) خلاصة العيوب المفتوحة من سجلّ المرحلة A

| المعرّف | الحالة على TEST | الدليل |
|---|---|---|
| VIS-01 | **مغلق** | عزل FSI/PDI في 4/4 تخصّصات |
| VIS-02 | **مغلق** | 0 نداء قبل التبويب · 1 بعده |
| VIS-02ب | **مغلق** | الأعمدة الأربعة الجديدة حاضرة · الشريحة مقصورة على المشروع |
| VIS-03 | **مغلق** | سطح خطأ في 1.53 ث · 0 إعادة محاولة |
| VIS-04 | **مغلق** | ✗ قبل الحفظ → ✓ بعده → ✓ باقيًا |
| VIS-05 | **مغلق** | منسدلة من الكتالوج + حقل تاريخ · 0 نصّ حرّ · إصدار منشور واحد (v3) |
| تعدّد الأسطر (5 أسطح) | **مغلق** | محفوظ عبر الحفظ وإعادة التحميل والاعتماد؛ SEO محصور في «ملاحظات» بالتصميم |

**عيوب البوّابة (لا المنتج) المكتشفة والمصلَحة في هذه المرحلة:** مسار `/app/reviews` المخترَع · قياس طابور أثناء التحميل · خنق شبكة عامّ يجوّع الخطوط · مُعبِّئ عامّ لا يناسب قالب SEO المحكوم.

---

## 9) حالة نهاية المرحلة

```
PHASE_C3_STATUS          = COMPLETE
EMPLOYEE_JOURNEYS        = 5/5   (4 ببنود عمل + SEO المحكوم)
REVIEWER_APPROVALS       = 5/5   (الطابور بعدها = 0)
VISUAL_GATES             = VIS-01..VIS-05 ALL_CLOSED
MULTILINE_ACROSS_SURFACES= PRESERVED (إنشاء · حفظ · إعادة تحميل · تعديل مستقلّ · اعتماد)
CONSOLE_ERRORS           = 0
API_FAILURES             = 0 عدا 404 المقصودة لسطحَي الخطأ والفراغ
HORIZONTAL_OVERFLOW      = 0 عند 1440 و390
MIGRATIONS_RUN           = 0
RAW_SQL_WRITE            = 0
RC_TOUCHED               = NO
PROD_TOUCHED             = NO
CLEANUP_DEBT             = 5 حسابات جديدة تُعطَّل + 7 حسابات R22C تُعاد إلى isActive=false
```

**الخطوة التالية — المرحلة D:** تنظيف TEST بتشغيل جافّ أوّلًا، تفكيك النفق والخادم المحلّيّ، إزالة الوصلة الرمزيّة لـ`node_modules` من شجرة العمل المعزولة، توثيق الأدلّة، التزامات مُنطَقة بالملفّ (لا `git add .`)، دفع الفرع، ثمّ Fast-Forward إلى `develop` **إن اجتازت كلّ البوّابات**.
