# ملحق — تصويب مرجعيّة عدد اختبارات الواجهة (2 سبتمبر 2026)

> **قاعدة التصويب:** التقارير التاريخيّة **لا تُعدَّل**. ما قالته لحظةَ القياس هو سجلّ صادق لتلك اللحظة، وتعديله بأثر رجعيّ يمحو أثر التغيّر ويجعل السجلّ غير قابل للتدقيق. التصويب يُسجَّل هنا، والتقرير الحاكم الجديد وحده يحمل الرقم الحاليّ.

## 1) المرجعيّة الحاليّة

```
FRONTEND_TEST_COUNT_AUTHORITY = 857/857
bb37d1a baseline              = 844
current 5b0febf               = 857
delta                         = +13 اختبارًا مقصودًا
missing tests                 = 0
```

المصدر المقيس: `evidence/measurements/r22b-fe-tests.log` — `857/857` · 75 ملفّ اختبار · 0 فشل، على `5b0febf` داخل شجرة العمل المعزولة `/private/tmp/r22b-final-closure-20260902`.

## 2) تفسير الفارق

| النقطة | العدد | ما تعنيه |
|---|---|---|
| `bb37d1a` (1 سبتمبر) | 844 | مرجعيّة الهوتفكس الوظيفيّ للأسطر المتعدّدة |
| `dc47c0b` (2 سبتمبر) | — | الالتزام الوظيفيّ للإغلاق البصريّ والتشغيليّ؛ **أضاف ملفَّي اختبار** |
| `5b0febf` (2 سبتمبر) | **857** | رأس `origin/develop` وقت هذا الإغلاق |

الفارق **+13 = زيادة مقصودة** ناتجة عن إضافة تغطية جديدة، لا عن تغيّر في سلوك اختبار قائم.

**الضابط الحاسم:** `missing tests = 0`. لم يُحذف اختبار، ولم يُعطَّل (`skip`/`only`)، ولم يُخفَّف تأكيد. `857 − 844 = 13` وكلّ الـ13 إضافات صافية. لو كان الفارق سالبًا أو مختلطًا (إضافات تُخفي حذفًا) لَما جاز اعتماد المرجعيّة الجديدة بلا جرد اسميّ للاختبارات المفقودة.

## 3) تصويب السجلّ التاريخيّ (بلا تعديل مصدره)

سطر مُسجَّل سابقًا في مواضع توثيقيّة سابقة: `FRONTEND_TEST_COUNT_AUTHORITY = 844/844`.

- **الحالة:** صحيح لحظةَ قياسه على `bb37d1a`؛ **متقادم** كمرجعيّة للرأس الحاليّ `5b0febf`.
- **الإجراء:** لم يُعدَّل أيّ تقرير تاريخيّ ولا أيّ ملفّ ذاكرة. هذا الملحق هو موضع التصويب الوحيد.
- **الاستعمال الصحيح:** أيّ مقارنة مستقبليّة تُبنى على `857/857` @ `5b0febf`، مع الاحتفاظ بـ`844 @ bb37d1a` كنقطة أساس تاريخيّة.

## 4) ما لم يُمسّ

| البند | الحالة |
|---|---|
| التقارير التاريخيّة | **لم تُعدَّل** |
| `MEMORY.md` وملفّات الذاكرة | **لم تُمسّ** (لا تصريح) |
| كود المنتج / كود الاختبار / الهجرات | **لم يُمسّ** |
