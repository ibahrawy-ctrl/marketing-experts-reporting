# R22B — تقرير الإغلاق النهائيّ (2 سبتمبر 2026)

> قاعدة الصدق المطبَّقة في كلّ سطر: **PROVEN / MEASURED / NOT_MEASURED / NOT_RUN / BLOCKED**.
> لا يُحوَّل تطابق كود إلى إثبات سلوك، ولا نجاح اختبار إلى إثبات بصريّ، ولا UAT جزئيّ إلى إغلاق كامل.

## 0) القرار الحاكم

| البند | القيمة |
|---|---|
| `FINAL_CLOSURE` | **PASS** |
| `RC_PROMOTION` | **NOT_AUTHORIZED** |
| `PROD_PROMOTION` | **NOT_AUTHORIZED** |
| `MULTILINE_GATE` | **PASS (PROVEN LIVE — 5/5 أسطح)** |
| `REVIEW_QUEUE_TRUTH` | **PASS (قبل=1 ← بعد=0)** |
| `VISUAL_GATE_VIS_01..05` | **PASS (5/5 بقياس حيّ)** |
| `TEST_GATES` | **PASS (857 + 618 + 2311)** |
| `MANIFEST_MATCH` | **EXACT** |
| `CLEANUP_INTEGRITY` | **PASS (FAILED=0 · MISMATCH=0)** |
| `REPORTING_UAT_CONFIDENCE` | **HIGH** |

## 1) مراجع الحقيقة (أُعيد قياسها في نهاية الجلسة)

| المرجع | القيمة | الحالة |
|---|---|---|
| `origin/develop` | `5b0febfb5ac62bba92cb6e49363d63fce115b568` | لم يتحرّك |
| `origin/main` | `508509ad8474b321c80cbdd48eb84ecb54bee212` | لم يُمسّ |
| الإنتاج | `d25dc696…` · قاعدة `reporting_prod` | لم يُمسّ |
| RC | `d25dc696…` · قاعدة `reporting_rc` | لم يُمسّ |
| TEST | `dc47c0be…` · قاعدة `reporting_test_uat` | سطح القياس |
| شجرة العمل المعزولة | `/private/tmp/r22b-final-closure-20260902` · فرع `closure/r22b-final-closure-20260902` @ `5b0febf` | **نظيفة (0 تغييرات مُتتبَّعة)** |
| التزامات دمج في `cd09b67..origin/develop` | **0** | Fast-Forward خالص |
| نَسَب `5b0febf` داخل `develop` | `YES` | مُتحقَّق |
| بصمة الحزمة المنشورة على TEST | `5b17f9d75ead23c564daf82c8abc04d1ac9b556ef9ec706f42cec37d1e239328` | مطابقة للبايتات المخدومة محلّيًّا، ولم تتغيّر طوال الجلسة |

**كلّ القياسات الحيّة أدناه جرت على هذه البصمة بعينها** — لا على مصدر ولا على بناء موازٍ.

## 2) بوّابات الاختبار (المرحلة 10) — MEASURED

| البوّابة | النتيجة | المصدر |
|---|---|---|
| الواجهة (vitest) | **857/857** · 75 ملفًّا · 0 فشل | `evidence/measurements/r22b-fe-tests.txt` |
| الخادم — وحدة (xUnit) | **618/618** · 0 فشل | `evidence/measurements/r22b-be-unit.txt` |
| الخادم — تكامل (xUnit) | **2311/2311** · 0 فشل · 11د42ث · `EXIT=0` | `evidence/measurements/r22b-be-int2.txt` |
| حارس الحزمة المبنيّة | **`BUNDLE_MULTILINE_GATE=PASS`** · 7/7 فحوص · `GATE_EXIT=0` | `evidence/measurements/r22b-fe-build-and-bundle-gate.txt` |

حارس الحزمة يفحص `dist/assets/*.js` نفسها لا المصدر، لأنّ سقوط هوتفكس 23 أغسطس صامتًا أثبت أنّ اختبارًا أخضر على المصدر لا يُثبت شيئًا عن الحزمة التي يحمّلها المتصفّح. النتائج المقيسة: `MULTILINE_ELEMENT` (`textarea` · `rows:4` · مرساة واحدة) · `RESIZE_Y=1` · `WHITESPACE_PRE_WRAP=27` · `pre-wrap+break-words=4` · `COMMENT_FIELD_PRESENT=2` · **`NO_ENTER_BLOCKER blocker=none`** · `NO_SECRETS=none`. الفحص السادس يتقاطع مستقلًّا مع القياس الحيّ لضغطات Enter الحقيقيّة في القسم 3 — دليلان متباينا المنهج يتّفقان.

### تصحيح مرجعيّة عدد اختبارات الواجهة (المرحلة 16)

```
FRONTEND_TEST_COUNT_AUTHORITY = 857/857
bb37d1a baseline              = 844
current 5b0febf               = 857
delta                         = +13 اختبارًا مقصودًا
missing tests                 = 0
```

سبب التصحيح: المرجعيّة السابقة `844/844` قِيست على `bb37d1a`، وهي **صحيحة لحظةَ قياسها**. الفارق **+13** يعود إلى ملفَّي اختبار أُضيفا في `dc47c0b` (التزام الإغلاق البصريّ والتشغيليّ)، وليس إلى تغيّر في اختبار قائم. **لم يُفقد أيّ اختبار** (`missing tests = 0`) ولم يُحذف ولا يُتجاوَز أيّ اختبار.

نطاق التصحيح: هذا التقرير **وحده**. التقارير التاريخيّة **لم تُعدَّل** — ما قالته لحظةَ القياس يبقى كما هو، والتصويب يُسجَّل في `ADDENDUM-TEST-COUNT-AUTHORITY-20260902.md` داخل هذا المجلّد. ملفّات الذاكرة **لم تُمسّ**.

### عيب بوّابة اكتُشِف وصُحِّح (وليس عيب منتج)
تشغيل أوّل لاختبارات التكامل أعطى **162 فشلًا** (`Failed! - Failed: 162, Passed: 2149, Total: 2311` · `EXIT=1`). السبب: صدَّرتُ `PHASE2_TEST_DB_CONNECTION` إلى **نفس** قاعدة `TEST_DB_CONNECTION`، بينما `Phase2WebApplicationFactory` يشترط قاعدة **مستقلّة** بالتصميم (`Phase2WebApplicationFactory.cs:69-72`). دمج القاعدتين شغّل عالمَي بذر في قاعدة واحدة فأنتج قالبين بكلّ عنوان وكسر كلّ `Assert.Single()`. بقاعدتين منفصلتين: **0 فشل**. الخطأ خطأ الحارس لا المنتج، وسُجِّل صراحةً بدل تمريره.

| التشغيل | السجلّ | التصنيف |
|---|---|---|
| الخاطئ (162 فشلًا · `EXIT=1`) | `evidence/measurements/r22b-be-int1-ERRONEOUS-GATE-SETUP.txt` | **خطأ إعداد بوّابة — لا يُقرأ حكمًا على المنتج** |
| المرجعيّ (2311/2311 · `EXIT=0`) | `evidence/measurements/r22b-be-int2.txt` | **التشغيل الحاكم** |
| الشاهد على `d25dc69` | `evidence/measurements/r22b-be-int-CONTROL-d25dc69-ABORTED.txt` | **مُجهَض — بلا سطر حكم وبلا `EXIT`؛ لا يُستشهَد به إثباتًا ولا نفيًا** (حُفِظ لئلّا يُخفى أنّ تشغيلًا شاهدًا بُدئ ثمّ تُرك) |

درس مُلزِم: الحكم يُقرأ من سطر `Passed!`/`Failed!` لا من رمز خروج الغلاف — رمز `0` في إشعار المهمّة كان خروج `; echo EXIT=$?` لا خروج `dotnet test`.

## 3) بوّابة الأسطر المتعدّدة (المرحلة 6) — **PROVEN LIVE على خمسة أسطح**

القياس بـ`Range.getClientRects().length` (صناديق الأسطر التي رسمها المتصفّح فعلًا) + `getComputedStyle`، **لا** بوجود صنف CSS.

| # | السطح | القياس الحيّ | الحكم |
|---|---|---|---|
| 1 | **الإدخال** — `textarea` تحت «إجراء الاعتماد» | `tag=TEXTAREA` · `rows=4` · `resize=vertical` · كُتبت 3 أسطر بضغط **Enter حقيقيّ** ⇒ `lines=3` و`matchesIntended=true` | **PASS** — Enter غير مُعترَض |
| 2 | **الحفظ في القاعدة** | `approval_steps.Comment`: `len=90` · `lines=3` · `position(E'\n')=27` | **PASS** |
| 3 | **عرض تعليق الاعتماد** (جلسة متصفّح أخرى) | `renderedLines=8` من 3 أسطر منطقيّة · `white-space:pre-wrap` · `overflow-wrap:break-word` · `direction:rtl` | **PASS** |
| 4 | **جرس الإشعارات** | نفس النصّ: `renderedLines=8` · `pre-wrap` · 4 إشعارات منها 4 متعدّدة الأسطر | **PASS** |
| 5 | **الأرشيف الإداريّ** (داخل نافذة التفاصيل لا الجدول) | خليّتان `td.whitespace-pre-wrap` بـ`renderedLines=11` و`5` | **PASS** |
| 6 | **البريد** — `EncodeWithLineBreaks` | `POST /api/email-control/templates/MANUAL_REMINDER/preview` ⇒ **3 × `<br />`** و`<b>` المُدخَل مُرمَّز إلى `&lt;b&gt;` | **PASS** — الترميز قبل كسر السطر، آمن ضدّ الحقن |
| 7 | **بقاء الأسطر عبر حفظ حقيقيّ + إعادة تحميل** | كُتبت 3 أسطر في «ملاحظات» بند العمل ← «حفظ» ← `reload()` ⇒ `found=true · lines=3` | **PASS** |

**سلوك سالب مُتحقَّق:** الوسوم المُدخَلة (`<b>&amp;</b>`) تُعرَض **نصًّا حرفيًّا** في الواجهة وتُرمَّز في البريد — لا تُفسَّر HTML.

**ملاحظة صدق:** `email_outbox` **فارغ (0 صفّ)** على TEST بالتصميم (`Email__Enabled=false`)، لذا دليل البريد من نقطة المعاينة على الخادم الحيّ لا من الصندوق.

## 4) حقيقة طابور المراجعة (المرحلة 8) — PROVEN

**لا يوجد مسار `/app/reviews`.** الطابور تبويب **«بانتظار اعتمادي»** داخل `/app/submissions`، وفتح التقرير عبر **`?open=<id>`** لا عبر `/app/submissions/<id>` (المسار الأخير لا يوجد — كان سببًا لقراءة كاذبة «صفحة فارغة» صُحِّحت).

| اللقطة | القياس |
|---|---|
| قبل قرار المراجِع | `rows=1` · `emptyText=false` (`C04`) |
| بعد «إعادة للتعديل» | `rows=0` · `emptyText=true` (`C07`) |

بطاقة «إجراء الاعتماد» **لا تظهر لغير المعتمِد المُسنَد** — قيست على ثلاثة تقارير `Submitted` بحساب Admin: `present=false` في الثلاثة. حوكمة سليمة، وليست عيبًا.

## 5) البوّابة البصريّة VIS-01..VIS-05 (المرحلة 5) — MEASURED LIVE

| المعرّف | الادّعاء | القياس الحيّ | الحكم |
|---|---|---|---|
| **VIS-01** | عزل اتّجاه تسميات خيارات المشروع | **5/5** خيارات تحمل `U+2068 … U+2069`؛ أوّل نقطة ترميز = `2068`؛ عيّنة: `"⁨R22C — مشروع SEO⁩ — ⁨R22C — عميل UAT (مؤقّت)⁩"` | **PASS** |
| **VIS-02** | تبويب «التقارير المرتبطة» في Project360 بأعمدة القرار | التبويب موجود · 3 صفوف · الترويسات تضمّ «آخر تحديث» و«بنود العمل» و«آخر قرار اعتماد» و«سبب الإرجاع» | **PASS** |
| **VIS-03** | لا إعادة محاولة على 4xx + رسالة موحّدة | طلبان فقط على المعرّف الوهميّ (`200`, `404`) بلا عاصفة إعادة · الرسالة: «المشروع غير موجود أو أنّه خارج نطاق صلاحيتك.» | **PASS** |
| **VIS-04** | مؤشّر حفظ ثابت | `[data-testid="draft-saved-indicator"]` حاضر بنصّ: «✅ حُفظت المسودّة — آخر حفظ ٢ سبتمبر ٢٠٢٦، ١٠:١٩ م» | **PASS** |
| **VIS-05** | حقول قالب مقالات SEO محكومة لا نصّ حرّ | الإصدار الحاكم `v3` وحده `catalog_backed=t` (v1/v2 = `f`) · `حالة المقال` = `Select` بخيارات الكتالوج · `تاريخ التسليم` = `Date` | **PASS** |

**ملاحظة بيانات بيئة (لا عيب منتج):** قيم `work_status` على TEST بالإنجليزيّة (`Draft/Revision/Approved/Published`).

## 6) البوّابات السالبة والوصوليّة والاستجابة (المرحلة 11)

| البوّابة | القياس | الحكم |
|---|---|---|
| التباين العالي (`forcedColors: active`) | `renderedLines=9` · `white-space:pre-wrap` محفوظ | **PASS** |
| الجوّال 390×844 | `renderedLines=9` · `overflowsX=false` (`scrollW=clientW=390`) | **PASS** — `break-words` يمنع التجاوز الأفقيّ |
| الوضع الداكن (`colorScheme: dark`) | `bodyBg = rgb(246,247,250)` — التطبيق **فاتح فقط** ولا يستجيب لـ`prefers-color-scheme`؛ الأسطر محفوظة | **NOT_APPLICABLE** — لا سمة داكنة في المنتج أصلًا (ليست انحدارًا) |
| حساب معطَّل | `403 auth.account_disabled` على الحسابين | **PASS** |
| متصفّح غير Chromium | لم يُشغَّل | **NOT_RUN** |
| الأداء تحت حِمل | لم يُقَس | **NOT_MEASURED** |

## 7) سلامة البيانات والتنظيف (المرحلة 9) — PASS

**كتابات مقصودة على TEST ضمن نطاق التذكرة، وكلّها أُعيدت:**

| العمليّة | التبرير | الاستعادة |
|---|---|---|
| تفعيل `r22c-lead@r22uat.test` و`r22bml-design@r22uat.test` | بلا معتمِد نشط تستحيل قياس بطاقة «إجراء الاعتماد» حيًّا | أُعيد التعطيل ⇒ `403` |
| تفعيل ثمّ إعادة تعطيل `r22bc-design-c1@r22uat.test` | مسار بديل استُبعِد (تقريره `Closed` نهائيّ) | أُعيد التعطيل |
| **إعادة تعيين كلمة مرور `r22bml-design@r22uat.test`** عبر `POST /api/directory/users/{id}/reset-password` | كلمة المرور القديمة مجهولة (حساب تذكرة سابقة)؛ لا فقد لأنّها كانت مجهولة أصلًا | الحساب معطَّل الآن |
| إرسال تقرير `1d761821` ثمّ إرجاعه بتعليق قياس | إثبات دورة المراجعة كاملة | التقرير عاد إلى `Returned` — **نفس حالته قبل الجلسة** |

**التحقّق الختاميّ على القاعدة:**
- حسابات `%r22uat%`: **26 إجمالًا · 1 نشط فقط** (حساب المشرف الأداتيّ) — مطابق تمامًا لحالة ما قبل الجلسة.
- توزيع حالات التسليمات: `Closed=30 · Returned=9 · Draft=7 · Submitted=3` — **مطابق للأساس**، صفر صافي إنشاء.
- `APPLIED` = 5 · **`FAILED = 0`** · **`MISMATCH = 0`**.
- لم يُستعمل حذف صلب إطلاقًا (`DELETE` للمستخدم = حذف صلب في هذا النظام) — التنظيف بالتعطيل حصرًا.

## 8) رحلات UAT الموثَّقة (المرحلة 7) — 18 رحلة

| # | الرحلة | الشرط المسبق | الإجراء | المتوقَّع | الفعليّ | الدليل | الحكم |
|---|---|---|---|---|---|---|---|
| U01 | دخول الموظّف | حساب مُفعَّل | `/login` | وصول `/app` | `employeeLogin=OK` | `cycle.json` | PASS |
| U02 | تحرير متعدّد الأسطر | تقرير `Returned` | كتابة 3 أسطر في «ملاحظات» | `lines=3` | `typedLines=3` | `V01` | PASS |
| U03 | حفظ حقيقيّ | زرّ «حفظ» | نقر | مؤشّر حفظ ثابت | نصّ مؤشّر بتاريخ | `V01` | PASS |
| U04 | بقاء بعد إعادة تحميل | بعد الحفظ | `reload()` | 3 أسطر باقية | `found=true·lines=3` | `V02` | PASS |
| U05 | إرسال للاعتماد | تقرير قابل للإرسال | «إرسال للاعتماد» | يدخل طابور القائد | طابور القائد = 1 | `C03`,`C04` | PASS |
| U06 | دخول القائد | حساب مُفعَّل | `/login` | `/app` | `leadLogin=OK` | `cycle.json` | PASS |
| U07 | طابور «بانتظار اعتمادي» | تقرير مُرسَل | فتح التبويب | صفّ واحد | `rows=1` | `C04` | PASS |
| U08 | ظهور بطاقة الاعتماد | القائد هو المعتمِد | `?open=<id>` | البطاقة ظاهرة | `present=true` | `C05` | PASS |
| U09 | حقل تعليق متعدّد الأسطر | البطاقة ظاهرة | Enter × 2 | 3 أسطر مقبولة | `matchesIntended=true` | `C05` | PASS |
| U10 | قرار «إعادة للتعديل» | تعليق مكتوب | نقر | القرار يُسجَّل | `Level=4·Returned` | `C06` | PASS |
| U11 | الطابور بعد القرار | بعد الإرجاع | إعادة فتح التبويب | صفر صفوف | `rows=0` | `C07` | PASS |
| U12 | الموظّف يقرأ التعليق | جلسة أخرى | `?open=<id>` | 3 أسطر مرسومة | `renderedLines=8` | `C08` | PASS |
| U13 | جرس الإشعارات | إشعار مُولَّد | فتح الجرس | نصّ متعدّد الأسطر | `renderedLines=8` | `C09` | PASS |
| U14 | الأرشيف الإداريّ | مشرف | فتح نافذة التفاصيل | تعليق متعدّد الأسطر | `11` و`5` سطرًا | `live-02` | PASS |
| U15 | Project360 «التقارير المرتبطة» | مشروع من التطبيق | فتح التبويب | أعمدة القرار | 11 ترويسة · 3 صفوف | `V06` | PASS |
| U16 | مشروع خارج النطاق | معرّف وهميّ | فتح `/360` | رسالة موحّدة بلا تعداد | «غير موجود أو خارج نطاق صلاحيتك» | `V07` | PASS |
| U17 | معاينة البريد | مشرف | `POST …/preview` | `<br />` + ترميز | 3 × `<br />` · `&lt;b&gt;` | `§3` | PASS |
| U18 | حساب معطَّل | بعد التنظيف | محاولة دخول | 403 | `auth.account_disabled` | `§7` | PASS |

## 9) وصفة القياس على الأصل الحقيقيّ (بوّابةٌ تخاطب `localhost` لا تُثبت شيئًا)

1. نفق SSH: `-L 15091:127.0.0.1:5091` إلى خدمة `khubara-reporting-test`.
2. `rsync` لبايتات `/opt/reporting-test/frontend/dist` + خادم HTTPS محلّيّ على `127.0.0.1:8443` بـSPA fallback وتمرير `/api`,`/hubs`,`/health`.
3. `chromium.launch({ args: ['--host-resolver-rules=MAP test.emarketingacademy.net 127.0.0.1:8443', '--ignore-certificate-errors'] })`.

بهذا يرى المتصفّح الأصل الحقيقيّ `https://test.emarketingacademy.net`، وطبقة `auth_basic` في nginx تُتجاوَز **قياسًا** لا **أمنيًّا** (تبقى قائمة على الخادم).

## 10) تحفّظات مُلزِمة عند أيّ ترقية لاحقة

| # | التحفّظ | الحالة |
|---|---|---|
| R1 | **`TEST-ISO-01`** — القياس عبر أصل محلّيّ يخدم بايتات TEST خلف نفق؛ لم يُقَس عبر nginx العامّ بذاته | `NOT_MEASURED` |
| R2 | **حزمة الخادم** (ثنائيّات `.NET` المنشورة على TEST) لم تُطابَق بالبايت مع بناء مستقلّ — التطابق الدقيق مثبَت للواجهة فقط | `NOT_MEASURED` |
| R3 | متصفّح غير Chromium (Firefox/WebKit) | `NOT_RUN` |
| R4 | قياس الأداء تحت حِمل | `NOT_MEASURED` |
| R5 | `origin` غير قابل للجلب من هذه الجلسة (`Permission denied (publickey)`) — قِيست المراجع البعيدة من مراجع التتبّع المحلّيّة | `MEASURED_LOCALLY` |
| R6 | RC والإنتاج لم يُقاسا ولم يُمسّا في هذه الجلسة | بالتصميم |

## 11) قرار النشر (المرحلة 14)

- `RC_PROMOTION = **NOT_AUTHORIZED**`
- `PROD_PROMOTION = **NOT_AUTHORIZED**`

غير مشروط: **لا نشر ولا هجرة ولا Merge/PR/Tag/Push بلا تصريح صريح جديد لكلّ عمليّة على حدة.** ما تمّ هنا قياسٌ وإثبات، لا ترقية.

## 12) الأدلّة

- `evidence/screenshots/` — 19 لقطة (`C01..C09` دورة حيّة · `V01..V07` بوّابات بصريّة · `live-01..03` إعادة قياس).
- `evidence/measurements/` — `live-cycle.json` · `vis-gates.json` · `live-recheck.json` · سجلّات الاختبارات الثلاثة.
