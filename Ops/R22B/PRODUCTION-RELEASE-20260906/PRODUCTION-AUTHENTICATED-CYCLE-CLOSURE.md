# R22B — إغلاق الدورة المصادَق عليها على الإنتاج (Employee ↔ TeamLeader)

- **الإصدار قيد التحقّق:** `c5e0202d0a528a1a45856790716e449b812f0184` على `https://reports.emarketingacademy.net`.
- **نافذة التنفيذ:** `2026-09-06T18:33:49Z` (خطّ الأساس) ← `2026-09-07T07:01:58Z` (البصمة النهائيّة).
- **الغرض:** إغلاق الحاجبَين المتبقّيَين من تقرير النشر — `EMPLOYEE_LOGIN` و`TEAM_LEADER_LOGIN` — وما ترتّب عليهما من `EMPLOYEE_REPORTING_OPERATIONAL = NOT_PROVEN` و`VIS-05 = PASS جزئيّ`.

---

## 1) الهويّتان وعلاقة المراجعة

| البند | القيمة | الحكم |
|---|---|---|
| الموظّف | جهاد صلاح · `0b2a60f1-de62-458b-9228-5671b4133e83` · `["Employee"]` · نطاق `own` | `EMPLOYEE_LOGIN = PASS` |
| قائدة الفريق | شيماء عيد · `0e30b66a-0301-4116-ac96-a034fafa424a` · `["TeamLeader"]` · نطاق `team` | `TEAM_LEADER_LOGIN = PASS` |
| علاقة المراجعة | مُثبَتة قبل أيّ كتابة عبر `approval_steps` المستوى 1 على **9 تسليمات سابقة** للموظّف نفسه (W28–W36) | `REVIEW_RELATIONSHIP = OPERATIONALLY_LINKED` |

آليّة الدخول: `evidence/production-authenticated/capture-login.mjs` — المالك يكتب بيانات الدخول بنفسه في نافذة مرئيّة؛ السكربت لا يقرأ ولا يطبع ولا يحفظ توكنًا أو كوكي أو كلمة مرور، ويُخرج فقط حقول `/api/auth/me`. الملفّان: `evidence/production-authenticated-cycle/session-identity-{employee,leader}.json` (البريد مُخفًى).

**حسابان لم يُستعملا ولماذا:** الجلسة الأولى الملتقَطة عادت بدور **CEO** فرُفضت بديلًا عن أيّ من الدورين؛ والثانية (سماح أبوالمجد) عادت بدور `Employee` بلا فِرَق يقودها (`APPROVER_FOR_TARGET_EMPLOYEE = 0`) فأُوقِفت الدورة وأُبلِغ المالك. لم تُنشأ حسابات ولم تُغيَّر كلمات مرور.

---

## 2) الـcanary الوحيد

| البند | القيمة |
|---|---|
| المعرّف | `96613974-ed0d-41bc-8cae-78c4511a4004` |
| القالب | تقرير متابعة مقالات SEO الأسبوعي `46e100e3-b3ea-4b38-8447-3105d38b7bc7` |
| نسخة القالب | `32c40a55-9527-471d-92d6-9c23dca6b6ca` = **v7 منشورة** — أوّل تمرين إنتاجيّ حقيقيّ للنسخة التي زرعها هذا الإصدار عند الإقلاع |
| الفترة | `2026-W37` (`2026-09-05`←`2026-09-11`، `isOpen=true`) — كانت **خالية تمامًا** على مستوى النظام قبل الـcanary |
| الوسم | `R22B-PROD-CANARY-20260906` في كلّ حقل نصّيّ |
| المشروع | `481934f9-90e9-43fe-8ed2-0e631e95ea50` — «SEO — شركة خبراء التسويق» (كيان داخليّ، لا عميل خارجيّ) |
| الإنشاء | `2026-09-06T18:50:47Z` · الإغلاق `2026-09-06T18:59:52Z` |

اختيار الفترة تمّ من مصدر الخادم (`/api/reporting-calendar/my-cycles?context=Report`، حقل `isCurrent`) لا من نصّ الصفحة، بعد أن تبيّن أنّ قراءة النصّ تلتقط `2026-W36` من جدول التقارير القائم خطأً.

---

## 3) الدورة الكاملة — كلّ خطوة عبر الواجهة الرسميّة

| # | الخطوة | الفاعل | النتيجة | الدليل |
|---|---|---|---|---|
| 1 | إنشاء التقرير | الموظّف | `Draft` · إجمالي التسليمات 347→348 | `journeys/A-…txt` |
| 2 | تعبئة الحدّ الأدنى + نصّ ثلاثيّ الأسطر في حقلَين (رأس التقرير وبند العمل) | الموظّف | `FIELDS_FILLED` | `journeys/A-…txt` |
| 3 | حفظ المسوّدة | الموظّف | `SAVE_DRAFT = PASS` (`draft-saved-indicator`) | `journeys/A-…txt` |
| 4 | إعادة تحميل والتحقّق من الثبات | الموظّف | `DRAFT_RELOAD = PASS` · حقلان بالوسم · سطران جديدان في كلٍّ · `white-space: pre-wrap` | `journeys/A-…txt` |
| 5 | الإرسال للاعتماد | الموظّف | `Submitted` · المعتمِد الحاليّ = شيماء عيد | `journeys/A-…txt` |
| 6 | ظهوره في طابور المراجعة | القائدة | `LEADER_QUEUE_CONTAINS_CANARY = PASS` — **صفّ واحد لا أكثر** | `journeys/B-…txt` |
| 7 | كتابة سبب من ثلاثة أسطر **بضغطات Enter حقيقيّة** | القائدة | `LEADER_COMMENT_TYPED_WITH_ENTER = PASS` (145 حرفًا · سطران جديدان) | `journeys/B-…txt` |
| 8 | «إعادة للتعديل» | القائدة | `Returned` · الزرّ كان معطَّلًا قبل كتابة السبب ثمّ فُعِّل | `journeys/B-…txt` |
| 9 | قراءة تعليق الإرجاع | الموظّف | `RETURN_COMMENT_MULTILINE_AT_EMPLOYEE = PASS` · 3 أسطر بصريّة · `clipped=false` | `journeys/C-…txt` |
| 10 | تعديل (حالة المقال `Draft`→`Published`) + حفظ + إعادة تحميل | الموظّف | `EDIT_PERSISTED = PASS` — التعديل ثبت والأسطر المتعدّدة لم تتأثّر | `journeys/C-…txt` |
| 11 | إعادة الإرسال | الموظّف | `Submitted` | `journeys/C-…txt` |
| 12 | «اعتماد» | القائدة | **`Closed`** · `CurrentApproverId = NULL` · خروجه من «بانتظار اعتمادي» | `journeys/D-…txt` |
| 13 | الرؤية النهائيّة | الموظّف | «تقاريري»: صفّ `2026-W37` بحالة **مُغلق** | `journeys/E-…txt` |
| 14 | الرؤية النهائيّة | القائدة | قائمة النطاق مع شريحة «المغلقة» + فترة `2026-W37`: صفّ واحد **مُغلَق** | `journeys/E2-…txt` |

خطوات الاعتماد في القاعدة: المستوى 1 `Returned` (تعليق 145 حرفًا · سطران جديدان · `2026-09-06T18:56:00Z`) ثمّ المستوى 2 `Approved` (`2026-09-06T18:59:52Z`).

**`EMPLOYEE_REPORTING_OPERATIONAL = PASS` · `TEAM_LEADER_APPROVAL_FLOW = PASS`.**

---

## 4) الأسطر المتعدّدة — أربع نقاط قياس مستقلّة

| نقطة القياس | الوسيلة | النتيجة |
|---|---|---|
| عند الإدخال (بعد الحفظ وإعادة التحميل) | قيمة `textarea` | حقلان بالوسم · سطران جديدان في كلٍّ · `pre-wrap` |
| عند المراجعة (القائدة) | عقدة `<p>` مُصيَّرة | `newlines=2` · `pre-wrap` · ارتفاع 78px / سطر 20px ≈ 4 أسطر بصريّة · `clipped=false` |
| بعد الإرجاع (الموظّف يقرأ تعليق القائدة) | عقدة `<p>` مُصيَّرة | `newlines=2` · `pre-wrap` · 60px/20px = 3 أسطر · `clipped=false` |
| بعد الاعتماد (الدوران على التقرير المغلق) | عقدتان مُصيَّرتان | **2/2 سليمة** لكلٍّ من الموظّف والقائدة · `pre-wrap` · `clipped=false` · تعليق الإرجاع محفوظ بأسطره |

**`VIS-01 = PASS` على دورة قرار حيّة كاملة، لا على بيانات قائمة فقط.**

---

## 5) بوّابة VIS-02..VIS-05 على المسار المتأثّر

خمسة أسطح × دورين × مقاسَين (1440 و390) = **20 قياسًا**، كلّها `dir=rtl` · `docOverflow=0` · صفر عنصر منسايح خارج حاويات التمرير المقصودة · `spinners=0`. الملفّ: `evidence/production-authenticated-cycle/vis-gate-and-reporting-truth.{txt,json}`.

| البند | الحكم | الدليل |
|---|---|---|
| VIS-01 التعليقات متعدّدة الأسطر | **PASS** | أربع نقاط قياس أعلاه على دورة حيّة |
| VIS-02 الرفض خارج النطاق | **PASS** | مشروع وهميّ: `404` لا `403` · «المشروع غير موجود» · `spinners=0` · لكلا الدورين |
| VIS-03 RTL والانسياح والقصّ | **PASS** | 20/20 |
| VIS-04 إنهاء التحميل وسلامة الكونسول/الشبكة | **PASS** | كونسول 0 · شبكة 0 · 5xx 0 · 4xx 0 عبر كلّ الرحلات والبوّابة |
| VIS-05 الأسطح الحاكمة **مع أفعال القرار** | **PASS** (كان «جزئيّ — عرضًا فقط») | حفظ · إرسال · إرجاع · إعادة إرسال · اعتماد — نُفِّذت كلّها بأدوار حقيقيّة |

**تصحيح منهجيّ مسجَّل:** أوّل قياس للانسياح استعمل معيارًا مختلفًا عن البوّابة السابقة (عدّ كلّ عنصر يتجاوز عرض المستند) فأعطى `overflow>0` على أربعة أسطح عند 390. إعادة القياس بالصيغة نفسها المستعملة سابقًا (`documentElement.scrollWidth − clientWidth`) مع استثناء العناصر داخل حاويات `overflow-x:auto` المقصودة أعطت **0 على العشرين**. المُخرَجان محفوظان.

---

## 6) حقيقة التقارير

- **`REPORTING_TRUTH = PASS`.** تبويب «التقارير المرتبطة» في Project 360 للمشروع `481934f9…` يُظهر الـcanary بوسمه وفترته `2026-W37` واسم الموظّف — **لكلا الدورين**.
- الحالة النهائيّة في مرشّح التقارير **`Closed`** لا `Approved`، مطابِقةً للقاعدة المسجَّلة سابقًا.
- **رصد غير حاجب:** الشريحة الافتراضيّة في قائمة النطاق لا تعرض التقارير المغلقة؛ يلزم اختيار شريحة «المغلقة» صراحةً. ليس فقدان صلاحيّة: القراءة المباشرة `GET /api/submissions/{id}` تُرجِع `200` للقائدة مع الوسم وحالة `Closed`.
- **الأرشيف** (`/app/admin/archive`) سطح إداريّ خارج نطاق الدورين؛ لا ينطبق على هذه الدورة.

---

## 7) الضوابط السالبة — صفر أثر جانبيّ

بصمة واحدة الصيغة (`tools/db-fingerprint.sql`) قبل الاعتماد وبعده:

| المقياس | قبل (`18:59:17Z`) | بعد (`07:01:58Z`) | الحكم |
|---|---|---|---|
| `OTHER_SUBS_FP` (كلّ تسليم عدا الـcanary: الحالة + آخر تحديث + المعتمِد) | `840e9d3904da77ee5899532c40865458` | **مطابق** | صفر تغيّر |
| `USERS_FP` | `34/a4a064fcf929273027bcec5f30e8b976` | **مطابق** | صفر تغيّر |
| `OTHER_STEPS_FP` | `399/efefce858e7967dec0c74260c3e7f364` | **مطابق** | صفر تغيّر |
| `LEDGER_FP` / العدد | `fb691940aa3b9e208b0228d522a203b8` / 115 | **مطابق** | لا خصم رصيد (القالب متابعة/استبيان) |
| `KPI_EVAL` · `PROJECTS` · `CLIENTS` · نسخ القوالب | 102 · 35 · 11 · 108/75 | **مطابق** | صفر تغيّر |

مقارنة بخطّ الأساس `18:33:49Z` (347: `Closed 218` · `Submitted 70` · `ApprovedByDirectManager 35` · `Returned 15` · `Draft 9`) صار التوزيع 348 بـ`Closed 219` وبقية الفئات كما هي حرفيًّا ⟹ **الـcanary هو الفارق الوحيد**.

**تحفّظ مسجَّل:** صيغة بصمتَي خطّ الأساس (`BASELINE_USERS_MD5` و`BASELINE_OTHER_SUBS_MD5` في `db-baseline-20260906T1833Z.txt`) لم تُحفَظ في ملفّ، فتعذّرت إعادة إنتاجها حرفيًّا. عُوِّض ذلك ببصمة جديدة موحَّدة الصيغة قبل/بعد الاعتماد (أعلاه) وبمطابقة توزيع الحالات مع خطّ الأساس فئةً فئة.

---

## 8) مصير الـcanary

**`CANARY_CLEANUP = RETAINED_WITH_VALID_AUDIT_REASON`.**

1. نموذج النظام لا يتيح حذفًا لتسليم `Closed`: زرّ «حذف المسودة» يُصيَّر فقط عند `sub.status === 'Draft'` (`reporting-frontend/src/pages/SubmissionsPage.tsx:455` و`:1143`).
2. `admin-delete` محصور بـ`Admin`/`CEO`/`GeneralManager` (`SubmissionsPage.tsx:846`) — لا يملكه أيّ من الدورين المُصرَّح بهما، واستعمال جلسة CEO ممنوع بنصّ التكليف كما هو ممنوع إنشاء حسابات.
3. قاعدة الدومين: **لا حذف صلب**؛ التصحيح بحركة عكسيّة.
4. حذفه يُتلف سلسلة التدقيق التي هي نفسها دليل الإصدار.
5. موسوم بلا لبس بـ`R22B-PROD-CANARY-20260906` في كلّ حقل نصّيّ، وعلى فترة `2026-W37` وحدها.

---

## 9) النظافة والخصوصيّة

- **حالة جلسات المتصفّح حُذفت من القرص** بعد الانتهاء: `/private/tmp/prod-auth/{employee,leader,ceo}` وملفّات الهويّة الخام. الباقي هو `capture-login.mjs` فقط.
- عناوين البريد **مُخفاة** في كلّ ملفّات هذه الحزمة.
- **47 لقطة شاشة لم تُودَع في Git** لأنّها تحوي بيانات إنتاج حقيقيّة غير مموّهة، وإعادة التقاطها بطبقة التمويه تستلزم جلسة حيّة انتهت صلاحيّتها. بصماتها الكاملة في `screenshot-inventory-sha256.txt` والملفّات محفوظة محلّيًّا في `/private/tmp/prod-verify/{journey-shots,vis-shots}` خارج المستودع.

---

## 10) رصد تشغيليّ مستقلّ عن R22B

أثناء اختيار الزوج المناسب تبيّن أنّ **11 تسليمًا عالقة بمعتمِدين غير صالحين**: ثلاثة عند بسنت محمد (`IsActive=false`) · اثنان عند أميرة عبدالفتاح (`IsActive=false`) · ستّة بـ`CurrentApproverId = NULL`. كما أنّ الفريق `777` ما زال يسجّل قائدًا معطَّل الحساب. **ليس أثرًا لهذا الإصدار ولا لهذه الدورة**، ويُرفع للمالك كبند تشغيليّ مستقلّ.

---

## 11) الحكم

```
EMPLOYEE_LOGIN                        = PASS
TEAM_LEADER_LOGIN                     = PASS
EMPLOYEE_REPORTING_OPERATIONAL        = PASS
TEAM_LEADER_APPROVAL_FLOW             = PASS
REPORTING_TRUTH                       = PASS
VIS_01                                = PASS
VIS_02                                = PASS
VIS_03                                = PASS
VIS_04                                = PASS
VIS_05                                = PASS
CANARY_CLEANUP                        = RETAINED_WITH_VALID_AUDIT_REASON
PRODUCTION_WRITES_BY_TASK             = 1 canary فقط (348 تسليمًا؛ صفر أثر على أيّ سجلّ آخر)
COMMENT_MULTILINE_PRODUCTION_RELEASE  = PASS
R22B_REPORTING_PRODUCTION_RELEASE     = PASS
```
